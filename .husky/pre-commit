#!/usr/bin/env sh

# Lint staged files only (fast). This is a live multi-agent worktree, so never
# let lint-staged create its default temporary git stash of unrelated work.
bunx lint-staged --no-stash

# Typecheck full project (catches type errors in unchanged files)
bun run typecheck
