.venv/
__pycache__/
*.pyc

# Runtime voice diagnostics (raw vs. cleaned transcripts) — not source.
voice_transcripts.json

# Runtime pointer for /recent_report.html (most recently processed document).
recent_report.json

# Runtime state for the fail-loud intake-halt alert (active fault + ack flag).
intake_halt.json
category_undo_journal.json
category_undo_journal.json.tmp
.cache/
.venv-dev/
