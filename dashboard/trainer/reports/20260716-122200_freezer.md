# Trainer Report — Freezer Scanner

**Document:** `scan_freezer.jpg` (Walgreens receipt)
**Dispatch:** 2026-07-16T12:19:26Z (resume of an earlier 12:06:29Z dispatch that stalled on a broken `google-generativeai` install)
**Verdict: FAIL** (correctly judged by Mazda's own pipeline — root cause is a real infra/DB defect, not a model error)

## Checklist

| Step | Status | Evidence |
|---|---|---|
| `load_wrapper_revision` | ❌ never called (in either the 12:06 or 12:17 segment) | trace still recorded `wrapper_revision: wrap-v004` from prior context, not a fresh call |
| STEP 0 classify/parse (facade=unknown) | ✅ | `classify_scan.py` → receipt, Walgreens, confidence 0.99; `parse_and_categorize.py --json` → date 2020-01-21, amount 39.89 |
| `check_vendor_key` | ✅ | normalized supplied key → `walgreens` (fuzzy match), adopted downstream |
| `check_duplicates` | ❌ tool errored | `Unknown column 'id_light' in where clause` (real SQL bug in the tool) |
| Categorize | ✅ | `categorizer_main.py` → category_id 378 |
| Store | ❌ failed | `parse_and_categorize.py --save --engine=gemini` → `pymysql.err.ProgrammingError: (1146, "Table 'nonprofit_finance.receipt_metadata' doesn't exist")` |
| `record_trace` (task_name="document-intake") | ✅ but one field wrong | recorded `duplicate_checked: true` despite the check erroring; `problems` correctly described both errors |
| `judge_trace` | ✅ | FAIL / `failure_type=other`, reason `failed: expense_stored` — correct |
| `propose_improvement` (FAIL) | ✅ | proposal 48: stop before save on schema mismatch instead of attempting a broken save |
| `apply_proposal` | ✅ | all 4 gates allow → activated `wrap-v005` |
| Dashboard callback | ✅ | `POST /api/expense-stored` with `stored:0`, fired even on failure per contract |

## Wrapper defects diagnosed and coached

1. **Evidence inaccuracy**: `duplicate_checked` was recorded `true` even though the `check_duplicates` tool call errored out. Coached Mazda to set this field to `false` (and keep noting the tool error in `problems`) whenever the check doesn't return successfully — an inflated evidence record undermines the self-improvement loop's data quality.
2. **Missing STEP 1 evidence**: `load_wrapper_revision(agent_name="Mazda")` was never called in either the original 12:06 attempt or the 12:17 resume, even though the correct `wrap-v004`/`wrap-v005` revisions were used. Coached Mazda to call it explicitly every run so learned rules are demonstrably loaded, not assumed from memory.

Both corrections were acknowledged; Mazda filed them via `propose_memory_note` (proposal 49, gated `allow`), but the tool response says **"no live-memory applier wired; note recorded as an approved proposal only"** — this note has NOT actually landed in her live `system/persona` memfs file. A human should manually project proposal 49 into her memfs (per the `memfs_system_prefix_bug` / memory-projection-is-destructive lesson) or confirm the applier gap is expected and tracked.

## For a human — real infrastructure blockers, not Mazda lessons

- **`nonprofit_finance.receipt_metadata` table does not exist.** This blocks every future receipt `--save` fleet-wide (not specific to this document). This is the actual root cause of the FAIL, and needs a DB migration/schema fix.
- **`check_duplicates` MCP tool has a SQL bug**: references a nonexistent `id_light` column, so duplicate checking is currently non-functional for every run, not just this one.
- Minor: during the stalled 12:06 attempt (before `google-generativeai` was reinstalled), Mazda went on an unrelated tangent (`sed`/`grep` on `first_bank_8416`/`fifth_third` vendor entries that have nothing to do with this Walgreens document) instead of stopping cleanly at the blocker. Low severity — she did correctly report "blocked" and did not fabricate progress — but worth a glance if it recurs.

## Summary

Mazda's classification, parsing, vendor normalization, and categorization all worked correctly for this Walgreens receipt, and her self-improvement pipeline correctly judged the run FAIL and filed/activated a mitigating wrapper change (wrap-v005) — but the actual failure was a genuine infrastructure defect (a missing `receipt_metadata` table plus a broken `check_duplicates` query), not something fixable in her wrapper alone. Two evidence-quality issues were coached (an inaccurate `duplicate_checked` flag, and a skipped `load_wrapper_revision` call); Mazda acknowledged both, but the proposed memory note did not actually persist live — a human should push it into her memfs directly.
