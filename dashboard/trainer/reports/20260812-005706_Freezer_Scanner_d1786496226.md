# Mazda Trainer Report — Freezer Scanner

**Verdict: PASS**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1786496222210140876_733f889b6d08.jpg`
- Dispatch: 2026-08-12T00:57:06Z
- Conversation: `conv-be70ac28-81a6-4bb8-8cdb-53e9c9ff4ea1`
- Facade result: `doc_kind=unknown, confidence=0.0` (STEP 0 vision classification required)
- Run duration: ~1m46s dispatch → judge_trace (well inside the 15-minute window)

## Checklist (all with successful tool returns, verified in transcript)

1. `load_wrapper_revision` (00:57:11) — `wrap-v005`, active, 4 accumulated rules loaded. ✅
2. STEP 0 — facade `unknown`, so `classify_scan.py` via `executor_run` (00:57:32) → `doc_type=receipt, confidence=0.99, merchant=MR BURGER RESTAURANT 1`. Gemini vision hit a 401 (expired/invalid OAuth), correctly fell back to OpenAI Codex CLI. ✅
3. Receipt branch parse — `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_911f9921a1b4.json` (00:57:52). Gemini parse also 401'd, fell back to `chatgpt-oauth`. Returned `expense_scope=full_receipt`, single `SALE` line item = $31.19, `handwritten_arithmetic_total=null` — correctly not itemized (one line, exact reconciliation). ✅
4. `check_vendor_key` (00:58:00) → normalized `mr_burger_restaurant_1` → `mr_burger`; Mazda adopted the normalized key everywhere downstream. ✅
5. `check_duplicates` (00:58:04) → MCP call failed (`Access denied for user 'adamsl'@'localhost'`, no DB creds on that path). Per her own wrapper's **Rule 3** (added 2026-08-06 from a prior coaching cycle), she ran an authenticated equivalent duplicate check via `executor_run` (00:58:11) → `[]`, no match, and only then recorded `duplicate_checked=true`. This is exactly the corrected behavior the rule exists for — good evidence the earlier wrapper fix is sticking. ✅
6. Categorize — `categorizer_main.py --provider=auto` (00:58:21) → `category_id=137` via gemini provider (this leg succeeded, unlike vision). `check_category` (00:58:28) confirmed `category_id=137` matches vendor `mr_burger`. Non-null, positive category. ✅
7. Store — `parse_and_categorize.py --save --category-id=137 --parsed-json <same artifact>` (00:58:38) → `success:true, expense_id:2027, parse_artifact_verified:true, duplicate:false`. Artifact reused from STEP 0's parse (same file path), satisfying the no-second-vision-pass requirement. ✅
8. `record_trace` (00:58:49) — `task_name="document-intake"`, full IntakeVerificationEvidence with `stored:true, expense_id:2027, is_duplicate:false`, honest `problems` list documenting both Gemini fallbacks and the check_duplicates auth workaround. ✅
9. `judge_trace(123)` (00:58:52) → `PASS`. ✅
10. Dashboard notify — confirmed via `/api/expense-stored-events?since=1786496226`: event present with matching `conversation_id`/`dispatched_at`, `expense_id:2027`, `stored:1`. ✅

No `propose_improvement` needed (PASS). No corrective message sent to Mazda — she did nothing wrong.

## Supporting-document check

`GET /api/supporting-documents?expense_id=2027` → `receipt_url` present, `documents[0]` = `{type:receipt, label:"View Receipt", available:true}`. Dialog-visibility contract satisfied. ✅

**Red box did NOT render.** `GET /supporting-document/2027/receipt` served bytes byte-identical to the raw archived file (241257 bytes, `diff` confirms no difference), and the server log recorded:
`[supporting-document] opened without highlight expense_id=2027 type=receipt reason=No high-confidence expense row was found in the image.`
This is the exact failure string the Trainer instructions say was fixed 2026-08-08 via `CodexCliImageRegionFallbackMatcher`. Per those instructions, its recurrence here is a **genuine regression, not a known/tolerated gap** — this is a straightforward single-line card-sale slip, not the check-style/handwritten class the fallback was built for, so the primary OCR matcher failing on it is unexpected. This is a dashboard defect, not Mazda's — filed below.

## Escalation 1 — receipts archived under the wrong month

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  parse_and_categorize.py's `_parse_receipt_date_from_stem()` (lines 960-985) scans
  the underscore-split filename stem LEFT TO RIGHT for the first pair of adjacent
  digit-only tokens and treats them as (month, day). Vendor names ending in a bare
  numeral collide with this scan: filename stem
  "mr_burger_restaurant_1_04_17_25_31_19" tokenizes to
  ['mr','burger','restaurant','1','04','17','25','31','19']. At idx=3 the tokens
  '1' and '04' are both digit-only and pass the 1-2-digit-length/range checks, so
  the function returns month=1, day=4 ("January 4") and returns BEFORE ever
  reaching the real date tokens '04','17','25' at idx=4. move_receipt_to_month_day_dir()
  then archives the receipt under readable_documents/receipts/2025/january/january_04/
  even though the receipt's actual transaction date (and the MM_DD_YY embedded in
  the same filename) is 2025-04-17.

  Confirmed reproducible, not a one-off: this run's receipt
  (mr_burger_restaurant_1_04_17_25_31_19.jpg, expense_id 2027) landed in
  january_04/, and an earlier receipt from the same vendor
  (mr_burger_restaurant_1_04_10_25_31_40.jpg, actual date April 10) is
  ALREADY sitting in the same wrong january_04/ folder — any vendor name
  ending in a digit 1-12 followed by a 2-digit token in the valid day range
  will misfile the same way.

  Expected: archive path readable_documents/receipts/2025/april/april_17/...
  Actual:   archive path readable_documents/receipts/2025/january/january_04/...

  Suggested fix direction: the scanner should anchor on the LAST valid
  month/day(/year) token run in the stem (the canonical MM_DD_YY suffix this
  same codebase writes), not the first, or should require an immediately
  following 2-digit year token to disambiguate a real date from an incidental
  digit in the merchant name.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786496222210140876_733f889b6d08.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=137 --parsed-json /tmp/mazda_receipt_911f9921a1b4.json --engine=gemini
```

## Escalation 2 — red-box annotation regression on a plain receipt

```
repo_path: /home/adamsl/letta-code
bug_description: |
  Opening "View Receipt" for the receipt stored by this run (expense_id 2027,
  GET /supporting-document/2027/receipt) served the original unannotated
  image (byte-identical to the archived file on disk) instead of a red-boxed
  copy, logging:
  "[supporting-document] opened without highlight expense_id=2027 type=receipt
  reason=No high-confidence expense row was found in the image."

  This exact message is the one documented in the Mazda Trainer instructions
  as fixed 2026-08-08 via CodexCliImageRegionFallbackMatcher
  (dashboard/codex_image_region_fallback.py) for check-style/handwritten
  receipts. This receipt is not that class — it is a plain single-line
  card-sale slip (MR BURGER RESTAURANT 1, one "SALE" line, $31.19, clearly
  printed) — so the primary OCR-based matcher in ImageExpenseDocumentAnnotator
  (dashboard/document_annotation.py) failing to find a high-confidence row on
  this simple document is unexpected and, per the Trainer's own instructions,
  should be treated as a genuine regression rather than a tolerated gap.
  Not investigated further per Trainer scope (no rol_finances/dashboard code
  execution permitted) beyond confirming served-bytes == raw file and reading
  the log line above.
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/january/january_04/mr_burger_restaurant_1_04_17_25_31_19.jpg
  failing_command: GET /supporting-document/2027/receipt
```

## For a human

- Two application-level bugs found and escalated above (wrong-month receipt archiving; red-box annotation regression on a plain receipt). Neither is a Mazda coaching item.
- The wrong-month archive bug likely affects other historical receipts whose vendor name ends in a digit — worth a backfill audit similar to the 2026-08-08 `backfill_source_file.py` fix once patched.
- Gemini vision/parse both hit `401 ACCESS_TOKEN_TYPE_UNSUPPORTED` this run; fallbacks (Codex CLI / chatgpt-oauth) covered it fine, but if Gemini stays down the categorizer fallback chain should be watched (this run's categorizer call itself succeeded via `gemini`, so that leg is currently fine).

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
