# Trainer Report — Freezer Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786488497259991656_854e99a1d900.jpg`
**Dispatch:** 2026-08-11T22:48:20Z
**Conversation:** conv-4d1e458b-3150-4900-bd5d-23b901463315

Verdict: PASS

## Summary

Facade classification was `unknown` (no extractable text), so Mazda correctly ran STEP 0
vision classification herself. `classify_scan.py` (after a Gemini 401 auth failure,
correctly falling back to OpenAI Codex CLI) returned `doc_type=other, confidence=0.99,
merchant="Delta Dental Plan of Michigan", reason="explanation of benefits/claim summary,
not a receipt, invoice, bank statement, or tax form."` Per the intake contract, an
explicit `doc_type=other` with a clear unsupported-document reason is a correct
fail-closed outcome requiring no investigate/categorize/store steps — Mazda handled this
exactly right.

## Step-by-step checklist (all evidence = successful tool returns in this conversation)

1. `load_wrapper_revision(agent_name="Mazda")` → 22:48:26, `wrap-v004` loaded. ✓
2. STEP 0 `executor_run` → `classify_scan.py` → 22:48:44, `doc_type=other`, confidence
   0.99, correct Gemini→Codex-CLI fallback path exercised. ✓
3. No investigate/categorize/store steps run — correct, per unsupported-document
   routing. ✓
4. `record_trace` → 22:48:54, trace_id 109, `task_name="document-intake"`,
   `doc_kind="other"`, `vendor_key_recognized:false`, `duplicate_checked:false`,
   `stored:false`, `expense_id:null`, `problems` array correctly states no
   investigate/categorize/store steps ran because `doc_type=other` is unsupported. ✓
5. `judge_trace(109)` → 22:48:59 → **FAIL** (`missed_vendor_key`), reasons:
   `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored`
   all "failed." See Escalation below — this is a rubric bug, not a Mazda defect.
6. `propose_improvement` → 22:49:05, proposal 58 filed, correctly describing the rubric
   gap. ✓
7. `apply_proposal(58)` → 22:49:09 → `"Proposal 58 is already satisfied by wrap-v004; no
   duplicate rule or wrapper revision was created."` — confirms Rule 2 (proposal 40,
   2026-08-06) already exists in the active wrapper and covers exactly this case.
8. Dashboard notify → `executor_run` curl POST `/api/expense-stored` → 22:49:20,
   `{"ok": true}`, `outcome="UNSUPPORTED_DOCUMENT"`. Confirmed independently via
   `/api/expense-stored-events?since=1786488500` — event present with matching
   `conversation_id` and `dispatched_at`. ✓

Every required step for this document class is present with a successful tool return.
No prose-only claims were relied on for grading (the assistant's closing summary claimed
the dashboard notification was "sent successfully," which is true, but was independently
verified against the events endpoint rather than trusted on its own).

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  `judge_trace` (the deterministic intake-verdict rubric behind the self-improvement
  framework, served by mazda-tools-mcp.service :8791) does not implement the
  doc_kind=other/unsupported-document exemption that the active wrapper (wrap-v004,
  Rule 2 / proposal 40, filed 2026-08-06) already documents in Mazda's instructions:
  "When classify_scan.py returns doc_type=other with a clear unsupported-document
  reason, record the unsupported result and ensure the intake rubric does not require
  vendor/category/duplicate/storage checks."

  On this run Mazda did everything the contract requires for an unsupported document
  (classified, recorded a trace with duplicate_checked/stored=false and a clear
  `problems` explanation, and skipped investigate/categorize/store). judge_trace(109)
  still returned FAIL, citing `vendor_key_resolved`, `category_resolved`,
  `duplicate_check_ran`, and `expense_stored` as failed checks — fields that should not
  apply to a doc_kind=other trace at all.

  Mazda correctly proposed the fix (proposal 58), and apply_proposal reported it was
  "already satisfied by wrap-v004; no duplicate rule or wrapper revision was created" —
  i.e. the wrapper *text* already states this rule, but the deterministic judge
  *implementation* does not branch on doc_kind/problems to apply it. This is a gap
  between the wrapper's instruction text and the actual rubric code (likely in
  tools/self_improving_agent's evaluation/verdict-judge implementation) — text in the
  LLM-facing instructions field has no effect on the rule-based judge unless the rubric
  code itself was changed. Since proposal 40 (2026-08-06) apparently never resulted in
  an actual code change, this is a recurring/never-fixed defect, not a one-off.

  Expected: judge_trace should recognize doc_kind="other" (or an equivalent
  unsupported-document marker in the trace) and exempt vendor_key_resolved /
  category_resolved / duplicate_check_ran / expense_stored from the pass/fail
  requirements, mirroring the existing statement-row carve-out for uncategorized
  vendors.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786488497259991656_854e99a1d900.jpg
  failing_command: judge_trace(trace_id=109) via conv-4d1e458b-3150-4900-bd5d-23b901463315
  trace_id: 109
  proposal_ids: [40, 58]

## For a human

No coaching was sent to Mazda — this is an application-code defect (judge rubric), not
a wrapper/instruction gap she can fix by retrying. She already correctly re-proposed the
existing rule and confirmed it's a duplicate of proposal 40. Someone should look at the
`judge_trace` implementation in `tools/self_improving_agent` (or wherever it's actually
served — mazda-tools-mcp.service :8791) and wire the doc_kind=other exemption into the
rubric code itself, then verify proposal 40/58 are marked resolved once fixed.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
