# Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Document:** `scan_freezer_1784825236727004343_f0f01342e94a.jpg`
- **Dispatch:** 2026-07-23T16:47:20Z
- **Mazda conversation:** conv-53b07f20-f70c-4728-9e63-a558b72049d4
- **Run window observed:** 16:47:20 – 16:48:09 (49s, no stalls)
- **Verdict: PASS** (Mazda's conduct fully satisfied the contract; the judge's FAIL was a rubric miscalibration that Mazda correctly diagnosed and self-corrected)

## What happened

The facade returned `doc_kind=unknown, confidence=0.0` (expected for a JPEG scan), so Mazda
correctly entered STEP 0 and classified the image herself:

1. `load_wrapper_revision(Mazda)` → wrap-v036. ✅
2. STEP 0 classify — `executor_run classify_scan.py` → `{"doc_type":"other","confidence":0.99,
   "merchant":null,"reason":"The scanned page appears blank with no visible financial document
   content or merchant information."}`. Per the manual, `doc_type=other` is the one explicitly
   unsupported case — no receipt/invoice/statement branch applies, and no vendor/category/
   duplicate/store steps are expected. Mazda correctly stopped here rather than inventing data. ✅
3. `record_trace(task_name="document-intake", ...)` — honest evidence: `doc_kind:"unknown"`,
   `vendor_key:null`, `category_id:null`, `stored:false`, `problems:["classify_scan.py returned
   doc_type=other: ... blank ..."]`. ✅
4. `judge_trace` → **FAIL** (`failure_type=unsupported_document`), reasons: `document_classified`,
   `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored` all "failed".
   This is a **rubric defect, not a Mazda defect** — the deterministic judge is scoring
   stage-completeness without an exemption for a correctly-detected unsupported document.
5. STEP 7 close-the-loop — `propose_improvement(trace_id=153, failure_type=unsupported_document,
   ...)` → proposal 112, then `apply_proposal(112, "...record an explicit unsupported-document
   trace and do not expect vendor/category/duplicate/store stages to run")` → **activated**,
   all 4 gates passed (safety/cost/regression/usefulness), new wrapper `wrap-v037` (learned rule
   #20), snapshot 32. ✅
6. STEP 8 dashboard notify — `executor_run curl POST /api/expense-stored` with
   `stored:0, parsed:0, expense_ids:[], duplicate_expense_ids:[]`, `doc_kind:"unknown"`,
   correct `conversation_id`/`dispatched_at`. Returned `{"ok": true}`. ✅

Every required step is present with a successful tool return, in the correct order, and the
evidence is truthful (no invented vendor/category/store data for a blank page).

## Wrapper defect diagnosed

Not a Mazda-side defect. The intake judge (deterministic rubric in the evaluation factory)
still penalizes a correctly-short-circuited `doc_type=other` run for not completing
vendor/category/duplicate/store stages, even though the manual is explicit that those stages
don't apply. This is exactly what Mazda's `propose_improvement`/`apply_proposal` targeted —
but note the fix she can apply from inside the conversation only edits her own **wrapper
instructions** (what she's told to do), not the judge's scoring code. Wrapper rule #20 tells
her to "record an explicit unsupported-document trace," which she already did correctly this
run — so if the judge's stage-completeness check is unconditional, the same FAIL may recur on
the next blank/unsupported scan regardless of instruction wording.

**Flag for a human:** if `failure_type=unsupported_document` keeps appearing on legitimately
blank/unsupported scans across future runs despite repeated wrapper patches, the actual fix
belongs in the judge/evaluator code (an exemption when `doc_type=other`/blank is the honest,
correctly-detected outcome), not in another wrapper instruction — this may be a case the
learned-instruction mechanism structurally cannot fix.

## Coaching sent to Mazda

None. No corrective message was needed — her run already fulfilled the contract end to end,
including the required propose/apply-improvement response to the FAIL verdict.

## Anything else for a human

Nothing blocking. Worth watching whether `unsupported_document` FAILs recur despite the new
wrap-v037 rule (see above).
