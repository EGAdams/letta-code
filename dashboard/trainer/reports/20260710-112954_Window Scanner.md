# Trainer Report — Window Scanner

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan.jpg`
- **Scanner**: Window Scanner
- **Facade result**: `{"ok": true, "doc_kind": "unknown", "confidence": 0}`
- **Dispatch timestamp**: 1783682474 (2026-07-10T11:21:14Z)
- **Verdict**: **STALLED**

## What I observed

I polled Mazda's transcript (`GET /v1/agents/agent-6b536cf4-ec88-4290-b595-fed21d14bd8e/messages`)
starting immediately after dispatch and every ~60s for the full 15-minute grace window
(until 2026-07-10T11:36:14Z).

At no point did any message with a timestamp ≥ 11:21:14Z appear. Mazda's transcript is
completely silent for this dispatch.

Her most recent activity before the window closed:

- A `record_trace` / (presumably) `judge_trace` cycle for a **different** document —
  `scan_freezer.jpg` (Freezer scanner), ending at 11:20:23–11:20:32Z, i.e. **before** our
  dispatch even fired. That run's evidence: `vendor_key: "meijer"`, `category_id: 3`,
  `stored: true`, `expense_id: 1489` — looks like a clean prior PASS, unrelated to this
  report.
- No `user_message` referencing `scan.jpg` or "Window Scanner" appears anywhere in the last
  80 messages.
- `curl http://localhost:8765/api/expense-stored-events?since=1783682474` → `[]` (no store
  event for this document either, consistent with no run having started).

## Checklist against the contract

| Step | Evidence |
|---|---|
| Dispatch received | ✗ — no `user_message` after 11:21:14Z |
| `load_wrapper_revision` | not reached |
| STEP 0 (classify/parse, doc_kind=unknown) | not reached |
| `check_vendor_key` / `check_duplicates` | not reached |
| Categorize | not reached |
| Store | not reached |
| `record_trace` (task_name=document-intake) | not reached |
| `judge_trace` | not reached |
| `propose_improvement` | not reached |
| Dashboard notify | not reached |

## Diagnosis

This is **not a Mazda wrapper defect** — she cannot be judged on a message she never
received. The failure is upstream, in message delivery from `process_scanned_document` /
`_notify_mazda_of_scan` to the Letta server, or possibly the dispatch fired against a stale
run_id/agent state that never surfaced as a new turn. Candidate causes to check:

1. Whether `_notify_mazda_of_scan` actually issued the HTTP POST to
   `$LETTA_BASE_URL/v1/agents/$MAZDA_AGENT_ID/messages` for this scan (check dashboard
   server logs / `/tmp/dashboard_8765.log` around 11:21:14Z).
2. Whether Mazda was still mid-turn on the prior `scan_freezer.jpg` run and the new
   message queued/dropped rather than starting a new turn (her last `assistant_message`
   was at 11:20:32Z, only ~40s before this dispatch — tight but should have been clear).
3. Letta server health at dispatch time (transient outage/5xx swallowed by the
   fire-and-forget call).

## Action taken

- **No message sent to Mazda** — there is nothing for her to learn from a dispatch that
  never arrived; coaching her here would be nonsensical (per the "never blame the model"
  principle, this is also not a "blame the model" situation — it's a delivery-layer gap).
- Flagging for a human: check `_notify_mazda_of_scan` / dashboard server logs for this
  scan's dispatch attempt, and confirm the Letta server was reachable at 11:21:14Z.
