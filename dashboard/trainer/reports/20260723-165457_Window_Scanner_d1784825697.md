# Mazda Trainer Report — Window Scanner

- **Document:** `scan_1784825691631666435_c1b88840bbd5.jpg` (Window Scanner)
- **Dispatch time:** 2026-07-23T16:54:57Z (`dispatch_at=1784825697`)
- **Mazda conversation:** `conv-30b82e7b-bdcc-4cec-85d6-94e523d8496b`
- **Verdict: PASS**

## Facade result
`doc_kind=unknown, confidence=0.0` → STEP 0 (Mazda self-classifies) correctly triggered.

## Step-by-step evidence

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | returned `wrap-v037`, active |
| STEP 0 classify | `executor_run classify_scan.py` → `doc_type=receipt, merchant=Advance Auto Parts, confidence=0.99`. Gemini hit a 429 quota error mid-call; tool self-healed via ChatGPT OAuth fallback and still returned a valid classification. |
| Parse | `parse_and_categorize.py --json --engine=gemini` → Gemini 429 again, self-healed via chatgpt-oauth fallback (`model_provider: chatgpt-oauth, model_name: gpt-5.4`), full itemized receipt extracted (Advance Auto Parts, $4.76, 03/17/25). |
| `check_vendor_key` | Called with `vendor_key=advance_auto_parts`. Tool fuzzy-matched this to an **unrelated** existing key, `5_3_mortgage_ln_xxxxxx5194_paid_by_auto_billpayer` — a bogus match (auto-parts receipt vs. a mortgage-loan billpayer key). Mazda correctly rejected this normalization, kept her own derived key `advance_auto_parts`, and recorded the anomaly explicitly in the trace's `problems` field. |
| `check_duplicates` | not a duplicate (`in_database: false`) |
| Categorize | `categorizer_main.py` → `category_id: 373` (valid, non-null/non-zero) |
| Store | `parse_and_categorize.py --save --category-id=373 --engine=gemini` → `{"success": true, "expense_id": 1539}` |
| Itemize | `itemize_existing_expense` → `itemizable:false` ("only one line item; a single line equal to the charge is standalone, not itemized") — correct fail-closed non-itemization, left STANDALONE |
| `record_trace` | `task_name="document-intake"`, evidence JSON matches reality (`vendor_key=advance_auto_parts`, `vendor_key_recognized=true`, `category_id=373`, `stored=true`, `expense_id=1539`), `problems` lists both the vendor-key fuzzy-match anomaly and both Gemini quota fallbacks |
| `judge_trace` | `PASS`, `failure_type=none`, "all deterministic intake checks passed" |
| Dashboard notify | `POST /api/expense-stored` → `{"ok": true}`, confirmed via `/api/expense-stored-events`: `expense_id=1539`, `conversation_id` and `dispatched_at` match this run exactly |

## Wrapper defect diagnosed

None in Mazda's own wrapper. She handled two upstream infrastructure hiccups correctly on her own:

1. **Gemini free-tier quota exhaustion** (classify + parse) — both self-healed via the existing ChatGPT OAuth fallback chain, as designed.
2. **`check_vendor_key` fuzzy-match bug** — the tool itself proposed an unrelated normalized vendor key (an auto-parts receipt matched to a mortgage-loan key). This is a defect in the vendor-key fuzzy-matching logic behind `check_vendor_key` (likely too loose a similarity threshold, or a substring-match quirk on "auto"), not something in Mazda's instructions. Mazda's handling of it was exactly right — she rejected the bad normalization, kept the clean key, and logged the anomaly in `problems` rather than silently either accepting the bogus key or hiding the issue. No coaching sent; no `propose_improvement` needed since the run PASSed.

**Recommend investigating separately (not a Mazda/Trainer action item):** the `check_vendor_key` fuzzy-normalization logic — a wrong-vendor match like this could someday get stored on a run where the model isn't careful enough to reject it, silently misfiling a real vendor under an unrelated key.

## Lesson sent to Mazda

None — no coaching needed, run passed cleanly on the first attempt.

## For a human

Worth a look, low priority: the `check_vendor_key` tool's fuzzy matcher proposed `advance_auto_parts` → `5_3_mortgage_ln_xxxxxx5194_paid_by_auto_billpayer`, a nonsensical cross-vendor match. Mazda avoided the bad outcome this time by exercising judgment, but the underlying matcher defect should be tightened so future runs (possibly on a weaker model) don't inherit a bad vendor key from it.
