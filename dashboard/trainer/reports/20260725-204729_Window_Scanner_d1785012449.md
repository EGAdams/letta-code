# Trainer Report — Window Scanner

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785012446454961912_9c449c168bb2.jpg`
- **Dispatch**: 2026-07-25T20:47:29Z (`conv-5f4b2c43-e1d5-47ca-a559-4759c2ea7012`)
- **Facade result**: `doc_kind=unknown`, confidence 0.0 → Mazda required to classify herself (STEP 0)
- **Verdict: PASS** (correct fail-closed handling of a genuine tool-level bug — no coaching sent)

## Checklist / evidence

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v048` — ✅ (20:47:33)
2. STEP 0 classification: `executor_run classify_scan.py` → `doc_type=moms_ledger, confidence=0.95, merchant="JetBlue Business Mastercard"` — ✅ (20:47:47)
3. Correct branch routing: `moms_ledger` → ran `tools/categorizer/moms_ledger_reconciler.py --image <scan>` — ✅ (20:50:12)
   - Tool returned `returncode:1`, `{"ok": false, "problems": ["ValueError: Unsupported scheduled_date format: '04-24'"]}` — a real defect in the reconciler's date parser (handwritten `MM-DD` scheduled dates aren't accepted), not something fixable by agent behavior.
4. Fail-closed: no expenses stored, no receipt/statement branch substituted — ✅
5. `record_trace(task_name="document-intake", ...)` — ✅, evidence JSON correctly shows `doc_kind=moms_ledger`, `stored=false`, `expense_id=null`, `is_duplicate=false`, `duplicate_checked=false`, `problems=["moms_ledger_reconciler failed: ValueError: Unsupported scheduled_date format: '04-24'"]` — matches observed tool return exactly.
6. `judge_trace(trace_id=199)` → `FAIL / missed_duplicate` — consistent with `ok:false` (contract requires `ok=true` + all rows matched for a moms_ledger PASS).
7. `propose_improvement` (proposal 148) — root-caused to `moms_ledger_reconciler.py`'s date parser, `suggested_patch` correctly targets the tool code, not a hallucinated prose fix — ✅
8. `apply_proposal(148, ...)` — gates all `allow`, auto-activated `wrap-v049` (self-improvement loop's own automated low-risk path, not something the Trainer needed to intervene on).
9. Dashboard callback (`POST /api/expense-stored` via `executor_run curl`) — ✅ fired, `returncode:0`, `{"ok":true}`. Payload correctly carries `stored:0, parsed:0, expense_ids:[], duplicate_expense_ids:[], doc_kind:"moms_ledger", vendor:"JetBlue Business Mastercard"`.

## Wrapper defect identified

Not a Mazda/wrapper defect — a **tool-code bug** in `tools/categorizer/moms_ledger_reconciler.py`: its date parser rejects handwritten `MM-DD`-format scheduled dates (e.g. `04-24`) with no year. This is outside the self-improvement loop's normal "wrapper" scope (prompts/instructions/tool descriptions), since it requires an actual code fix to the reconciler script. Mazda's own `apply_proposal` instruction note ("normalize handwritten scheduled_date values... before reconciliation") is not directly actionable by her, since the reconciler parses dates internally from the image, not from an argument she supplies — the real fix has to land in the reconciler's Python source.

## For a human

Recommend a developer patch `moms_ledger_reconciler.py` to accept/normalize bare `MM-DD` scheduled-date tokens (assume current or statement year) rather than raising `ValueError`. No dashboard/DB action needed — Mazda's handling of this run was correct and complete.
