# Mazda Trainer Report

- Scanner: `Window Scanner`
- Dispatch: `2026-07-16T16:32:28Z`
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan.jpg`
- Verdict: `PASS` after coaching

## Checklist

- `load_wrapper_revision` succeeded at `16:32:32Z` with `wrapper_revision=wrap-v010`.
- STEP 0 succeeded via `executor_run` at `16:34:54Z`.
  - `classify_scan.py` returned `doc_type=receipt`, `confidence=0.95`, merchant `Jacob Menninga`.
  - `parse_and_categorize.py --json` returned `transaction_date=2025-01-31`, `merchant_name=Jacob Menninga`, `total_amount=113.50`.
- `check_vendor_key` succeeded at `16:34:58Z`.
  - `vendor_key=jacob_menninga`
  - `recognized=false`
- `check_duplicates` succeeded at `16:35:07Z`.
  - `expense_date=2025-01-31`
  - `amount=113.50`
  - `is_exact_duplicate=true`
- `record_trace` succeeded at `16:35:12Z`.
  - `task_name=document-intake`
  - `trace_id=77`
  - evidence recorded `stored=false`, `expense_id=null`, `is_duplicate=true`
- `judge_trace` succeeded at `16:35:16Z`.
  - verdict: `NEEDS_REVIEW`
  - reason: vendor key not in known-vendor map
- Initial run stopped short of the required categorization and dashboard callback.
- I sent a corrective message telling Mazda to finish `categorizer_main.py` and `POST /api/expense-stored`, and to remember that duplicate detection does not replace those steps.
- Mazda completed the missing work at `16:36:31Z`.
  - `categorizer_main.py` returned `category_id=null`
  - `POST /api/expense-stored` succeeded
  - dashboard event recorded `parsed=1`, `stored=0`, `expense_id=null`

## Wrapper Defect

Mazda prematurely treated the exact-duplicate result as the end of intake and skipped required post-duplicate steps. The wrapper needs to make categorization and dashboard notification explicit even when storage is skipped.

## Lesson Sent

Duplicate detection does not replace categorization or the dashboard callback; even when a receipt is an exact duplicate, still run `categorizer_main.py` on the parsed vendor input and `POST /api/expense-stored` with the duplicate run's actual values and zero stored ids.

## Human Review

- The judge returned `NEEDS_REVIEW` even though the run ended as a proven duplicate, which should be treated as a pass for intake completeness.
- `categorizer_main.py` could not resolve a category because `gemini` CLI was not installed or not on `PATH`.
