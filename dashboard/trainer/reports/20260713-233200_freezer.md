# Trainer Report — Freezer Scanner (manual re-run coaching)

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Scanner**: Freezer Scanner
- **Dispatch time**: 2026-07-13T22:48:39Z (facade `doc_kind=unknown`, note: manual re-run requested to fix a stale duplicate reference)
- **Verdict**: **PASS**

## Context

This was not a fresh scan — it's a manual re-run coaching pass on an AT&T internet
bill scan that a *prior* trainer instance had already walked through earlier in this
same session (22:48–23:04Z). That prior run correctly classified the document
(`classify_scan.py` → receipt, merchant AT&T, confidence 0.95), correctly identified
it as a likely duplicate of an existing AT&T bill, and recorded `trace_id=64` with
`is_duplicate=true`, `expense_id=1501`, `stored=false` — but judged **NEEDS_REVIEW**
because `vendor_key=at_t` was not yet in the known-vendor map.

Since then, two facts changed that made trace 64 stale:
1. The vendor map was fixed: `at_t` → category 124.
2. `expense_id=1501` (the duplicate match cited in trace 64) turned out to be a
   phantom/bad record and was deleted; the real matching expense for this same bill
   is `expense_id=565`.

My job this run was to coach Mazda to re-verify and record a truthful, current trace
rather than leave the stale NEEDS_REVIEW trace as the last word.

## Checklist — evidence from the transcript (23:31:09–23:31:36Z)

| Step | Evidence |
|---|---|
| Re-check vendor key | `check_vendor_key(vendor_key="at_t")` → `recognized: true` (was `false` in the earlier trace) ✅ |
| Correct duplicate reference | `record_trace` evidence JSON: `expense_id: 565`, `problems: ["duplicate of current AT&T bill expense_id 565; earlier cited expense_id 1501 was superseded/deleted"]` ✅ — no longer references the deleted 1501 |
| `record_trace` (task_name=`document-intake`) | Returned `trace_id: 65, idempotent: false, message: "Trace 65 saved"` ✅ |
| `judge_trace(65)` | Returned `{"verdict": "PASS", "failure_type": "none", "reasons": ["all deterministic intake checks passed"]}` ✅ |
| Dashboard notify | `executor_run` → `curl POST /api/expense-stored` with `duplicate_expense_ids:[565], stored:0, expense_ids:[]` → `{"ok": true}` ✅ |
| `propose_improvement` | Not called — correctly skipped, since verdict is PASS, not FAIL |

All required steps for a duplicate-detected PASS run are present with successful tool
returns, not just prose claims.

## Wrapper defect diagnosed

None new this run — this was a data/config fix (vendor map entry + a bad DB record),
not a wrapper defect. The wrapper (Rule 3: "doc_type=receipt is binding, keep going")
worked correctly in the *original* 22:48–23:04Z run; Mazda followed it faithfully both
times.

## Lesson sent to Mazda

Coached her (23:31:09Z message) to: re-run `check_vendor_key`, replace the stale
`expense_id=1501` reference with the correct `565`, re-record a truthful trace,
re-judge, and re-fire the dashboard callback with `duplicate_expense_ids=[565],
stored=0`. Also asked her to record a memory note: **when a previously-cited
duplicate expense_id turns out to have been deleted/superseded, verify the current
expense id against `check_duplicates`/the live DB rather than reusing a stale id from
an earlier trace.**

## Anything a human should look at

- Mazda's final reply doesn't confirm whether she actually wrote the memory note I
  asked for (it only says "Done" + summarizes the 4 tool steps). Not blocking —
  trace/judge/callback are all correct and verified — but worth a spot-check of her
  `system/` memfs on the next unrelated run to confirm the note landed, since a
  repeat of "cite a since-deleted expense_id" would otherwise recur.
- Trace 64 (NEEDS_REVIEW, stale) remains in the evidence store alongside the new
  trace 65 (PASS) — this is expected/append-only behavior, not a bug, but anyone
  auditing trace history for this document should read trace 65 as authoritative.

## Summary

Mazda correctly re-verified vendor recognition (now `recognized=true` under the fixed
map), replaced the stale duplicate reference (deleted `expense_id=1501`) with the
correct current match (`expense_id=565`), recorded a truthful `document-intake` trace
(trace_id=65), received a clean **PASS** from `judge_trace`, and successfully notified
the dashboard with `duplicate_expense_ids=[565]` and `stored=0` — every step is backed
by a successful tool return, not just her prose summary. No wrapper defect surfaced;
this was purely a stale-reference correction after an upstream data fix (vendor map +
deletion of the phantom expense 1501).
