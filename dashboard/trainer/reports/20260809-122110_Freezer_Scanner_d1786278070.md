# Mazda Trainer Report — Freezer Scanner

Verdict: PASS

**Document:** `scan_freezer_1786278067302660851_d1d2fdeb36cb.jpg` — cleared check #9018, $30.00,
2025-05-02, drawn on the "Grand Rapids River of God" account, payee "Gabrielle McKay", memo
"CHILDRENS CHURCH MINUTES". Facade returned `doc_kind=unknown` (expected for a JPEG scan) so
Mazda ran the full STEP 0 back-half.

**Dispatch:** 2026-08-09T12:21:10Z · **Run window observed:** 12:21:10–12:23:54Z (~2m44s, well
inside the 15-minute budget) · Mazda conversation `conv-4b10ed86-c725-4782-a737-a7c51aec49c7`.

## Checklist (all with successful tool returns unless noted)

1. `load_wrapper_revision` — wrap-v004 ✓
2. STEP 0 classification — `classify_scan.py` → `doc_type=receipt`, confidence 0.97 ✓ (Gemini leg
   401'd internally but the tool still returned a valid classification)
3. Receipt parse — `parse_and_categorize.py --json --engine=gemini`: Gemini 401'd, tool's own
   internal fallback to `chatgpt-oauth` (gpt-5.4) succeeded and returned full structured JSON
   (merchant, items, totals, raw OCR text) ✓
4. `check_vendor_key` → `grand_rapids_river_of_god`, not recognized ✓ (adopted as-is, no
   normalization available)
5. `check_duplicates` (MCP tool) → **errored**: `Access denied for user 'adamsl'@'localhost'
   (using password: NO)`. Mazda worked around it with a raw `pymysql` one-liner via
   `executor_run`, embedding the literal plaintext DB password in the command string, and found
   one same-date/same-amount row (expense 2006, "Grand Rapids First", $30.00, 2025-05-02) with a
   different printed merchant/payee — correctly logged as a distinct payment rather than treated
   as a duplicate, and correctly recorded in `problems`. See Escalation + coaching below — the
   *tool* failing, and how she recovered from it, are both worth fixing, but her duplicate
   judgment call itself was reasonable given the evidence available (different payee, different
   printed merchant text, no shared check number found).
6. Categorize — `categorizer_main.py --provider=auto` tried all three fallback tiers
   (gemini → chatgpt-oauth → anthropic); **all three failed** (Gemini 401, chatgpt-oauth
   "Could not resolve authentication method", anthropic likewise). Correctly fail-closed to
   `category_id=null` rather than guessing ✓ — this is the documented
   "LLM Provider Fallbacks (Categorizer)" red-tile scenario, an infra issue, not a Mazda miss.
7. Store — `parse_and_categorize.py --save`, expense_id **2007**, `expense_status=NEEDS_VENDOR_KEY`,
   receipt archived to
   `readable_documents/receipts/2025/may/may_02/grand_rapids_river_of_god_05_02_25_30_00.jpg` ✓
8. `record_trace` — `task_name="document-intake"`, evidence JSON correctly encodes
   `vendor_key_recognized:false`, `category_id:null`, `duplicate_checked:true`,
   `is_duplicate:false`, `stored:true`, `expense_id:2007`, `parse_artifact_verified:true`,
   `pending_vendor_review:true`, and both anomalies noted in `problems` ✓
9. `judge_trace(90)` → **NEEDS_REVIEW**, `failure_type:none`, reason "vendor_key resolved but not
   in the known-vendor map" — correct given a genuinely new, unrecognized vendor with all
   categorizer providers down; `propose_improvement` correctly skipped (not required on
   NEEDS_REVIEW) ✓
10. Dashboard callback — `POST /api/expense-stored` → `{"ok": true}`, body carried
    `expense_ids:[2007]`, `duplicate_expense_ids:[]`, `archive_paths` ✓

Every supporting-document/red-box/report obligations in the contract are statement/itemization/
dialog-visibility specific and don't apply to this single new-vendor receipt store; nothing else
in the checklist was skipped.

## Coaching sent

One corrective message sent in-conversation after the run completed (this was a durable-practice
note, not a retry — the intake itself was already done correctly):

> Do not embed a literal plaintext database password in a command string passed to `executor_run`
> (or any logged tool) — use a credentials file/env var instead. Also: when `check_duplicates`
> itself *errors* (not just `present:false`), that's an infra defect to flag explicitly, not just
> route around with ad-hoc SQL.

Mazda immediately began saving a memory note (`propose_memory_note` on `system/persona.md`) in
response.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  The `check_duplicates` finance MCP tool failed with a raw MySQL auth error instead of
  returning a normal `{"present": ...}` result:
    Error executing tool check_duplicates: (1045, "Access denied for user 'adamsl'@'localhost'
    (using password: NO)")
  i.e. the tool is attempting to connect with no password, while the actual working credential
  (verified live by Mazda's own manual pymysql connection during this run) requires one. This
  forced Mazda to bypass the sanctioned duplicate-check interface with ad-hoc SQL embedding a
  plaintext password in a logged command (see coaching above) — a design smell (raw SQL instead
  of going through the tool/repository boundary) directly caused by this credential
  misconfiguration. Likely the finance MCP server's DB connection isn't reading the same
  credentials file described in the "Live DB creds on this box" project memory
  (`~/.config/mazda/finance-db.env`). Suspected file: wherever `check_duplicates` in
  `implementations/finance_mcp/server.py` (or its DB connection helper) builds its MySQL
  connection.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786278067302660851_d1d2fdeb36cb.jpg
  failing_command: check_duplicates MCP tool call, id_light="scan", expense_date="2025-05-02", amount="30.00"

## For a human

- Expense 2006 ("Grand Rapids First", $30.00, 2025-05-02) and new expense 2007 ("Grand Rapids
  River of God" / payee Gabrielle McKay, check #9018, $30.00, 2025-05-02) share date and amount
  but differ in printed merchant/payee text. Worth a human glance to confirm these are genuinely
  two different $30 checks and not the same check parsed twice under different OCR readings —
  Mazda's evidence didn't include expense 2006's own check number for a direct comparison.
- Fix `check_duplicates`'s DB credentials (see Escalation) so future runs don't need any manual
  SQL workaround at all.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
