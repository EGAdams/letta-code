# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Document:** `scan_freezer_1784760111518864675_8643c19b3ba0.jpg`
- **Dispatch:** 2026-07-22T22:41:56Z
- **Conversation:** conv-deb24ff8-a656-4b84-84f4-b832b2114fa5
- **Verdict: PASS**

## Checklist (all with successful tool returns)

1. `load_wrapper_revision` → wrap-v032, active. ✅
2. Facade returned `doc_kind=unknown` → STEP 0 correctly triggered:
   `classify_scan.py` (executor_run) → `doc_type=receipt`, confidence 0.95, merchant "Consumers Energy" (utility bill payment stub). ✅
   `parse_and_categorize.py --json` → parsed transaction_date 2025-01-23, total $222.65, merchant Consumers Energy. ✅
3. `check_vendor_key("consumers_energy")` → normalized/fuzzy-matched to `consumers_7996`, recognized=true. Mazda adopted the normalized key downstream. ✅
4. `check_duplicates` (date 2025-01-23, amount 222.65) → not a duplicate. ✅
5. `categorizer_main.py` (executor_run, provider=gemini) → `category_id=3` (valid, non-null). ✅
6. Store: `parse_and_categorize.py --save` → `expense_id=1521`. ✅
7. `itemize_existing_expense` → single line item ("Amount Due", $222.65) reconciles exactly to the charge (`difference=0.00`), `itemization_parent_id=1521 == expense_id`, one child id `[1522]`, `itemization_reconciled=true`. Correct per contract. ✅
8. `record_trace(task_name="document-intake")` — evidence JSON matches reality: `doc_kind=receipt`, `vendor_key=consumers_7996`, `vendor_key_recognized=true`, `category_id=3`, `duplicate_checked=true`, `is_duplicate=false`, `stored=true`, `expense_id=1521`, itemization fields consistent. One non-blocking `problems` note: a warning that `source_file` couldn't be updated on the receipt metadata row — cosmetic, does not affect the finance data. ✅
9. `judge_trace(trace_id=128)` → **PASS**, failure_type=none. ✅
10. Dashboard callback: `POST /api/expense-stored` → `{"ok":true}`. Confirmed via `/api/expense-stored-events?since=1784760116`: event's `conversation_id` and `dispatched_at` match this exact dispatch, `expense_ids=[1521]`, `stored=1`, `duplicate_expense_ids=[]`. ✅

## Wrapper defect diagnosed

None. This run followed the full contract cleanly, including the STEP 0 vision-classification branch (facade returned `unknown`) and the itemization step for a single-line-item receipt.

## Lesson sent to Mazda

None needed — no coaching required.

## For a human

Nothing to flag. Minor cosmetic warning in the trace (`source_file` update on receipt metadata failed) is not user-facing and did not affect storage or categorization; not worth pursuing.
