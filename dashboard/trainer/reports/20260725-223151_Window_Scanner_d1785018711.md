# Mazda Trainer Report

- Scanner: `Window Scanner`
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785018707102676935_ca5d6ea799c7.jpg`
- Dispatch time: `2026-07-25T22:31:51Z`
- Conversation: `conv-734b1eaf-d1e3-46e9-84b9-bef2104fda2d`
- Verdict: `PASS`

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded with `wrap-v051`.
- `classify_scan.py` ran via `executor_run` and returned `doc_type=receipt`, `confidence=0.99`, `merchant=Walgreens`.
- `parse_and_categorize.py --json --write-parsed-json ...` ran via `executor_run` after the classifier fallback path.
- `check_vendor_key` returned `recognized=true` and normalized the vendor key to `walgreens`.
- `check_duplicates` returned `is_exact_duplicate=true` with canonical duplicate `expense_id=1580`.
- `categorizer_main.py` ran via `executor_run` and returned `vendor_key=walgreens`, `category_id=378`.
- `record_trace` succeeded with `task_name="document-intake"` and trace id `211`.
- `judge_trace(211)` returned `PASS`.
- Dashboard callback ran via `curl POST /api/expense-stored` and returned `{"ok": true}`.
- The stored event confirms duplicate-only handling: `stored=0`, `expense_id=null`, `duplicate_expense_ids=[1580]`.

## Notes

- No coaching message was needed.
- No wrapper defect was identified in this run.
- This was a duplicate-only receipt intake, so no new expense was stored and no `report.html` was expected.
