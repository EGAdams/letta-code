# Mazda Trainer Report — Window Scanner

Verdict: PASS

**Document:** `window_scan_1785687835512132112_78e6445157c3.jpg` (Window Scanner)
**Dispatch:** 2026-08-02T16:23:58Z, conversation `conv-b7cc6d8d-ab3a-41f8-b2ba-121ef602e093`
**Facade result:** `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required.

## Checklist (all confirmed by successful tool returns)

1. `load_wrapper_revision` — `wrap-v071` loaded. ✅
2. STEP 0 self-classification — `classify_scan.py` → `doc_type=receipt, confidence=0.95, merchant="Grandville Eye Care, P.C."`. ✅
3. Receipt parse — `parse_and_categorize.py --json --engine=gemini` → transaction_date 2025-03-04, amount 93.00, merchant "Grandville Eye Care, P.C.". ✅
4. `check_vendor_key` — supplied `grandville_eye_care_p_c` normalized to known `grandville_ago_42` (fuzzy match), `recognized:true`; Mazda adopted the normalized key. ✅
5. `check_duplicates` — `is_exact_duplicate:false, in_database:false`. ✅
6. Categorization — `categorizer_main.py` (gemini provider) → `category_id:381`. ✅
7. Save — `parse_and_categorize.py --save --category-id=381` → expense_id **1986** created, no `halted`. ✅
8. Itemization — `itemize_existing_expense` correctly refused: `itemizable:false, reason:"only one line item; a single line equal to the charge is standalone, not itemized"`. Correct fail-closed outcome, row remains standalone. ✅
9. `record_trace` — `task_name="document-intake"`, evidence JSON matches reality exactly (`doc_kind:receipt`, `vendor_key:grandville_ago_42`, `vendor_key_recognized:true`, `category_id:381`, `duplicate_checked:true`, `is_duplicate:false`, `stored:true`, `expense_id:1986`, `intake_halted:false`). Trace 345 saved. ✅
10. `judge_trace(345)` → **PASS**, `failure_type:none`. ✅
11. Dashboard callback — `/api/expense-stored-events?since=1785687838` shows a matching event: `conversation_id` and `dispatched_at` both match this dispatch, `expense_id:1986`, `archive_paths` correct (`receipts/2025/march/march_04/grandville_eye_care_p_c_03_04_25_93_00.jpg`). ✅

## Supplementary checks (Trainer's own verification)

- **Dialog visibility**: `POST /api/supporting-documents` for expense 1986 returns `receipt_url` populated, `documents[0]` = `{type:receipt, label:"View Receipt", available:true}`; the other three evidence types correctly `available:false` (none exist for this receipt-only intake). ✅
- **Red box**: `GET /supporting-document/1986/receipt` returned bytes that differ from the raw incoming-scan file (569342 bytes, annotated copy served, not the original path unchanged) — confirms the row is boxed, not opened unhighlighted. ✅

No wrapper defect, no application defect. No coaching was needed — this was a clean, correctly-classified, correctly-declined-itemization single-item receipt run.

## For a human

Nothing to flag.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
