# Trainer Report — Freezer Scanner

**Dispatch:** 2026-08-11T22:28:57Z
**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786487332850037030_cf01d7dad4f0.jpg`
**Mazda conversation:** conv-04bca703-ac37-4096-88c3-f432c8f30fe8

Verdict: PASS

## What happened

Facade returned `doc_kind=unknown` (expected for a JPEG scan), so Mazda correctly ran
STEP 0 classification herself.

1. `load_wrapper_revision(agent_name="Mazda")` — ok, wrap-v004 (22:29:01).
2. STEP 0a classify-only `executor_run` → `classify_scan.py` → `{"doc_type":"other","confidence":0.98,"merchant":"DR DANIEL ROBERTS PLLC","reason":"This is a dental claim/EOB-style benefits summary, not a receipt, invoice, bank statement, or tax form."}` (22:29:20).
3. Per the dispatch's ROUTING PRECEDENCE ("Only explicit `doc_type=other` is unsupported"),
   Mazda correctly took **no further action** — no STEP 0b parse, no `check_vendor_key`,
   no `check_duplicates`, no categorizer, no store. This is the correct branch for a
   genuinely unsupported document type (an EOB/benefits summary is not a receipt, invoice,
   statement, or tax form).
4. `record_trace` (task_name="document-intake") with honest evidence: `doc_kind:"other"`,
   `vendor_key:null`, `category_id:null`, `duplicate_checked:false`, `stored:false`,
   `expense_id:null`, and a clear `problems` entry explaining the document is unsupported
   (22:29:30, trace_id 106).
5. `judge_trace(106)` → **FAIL**, `failure_type:missed_vendor_key`, reasons:
   `failed: vendor_key_resolved / category_resolved / duplicate_check_ran / expense_stored`
   (22:29:34).
6. `propose_improvement` + `apply_proposal` (22:29:58–22:30:03) — activated cleanly
   (all gates `allow`).
7. STEP 8 dashboard callback `POST /api/expense-stored` with `stored:0`, empty id lists,
   `outcome:"EVIDENCE_ATTACHED"` — ran and returned `{"ok":true}` (22:29:51).

## Grading

Mazda's actual intake-contract behavior for this document was correct: she classified
accurately (dental EOB, `doc_type=other`, confidence 0.98), correctly withheld
vendor/category/duplicate/store work (none of that applies to an unsupported document
type), recorded truthful trace evidence, judged, proposed+applied an improvement, and
notified the dashboard. Nothing in her transcript is a wrapper defect.

The `judge_trace` **FAIL** itself is not evidence of a Mazda mistake — the intake rubric
unconditionally requires `vendor_key_resolved`/`category_resolved`/`duplicate_check_ran`/
`expense_stored` regardless of `doc_kind`, so any correctly-identified unsupported
document (`doc_kind=other`) will always score FAIL even though nothing should have been
attempted. Mazda's own `propose_improvement` diagnosed this exact rubric gap correctly.
Her `apply_proposal` note ("ensure the intake rubric does not require vendor/category/
duplicate/storage checks" for `doc_kind=other`) is a wrapper-level instruction and cannot
actually change the deterministic judge/evaluator code — the rubric itself needs a code
fix, which is out of Mazda's reach. See Escalation below.

No lesson was sent to Mazda in this run — none was needed.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  The `document-intake` judge rubric (self-improvement evaluator/verdict-judge, likely in
  `tools/self_improving_agent`/`agent_self_improvement`'s evaluation factory) unconditionally
  scores `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, and
  `expense_stored` as required checks, even when the recorded trace evidence has
  `doc_kind:"other"` (a correctly-classified unsupported document per the dispatch's own
  routing precedence: "Only explicit doc_type=other is unsupported"). This produces a false
  FAIL (`failure_type=missed_vendor_key`) on trace_id 106 for a dental claim/EOB document
  that Mazda correctly declined to process further. Expected: when `doc_kind=other`, the
  rubric should skip the vendor/category/duplicate/store checks and score the run PASS
  (or a dedicated "correctly rejected as unsupported" verdict) based on whether
  classification + honest trace + judge + callback all completed. Mazda's own
  `apply_proposal` (proposal_id 56) can only edit her wrapper instructions, not this
  rubric's code, so this recurs on every future unsupported-document scan until fixed
  in code.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786487332850037030_cf01d7dad4f0.jpg
  trace_id: 106
  failing_call: judge_trace(trace_id=106)

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
