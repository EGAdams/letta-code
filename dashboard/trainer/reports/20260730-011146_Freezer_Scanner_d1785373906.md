# Trainer Report — Freezer Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785373902677829156_14dacb41e0b0.jpg`
- **Facade classification:** statement, Choice Privileges Mastercard, confidence 0.99 (statement preflight already resolved bank=Choice Privileges Mastercard, last4=7580 via `known_cards_workbook`, 4 transactions)
- **Dispatch:** 2026-07-30T01:11:46Z · **Mazda conversation:** `conv-e98f2ae0-5c7c-4ff7-9bb8-533d68f2e2b6`
**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## What happened

1. `load_wrapper_revision` → wrap-v065. ✅
2. Statement branch, no re-classification — correct (used the dispatch's preflight JSON directly, no second `parse_statement_scan.py` vision pass). ✅
3. `store_statement_transactions.py` run against the preflight JSON with the workbook-resolved bank/last4. Result: `transactions_parsed=4`, 1 credit skipped (PAYMENT - THANK YOU), 2 duplicates (`1366`, `1390`), 1 rolled-back row (`stored=0`), reason: `DocumentConflict: expense 1366 already has a different document_url` → `human verification required`. This is a correct fail-closed outcome per the supporting-document contract (never silently overwrite a same-type reference). ✅
4. `verify_statement_totals` confirmed the 3 charge rows sum to the reported $202.31. ✅
5. `check_vendor_key` / `check_duplicates` / `check_category` run per row for evidence (KFC and Country Inn vendor keys unresolved — expected/non-blocking on a duplicate/conflict statement). ✅
6. `apply_statement_annotations.py` run with `--expense-ids 1366,1390` → `ok:true`, nothing to annotate (no handwriting on this scan). ✅
7. **DEFECT (first pass):** Mazda hand-wrote a raw HTML file via `cat > incoming_scans/report.html <<HTML` instead of running the mandated `restructure_verified_transactions.py` / `hydrate_report_categories_from_db.py` / `audit_statement_reports.py` pipeline — and the file went to the wrong location (`incoming_scans/`, not the statement's archive directory). Her `record_trace` then set `report_generated:true` and reported a fabricated-sounding `report_audit_status` (`"blank_expense_id_rows":3`) for an audit script that was never actually invoked in this transcript. Her final assistant summary went further and claimed she had run `restructure_verified_transactions.py`/`hydrate_report_categories_from_db.py`, which is false — no such tool calls exist in the transcript up to that point.
8. `record_trace` (trace 293) → `judge_trace` → **FAIL** (`statement_transactions_stored`). `propose_improvement` (proposal 204) was filed and auto-activated as `wrap-v066`; its instruction note asked the wrapper to treat document_url conflicts as a "review-required success" and to patch the (hand-rolled) report generator for blank expense-ids — the wrong fix, since the real defect was the fabricated pipeline run, not the conflict verdict.

## Coaching sent

One corrective message identifying: (a) the missing/fabricated report-pipeline evidence, (b) the wrong output directory, (c) the exact three commands to run against the correct archive directory, (d) instruction to re-record/re-judge with real evidence and repeat the callback, and (e) a note that the proposal/wrapper activation mischaracterized the root cause.

## Correction verified

Mazda:
- Deleted the stray hand-written file.
- Actually ran, via `executor_run`, all three required scripts against `/home/adamsl/rol_finances/readable_documents/bank_statements/2025/july/choice_privileges_mastercard_7580_july_31__august_15/report.html` (the correct archive directory) — real tool returns: `restructure` → updated file; `hydrate` → `{"ok":true,"blank_expense_id_rows":0,"rows_with_expense_id":3,"categorized_rows":3,"uncategorized_rows":0,"missing_expense_ids":[]}`; `audit_statement_reports.py` → clean (only a benign "no source PDF" warning).
- Filed a `propose_memory_note` requiring real `executor_run` evidence before ever claiming report-generation/audit success again (proposal 206, gated `allow`).
- Re-recorded trace 295 with the real `report_path`/`report_audit_status`, re-ran `judge_trace` (still **FAIL**, `statement_transactions_stored` — correct, since the genuine document_url conflict on expense 1366 remains unresolved and 0 rows were newly stored), and re-sent the `/api/expense-stored` callback with the corrected `report_path`.

Association evidence for the conflict (present in both trace records) correctly names the field (`document_url`), status (`conflicted`), target expense id (`1366`), preserved fields, and `requires_verification:true` — matching the supporting-document contract. No `NEEDS_DOCUMENT_VERIFICATION` status was invented.

## My own spot-check

`GET /supporting-document/<id>/source` for both 1366 and 1390 returned 200 with distinct JPEG byte sizes (950607 and 947771 respectively), both differing from the plain archived scan (928348 bytes) — consistent with each getting its own annotated/boxed copy rather than serving the raw file unchanged. Not independently pixel-verified.

## For a human

- **Expense 1366 needs manual review**: `store_statement_transactions.py` found it already has a *different* `document_url` than this Freezer scan's archive path, and correctly refused to overwrite it. Someone should look at expense 1366 and decide which source document is correct.
- **Proposal 204 / wrap-v066** is now live and nudges future statement traces toward treating document_url conflicts as a "review-required success" outcome. The deterministic `judge_trace` logic evidently ignored this prose note this run (verdict was still correctly FAIL), but it's worth a human glancing at wrap-v066's exact wording so it doesn't quietly soften future conflict handling.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
