# Trainer Report — PDF intake (prime_chase_5783_whole_year_2025)

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

- Scanner: PDF intake
- Document: `/home/adamsl/rol_finances/readable_documents/bank_statements/january/prime_chase_5783_whole_year_2025/report.html`
- Dispatch timestamp: 1787234656 (2026-08-20T14:04:16Z)
- Mazda conversation: `conv-9c2c3b87-e6a0-4b39-855c-456e0b7ec8b3`
- Facade result: `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` — STEP 0 classification would have been required had the run started.

## What I observed

`GET /v1/conversations/conv-9c2c3b87-e6a0-4b39-855c-456e0b7ec8b3` shows:

- `created_at` / `updated_at`: 2026-08-20T14:04:16Z / 14:04:17Z — no activity since creation.
- `in_context_message_ids`: exactly one message.
- `GET .../messages?limit=60` returns exactly **one** message: a `system_message` (the standard Letta Code persona/system prompt), timestamped identically to conversation creation. There is no `user_message` carrying the dispatch text, and no `assistant_message` or `tool_call_message` from Mazda at all.

In other words, the conversation was created and the system prompt rendered, but the actual dispatch (the pipeline instructions this Trainer received in its own user message) was never posted into this conversation, so Mazda never had anything to respond to. This is not a case of Mazda missing steps or stopping early — she never received a turn.

I polled this conversation repeatedly over roughly 8+ minutes of active waiting; by the time of this report, **~23 minutes** had elapsed since the dispatch timestamp (well past the 15-minute deadline), with the message count unchanged at 1 the entire time.

I also checked `GET /api/expense-stored-events?since=1787234656` — empty, consistent with no work having occurred.

## Checklist against the contract

Every required step (load_wrapper_revision, STEP 0 classification, investigate, categorize, store, record_trace, judge_trace, dashboard notify) is **absent** — none could have run because Mazda never received the dispatch turn.

## Wrapper defect diagnosis

This is not a wrapper defect in Mazda's prompts/tools/memory — there is nothing for her to have done differently. This is an **infrastructure problem**: the dispatcher (whatever process creates the conversation and posts the dispatch user message to Mazda) created the conversation shell but failed to deliver the dispatch message itself. No coaching message was sent to Mazda, since there is no defect in her behavior to correct — she was never invoked.

## For a human

- The dispatch pipeline for this scanner run (PDF intake / `prime_chase_5783_whole_year_2025`) needs investigation: the conversation `conv-9c2c3b87-e6a0-4b39-855c-456e0b7ec8b3` exists against the correct Mazda agent (`agent-6b536cf4-ec88-4290-b595-fed21d14bd8e`) but only ever received the system prompt, never the dispatch user message.
- Recommend re-dispatching this document once the delivery issue is fixed. No data was written, so a clean re-run is safe.
- Note the input path itself is unusual: the "Scanned image" path supplied is a `report.html`, not an image — worth double-checking the dispatcher pointed at the right source file for this document once redelivery is investigated.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
