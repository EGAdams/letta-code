# Trainer Report — Freezer Scanner

- **Dispatch:** 2026-07-29T13:01:10Z
- **Scanner:** Freezer Scanner
- **Document:** `scan_freezer_1785330063408672399_e4e29af60f85.jpg`
- **Mazda conversation:** `conv-d6423b25-796b-4359-a5c7-18130a2cee89`
- **Run duration:** ~3m17s (13:01:17 wrapper load → 13:04:27 final assistant message)
- **Verdict: PASS**

## Facade result
`doc_kind: unknown, confidence: 0.0` → STEP 0 self-classification correctly required and performed.

## Step-by-step checklist (all with successful tool returns)

1. `load_wrapper_revision` → `wrap-v062`, active. ✓
2. STEP 0 `classify_scan.py` → `doc_type=receipt, confidence=0.99`, reason: "paid invoice — handwritten 'love offering' note dated 4-8-25, signature/initial, total $67.25 circled." Correctly a receipt (paid invoice), not a statement or Mom's ledger — no letterhead/account-cycle structure, and the circle marks the **total**, not individual item rows, so `expense_scope=full_receipt` (not `marked_items`) is the right call. ✓
3. `parse_and_categorize.py --json` → merchant "Jacob Menninga," date 2025-03-31, total $67.25, 5 line items (sermon-editing services), parse artifact written to `/tmp/mazda_receipt_59501182b5a6.json`. ✓
4. `check_vendor_key` → `jacob_menninga`, recognized=true; adopted. ✓
5. `check_duplicates` → `is_exact_duplicate=false, in_database=false`. ✓ **See anomaly below.**
6. `categorizer_main.py --provider=auto` → `category_id=197` (nonzero, valid). ✓
7. Store: `parse_and_categorize.py --save --category-id=197 --parsed-json ... --engine=gemini` → `success:true, expense_id:1678, parse_artifact_verified:true, expense_scope=full_receipt, duplicate:false`. Scope/date/amount/merchant all match STEP 0 + parse evidence — no drift. ✓
8. `itemize_existing_expense` → 5 children (1679–1683), line totals 19.5+18.0+9.75+4.5+15.5 = 67.25 (reconciles exactly to parent charge), `itemization_reconciled=true`, `itemization_parent_id=1678`. ✓
9. `record_trace` → `task_name="document-intake"`, trace_id 279, full evidence JSON (doc_kind=receipt, vendor_key_recognized=true, category_id=197, stored=true, expense_id=1678), `problems` correctly surfaces a real store-script warning (see below). ✓
10. `judge_trace(279)` → **PASS**, failure_type=none. ✓
11. Dashboard callback `POST /api/expense-stored` → confirmed via `GET /api/expense-stored-events?since=1785330070`: event present with matching `conversation_id` and `dispatched_at`, `expense_ids:[1678]`. ✓

All 11 contract steps present with successful tool returns; nothing relied on prose-only claims.

## Anomalies for human review (no coaching sent — run is a genuine PASS, nothing to correct)

1. **Garbled `check_duplicates` tool-call arguments.** The `description` argument for that one call contained injected-looking noise — Thai/Chinese characters and literal text `analysis to=functions.executor_run` — consistent with the cheap mini model leaking raw reasoning-model tokens into a tool argument. The call still succeeded and returned a sane result, and the corrupted text never propagated downstream (all later steps — categorizer, save, trace, callback — used the clean description "Jacob Menninga"). No data was corrupted this run, but worth watching for a recurrence where such noise reaches a field that actually gets persisted (e.g. `description` on the store call itself).
2. **Store-script warning:** `Could not update source_file to /home/adamsl/rol_finances/readable_documents/receipts/2025/march/march_31/jacob_menninga_03_31_25_67_25.jpg` — the receipt file was physically moved to the archive path, but the DB update of `source_file`/`receipt_url` for that move apparently failed. Mazda faithfully surfaced this in `problems` rather than hiding it (correct behavior). Not verified by the Trainer whether "View Receipt" for expense 1678 still resolves — the dashboard's receipt-lookup fallback chain (date+amount → stem-flex → basename) should cover this, but a human may want to confirm `receipt_url`/`source_file` is consistent in the DB for expense 1678.

## Summary

This Freezer Scanner run (Jacob Menninga sermon-editing receipt, $67.25, paid via "love offering") was processed correctly end-to-end: self-classification, parsing, vendor/duplicate checks, categorization, save, itemization (5 line items reconciling exactly), trace recording, judging, and the dashboard callback all completed with successful tool returns, and the judge verdict of PASS is well-supported by the evidence. No coaching was necessary. Two non-blocking anomalies are flagged above for a human to keep an eye on: garbled (but harmless) tool-call text possibly from model output leakage, and a store-script warning about updating `source_file` on the moved receipt file that didn't block storage but should be spot-checked.
