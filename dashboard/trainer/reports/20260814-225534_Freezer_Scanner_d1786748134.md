# Trainer Report — Freezer Scanner

**Scanner:** Freezer
**Document:** `.../incoming_scans/scan_freezer_1786748130216314543_9c8b9d1e0f82.jpg`
**Dispatch:** 2026-08-14T22:55:34Z
**Mazda conversation:** conv-376752b1-56c3-41f6-b5d3-91f7254e14b5
**Run duration:** ~2m21s (22:55:35 → 22:57:56), completed well within the 15-minute window — no coaching needed, no polling required.

**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

## What the document actually is

A paid gas-pump card slip: `DATE 5/29/23, TRAN#90592, PUMP#06, PRODUCT: BLUE, GALLONS: 14.884,
PRICE/G: $3.269, FUEL SALE $48.66, CREDIT $48.66, MASTERCARD ****4882`. **No merchant/station name
is printed anywhere on the slip** — confirmed independently by both `classify_scan.py`
(`merchant: null`, confidence 0.98) and the full-vision parse (`merchant_name: "null"`). The only
identity-adjacent token is a card-network "beneficiary hint" (`pmcar`), which Mazda correctly
treated as account metadata, not vendor evidence, per her own description field.

## Step-by-step checklist (all with successful tool returns unless noted)

1. `load_wrapper_revision` → wrap-v079. ✓
2. Facade returned `doc_kind=unknown` → STEP 0 required. `classify_scan.py` → `doc_type=receipt`,
   confidence 0.98. ✓
3. `parse_and_categorize.py --json --engine=gemini --write-parsed-json`: Gemini hit its free-tier
   429 quota, and the tool **correctly fell back to `chatgpt-oauth`** (`model_provider:
   "chatgpt-oauth"`, `model_name: "gpt-5.4"`) and produced a verified parse artifact
   (`/tmp/mazda_receipt_944f51124320.json`) with real OCR'd raw text confirming no merchant line
   exists. ✓
4. `check_vendor_key(vendor_key="null")` → correctly `recognized:false`. ✓
5. `check_duplicates` with the real extracted date (`2023-05-29`, not a placeholder) → no match. ✓
6. `categorizer_main.py --provider=auto` → tried `gemini` → `chatgpt-oauth` → `anthropic`, all
   returned nothing useful given `vendor_key='null'`; final leg errored with "Could not resolve
   authentication method" for the anthropic tier. Correctly fail-closed to `category_id: null`. ✓
   (Anthropic-leg auth error is a possible categorizer-fallback config gap — flagged below, but
   moot here since there was no vendor text for any provider to research.)
7. **Store**: `parse_and_categorize.py --save --parsed-json <verified artifact> --engine=gemini`
   (no `--invoice`, correct — this is a receipt). Reused the exact STEP 0 artifact
   (`parse_artifact_verified: true`), did **not** re-run vision, and did **not** invent a merchant.
   The tool itself hard-rejected: `"error": "A verified merchant/counterparty is required before
   saving"`. This is the current documented policy for receipts (unlike statements, which get a
   `NEEDS_VENDOR_KEY` placeholder) — correct fail-closed behavior given wrap-v079. ✓
8. `record_trace(task_name="document-intake", ...)` — evidence matches reality exactly:
   `stored:false`, `expense_id:null`, `vendor_key:null`, `pending_vendor_review:true`, `problems`
   accurately describe the merchant gap. ✓
9. `judge_trace` → `FAIL / missed_vendor_key` (rules: `vendor_key_resolved` failed,
   `expense_stored` failed). Consistent with current documented rules for receipts. ✓
10. `propose_improvement` (required on FAIL) → filed proposal 256: extend the statement branch's
    `NEEDS_VENDOR_KEY` pending-review pattern to merchantless-but-otherwise-valid receipts instead
    of losing the document. ✓
11. `apply_proposal` → auto-activated as wrap-v080 (all 4 gates: safety/cost/regression/usefulness
    = allow). Not a Mazda decision; this is the self-improvement framework's own gating. ✓
12. Dashboard notify (`/api/expense-stored`) — sent **after** trace/judge, `stored:0`,
    `status:"awaiting_vendor_review"`, correct association metadata. ✓ Confirmed received:
    `GET /api/expense-stored-events?since=1786748134` shows the event with matching
    `conversation_id` and `dispatched_at`.

## Assessment

Mazda executed every required step, in order, with correct tool returns, and did not invent a
merchant or misrepresent the outcome. The FAIL verdict is not a Mazda execution mistake — it is
the correct, designed consequence of a receipt that genuinely prints no merchant name, under the
current (receipt ≠ statement) policy that a resolvable vendor is required before storage. She
self-diagnosed the real problem (a merchantless-but-valid receipt currently has no durable home —
it isn't archived to `readable_documents/receipts/` and isn't stored as a reviewable placeholder,
so it risks disappearing) and filed a proposal for it herself. No coaching message was needed or
sent.

## Note for a human (not an Escalation — unverified suspicion, not observed evidence)

I could not verify this myself (Bash allowlist forbids reading `rol_finances` source), but the
`merchant_required` rejection in `parse_and_categorize.py --save` looked like a hard validation in
the tool's own code, not a heuristic a wrapper instruction can route around. Proposal
256/wrap-v080's instruction note tells Mazda to save such receipts as a `NEEDS_VENDOR_KEY`
placeholder using "a neutral system-generated identity" — if the underlying `--save` code path has
no actual flag/branch for that (and just hard-rejects on missing merchant regardless of prompt
instructions), the next merchantless-receipt run will hit the identical rejection despite the
"activated" wrapper revision, and re-loop into another FAIL/propose cycle. Worth a human spot-check
of `parse_and_categorize.py`'s `--save` argument surface before trusting wrap-v080 to actually
change behavior. Separately, this fuel receipt's physical/scanned copy currently exists only under
`incoming_scans/` (unique per-scan filename, so not at *immediate* overwrite risk) — not under the
durable `readable_documents/receipts/` root — so it should be manually reviewed/filed before that
staging copy is ever cleaned up.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
