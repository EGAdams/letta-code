# Trainer Report — Window Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785062385339770042_48743fac7a17.jpg`
- **Dispatch:** 2026-07-26T10:39:50Z
- **Mazda conversation:** `conv-c724c212-0d30-438a-be07-ede6498cb292`
- **Verdict: PASS** (process-compliant; the run's own FAIL judgment was correct and fail-closed, not a Mazda defect)

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 10:39:53 — `wrap-v056` loaded | ✓ |
| STEP 0 classification (facade returned `doc_kind=unknown`) | `executor_run classify_scan.py` @ 10:42:00 → `doc_type=moms_ledger, confidence=0.9` (credit-card statement annotated with personal notes/category shorthand) | ✓ |
| Routed to Mom's-ledger reconciliation branch | `executor_run moms_ledger_reconciler.py --image ...` @ 10:47:02, `vision_provider=codex-cli` (correct — Gemini-exhaustion fallback) | ✓ |
| Reconciler result | `ok:false`, `transactions_read=30`, `matched=27`, `categories_confirmed=[]`, `categories_applied=[]`, `unmatched=3` (2 `not_found`: "ROKU FOR FUBO…" and "PAYMENT - THANK YOU" [a credit/payment row, not an expense]; 1 `ambiguous`: "COMFORT INN…" → ids 642/1479) | Correctly fail-closed — no guessing on ambiguous/missing rows |
| `record_trace` | trace_id 233, `task_name="document-intake"`, `doc_kind=moms_ledger` | ✓ |
| `judge_trace` | FAIL, `failure_type=missed_duplicate`, reasons `failed: ledger_rows_matched`, `failed: ledger_categories_reconciled` — matches reality (not every row matched, zero categories applied) | ✓ correct verdict |
| `propose_improvement` | proposal 171 — suggests a bounded retry/normalizing fallback for `ok:false` moms_ledger runs before failing | ✓ (required on FAIL) |
| `apply_proposal` | activated proposal 171 → new wrapper `wrap-v058` (all 4 gates: allow) | Extra self-improvement action Mazda takes autonomously; not required by the Trainer contract but not a violation |
| Dashboard callback | `POST /api/expense-stored` @ 10:47:42 → `{"ok":true}`; confirmed live via `/api/expense-stored-events` with `duplicate_expense_ids` = the 27 matched ids, `expense_ids:[]`, `stored:0`, `parsed:30`, correct `conversation_id`/`dispatched_at` | ✓ |

## Assessment

The facade returned `doc_kind=unknown`, so Mazda correctly ran STEP 0 classification herself, got `doc_type=moms_ledger`, and routed to `moms_ledger_reconciler.py` per the explicit-classifier-routing rule. The reconciler genuinely could not resolve 2 rows (one of which — "PAYMENT - THANK YOU" — is a credit-card *payment*, not an expense, so a real non-match is expected there) and found one true ambiguous match (two existing expense rows with the same merchant). It also confirmed/applied zero categories, since none of the matched rows carried the ledger's shorthand category marks this run. Per the manual, `moms_ledger_reconciler.py` must return `ok=true` with every row matched for PASS — a partial match is correctly a FAIL, not something Mazda should have forced through by guessing.

Mazda then completed the full contract: recorded the trace, judged it FAIL, filed a proposal, and sent the required dashboard no-store callback naming every matched expense id. No coaching was needed — there was no wrapper defect to correct; the reconciler behaved exactly as the fail-closed design requires given genuinely unresolved input.

## Nothing further needed

No message was sent to Mazda. The run is complete and matches the contract; the `missed_duplicate` failure_type is a slightly loose label for "unmatched ledger rows" but is a reasonable characterization within the matching/duplicate-detection family and doesn't rise to a defect worth a corrective message.
