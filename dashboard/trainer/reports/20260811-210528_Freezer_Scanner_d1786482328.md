# Trainer Report — Freezer Scanner

**Verdict: PASS**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1786482318263844604_99e502237e9c.jpg` → USPS receipt, 2025-03-14, $10.19
- Dispatch: 2026-08-11T21:05:28Z
- Conversation: `conv-d7e60d47-8be5-4347-a53c-9eb73079ed5b`
- Run duration: ~2.5 minutes (21:05:28 → 21:08:05), no coaching needed

## Contract checklist

1. `load_wrapper_revision` — ✅ wrap-v004 (21:05:35)
2. STEP 0 (facade returned `doc_kind=unknown`) — ✅ `classify_scan.py` correctly identified
   `receipt`, merchant USPS, confidence 0.96. `parse_and_categorize.py` ran; Gemini 401'd,
   fell back to ChatGPT OAuth (`gpt-5.4`) successfully. Parse returned two line items
   (`1st Class Pkg` $6.00, `Ground Adv` $4.19), `expense_scope=full_receipt`, both
   `selected_for_expense=true` — no marked/circled selection on this receipt, correct scope.
3. Investigate — ✅ `check_vendor_key` recognized `united_states_postal_service`.
   `check_duplicates` **errored** (MySQL access-denied — see Escalation); Mazda did not
   silently skip it — she ran an equivalent authenticated duplicate query via `executor_run`
   with correct DB creds, found no existing match, and documented the substitution in her
   `problems` evidence. Acceptable workaround, correctly disclosed.
4. Categorize — ✅ `categorizer_main.py` (gemini tier) → category 144 "Postage",
   `check_category` confirmed `matches_vendor=true`.
5. Store — ✅ `parse_and_categorize.py --save` → `expense_id=2012`, receipt archived at
   `readable_documents/receipts/2025/march/march_14/united_states_postal_service_03_14_25_10_19.jpg`.
   `itemize_existing_expense` was correctly attempted (2 line items summing exactly to
   $10.19) but **errored** with the same MySQL access-denied fault as `check_duplicates`
   (see Escalation). Mazda recorded this honestly (`itemized:false`,
   `itemization_reconciled:false`, `problems` entry), leaving expense 2012 STANDALONE
   at the header category rather than guessing a split — correct fail-closed behavior
   given the tool itself was broken, not a business-logic refusal.
6. `record_trace` — ✅ `task_name="document-intake"`, trace_id 100, full evidence JSON
   including the two tool-auth problems.
7. `judge_trace` — ✅ PASS, `failure_type=none`.
8. `propose_improvement` — n/a (no FAIL verdict).
9. Dashboard notify — ✅ `POST /api/expense-stored` returned `{"ok": true}` with
   `expense_id=2012`, `expense_ids=[2012]`, correct `conversation_id`/`dispatched_at`.

## Independent verification (Trainer-side)

- `POST /api/supporting-documents {"expense_id":2012}` → `receipt_url` available,
  `View Receipt` action present; `document_url`/`scanned_statement_url`/`moms_ledger`
  correctly absent (none apply to a plain receipt).
- `GET /supporting-document/2012/receipt` served 345244 bytes vs. 338622 bytes / different
  md5 on the raw archived file — confirms the red-box annotated copy is being served, not
  the unannotated original. Red box / dialog visibility contract satisfied.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  The `check_duplicates` and `itemize_existing_expense` finance-MCP tools both fail with
  `OperationalError: (1045, "Access denied for user 'adamsl'@'localhost' (using password: NO)")`.
  Both appear to connect to MySQL without credentials (default local-socket auth), unlike
  every other DB-touching call in this same run (`categorizer_main.py`,
  `parse_and_categorize.py --save`, and Mazda's own manual duplicate-check workaround via
  `executor_run`), which all explicitly pass `DB_HOST=127.0.0.1 DB_PORT=3306
  NON_PROFIT_USER=adamsl NON_PROFIT_PASSWORD=... NON_PROFIT_DB_NAME=nonprofit_finance` and
  succeed. This is not a Mazda wrapper defect — she used both tools correctly and, when they
  failed, substituted an authenticated equivalent for the duplicate check and left the
  itemization fail-closed (standalone, undivided) rather than guessing. Net effect this run:
  expense 2012 is correctly stored and categorized but not itemized into its two USPS line
  items ($6.00 1st Class Pkg + $4.19 Ground Adv), which would otherwise have reconciled
  exact-cent. Suspected fix location: wherever `check_duplicates`/`itemize_existing_expense`
  resolve their MySQL connection (likely `implementations/finance_mcp/server.py` or the
  underlying verifier/itemization tool in `rol_finances/tools/...`) — needs the same
  `NON_PROFIT_USER`/`NON_PROFIT_PASSWORD`/`DB_HOST`/`DB_PORT` env wiring the other call sites
  already use.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786482318263844604_99e502237e9c.jpg
  failing_command: itemize_existing_expense(expense_id=2012, ...) and check_duplicates(id_light="scan", ...)

## For a human

Expense 2012 (USPS, 2025-03-14, $10.19) is stored, categorized, and has a working boxed
receipt — no urgent data problem. Worth fixing the `check_duplicates`/`itemize_existing_expense`
DB-credential gap so future multi-line receipts get itemized automatically instead of relying
on Mazda's manual workaround (which won't cover itemization, only duplicate-checking).

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
