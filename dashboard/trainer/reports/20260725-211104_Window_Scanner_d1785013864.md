# Mazda Trainer Report

- Scanner: Window Scanner
- Dispatch time: 2026-07-25T21:11:04Z
- Document: statement, Barclays Bank Delaware, account 4882
- Verdict: FAIL

## Checklist
- `load_wrapper_revision(agent_name="Mazda")`: seen, success (`wrap-v049`).
- Statement store: seen, but first run failed partially with `ok:false`, `failed:1`, `stored:0`, then a second run stored 9 rows but still failed on `MISTER CAR WASH #0465 GRAND RAPIDS MI` with `DataError` / missing vendor key.
- `apply_statement_annotations.py`: attempted, but the command was malformed and did not complete the required annotation pass.
- Dashboard report `report.html`: not completed in the transcript.
- `record_trace(task_name="document-intake")`: not seen.
- `judge_trace(...)`: not seen.
- Dashboard callback `/api/expense-stored`: not seen.

## Defect Diagnosed
- Statement wrapper failed closed too late: Mazda allowed a partial store instead of rejecting or parking the whole statement when one row could not be resolved.
- Post-store statement workflow was incomplete: the required annotations, report generation, hydration, audit, trace, judge, and callback chain never completed.
- The annotation command usage was wrong: `--image` was omitted and `--expense-ids` was not passed in the required form.

## Lesson Sent
- "This statement run failed closed in the wrong place. The contract requires you to treat a single failed row as a statement-level rejection, not a partial success. `store_statement_transactions.py` returned `ok:false` with `failed:1` and only 9 stored rows because `MISTER CAR WASH #0465 GRAND RAPIDS MI` had no resolved vendor key / triggered the DataError. For statement intakes, that means you must stop the partial store, reject the whole statement to the statement-needs-review path, and only proceed after the statement is either fully stored or explicitly parked for review.

You also missed the required post-store wrapper steps. If the store succeeds, call `apply_statement_annotations.py --image /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785013860978749608_37871ba59271.jpg --expense-ids <comma-separated stored and duplicate ids>` using one comma-separated `--expense-ids` value, then build `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/report.html`, run `restructure_verified_transactions.py`, `hydrate_report_categories_from_db.py`, and `audit_statement_reports.py`, then record `document-intake`, judge it, and send the dashboard callback. Record this lesson in memory and rerun the now-fixed statement workflow for this same document."

## Human Follow-up
- Check why the statement store path allowed a partial insert instead of a fail-closed rejection for the unresolved `MISTER CAR WASH` row.
- Check the annotation runner invocation shape and the statement-report pipeline wiring.
