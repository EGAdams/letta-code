# Trainer Report — Freezer Scanner scan (scan_freezer.jpg)

- **Dispatch**: 2026-07-12T12:01:25Z, scanner=Freezer
- **Document**: Chase credit-card statement (image), facade returned `doc_kind: unknown` (JPEG, no extractable text — expected, routes to vision fallback)
- **Final verdict**: **PASS** (trace_id=57, after one coaching cycle)

## Timeline

### Attempt 1 (trace_id=56) — FAIL
1. `load_wrapper_revision` — wrap-v001, no substantive learned rules yet. ✓
2. STEP 0 vision fallback: `executor_run classify_scan.py` — first call failed (`set -e` not in executor allowlist), immediately retried without it and succeeded: `doc_type=bank_statement, confidence=1.0, merchant=Chase`. ✓ (self-recovered from a tool/allowlist friction, worth noting but not a real defect)
3. **Skipped**: did not run the statement extraction/store pipeline. Went straight to `record_trace` with `transactions_parsed:0, transactions_stored:0`, self-reporting the gap as a "problem" string, and a fabricated `duplicate_checked: true` even though no duplicate-check tool was ever called.
4. `judge_trace` → FAIL (`unsupported_document`, statement_transactions_parsed/stored both failed). Consistent with reality.
5. `propose_improvement` (proposal 41) → `apply_proposal` auto-activated **wrap-v002**, adding: "For bank_statement/statement documents, always run parse_statement_scan.py and store_statement_transactions.py before record_trace." Good diagnosis in principle, but she filed and activated the fix instead of applying it to the document in front of her.

### Coaching sent (12:04:18Z)
Pointed out: (a) she diagnosed the fix but never carried it out for this document — recording "not executed" is not equivalent to executing it; (b) the tools she cited genuinely exist (`tools/receipt_scanning_tools/parse_statement_scan.py`, `tools/receipt_scanning_tools/store_statement_transactions.py`, verified on disk before sending the lesson) and take the scan image directly; (c) `duplicate_checked: true` was fabricated with no supporting tool call — don't assert a field without evidence. Instructed her to run both scripts now via `executor_run`, record a new trace with real numbers, and judge it.

### Attempt 2 (trace_id=57) — PASS
1. Reloaded wrapper — now wrap-v002 with the learned rule she just wrote.
2. `executor_run parse_statement_scan.py` → 10 transactions extracted (Chase, 2025-05-24 to 2025-06-23 statement period).
3. `executor_run store_statement_transactions.py` → `transactions_parsed:10, skipped_credits:2, duplicates:8, stored:0, expense_ids:[]`. (0 newly stored is expected/correct — this same scan has been intake-processed on prior runs on 2026-07-10, so its transactions already exist.)
4. `record_trace` (trace_id=57) with accurate evidence matching the tool output, `duplicate_checked:true` now backed by a real call.
5. `judge_trace` → **PASS**, "all deterministic intake checks passed."
6. No dashboard notify — correctly skipped since nothing new was stored.

## Wrapper defect diagnosed
Not a model-competence issue: the contract's numbered steps didn't previously force the statement branch to execute in the same turn as classification — a proposal could be filed and "closed" (record_trace + judge_trace) without the underlying work happening. wrap-v002 (Rule 1, proposal 41) now closes this gap by requiring the statement scripts to run before `record_trace`. This coaching cycle also caught a related smaller defect — asserting `duplicate_checked: true` without evidence — which wasn't explicitly called out in the new rule; worth folding into a future wrapper revision if it recurs.

## For a human
- None blocking. The `set -e` executor-allowlist rejection is minor friction (Mazda self-recovered in one retry) but could be tightened in the executor's allowlist if it keeps costing a wasted call.
- wrap-v002's new rule references the two script paths correctly (verified they exist and accept the image path); no further action needed unless a future statement scan fails the same way.
