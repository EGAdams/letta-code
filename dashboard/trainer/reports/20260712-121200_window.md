# Trainer Report — Window Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan.jpg`
**Scanner:** Window Scanner
**Dispatch time:** 1783857332 (2026-07-12T11:55:32Z)
**Facade result:** `{"ok": true, "doc_kind": "unknown", "confidence": 0}`
**Verdict: STALLED** (infrastructure — dispatch never reached Mazda)

## What happened

Polled Mazda's transcript (`GET /v1/agents/agent-6b536cf4.../messages`) every ~60s from
dispatch time through 2026-07-12T12:11:58Z (16.5 minutes). No user_message referencing
this document (`scan.jpg`, "Window Scanner") ever appears in her transcript — searched
explicitly for `scan.jpg` (excluding `scan_freezer.jpg`) and for `Window Scanner`: zero
matches at any point in the observation window.

The only intake activity Mazda's transcript shows during this window is an **unrelated
run**: a Freezer Scanner document (`scan_freezer.jpg`, a Chase bank statement) dispatched
at 2026-07-12T12:01:26Z — six minutes *after* this run's dispatch timestamp — which she
processed to completion (trace 56 FAIL → self-corrected via a Trainer coaching message →
trace 57 PASS, `wrap-v002` activated). That run is a different scanner, different image,
different dispatch message, and is not evidence this dispatch was picked up.

## Checklist against the contract

| Step | Evidence |
|---|---|
| 1. `load_wrapper_revision` | Not observed for this document |
| 2. STEP 0 vision classify (doc_kind=unknown) | Not observed |
| 3. Investigate (`check_vendor_key` / `check_duplicates`) | Not observed |
| 4. Categorize | Not observed |
| 5. Store | Not observed |
| 6. `record_trace` (task_name=document-intake) | Not observed |
| 7. `judge_trace` | Not observed |
| 8. `propose_improvement` | N/A (no trace) |
| 9. Dashboard notify | Not observed |

No step in the contract was ever entered. Mazda cannot be faulted — there is no evidence
she ever received this dispatch as an inbound message.

## Diagnosis

This is not a wrapper defect in Mazda's instructions, tools, or memory — she never got a
chance to act. The defect (if any) is upstream, in the scan → Mazda dispatch path
(`_notify_mazda_of_scan` / the Letta `POST .../messages` call from `server.py` for the
Window Scanner branch specifically, since the Freezer Scanner dispatch six minutes later
succeeded). Possible causes worth checking: the dispatch call for this specific scan
failed or was swallowed silently (fire-and-forget — a broken call wouldn't surface
anywhere), a request to the wrong agent/endpoint, or a Window-Scanner-specific code path
issue in `process_scanned_document`.

## Lesson sent to Mazda

None. Sending Mazda a corrective message would be incorrect here — she has no memory of a
document she was never told about, and there is nothing in her behavior to fix.

## For a human to look at

- Check `/tmp/dashboard_8765.log` (or wherever `_notify_mazda_of_scan` logs) around
  2026-07-12T11:55:32Z for the Window Scanner scan to confirm whether the dispatch POST
  to Mazda's `/v1/agents/.../messages` was actually attempted, and if so, what response it
  got.
- Confirm `scan.jpg` was correctly staged/renamed before the dispatch fired — if the file
  overwrite/staging race left the image byte-identical to a prior run, some idempotency
  guard elsewhere could have suppressed the send (speculative — needs log confirmation).
- If the dispatch truly never fired, this document is still unprocessed and should be
  reprocessed once the delivery bug is found.
