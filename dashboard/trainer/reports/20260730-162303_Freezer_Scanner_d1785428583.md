# Mazda Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1785428579789786404_8257a943a13e.jpg`
- **Dispatch**: 2026-07-30T16:23:03Z (`d1785428583`)
- **Conversation**: `conv-1d4f57e3-c609-415e-80ec-21d00325cee9`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Checklist (all with successful tool returns observed after dispatch)

1. `load_wrapper_revision(Mazda)` → `wrap-v067` loaded. ✅
2. Facade returned `doc_kind=unknown` → STEP 0 triggered. `executor_run classify_scan.py` →
   `doc_type=receipt, confidence=0.95, merchant="Priority Health"`, reason: "shows a recorded
   payment and $0.00 outstanding balance" — a visibly-paid bill, correctly routed as receipt
   per the contract's "a visibly paid invoice is a receipt" rule. ✅
3. `parse_and_categorize.py --json --engine=gemini --write-parsed-json` → parsed
   date `2025-02-12`, amount `79.30`, one line item ("Current invoice amount"),
   `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` (no marks/arithmetic on
   this document, so full-receipt scope is correct, not a `marked_items` case). ✅
4. `check_vendor_key` → normalized `priority_health` → `priority_health_9392` (fuzzy match,
   adopted). `check_duplicates` → not a duplicate, `in_database=false`. ✅
5. `categorizer_main.py --provider=auto` → `category_id=243` (nonzero, valid; provider
   `gemini` succeeded, no fallback needed). ✅
6. `parse_and_categorize.py --save --category-id=243` → `success:true, expense_id:1705,
   parse_artifact_verified:true, expense_scope:full_receipt` — matches STEP 0/parse
   evidence, no drift. ✅
7. `itemize_existing_expense` → `itemizable:false, reason:"only one line item; a single line
   equal to the charge is standalone, not itemized"` — correct fail-closed non-itemization,
   row left standalone. ✅
8. `record_trace(task_name="document-intake")` → `trace_id=309`, evidence JSON matches
   observed reality (`stored:true, expense_id:1705, vendor_key:priority_health_9392,
   category_id:243, is_duplicate:false`). ✅
9. `judge_trace(309)` → `verdict:PASS, failure_type:none`. ✅
10. Dashboard callback `POST /api/expense-stored` → confirmed via
    `/api/expense-stored-events?since=1785428583`: event present with matching
    `conversation_id` and `dispatched_at`, `expense_ids:[1705]`, `stored:1`. ✅

## Supporting-document / red-box verification (independently checked, not from Mazda's prose)

- `GET /supporting-document/1705/receipt` → `200`, valid JPEG, 673,109 bytes.
- Cached annotated copy exists at
  `dashboard/.cache/document-annotations/expense-1705-c3f2d6889ead237278da1452.jpg`
  (673,109 bytes) and differs in size from the raw archived original (654,678 bytes),
  confirming a red box was actually drawn — not an unannotated passthrough. No
  "opened without highlight" line in the dashboard log for this expense.

## Minor defect found (not a Mazda failure — dashboard/tool defect for a human)

The `--save` step's stdout logged:
`WARNING: Could not update source_file to /home/adamsl/rol_finances/readable_documents/receipts/2025/february/february_12/priority_health_02_12_25_79_30.jpg`
after the file was moved into the archive. The expense's DB `source_file` column
likely still holds a stale/pre-move path. This did **not** break anything in this run —
the dashboard's receipt resolver fell back to its stem/basename matching and served the
correctly annotated image — but the `source_file` update failure in
`parse_and_categorize.py`'s save path is worth a human looking at, since it's exactly the
kind of latent bug the fallback matching was designed to paper over.

## Coaching sent

None. No defects required correction.

## Summary

This was a clean, correct, single-pass run: the facade came back `unknown`, Mazda classified
the paid Priority Health bill herself as a receipt (0.95 confidence), parsed it, normalized
the vendor key, checked for duplicates, categorized to a valid category, stored it as
expense 1705 with `parse_artifact_verified=true` and no scope drift, correctly declined to
itemize a single-line receipt, recorded and judged the trace (PASS), and notified the
dashboard. Independent verification confirmed the Set Category dialog's "View Receipt"
action serves a properly red-boxed annotated image. One non-blocking wrapper/tool defect
(a `source_file` column update warning during save) is flagged for a human to investigate
but did not affect this run's outcome.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
