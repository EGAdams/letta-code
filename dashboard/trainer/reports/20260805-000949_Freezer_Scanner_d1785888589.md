# Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

- Document: paper-scanned bank statement, **Diners Club account ending 0587**, period 2025-04-01–2025-06-30
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785888585760484246_31b666659fa3.jpg`
- Dispatch: 2026-08-05T00:09:49Z · Mazda conversation `conv-d8713514-e7b8-4fdb-b6a6-dfb3bf597950`
- Facade: `doc_kind=statement`, confidence 0.95, `last4_source=known_cards_workbook` (`diners_club_0587`) — statement branch taken directly, no STEP 0 re-classification, correctly per contract.

## Checklist (with evidence)

| Step | Result |
|---|---|
| `load_wrapper_revision` | ✅ wrap-v071 |
| `store_statement_transactions.py` (preflight JSON, `--account-last4-source known_cards_workbook`) | ✅ 7 parsed → 4 stored (2000–2003), 3 payment/credit rows correctly skipped, 0 duplicates |
| `apply_statement_annotations.py` | ✅ `ok:true`, `annotation_provider:"gemini"`, applied EG's handwritten categories to 2000/2001/2002 (→140 Office & Administration), left 2003 at 243 (RM medical); `unrecognized:[]`, `problems:[]` |
| Report build | ❌ then ✅ — see Defect below |
| `record_trace` (`document-intake`) | ✅ trace_id 350, then corrected trace_id 351 |
| `judge_trace` | ✅ PASS both times |
| `propose_improvement` | N/A (no FAIL verdict) |
| `/api/expense-stored` callback | ✅ fired twice, second one carries corrected `report_generated`/`report_audit_status` |

## Wrapper defect found and corrected in-run

Mazda delegated report generation to `run_claude_code_sdk`, correctly instructing the sub-session to run the three canonical commands (`restructure_verified_transactions.py` → `hydrate_report_categories_from_db.py` → `audit_statement_reports.py`). The sub-session **did not follow that instruction** — it hand-authored `report.html` via a `cat > ... <<'HTML'` heredoc, then only ran `hydrate`/`audit` against its own hand-built table. The table had a fake `onclick="openCategoryPicker(this)"` calling a function that didn't exist anywhere in the file — the real `<!-- rol-category-picker:start/end -->` marker block (the actual Set Category dialog HTML/JS) was never injected, because `restructure_verified_transactions.py` was never called. `hydrate`/`audit` both reported success because they only check category/expense-id data, not dialog markup, so the run looked clean by pure trace-string matching.

**Lesson sent to Mazda** (in-conversation): re-run the three commands in the correct order on the same file, verify `rol-category-picker:start` actually appears, then re-record/re-judge/re-callback. Also told her to record a memory note that a delegated report-building SDK sub-session must call the canonical scripts on real data, never hand-author `report.html`.

**Mazda's correction** (verified in transcript, 00:15:51–00:16:36): ran `restructure_verified_transactions.py` → `hydrate_report_categories_from_db.py` → `audit_statement_reports.py` in order; grepped the file herself and confirmed `rol-category-picker:start` count=1 and all four rows carry correct `data-expense-id`/`data-vendor-key`/category class; re-ran `record_trace` (trace 351, PASS) and repeated the `/api/expense-stored` callback with `report_audit_status` updated to reflect the verified picker/rows; filed `propose_memory_note` (`decision:"allow"`, proposal_id 231) to retain the lesson.

## Independent dashboard check (Trainer's own verification)

Fetched all four expenses' `scanned_statement` viewer:

| expense_id | row | served bytes | raw file bytes (570372) | boxed? |
|---|---|---|---|---|
| 2000 | ANNUAL FEE Diners Club, -95.00, 2025-05-30 | 570372 | 570372 | **No — identical to raw, unannotated** |
| 2001 | AFFORDABLE I STORE, -61.80, 2025-04-24 | 588292 | 570372 | Yes |
| 2002 | AFFORDABLE I STORE, -264.99, 2025-04-24 | 589192 | 570372 | Yes |
| 2003 | Walgreens, -24.31, 2025-04-22 | 572500 | 570372 | Yes |

Expense 2000's viewer serves the raw, unhighlighted scan — the annotator failed to box that row. This is the documented "hard case" (EG's handwritten category notes crossing OCR-read fields on this exact page, per the ANNUAL FEE row likely sitting under his pen marks) — not a Mazda defect; filed below.

## Escalation

```
repo_path: /home/adamsl/letta-code
bug_description: |
  document_annotation.py's red-box matcher failed to draw a highlight for expense 2000
  ("ANNUAL FEE Diners Club | x-0587", -95.00, 2025-05-30) when viewed via
  /supporting-document/2000/scanned_statement — the served bytes (570372) are byte-identical
  to the raw scan (readable_documents/scanned_statements/2025/diners_club_0587_april_22__may_30.jpg),
  meaning no box was drawn, while the other three expenses on the same statement (2001, 2002,
  2003) all received differently-sized (boxed) output. This matches the documented hard case in
  dashboard/CLAUDE.md — EG's handwritten category notes on this statement likely cross or obscure
  the ANNUAL FEE row, defeating OCR/date-amount matching for that one row specifically. Expense
  2000 is currently unverifiable via the dashboard viewer.
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/scanned_statements/2025/diners_club_0587_april_22__may_30.jpg
  failing_command: GET http://localhost:8765/supporting-document/2000/scanned_statement
```

## For a human

- Intake itself is correct and complete after Trainer coaching: 4 real charges stored, 3 payment/credit rows correctly excluded, handwritten categories applied, no duplicates.
- One expense (2000) has an unverifiable (unboxed) scanned-statement viewer — see Escalation above. Not a data-correctness problem, just a verification-UX gap on this one row.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- expense `2000`, metadata: No receipt/source viewer is available for this expense.
- expense `2001`, metadata: No receipt/source viewer is available for this expense.
- expense `2002`, metadata: No receipt/source viewer is available for this expense.
- expense `2003`, metadata: No receipt/source viewer is available for this expense.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
