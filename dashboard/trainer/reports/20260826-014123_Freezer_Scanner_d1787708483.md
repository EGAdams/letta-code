# Trainer Report — Freezer Scanner

**Document:** `scan_freezer_1787708480239969827_c8b4900cb0b1.jpg`
**Dispatch:** 2026-08-26T01:41:23Z (unix 1787708483)
**Mazda conversation:** conv-add1518e-a40c-42cc-8925-aba8d4d5ca1a

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## What the image actually shows

Viewed directly: an almost entirely blank sheet of paper with one small torn/pasted
paper scrap near the top-center. The scrap has a tiny printed calculator-tape-style
fragment (`37.48 +`, `0.00`, `0.00 *`) and handwriting reading approximately
`37.48 / A0Z(?) / July 2025`. There is no merchant name, no item list, no receipt
header/footer, no invoice structure, and no Mom's-ledger-style composite layout.
This independently confirms the classifier's call — there is no vendor or line-item
evidence to parse a receipt/invoice from, and it is not a statement or ledger.

## Step-by-step checklist

1. **`load_wrapper_revision(agent_name="Mazda")`** — ✅ returned `wrap-v081` (active).
2. **Facade result was `doc_kind=unknown`**, so STEP 0 applied. Mazda ran
   `classify_scan.py` via `executor_run` on the correct dispatch path
   (`.../incoming_scans/scan_freezer_1787708480239969827_c8b4900cb0b1.jpg`) — ✅
   successful tool return: `{"doc_type":"other","confidence":0.99,"merchant":null,
   "reason":"mostly blank sheet with handwritten notes and no identifiable purchase,
   statement, invoice, or tax-document structure."}`.
3. **`doc_type=other`** is the one explicitly unsupported type per the contract, so no
   investigate/categorize/store branch was required, and none was run (correct — running
   parse/store on a non-document would itself have been a wrapper defect).
4. **`record_trace`** — ✅ `task_name="document-intake"` exactly. Evidence JSON matches
   the classifier output field-for-field: `doc_kind="other"`, `classification_confidence
   =0.99`, `vendor_key=null`, `stored=false`, `expense_id=null`, `is_duplicate=false`,
   `intake_halted=false`, `problems` names the unsupported-document reason. Trace id 480.
5. **`judge_trace(480)`** — ✅ returned `PASS`, `failure_type=none`,
   `"all deterministic intake checks passed"`.
6. **`propose_improvement`** — correctly not called (verdict is PASS, not FAIL).
7. **Dashboard notify** — ✅ `curl POST /api/expense-stored` after trace/judge, with
   `stored=0`, `parsed=0`, `expense_id=null`, `archive_paths=[]`. Confirmed independently
   via `/api/expense-stored-events?since=1787708483`: the one matching event has
   `conversation_id=conv-add1518e-a40c-42cc-8925-aba8d4d5ca1a`,
   `dispatched_at=1787708483.0979967` (matches dispatch), `doc_kind="other"`,
   `stored=0`, `expense_id=null` — consistent with the transcript.

## Wrapper defect

None. Every required step ran, in order, with successful tool returns, and the
evidence/trace/judge/callback all agree with each other and with independent visual
inspection of the source image.

## Lesson sent to Mazda

None needed — no coaching message sent.

## For a human

Nothing actionable. This scrap of paper (a torn calculator-tape fragment showing
`$37.48` plus a handwritten date "July 2025") is not machine-processable as a finance
document; if EG intended this as a real expense he should rescan a legible original
receipt or statement rather than a hand-torn fragment.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
