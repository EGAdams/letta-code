# Trainer Report — Window Scanner

- **Document**: `window_scan_1785439191112841505_3782d93863df.jpg` (facade: `doc_kind=unknown`, JPEG scan)
- **Dispatch**: 2026-07-30T19:19:55Z (unix 1785439195)
- **Mazda conversation**: `conv-edca65b6-354a-4e75-98f7-0aa661243b60`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Step-by-step evidence

1. `load_wrapper_revision` → `wrap-v068`, `found:true`. ✅
2. **STEP 0** (facade unknown) → `classify_scan.py` → `doc_type=receipt, confidence=1.0`, reason: "Official Receipt" confirming a $350.00 contribution already paid via check. ✅
3. **Parse** → `parse_and_categorize.py --json --engine=gemini` → extracted `merchant=The Missionary Church International`, `date=2025-03-26`, `amount=350.00`. ✅
4. **Investigate** → `check_vendor_key` → `the_missionary_church_international`, `recognized:false` (new vendor, correctly adopted the returned key) → `check_duplicates` → no exact/fuzzy duplicate. ✅
5. **Categorize** → `categorizer_main.py --provider=auto` → `category_id=353` (nonblank, valid — fail-closed gate satisfied). ✅
6. **Store** → `parse_and_categorize.py --save --category-id=353` (receipt branch, correctly *not* `--invoice` — this receipt shows payment already made) → `expense_id=1706`. Archived to `readable_documents/receipts/2025/march/march_26/the_missionary_church_international_03_26_25_350_00.jpg`. ✅
7. **Itemization** → `itemize_existing_expense` correctly returned `itemizable:false` (single line item equal to the charge is standalone, not itemized) — correct fail-closed non-itemization, row remains STANDALONE. ✅
8. `record_trace` (`task_name="document-intake"`, trace_id 312) → saved. ✅
9. `judge_trace(312)` → **`NEEDS_REVIEW`**, `failure_type: none`, reason: "vendor_key resolved but not in the known-vendor map." This is the expected outcome for a first-time vendor and is not a wrapper defect — category resolution and storage both succeeded regardless of vendor-map recognition. Since this is not a `FAIL` verdict, `propose_improvement` was correctly not called.
10. **Dashboard callback** → `POST /api/expense-stored` → `{"ok": true}`. Confirmed via `/api/expense-stored-events?since=1785439195`: event's `conversation_id` and `dispatched_at` match this exact dispatch, `expense_ids:[1706]`, `stored:1`. ✅

## Supporting-document / red-box check

- `receipt_url` correctly points at the archived receipt image.
- `GET /supporting-document/1706/receipt` → 200, but **served bytes are byte-identical (md5 match) to the raw archived file** — no red box was drawn.
- Dashboard log confirms: `[supporting-document] opened without highlight expense_id=1706 type=receipt reason=No high-confidence expense row was found in the image.`
- Per the Trainer contract, a missing box on a matched receipt is a **dashboard-annotator defect, not a Mazda failure** — filing here rather than coaching Mazda to "fix" it. The document is a simple single-line "Official Receipt" (not an itemized multi-row receipt), so `document_annotation.py`'s line-matcher failed to find a high-confidence row to box even on straightforward content. Worth a look by whoever owns `document_annotation.py`.

## Coaching sent

None. All required steps were completed correctly and in order on the first attempt; the `NEEDS_REVIEW` verdict is an expected non-failure state, not a defect.

## Flag for a human

- `document_annotation.py` failed to draw a red box on a plain single-line receipt image (expense 1706) — worth investigating why "No high-confidence expense row was found" fired on straightforward content, not just the known handwritten-notes hard case.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
