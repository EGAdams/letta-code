# Mazda Trainer Report

Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1789506148186756108_a3f23ae1c20b.jpg`  
Scanner: Window Scanner  
Dispatch: `1789506152` (`2026-09-15T21:02:32Z`)  
Conversation: `conv-d6a34ac0-25ae-4410-8a05-f2246eaa8a2a`  
Verdict: CORRECTED

## Checklist

- PASS — `load_wrapper_revision` succeeded (`wrap-v115`).
- PASS — `classify_scan.py` succeeded: invoice, Priority Health, confidence 0.99. The parse artifact succeeded with date `2025-08-12`, total `$73.60`, and `full_receipt` scope.
- PASS after correction — `check_vendor_key` normalized to `priority_health_9392`; categorizer returned category `243`.
- CORRECTED — the initial run omitted the required explicit `check_duplicates` call and relied on save's internal duplicate guard. After coaching, `check_duplicates` succeeded with exact duplicate `expense_id=3325`.
- CORRECTED — initial trace/callback used `expense_id=3325` for a duplicate-only result and used the filename-derived timestamp. Corrected trace `563` has `stored=false`, `expense_id=null`, `is_duplicate=true`; corrected callback has `stored=0`, `expense_id=null`, `duplicate_expense_ids=[3325]`, and exact `dispatched_at=1789506152`.
- PASS — corrected `record_trace` succeeded (`trace_id=563`, `task_name=document-intake`) and `judge_trace(563)` returned PASS.
- PASS — corrected dashboard callback returned `{"ok":true}` and the matching event contains the exact document path, conversation ID, dispatch timestamp, and duplicate ID.
- PASS — no new expense was inserted; this is a proven idempotent duplicate.

## Coaching

I sent Mazda: “The save-stage duplicate result does not replace the required `check_duplicates` step. Do not store again; run the explicit check, then record duplicate-only evidence with `stored=false`, `expense_id=null`, `duplicate_expense_ids=[3325]`, and callback `dispatched_at=1789506152`; record, judge, and callback again.” She completed all corrections and submitted memory proposal `315`.

The defects were wrapper-contract application issues (missing stage guard and incorrect duplicate/dispatch identity fields), not an application defect requiring escalation. No escalation filed. The memory tool reports `applied:false` because no live-memory applier is wired; proposal `315` was allowed and recorded for human follow-up.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
