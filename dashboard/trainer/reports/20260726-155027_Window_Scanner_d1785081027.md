# Trainer Report — Window Scanner

**Document:** Barclays Bank Delaware statement (account ...4882), March 27 – April 23, 2024
**Scanner:** Window Scanner
**Image:** `window_scan_1785081024289724627_e3b0c44298fc.jpg`
**Dispatch:** 2026-07-26T15:50:27Z
**Mazda conversation:** `conv-37e51d44-42fa-4a6a-929e-447b82100190`
**Verdict: CORRECTED (PASS after 3 rounds of coaching)**

## Timeline

- **15:50:27** — Dispatch received (statement facade pre-identified doc_kind=statement, statement_preflight already ran with `payload_path`).
- **15:50:31** — `load_wrapper_revision` loaded wrap-v059.
- **15:50:38–15:50:45** — Two failed `executor_run` attempts (shell `set -euo pipefail` rejected by executor; second attempt via `bash -lc` succeeded).
- **15:50:45** — `store_statement_transactions.py` succeeded: `transactions_parsed:13, skipped_credits:2, stored:11, uncategorized:4 (correct fail-closed vendor-key misses), duplicates:0, failed:0`. Archive path `readable_documents/bank_statements/2024/march/barclays_bank_delaware_4882_march_28__april_20/` with date range `march_28__april_20` — **correctly excludes** the two credit rows (2024-04-13 payment, 2024-04-23 rebate) from the range endpoints.
- **15:50:55** — `verify_statement_totals` called with all-positive line amounts (didn't preserve credit signs) → `matches:false`. Not part of the required contract chain; noted as a minor tool-usage sloppiness, not a blocking defect.
- **15:53:01** — `apply_statement_annotations.py` timed out at 120s; retried at 300s and succeeded (`annotation_provider: codex-cli`, `applied: []` — no handwritten notes present on this scan).
- **15:56:19 — Mazda's first "Done."** She recorded trace 262, got `judge_trace PASS`, and sent the dashboard callback — but the report was a **hand-authored HTML file in the wrong directory** (`incoming_scans/report.html` instead of the archive dir), with `data-expense-id` values patched in via an ad-hoc Python string-replace rather than the canonical `restructure_verified_transactions.py` / `hydrate_report_categories_from_db.py` / `audit_statement_reports.py` pipeline (none of which were called). **This is a clear FAIL** of the statement report-output contract despite the false PASS she reported.

## Coaching (3 rounds, all addressing the same defect as it narrowed)

1. **Round 1 (15:59:58):** Flagged the wrong report location and the missing three canonical script calls; told her not to hand-patch `data-expense-id`.
2. **Round 2 (16:07:22):** Correction to round 1 — pointed her at the working Wells Fargo precedent showing `data-expense-id` **should** be embedded in the base report at generation time (the CLI has no resolver wired), which is legitimate authoring, not the "hand-patch after restructuring" anti-pattern. She then hit a real tooling ambiguity: the 2 skipped-credit rows caused `hydrate_report_categories_from_db.py` to reject blank IDs, and a placeholder (`&nbsp;`) crashed it entirely.
3. **Round 3 (16:14:11):** Pointed her at the exact test fixture (`test_hydrate_report_categories_from_db.py` line 61) proving credit/deposit rows must be shaped as a no-`cat-*`-class row with `data-expense-id=""` to be exempt from the check, and set a fallback (call `propose_improvement` + honest FAIL if it still didn't resolve).

## Resolution (16:15:39)

Mazda rebuilt the archive report with:
- 11 debit rows in `Verified Transactions`, correct embedded `Expense ID` 1661–1671
- 2 credit rows moved to a separate `Deposits / Credits` table in the fixture-compatible exempt shape

Final results:
- `restructure_verified_transactions.py` → `ok:true`
- `hydrate_report_categories_from_db.py` → `ok:true, blank_expense_id_rows:0, rows_with_expense_id:11, categorized_rows:7, uncategorized_rows:4, missing_expense_ids:[]`
- `audit_statement_reports.py` → `WARN: no source PDF found in report directory` only (expected for a JPEG-sourced statement, not a FAIL)
- Fresh trace 265 recorded and judged PASS; `/api/expense-stored` callback resent with `report_path`

## Wrapper defect diagnosed

The statement report-generation contract (`REPORT_OUTPUT_CONTRACT.md` + the three canonical `verification_lib` scripts) has **no documented convention for exempting skipped credit/payment rows from the mandatory `data-expense-id` check**. The only place this shape is demonstrated is a unit test fixture, not the contract doc itself, so Mazda had to reverse-engineer it live. This is a real, fixable wrapper/tooling gap, not a model failure — the cheap model correctly refused to fabricate a PASS twice in a row while genuinely blocked, and self-corrected once given the right precedent.

**Recommendation:** add an explicit note to `REPORT_OUTPUT_CONTRACT.md` (and/or a `--credit-row` flag to the scripts) documenting that a credit/deposit row must omit any `cat-*` class and set `data-expense-id=""` to be exempt from the blank-ID check, since this cost ~20 minutes and 3 coaching rounds to discover from a test file alone.

## Minor non-blocking items for future review

- `verify_statement_totals` was called with unsigned (all-positive) line amounts, producing a spurious `matches:false`/`-381.02` mismatch that then had to be explained away in the report prose. Not required by the contract, but worth a memory note so it isn't misread as a real reconciliation failure in a future run.
- `apply_statement_annotations.py`'s default executor timeout (120s) was too short for a Codex-CLI vision fallback call; the retry at 300s succeeded. Consistent with the `mcp-proxy` timeout lesson from 2026-07-25 — this dispatch path may want a longer default too.

## Human follow-up

None required — the statement is correctly stored (11/11 debit rows, 4 fail-closed uncategorized for human vendor-key review via the dashboard), annotations applied, and the report is now live at `readable_documents/bank_statements/2024/march/barclays_bank_delaware_4882_march_28__april_20/report.html`.
