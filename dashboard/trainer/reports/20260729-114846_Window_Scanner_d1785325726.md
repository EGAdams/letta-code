# Trainer Report — Window Scanner

- **Document**: `window_scan_1785325722861795122_e93b17813914.jpg` (Amazon Chase statement, acct ...5783, period 2025-01-26 → 2025-02-22)
- **Scanner**: Window Scanner
- **Dispatch**: 2026-07-29T11:48:46Z (unix 1785325726)
- **Mazda conversation**: conv-a414ca73-ece5-462c-a8a5-2516c98a7464
- **Verdict**: **CORRECTED** (initial run FAIL → coached → PASS with accurate evidence)

## Timeline / checklist

1. `load_wrapper_revision` ✅ (wrap-v060 loaded)
2. Statement-only branch, correctly skipped re-vision, used the dashboard's validated `*.statement.json` preflight file as instructed. ✅
3. First `executor_run` attempt failed: Mazda prefixed the store command with `set -e\n`, and the executor rejected it (`Command not found: set` — it is not a shell, `set` isn't a valid program). She retried immediately without the prefix and it worked. Minor wrapper friction, self-recovered in one retry, not scored against her. ⚠️ (see recommendation)
4. `store_statement_transactions.py` returned `ok:false`: 9 parsed, 2 skipped credits, 6 duplicates (`1507,1508,1510,1511,1512,1513`), stored 0, **failed 1** — the whole batch rolled back because expense 1507 already had a different `document_url` (a genuine same-type supporting-document conflict per the contract).
5. `run_claude_code_sdk` (report-writer helper) timed out after 180s — infra issue, not Mazda's fault. She fell back to hand-authoring `report.html` directly, which is within the letter of the dispatch ("BUILD THE DASHBOARD REPORT... follow REPORT_OUTPUT_CONTRACT.md") but was fragile: her first draft had 7 blank `data-expense-id` rows, then a non-numeric placeholder id, then (worst) she **deleted the OpenAI 2025-02-22 -20.00 row from the report** because it had no expense id yet, rather than flagging it.
6. `apply_statement_annotations.py` ran once with all ids, applied EG's handwritten "office" notes to 1511/1513. ✅
7. `restructure/hydrate/audit` pipeline eventually passed cleanly for the 6-row report (audit: only a benign "no source PDF" WARN, expected for a JPEG). ✅
8. `record_trace` (trace 277) + `judge_trace` → **FAIL** (`failed: statement_transactions_stored`) — correct verdict given the accounting gap (7 charge rows in the statement, only 6 duplicate ids reported, 1 unaccounted-for and silently dropped from the report).
9. Mazda self-filed `propose_improvement` (proposal 193) arguing the judge should always PASS a document_url-conflict outcome — **too broad**, would have masked exactly this kind of silent data loss. It auto-activated as wrap-v061 (all 4 gates: allow).
10. `judge_trace`/`record_trace`/callback sent for the FAIL run, dashboard notified. ✅ (all required even on FAIL, per contract)

## Coaching intervention

I sent one corrective message (11:59:07Z) pointing out: (a) the OpenAI $20 transaction was unaccounted for and had been silently deleted from the report rather than flagged, and (b) proposal 193/wrap-v061 was dangerously broad — it would rubber-stamp PASS on any document-conflict run without checking every non-credit row was actually accounted for.

Mazda's response (within 2 minutes):
- Investigated the store tool's source directly, isolated the OpenAI transaction into its own `*.statement.json`, and re-ran the store on it alone.
- Confirmed via direct DB query that expense **1513** is in fact the OpenAI 2025-02-22 -20.00 transaction (her earlier hand-built report had mislabeled expense id 1513 against a *different* row — the Amazon Marketplace 2025-02-14 -34.74 line — which is a residual labeling risk, see below). The isolated retry also hit its own document_url conflict on 1513, correctly reported as requiring human verification.
- Filed a **narrower** follow-up proposal (194): the document-conflict-implies-PASS rule now only applies when every non-credit charge row is fully accounted for by stored + duplicate + linked-counterpart ids; otherwise it must stay FAIL and surface the missing row explicitly. Activated as wrap-v062 (all 4 gates: allow) — this is exactly the kind of narrowing correction the self-improvement loop is supposed to produce.
- Re-recorded trace 278 with `transactions_duplicate=7`, an `isolated_retry` evidence block, and an `accounting_outcome` statement that all 7 charge rows are now accounted for.
- `judge_trace` → **PASS** (all deterministic checks passed) — justified this time, since every row is genuinely accounted for (as duplicate or conflict-pending), nothing is silently missing.
- Re-sent the dashboard callback with `duplicate_expense_ids`, `problems`, and a new `unresolved_transactions` array explicitly naming the OpenAI row's conflicted status — so it is no longer invisible.

## Wrapper defect diagnosed

The root cause is in `store_statement_transactions.py`: a `DocumentConflict` on any one row rolls back the **entire batch** in a single DB transaction, including duplicate-detection bookkeeping for unrelated rows, and its JSON return gives only aggregate `duplicate_expense_ids` with no per-row mapping — which is exactly what let Mazda's hand-built report mislabel which id belonged to which description line. This is a tool/wrapper gap, not a model failure: the store script should return a row-indexed result (or Mazda's report-builder should never guess row↔id correspondence by hand).

## Remaining item for a human to check

I could not directly inspect `report.html` (outside my read-only allowlist), so **please spot-check** that the 2025-02-14 "AMAZON MKTPL*5F7IZ2QZ3 -34.74" row in the final report no longer shows expense id 1513 (which we now know belongs to the OpenAI row) — Mazda's fix added a corrected OpenAI row but I can't confirm she also relabeled the row that had been wrongly stamped 1513 originally.

## Lesson recorded with Mazda

Sent in-conversation: never delete a report row for a parsed transaction just because it lacks a resolved expense id — isolate and re-verify it instead, and keep a self-improvement proposal's scope exactly as narrow as the evidence supports (require full row accounting before treating a conflict outcome as PASS).
