# Mazda Trainer Report

**Scanner:** Freezer
**Document:** `scan_freezer_1786725024552578022_7cd37c822b8b.jpg`
**Dispatch:** 2026-08-14T16:30:29Z
**Conversation:** conv-a42a4f09-39b4-47d3-8bb1-1a45e1beadba

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## What happened

Facade returned `doc_kind=unknown` (expected for a JPEG scan). Mazda ran STEP 0 vision
classification (`classify_scan.py`) which returned `doc_type=tax_document`,
`confidence=0.99`, merchant "Samaritan's Purse", reason: "2025 contribution summary used
for tax filing, not a purchase receipt or invoice." This is a real, tooled category in the
live `rol_finances` repo (`tools/test_classify_scan.py::test_heuristic_classifies_tax_document`,
`readable_documents/tax_documents/INDEX.md`, `tools/tax_docs/extract_tax_document_fields.py`)
— it is **not** covered anywhere in this Trainer's own dispatch contract, which only defines
receipt/invoice/statement/moms_ledger/other branches. That is a gap in my instructions, not a
Mazda defect.

Mazda investigated existing conventions before acting (grepped for `tax_document` handling,
read `INDEX.md`, located the extractor), archived the image to
`readable_documents/tax_documents/2025_samaritans_purse_contribution_summary/` following the
documented folder convention, and ran `extract_tax_document_fields.py`, which generated
`extracted_fields.json`, `detail.md`, `summary.md`. No numeric fields were extracted because
`pytesseract`/`pdf2image` are unavailable in this environment; she recorded this honestly as a
`problems` entry requiring human review/vision extraction rather than fabricating values —
correct fail-closed behavior. A contribution/giving summary of this kind is not itself a new
transaction to store (donations it summarizes are recorded elsewhere as they occur), so
`stored:false`/`expense_id:null` is the correct outcome, not a missed step.

`record_trace` used `task_name="document-intake"` exactly, with accurate evidence
(`doc_kind:"tax_document"`, `stored:false`, `duplicate_checked:false`, `is_duplicate:false`).
Dashboard callback confirmed via `/api/expense-stored-events?since=1786725029`: one event,
`conversation_id`/`dispatched_at` match this dispatch, `stored:0`/`parsed:0`/`doc_kind:tax_document`
— consistent with what actually happened.

## Checklist

| Step | Evidence | Verdict |
|---|---|---|
| `load_wrapper_revision` | wrap-v074 loaded | ok |
| STEP 0 classification | `classify_scan.py` → tax_document, 0.99 | ok |
| Investigate conventions | grep + INDEX.md read before acting | ok |
| Archive | copied to canonical `tax_documents/<slug>/` path | ok |
| Extract | `extract_tax_document_fields.py` ran, 3 artifacts written, honest `problems` note | ok |
| `record_trace` | task_name exact, evidence matches reality | ok |
| `judge_trace` | returned FAIL (rubric mismatch — see below) | see note |
| `propose_improvement` | called on FAIL, accurate diagnosis | ok |
| Dashboard callback | confirmed via events API, matches conversation/dispatch | ok |

## Judge FAIL was a rubric bug, not a Mazda failure

`judge_trace` failed the run on `vendor_key_resolved`/`category_resolved`/
`duplicate_check_ran`/`expense_stored` — a receipt-shaped rubric applied to a
non-transactional reference document. `FinanceEvaluationFactory`'s rule-based evaluator has
no verdict path for `doc_kind=tax_document`. This is an **application defect** (code, not
Mazda's wrapper) — see Escalation.

## Wrapper self-activation — flag for human review, not a coaching item

Mazda's `propose_improvement` (proposal 244) was correctly diagnosed. She then called
`apply_proposal` herself, which **auto-activated** `wrap-v075` (learned rule #57: for
`doc_kind=tax_document`, never require vendor/category/duplicate/storage) with
`pending_approval:false` — all four gates ("risk acceptable", "cost within budget", "no
regressions", "1 improvement(s)") passed with terse, generic justifications and zero human
involvement. The developer manual (Ch. 5 §6) describes activation as going "through a human
approval gateway"; this happened without one. The rule itself is reasonable in substance, but
its *scope* is broad and permanent: any future document the classifier calls `tax_document`
(correctly or not) will now silently skip vendor/duplicate/storage checks. A real receipt
misclassified into this bucket would go unstored with no safety net. This is not something to
coach Mazda on — she used the tools exactly as the self-improvement architecture provides —
but the gate chain's depth of scrutiny for a change that removes financial safety checks looks
too shallow, and it deserves a human or Suzuki look. Filed as an escalation, not a Mazda
lesson.

No corrective message was sent to Mazda — her handling of this document was correct and
complete.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  Two related application-level gaps surfaced by this run, neither a Mazda wrapper defect:
  1. FinanceEvaluationFactory's verdict_judge (tools/self_improving_agent /
     agent_self_improvement, evaluation family) has no verdict path for a non-transactional
     document kind. It applied receipt rubric checks (vendor_key_resolved, category_resolved,
     duplicate_check_ran, expense_stored) to doc_kind=tax_document and produced a false FAIL
     (trace_id 393) on a correctly-handled tax reference document. Needs a judge rule branch
     that tax_document (and similar non-expense doc kinds) are graded on
     archive_paths/extraction-artifact evidence, not expense-storage evidence.
  2. apply_proposal auto-activated proposal 244 -> wrap-v075 with pending_approval:false,
     bypassing the "human approval gateway" the developer manual (Ch.5 S6) describes. The
     activated rule (#57) permanently exempts doc_kind=tax_document from vendor/category/
     duplicate/expense-storage checks fleet-wide. Given a misclassification here would
     silently skip storing a real expense, recommend a human review the gate chain's
     usefulness/safety criteria for changes that remove financial safety checks, and confirm
     whether tax_document should require human approval before activation (or whether the
     auto-activation is in fact intended design for this class of change).
  Separately (lower priority, not blocking): pytesseract/pdf2image are unavailable in this
  executor environment, so tax_document numeric-field extraction always warns
  "No numeric fields detected." — may be worth installing those deps or wiring the same
  Gemini/Codex vision fallback already used for statements/receipts.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786725024552578022_7cd37c822b8b.jpg
  failing_command: judge_trace(trace_id=393); apply_proposal(proposal_id=244)

## For a human

- Consider adding a `tax_document` (and any other non-transactional doc-kind) branch to the
  Trainer's own dispatch contract so future Trainer runs don't have to re-derive this from
  scratch.
- Spot-check wrap-v075 rule #57's actual wording in Mazda's live wrapper to confirm it's
  scoped to genuine tax_document classifications and doesn't create an easy misclassification
  loophole for real receipts.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
