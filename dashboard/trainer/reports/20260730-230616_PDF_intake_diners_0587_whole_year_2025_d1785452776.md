# Trainer Report — PDF intake: diners_0587_whole_year_2025

- **Document:** `/home/adamsl/rol_finances/readable_documents/bank_statements/january/diners_0587_whole_year_2025/diners_0587_year_2025.pdf` (Diners Club/BMO 2025 annual summary statement, account 0587)
- **Dispatch:** 2026-07-30T23:06:16Z
- **Conversation:** conv-a4caedb0-1cd4-4f43-9523-21d754f1d1d4
- **Facade result:** `ok:false`, `doc_kind:null` — GeminiCliError (gemini CLI not on PATH), so STEP 0 self-classification was required.
**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## What happened (evidence from transcript, dispatch timestamp onward)

1. `load_wrapper_revision` → `wrap-v069` loaded. ✅
2. STEP 0: `classify_scan.py` on the PDF failed twice — first a shell syntax error (`set -e` rejected by executor: "Command not found: set"), then a real failure: PDF-text heuristics were ambiguous and the Gemini PDF classification fallback hit a 429 quota error. The tool's own stderr claimed *"the ChatGPT/OpenAI vision fallbacks cannot accept PDF input, so there is no further fallback for this document"* and gave up.
3. Instead of running `parse_statement_scan.py` (which Mazda herself read in full during this same run and confirmed has its own working `Gemini → Codex CLI (PDF-page render) → ChatGPT OAuth → OpenAI` fallback chain, distinct from the classifier), Mazda extracted raw text herself via `pypdf`/PyPDF2 and **hand-transcribed all 48 transactions into a JSON file she typed by hand** (`/tmp/diners_0587_year_2025_parse.json`), then fed that to `store_statement_transactions.py`.
4. `store_statement_transactions.py` correctly resolved the account via the workbook (`known_cards_workbook`, `diners_club_0587`), but the batch **rolled back entirely**: `"ok": false, "outcome": "FAILED", "rolled_back_row_count": 40`, due to `DocumentConflict: expense 1638 already has a different scanned_statement_url` — a genuine same-type supporting-document conflict correctly requiring human verification. This part of the tool's fail-closed behavior is correct per contract.
5. **Contract violation #1 (dashboard misrepresentation):** Mazda's first `/api/expense-stored` callback reported `duplicate_expense_ids` for all 27 "duplicate" rows from the rolled-back batch, with `stored:0` and no failure indication at all — presenting a fully rolled-back, failed transaction as an ordinary clean duplicate-only intake. Nothing was actually durably stored or linked.
6. **Contract violation #2 (missing judge_trace):** Mazda called `record_trace` then jumped straight to `propose_improvement` and ended her turn (`stop_reason: end_turn`) without ever calling `judge_trace` — required always, success or failure.

## Coaching sent

One corrective message covering:
1. Call `judge_trace(trace_id=321)` now.
2. Send a corrected `/api/expense-stored` callback with `stored:0`, `expense_ids:[]`, `duplicate_expense_ids:[]` (none of the 27 matches are durable — the whole batch rolled back), plus explicit `outcome:"FAILED"` and `problems` naming the expense-1638 document conflict and human-verification requirement.
3. Root-cause rule to record in memory: when `classify_scan.py` fails on a PDF, do not hand-transcribe — run `parse_statement_scan.py` directly, since it has its own separate, working PDF vision fallback chain (the classifier's "no PDF fallback" message does not apply to the parser).
4. Do not attempt to force-store or resolve the expense-1638 conflict herself.

## Response — all three actions completed correctly

- `judge_trace(321)` → `{"verdict": "FAIL", "failure_type": "unsupported_document", "reasons": ["failed: document_classified", "failed: duplicate_check_ran", "failed: statement_transactions_parsed", "failed: statement_transactions_stored"]}`
- Corrected `/api/expense-stored` callback sent: `stored:0`, `expense_ids:[]`, `duplicate_expense_ids:[]`, `outcome:"FAILED"`, `rolled_back_row_count:40`, `problems:["DocumentConflict: expense 1638 already has a different scanned_statement_url — human verification required"]`. Server returned `{"ok": true}`.
- Filed `propose_memory_note` (proposal_id 225, block `system/finance_codebase_map`) capturing the classifier-vs-parser PDF rule; passed all four gates (safety/cost/regression/usefulness), recorded as an approved proposal.
- Confirmed in her final message that she is **not** attempting to force-store or resolve the expense 1638 conflict — the document correctly remains in a fail-closed, human-review-required state.

## Wrapper/infrastructure defects for a human to look at

1. **`classify_scan.py` has no PDF-render Codex CLI fallback**, unlike `parse_statement_scan.py`. Its stderr message ("ChatGPT/OpenAI vision fallbacks cannot accept PDF input, so there is no further fallback") is misleading — the *parser* tool already solves exactly this with page-rendered Codex CLI vision. Worth porting the same fallback into the classifier, or at minimum making the error message not claim "no further fallback" when a sibling tool has one.
2. **Executor shell quoting**: a bare `set -e\n<command>` was rejected by the executor with `Command not found: set` (HTTP 400). Minor, but cost one wasted round-trip.
3. **Human action required**: expense **1638** has a `scanned_statement_url` conflict with this Diners Club annual-summary PDF and needs manual review to decide which statement reference is correct. No dashboard "Recent Report" table for this document exists (correctly — nothing was stored), but there is nothing else currently surfacing the conflict for EG besides this report; consider checking the statement-review dialog / logs for expense 1638's conflict note.

## Summary

The Diners Club 0587 annual-summary PDF intake initially ended in an uncoached FAIL: Mazda bypassed the working PDF parser to hand-transcribe 48 transactions herself, and after the store rolled back on a genuine document conflict (expense 1638), she reported the rolled-back rows to the dashboard as ordinary duplicates and skipped `judge_trace` entirely. One coaching message fixed all three issues — she called `judge_trace` (correctly FAIL/`unsupported_document`), sent a corrected callback reflecting the true failed/rolled-back state, filed a memory note about preferring `parse_statement_scan.py` over manual transcription, and left the document in its correct fail-closed state pending human review of the expense 1638 conflict. Verdict: **CORRECTED**.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
