# Mazda Trainer Report

- **Scanner:** Window Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_1784815683534153769_67b27065f71b.jpg`
- **Dispatch time:** 2026-07-23T14:08:07Z
- **Mazda conversation:** conv-9960d2b6-d290-438f-a9b2-82a4a51b9e6c
- **Verdict: PASS**

## Document identity

Chase Amazon credit card statement, account ending 5783, 7 transactions (3 payments/credits,
4 debit purchases), all dated January 2025. Facade preflight had already identified it as
`statement`, confidence 0.99, so no STEP 0 vision fallback was needed.

## Checklist (evidence from transcript, all timestamps ≥ dispatch)

1. `load_wrapper_revision` — 14:08:12, returned `wrap-v034` with the learned statement rule
   (Rule 1: for statements, always run `parse_statement_scan.py` then
   `store_statement_transactions.py` before `record_trace`). ✅
2. Statement branch via `executor_run`:
   - First attempt (14:08:19) failed — wrong cwd (`/home/adamsl`), `No such file or directory`.
     Self-corrected immediately with an absolute path on the next call (14:08:39), which
     succeeded (`cwd_resolved: /home/adamsl/rol_finances`). No DB side effects from the failed
     attempt. Minor/non-blocking — noted below, not coached.
   - `parse_statement_scan.py --bank-name 'Chase Amazon' --account-last4 5783` → 7 transactions
     parsed, `unreadable_count: 0`.
   - `store_statement_transactions.py` → `stored:0, skipped_credits:3, duplicates:4,
     duplicate_expense_ids:[1504,1505,1503,1506], uncategorized:0, failed:0, problems:[]`.
     3+4 = 7, every parsed row accounted for — no silent drops.
   - `account_last4_source: "operator"` (explicit `--account-last4 5783` flag, itself sourced
     from the facade's workbook resolution) — valid per contract.
   - `archive_paths`: one path under `bank_statements/2025/january/chase_amazon_5783_january_04__january_28/` — single year, consistent with all transactions falling in January 2025. ✅
3. `record_trace` (14:08:47) — `task_name: "document-intake"` exactly; evidence JSON
   (`transactions_parsed:7, transactions_stored:0, transactions_duplicate:4,
   transactions_skipped_credits:3, archive_paths, archive_years:[2025], problems:[]`) matches
   the store tool's actual return field-for-field. `trace_id: 135`. ✅
4. `judge_trace(135)` (14:08:51) — verdict `PASS`, `failure_type: none`, reason "all
   deterministic intake checks passed". ✅
5. Dashboard notify (14:08:58) — `POST /api/expense-stored` with matching
   `conversation_id` and `dispatched_at: 1784815687.0873063` (== this dispatch). Verified via
   `GET /api/expense-stored-events?since=1784815687`: event present, same ids/counts. ✅

## Assessment

The 3 "Payment Thank You - Bill Pay Service" rows are card payments (credits), correctly
excluded from expenses by the store tool itself (`skipped_credits`), not by agent judgment.
The 4 debit purchases (Amazon x2, Apple, OpenAI) all matched existing stored expenses
(1503-1506) and were correctly skipped as duplicates rather than double-stored — this
statement had evidently already been processed in a prior run. `stored:0` is the correct
outcome here, not a failure.

No wrapper defect diagnosed. No coaching message sent — the one transient cwd error in the
first `executor_run` call was self-corrected within the same run with no incorrect writes,
so it did not warrant intervention.

## Nothing for a human to look at.
