# Mazda Trainer report — Freezer Scanner

- Dispatch: `1783980831` (`2026-07-13T22:13:51Z`)
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Identity from this run: AT&T Internet bill/email notice, account `136186008`, due `2025-02-09`, total `$80.24`; handwritten “2/5 check”.
- Verdict: **FAIL**

## Contract evidence

- PASS — `load_wrapper_revision(agent_name="Mazda")` returned active `wrap-v002` at 22:13:55Z.
- PASS — STEP 0 `executor_run` called `classify_scan.py` with the required cwd/env; return code 0 classified `receipt`, confidence `0.95`, merchant `AT&T`.
- FAIL — Mazda did not run receipt parsing, derive/adopt a normalized vendor key, call `check_vendor_key`, call `check_duplicates`, run `categorizer_main.py`, or call `parse_and_categorize.py --save`.
- PASS (truthful failure trace) — `record_trace` used task name `document-intake`, wrapper `wrap-v002`, and accurately recorded `stored:false`; trace `62`.
- PASS — `judge_trace(62)` returned `FAIL`, failure type `missed_vendor_key`.
- PASS — Mazda filed proposal `45`; after coaching she also filed proposal `46` with the correct binding-routing guard.
- FAIL — no successful store return, expense ID, or dashboard POST. `/api/expense-stored-events?since=1783980831` returned `[]`; `recent_report.json` has no expense IDs and `stored:null`.

## Wrapper defect and coaching

Defect: the wrapper allowed classifier narrative (“bill/email notification”) to override its successful `doc_type=receipt` routing result, so Mazda prematurely stopped. Worse, Mazda activated proposal 45 as `wrap-v003`, teaching a conflicting short-circuit rule. Proposal 46 correctly says a STEP 0 receipt result is binding, but was only proposed, not activated. Human should review/roll back or supersede `wrap-v003` before the next scan.

Exact lesson sent once at 22:15:33Z:

> Corrective lesson for trace 62 / this AT&T scan: classify_scan.py successfully returned doc_type="receipt", confidence 0.95, merchant="AT&T". You then stopped and recorded that routing was incomplete, skipping the contract's required receipt parse, normalized vendor check, duplicate check, categorizer, and save. Wrapper defect: the procedure lacks a guard that a successful STEP 0 routing result is binding even when the reason describes a utility bill/email notice. For this run, continue NOW: call propose_improvement(trace_id=62, failure_type="missed_vendor_key", ...) because the judge returned FAIL; then run parse_and_categorize.py --json via executor_run with the exact cwd/env from dispatch, derive a real AT&T vendor key, call check_vendor_key and adopt its normalized key, check_duplicates (use 1970-01-01 only if parsing finds no date), run categorizer_main.py, store with parse_and_categorize.py --save, record a new truthful document-intake trace, judge it, and POST /api/expense-stored if storage succeeds. Record this durable rule in memory: once STEP 0 returns receipt, never stop based on prose document semantics; complete every receipt stage unless a tool return itself blocks the stage. Do not claim completion without successful tool returns.

Nearby-file audit: existing `att_02_06_25_80_24.jpeg` is a different monthly bill due `2025-01-09`; it does not prove this `2025-02-09` bill is a duplicate. The current scan is a real JPEG, so no extension/content anomaly was found.
