# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch: 2026-07-25T22:37:32.000Z
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785019047528848046_e343103e3604.jpg`
- Verdict: PASS

## Checklist

1. `load_wrapper_revision` seen at `2026-07-25T22:37:38+00:00` with `wrap-v052`.
2. `classify_scan.py` returned `doc_type=receipt`, `merchant=Burlington`, `confidence=0.99` at `2026-07-25T22:37:59+00:00`.
3. STEP 0 first failed on Gemini quota at `2026-07-25T22:38:26+00:00` (`429`).
4. Corrective coaching was sent to retry STEP 0 with the fallback parser path and continue the intake.
5. Corrected parse completed and wrote `/tmp/mazda_receipt_fea900b0e88d.json` at `2026-07-25T22:40:43+00:00`.
6. `check_vendor_key` returned normalized `vendor_key=burlington`; `check_duplicates` returned `is_exact_duplicate=false`.
7. `categorizer_main.py` returned `vendor_key=burlington`, `category_id=3`.
8. `parse_and_categorize.py --save` stored expense `1582` with `expense_date=2025-03-03` and `amount=44.97`.
9. `itemize_existing_expense` succeeded with exact reconciliation.
10. `record_trace` saved trace `215`; `judge_trace` returned `NEEDS_REVIEW`; `propose_memory_note` was invoked.
11. Dashboard callback completed and the event stream shows the stored receipt event for this dispatch with `expense_id=1582`.

## Wrapper Defect

The receipt parsing path initially stopped on a Gemini `429` quota error instead of flowing through the fallback engine. That is a wrapper routing defect: provider failure should not end STEP 0.

## Lesson Sent

I told Mazda to treat the Gemini quota failure as non-terminal, retry STEP 0 with the alternate ChatGPT-OAuth/Codex fallback parser path for the same image, write the source-bound parse artifact, then continue immediately through vendor check, duplicate check, categorization, save, trace, judge, and dashboard callback, and record the lesson in memory.

## Human Follow-up

`judge_trace` ended as `NEEDS_REVIEW` because Burlington is not in the known-vendor map. The intake itself completed successfully; a human may want to review whether the vendor map should learn Burlington.
