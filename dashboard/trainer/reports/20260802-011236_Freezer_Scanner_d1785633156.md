# Trainer Report — Freezer Scanner

**Document:** `scan_freezer_1785633152504449174_8ff098f6b075.jpg`
**Dispatch:** 2026-08-02T01:12:36Z (unix 1785633156)
**Mazda conversation:** `conv-79f2a336-eb4f-4603-993f-07977fdb11a2`

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), so Mazda correctly ran STEP 0 vision classification herself and completed the full receipt/invoice branch end to end.

## Step-by-step checklist

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v070` logged. ✅
2. STEP 0 `classify_scan.py` → `doc_type=receipt`, confidence 1.0, reason: handwritten initials + date "3-19" circled with a line pointing at the "Total due" $171.17, i.e. a **paid invoice → routed as receipt**. Correct per contract ("a visibly paid invoice is a receipt"). ✅
3. `parse_and_categorize.py --json` (STEP 0 parse) → 3 line items (Account charges $15.00, Internet $82.21, Phone $73.96) summing exactly to the printed total $171.17. `expense_scope: "full_receipt"`, `handwritten_arithmetic_total: null`, all `selected_for_expense: true`. Correct scope — the handwriting here confirms full payment, not a marked-item selection, and Mazda read it that way. ✅
4. `check_vendor_key` → `at_t`, recognized. `check_duplicates` → not a duplicate. ✅
5. `categorizer_main.py --provider=auto` → `category_id: 124` (provider `gemini`, first try succeeded). ✅
6. `parse_and_categorize.py --save --category-id=124 --parsed-json ...` → `{"success": true, "expense_id": 1975, ...}`, `parse_artifact_verified: true`, archived to `readable_documents/receipts/2025/march/march_01/at_and_t_03_01_25_171_17.jpg`. ✅
7. `itemize_existing_expense` → `itemizable: true`, `itemized: true`, difference `0.00`, `itemization_parent_id=1975`, children `[1976, 1977, 1978]`, all category 124. ✅
8. `record_trace(task_name="document-intake")` → evidence JSON matches reality (`stored:true`, `expense_id:1975`, itemization fields populated). ✅
9. `judge_trace(336)` → **PASS**, "all deterministic intake checks passed". ✅
10. Dashboard notify: `curl POST /api/expense-stored` → `{"ok": true}`, confirmed landed in `recent_report.json` under `scanner_intakes.Freezer Scanner`. ✅

No `propose_improvement` call — correctly omitted since the verdict was PASS.

## Supporting-document / dialog-visibility check (Trainer's own verification)

`POST /api/supporting-documents {expense_id}` for 1975 (parent) and all three children (1976/1977/1978) all correctly return `receipt_url` present, `available:true`, "View Receipt" action. ✅

**Red-box annotation — partial failure, application defect (not Mazda's).** Fetching `/supporting-document/<id>/receipt` for all four graded rows:

| id | role | amount | boxed? |
|---|---|---|---|
| 1975 | parent | $171.17 | ❌ no — `opened without highlight`, reason: "No high-confidence expense row was found in the image." |
| 1976 | child | $15.00 (Account charges) | ✅ yes — served bytes (586101) differ from raw file (577373) |
| 1977 | child | $82.21 (Internet) | ❌ no — same reason as above |
| 1978 | child | $73.96 (Phone) | ❌ no — same reason as above |

Only 1 of 4 graded rows got a highlight box; the other 3 served the raw unannotated image. Per the contract, this makes those three expenses unverifiable via the dashboard's red-box viewer, but it is `dashboard/document_annotation.py`'s matching logic, not Mazda's fault (she produced correct, reconciled OCR/line data). Filed below as an escalation candidate rather than a Mazda coaching item — no message was sent to her.

## Wrapper defect diagnosed

None. Every stage produced a successful tool return; no coaching was necessary.

## Escalation

repo_path: /home/adamsl/letta-code
bug_description: |
  document_annotation.py's line-matching failed to draw a highlight box for 3 of 4
  graded rows on this itemized AT&T receipt (parent expense 1975 and children 1977,
  1978), logging "[supporting-document] opened without highlight ... reason=No
  high-confidence expense row was found in the image." Only child 1976 ($15.00,
  "Account charges") was successfully boxed — confirmed by comparing served byte
  counts against the raw file (586101 vs raw 577373 for 1976; the other three all
  served exactly 577373, i.e. the untouched original). The receipt is a normal
  printed AT&T bill with 3 clearly itemized lines ($15.00 / $82.21 / $73.96) summing
  to the printed total $171.17 — not a hard case like crossed-out amount columns.
  Suspect the OCR/line-matching in the receipt annotation strategy is failing to
  reconcile the parent's "Total due $171.17" line and at least one of the line-item
  amounts against the parsed evidence. Worth checking whether the matcher requires
  an exact single-line OCR hit and doesn't fall back to a looser match the way the
  known "pen across the amount column" case does.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785633152504449174_8ff098f6b075.jpg
  archive_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/march/march_01/at_and_t_03_01_25_171_17.jpg
  failing_command: "GET /supporting-document/1975/receipt (and /1977, /1978)"
  expense_ids_unboxed: [1975, 1977, 1978]
  expense_id_boxed_ok: 1976

## Secondary note (non-blocking, worth a look)

`parse_and_categorize.py --save` logged a real Python warning during the save step
that did not block storage: `WARNING:__main__:Source-counterpart lookup failed: not
enough arguments for format string`. This looks like a string-formatting bug in the
supporting-document source-counterpart lookup (the logic that tries to find a
matching statement/document to cross-link on save). It didn't affect this run's
outcome (`linked_counterpart: false`, which is correct since no statement exists yet
for this expense), but the underlying format-string bug should be found and fixed —
otherwise cross-document linking could silently no-op even when a real counterpart
exists. Not filed as a full escalation block since it didn't change this run's
verdict and I don't have the exact source location, but flagging for a human to
grep `rol_finances/tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py`
for "Source-counterpart lookup failed".

## For a human

- The Freezer Scanner run itself is clean — nothing to fix on Mazda's side.
- Two dashboard-side defects surfaced (see Escalation + secondary note above),
  both in this repo / rol_finances, neither invalidating this run's PASS verdict.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1975`, receipt: No high-confidence expense row was found in the image.
- expense `1977`, receipt: No high-confidence expense row was found in the image.
- expense `1978`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
