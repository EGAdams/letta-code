# Mazda Trainer Report

- Scanner: Window Scanner
- Dispatch time: 2026-07-16T15:54:37Z
- Verdict: FAIL

## Evidence

- `load_wrapper_revision(agent_name="Mazda")` succeeded at 15:57:08 and returned `wrapper_revision=wrap-v006`.
- `classify_scan.py` on `scan.jpg` succeeded and returned `doc_type=invoice`, `confidence=0.99`, `merchant=Jacob Menninga`.
- `record_trace` saved `trace_id=74` with `task_name="document-intake"`, but the evidence JSON showed no vendor lookup, no duplicate check, no categorization, no store, `stored=false`, and `expense_id=null`.
- `judge_trace(trace_id=74)` returned `FAIL` with `failure_type=wrong_category`.
- `propose_improvement(trace_id=74)` succeeded and filed `proposal_id=53`.
- `apply_proposal(proposal_id=53)` succeeded and activated `wrap-v007`.
- No successful `check_vendor_key`, `check_duplicates`, categorizer, store, or dashboard notification call was observed for this dispatch.

## Diagnosis

Wrapper defect: the invoice path is still short-circuiting after classification instead of continuing through vendor resolution, duplicate check, categorization, and `parse_and_categorize.py --save --invoice` as required by the current dispatch contract.

## Lesson Sent

I sent Mazda a corrective message telling her to restart from the current dispatch, stop reusing stale context, and finish the full intake loop on `scan.jpg` by running the required steps through `record_trace` and `judge_trace` instead of stopping at a no-op probe.

## Human Follow-Up

Check the invoice handling wrapper and learned rules for any remaining short-circuit that treats invoices as non-stored placeholders instead of following the full intake path.
