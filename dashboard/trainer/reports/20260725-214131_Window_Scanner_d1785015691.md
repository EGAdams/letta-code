# Mazda Trainer Report

- Scanner: `Window Scanner`
- Dispatch time: `2026-07-25T21:41:31Z`
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785013860978749608_37871ba59271.jpg`
- Verdict: `FAIL`

## Checklist

1. `load_wrapper_revision(agent_name="Mazda")`
   - Pass. Seen at `2026-07-25T21:41:34+00:00`, wrapper `wrap-v050`.

2. Statement branch / store
   - Pass for storage. `store_statement_transactions.py` returned `ok:true`, `transactions_parsed:11`, `duplicates:10`, `stored:0`, `skipped_credits:1`, `account_last4:"4882"`, `account_last4_source:"operator"`, and `archive_paths` for the Barclays April 27 to May 25 archive.
   - The run was duplicate-only, so no new expenses were stored. That part is acceptable.

3. Statement annotations
   - Fail on the first two attempts. Mazda used a malformed shell fragment that triggered `/usr/bin/bash: -c: line 13: syntax error near unexpected token ';'`, then retried with unsupported flags (`--expense-id`, `--statement-json`) and hit the annotation script usage error.
   - Later she recovered the call shape and invoked `apply_statement_annotations.py --image ... --expense-ids 1568,1569,1570,1577,1571,1572,1573,1574,1575,1576`, but this still did not complete the full close-out.

4. Report generation / hydration / audit
   - Partial pass. `report.html` was created/updated in the scan directory and now contains the required `id="verified-transactions"`, `data-vendor-key`, and `rol-category-picker:start` markers.
   - Fail overall because the audit step was pointed at the wrong path at least once (`tools/python_tasks/audit_statement_reports.py` does not exist) and there was no verified final end-to-end close-out after that.

5. `record_trace`
   - Missing.

6. `judge_trace(trace_id)`
   - Missing.

7. `propose_improvement`
   - Missing. No trace was recorded, so there was no valid trace id to attach.

8. Dashboard notify
   - Missing. No confirmed `/api/expense-stored` callback appeared in the transcript for this run.

## Wrapper defect diagnosed

The statement post-store wrapper is brittle in two places:
- It built an invalid shell command for the annotation step, causing allowlist/syntax failures instead of a clean deterministic retry.
- It also used an incorrect audit path once, which prevented the report pipeline from finishing cleanly.

## Exact lesson sent to Mazda

I sent a corrective message telling Mazda that the statement was stored as duplicate-only rows, but the post-store flow was broken. I instructed her to:
- rerun annotations with `apply_statement_annotations.py --image <scan> --expense-ids <comma-separated duplicate ids>`
- build the exact `report.html`
- run `restructure_verified_transactions.py`, `hydrate_report_categories_from_db.py`, and `audit_statement_reports.py` on that report
- then call `record_trace(task_name="document-intake")`, `judge_trace(trace_id)`, and post the dashboard callback with `report_path`
- record the correction in memory and continue on the same immutable scan

## Human note

The statement itself looks clean and duplicate-only, but the intake is incomplete because the run never produced the required trace/judge/callback closure. The report artifact is present, but the wrapper still needs a robust statement close-out path that survives shell quoting and audit-path mistakes.
