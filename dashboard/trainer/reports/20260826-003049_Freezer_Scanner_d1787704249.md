# Mazda Trainer Report — Freezer Scanner

Verdict: CORRECTED

- Document: `scan_freezer_1787704245921240997_a92ab389b52d.jpg`
- Dispatch: 2026-08-26T00:30:49Z
- Conversation: `conv-858c6394-2603-4172-b90f-9df54b5dae13`
- Facade result: `doc_kind=unknown` → STEP 0 self-classification required.

## What the document actually is

An AOL email screenshot: Jacob Menninga → Rosemary Barnes, "External Hard Drive - Certificate
of Ownership." Body text states the price is $317.98 ($299.99 + $17.99 tax), references an
attached Amazon invoice, and asks Rosemary to pay him back. A handwritten annotation on the
page reads "pd 317.98 · 6.13.25 ✓" — a payment confirmation. Handwritten arithmetic
(299.99 + 17.99 = 317.98) matches the printed total exactly.

## First pass — FAIL

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | wrap-v081, returned successfully |
| STEP 0 classify | `classify_scan.py` → `doc_type=other, confidence=0.98`, reason: "printed email correspondence... not a standalone receipt, invoice, statement, ledger, or tax form" |
| Investigate/categorize/store | **skipped** — wrapper Rule 4 stops on `doc_type=other` |
| `record_trace` (469) | `stored=false`, `problems=["Unsupported document..."]` |
| `judge_trace` (469) | PASS (correctly consistent with the *reported* evidence, but the evidence itself was wrong) |
| Dashboard callback | sent, `doc_kind=other` |

This is exactly the class of failure the Trainer's own instructions name explicitly: "an email
screenshot containing an invoice is still an invoice; only `doc_type=other` is unsupported,"
and the cited historical regression is literally a **Jacob Menninga** invoice. The classifier's
raw "other" label should never have been accepted at face value here — the email states a
vendor/payee, a computed amount, and a payment request, and carries a handwritten paid
annotation. This is a wrapper (routing/classification) defect, not an application defect.

## Coaching sent

Sent one corrective message at 00:33:45Z: reclassify as invoice/receipt (override the raw
classifier label per the contract), resolve the payee as Jacob Menninga (not Amazon — he is
the one being reimbursed), treat the handwritten "pd 317.98 6.13.25" + matching arithmetic as
proof of a fully-paid receipt (`expense_scope=full_receipt`, not marked-items), and redo
trace/judge/callback for this same document.

## Second pass — CORRECTED

Mazda re-ran `classify_scan.py` (still returned `other` — confirmed classifier-level defect,
not a one-off), then manually applied the receipt/invoice override per the contract:

| Step | Evidence |
|---|---|
| Parse with overrides | `parse_and_categorize.py --json --transaction-date-override 2025-06-13 --total-amount-override 317.98 --merchant-name-override 'Jacob Menninga' --vendor-key-override jacob_menninga`; artifact confirms `expense_scope=full_receipt`, `handwritten_arithmetic_total=317.98` |
| `check_vendor_key` | `vendor_key=jacob_menninga`, `recognized=true` |
| `check_duplicates` | `is_exact_duplicate=false`, `in_database=false` |
| `categorizer_main.py` | `category_id=197` |
| Save | `parse_and_categorize.py --save --category-id=197 --parsed-json <corrected artifact>` → **expense_id 2242**, archived to `readable_documents/receipts/2025/june/june_13/jacob_menninga_06_13_25_317_98.jpg` |
| `itemize_existing_expense` | `itemizable=false`, reason: single line ($299.99) doesn't cent-reconcile to the $317.98 charge including tax → correctly left standalone |
| `record_trace` (473) | `doc_kind=receipt`, `vendor_key=jacob_menninga`, `category_id=197`, `stored=true`, `expense_id=2242`, `expense_scope=full_receipt`, problems array documents the override rationale |
| `judge_trace` (473) | PASS |
| Dashboard callback | sent, `expense_id=2242`, `doc_kind=receipt`, `vendor=Jacob Menninga` |
| `propose_memory_note` | proposal 268, gates all `allow`, recorded as approved persona-note: correspondence with vendor/amount/payment-ask + paid annotation = invoice/receipt evidence, never `other` |

## Independent verification (Trainer's own check)

- Fetched `GET /supporting-document/2242/receipt` immediately after storage: bytes were
  byte-identical (md5) to the raw file on disk — background annotation job had not yet
  completed (the app's own pending-highlight path, not a failure log).
- Re-fetched ~90 seconds later: served bytes now **differ** from the raw file (different
  md5/size) — the red-box annotation completed and is being served. Dialog-visibility/red-box
  contract is satisfied for expense 2242.
- `GET /api/supporting-documents?expense_id=2242` and a few guessed variants 404'd; not treated
  as a defect since the receipt viewer route itself worked and returned a correctly annotated
  image — I did not have the exact query-param contract for that endpoint and this doesn't
  affect the top-line verdict.

## For a human

- The freezer-scan classifier (`classify_scan.py` via Gemini, with Codex CLI fallback due to
  Gemini quota exhaustion) mislabels payment-request emails as `other` even when they name a
  vendor, an amount, and a payment ask — worth a prompt/rule fix at the classifier level so
  future Mazda runs don't need per-document trainer correction for this same pattern (this is
  the second observed instance of the "Jacob Menninga invoice" class).
- No application-defect escalation filed — the defect was fully correctable via wrapper
  coaching within this run.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
