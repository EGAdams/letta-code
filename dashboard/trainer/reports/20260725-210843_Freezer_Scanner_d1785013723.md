# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-25T21:08:43Z
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785013720902357119_d6e881a2d6ba.jpg`
- Verdict: PASS

## Checklist

- `load_wrapper_revision` ran successfully at `2026-07-25T21:08:51Z`.
- Mazda classified the scan as `receipt` with confidence `0.97`.
- Mazda ran `check_vendor_key` and got normalized vendor key `meijer`.
- Mazda ran `check_duplicates` and the save-stage duplicate guard matched existing `expense_id 1567`.
- Mazda ran `record_trace` with `task_name="document-intake"` and saved trace `200`.
- Mazda ran `judge_trace(200)` and received `PASS`.
- Mazda sent the dashboard `/api/expense-stored` callback with `stored=0`, `is_duplicate=true`, and `duplicate_expense_ids=[1567]`.

## Evidence

- `load_wrapper_revision` tool call and successful return.
- `check_vendor_key` successful return showing `meijer`.
- `check_duplicates` successful return showing duplicate `expense_id 1567` and `id_light meijer_03_11_25_22_89`.
- `record_trace` successful return:
  - `doc_kind=receipt`
  - `classification_confidence=0.97`
  - `vendor_key=meijer`
  - `category_id=3`
  - `duplicate_checked=true`
  - `is_duplicate=true`
  - `stored=false`
  - `expense_id=null`
  - `problems` note the duplicate guard matched the canonical stored record
- `judge_trace` successful return:
  - `trace_id=200`
  - `verdict=PASS`
  - `failure_type=none`
- Dashboard callback via `executor_run` completed successfully with HTTP `200` and JSON `{"ok": true}`.

## Notes

- This was a clean duplicate-only pass. Mazda did not store a new expense, which is correct because the final save-stage duplicate guard found the existing canonical record.
- The transcript shows Mazda observed a marked-items total of `25.56` during parsing, but the duplicate path matched the already-stored canonical receipt record for `22.89`. No additional correction was required for this run because no new expense was inserted.
