# Mazda Trainer Report — Window Scanner

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785428434474967384_ccc29fe7c026.jpg`
- **Dispatch**: 2026-07-30T16:20:37Z
- **Conversation**: `conv-78158d3a-8ae3-45ec-adeb-b8c2e305e021`
- **Facade result**: `doc_kind=unknown`, confidence 0.0 → STEP 0 vision classification required
- **Run duration**: ~92s (16:20:37 dispatch → 16:22:09 judge_trace)
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Checklist with evidence

1. `load_wrapper_revision` — wrap-v067, active. ✅ (16:20:42)
2. STEP 0 classification — `classify_scan.py` via `executor_run`. Gemini hit a 429 quota error;
   tool fell back to OpenAI Codex CLI as designed and returned `doc_type=receipt`,
   `confidence=0.98`, merchant "Intercessors for America". ✅ correct fallback behavior (16:21:10).
3. Receipt branch parse — `parse_and_categorize.py --json --engine=gemini --write-parsed-json`
   succeeded (Gemini worked for this call). Single line item ("Tax Deductible Gift Received",
   $30.50), `expense_scope=full_receipt`, `selected_for_expense=true`, `beneficiary_hint="Rosemary
   Barnes"` passed through verbatim. No marked-item ambiguity — one item, one amount. ✅ (16:21:31)
4. `check_vendor_key` → `vendor_key=intercessors_for_america`, `recognized=true`. Adopted. ✅ (16:21:35)
5. `check_duplicates` → not exact, but a fuzzy match on `check_11051_03_18_25_30_50` (expense id
   1547), same date (2025-03-18) and amount (30.50), explicit note: `"CONFIRM before storing"`
   because the existing row is instrument-only (a check number) with no payee. ✅ (16:21:40)
6. Mazda investigated expense 1547 directly (read-only `SELECT` via `executor_run`/pymysql) and
   confirmed it already carries `receipt_url` (an earlier scan of the same document),
   `document_url` (the March/April Fifth Third statement), and `category_id=190` (Gifts & Love
   Offerings) — matching the fuzzy candidate's identity. This satisfies the duplicate tool's
   "CONFIRM" requirement with real evidence rather than blind acceptance. ✅ (16:21:47)
7. `categorizer_main.py --provider=auto` → `category_id=190`, consistent with the existing
   expense's category. ✅ (16:21:57)
8. `record_trace` (`task_name=document-intake`) — `stored=false`, `expense_id=null`,
   `is_duplicate=true`, `duplicate_checked=true`, `vendor_key_recognized=true`, `category_id=190`,
   `problems` clearly explains the duplicate reasoning. Matches the duplicate-only evidence
   contract (no expense misrepresented as newly stored). ✅ trace_id 308 (16:22:05)
9. `judge_trace(308)` → `PASS`, `failure_type=none`. ✅ (16:22:09)
10. Dashboard callback `POST /api/expense-stored` — `stored=0`, `parsed=1`,
    `duplicate_expense_ids=[1547]`, `expense_ids=[]`. Fired after trace/judge as required. ✅
    (16:22:17, `{"ok": true}`)

No store was attempted for the receipt/invoice branch since a confirmed duplicate was found —
correct per contract ("a detected duplicate means: skip store, still trace + judge"). No
itemization needed (single line item). Supporting-document invariants: existing expense 1547
already has `receipt_url`/`document_url` populated from prior intake, so no new association write
was required or attempted.

## Wrapper defect diagnosed

None. This run exercised the Gemini→Codex-CLI classification fallback correctly, resolved a
genuinely ambiguous fuzzy duplicate with real confirming evidence instead of guessing, and
completed the full trace/judge/callback contract in under two minutes.

## Lesson sent to Mazda

None needed — no coaching message sent.

## For a human

Nothing actionable. Note only that Gemini's classification quota (`generativelanguage.googleapis.com`,
free tier, 20 req/day for `gemini-3-flash`) was already exhausted again today; the Codex CLI
fallback covered it transparently, consistent with prior known behavior.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
