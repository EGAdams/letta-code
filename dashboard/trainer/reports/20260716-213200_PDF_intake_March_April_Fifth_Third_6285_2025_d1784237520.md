# Trainer Report — PDF intake: March-April Fifth Third 6285 (2025)

- **Document**: `/home/adamsl/rol_finances/readable_documents/bank_statements/march/march_april_fifth_third_6285_2025.pdf`
- **Scanner/source**: PDF intake (not a physical scan)
- **Dispatch**: 2026-07-16T21:32:00Z
- **Mazda conversation**: `conv-ed609ee6-427e-4c36-a636-6407aa95bea7`
- **Verdict**: **CORRECTED** (was FAIL as originally run; corrected within this session to a legitimate fail-closed partial store)

## What happened (first pass, uncoached)

1. `load_wrapper_revision` — loaded `wrap-v022`, which contains explicit Rule 1/Rule 6: *"For bank_statement/statement documents, always run `parse_statement_scan.py` then `store_statement_transactions.py`."*
2. `classify_scan.py` via `executor_run` correctly identified the document: `doc_type=bank_statement`, `merchant=Fifth Third Bank`, `confidence=1.0`.
3. `run_claude_code_sdk` was tried and correctly rejected (venv-linked commands must go through `executor_run`).
4. Mazda then **ignored her own wrapper's Rule 1/6** and never called `parse_statement_scan.py`. Instead she spent 5 tool calls manually probing for PDF text-extraction libraries (`pdftotext`, `mutool`, `fitz`, `pypdf`, `PyPDF2`, `pdfplumber`, `pdfminer`) using a **bare `python3`** interpreter instead of `/home/adamsl/rol_finances/.venv/bin/python3` — so every module check falsely reported "not found," even though the correct interpreter has them.
5. She ran an unrelated `web_search` for the filename, then called `record_trace` (trace 93) and `propose_improvement` (proposal 71, `unsupported_document`) claiming no PDF library was available — a false premise caused entirely by the wrong-interpreter bug.
6. She **never called `judge_trace`** before stopping — a hard contract violation (required every run, success or failure).
7. She ended her turn without doing any of the required statement work.

## Wrapper defect diagnosed

Two distinct, fixable defects, not a model-capability problem:
- **Interpreter bug**: nothing in the dispatch/wrapper instructs "never use bare `python3` for venv-dependent probes" strongly enough — Mazda has correctly used `.venv/bin/python3` for `classify_scan.py` moments earlier in the same run but drifted to bare `python3` for her own ad-hoc diagnostics. This produced a self-inflicted false negative.
- **Rule-adherence gap**: Rule 1/Rule 6 in her own loaded wrapper explicitly named the two required tools for a classified bank statement, and she skipped straight to freelancing a PDF-text-extraction workaround instead of calling them. `judge_trace` was also skipped outright.

## Coaching sent (3 corrective messages, all in-conversation)

1. Pointed out both violations (skipped Rule 1/6 tools; skipped `judge_trace`); gave exact `executor_run` commands for `parse_statement_scan.py` / `store_statement_transactions.py`; required `judge_trace(93)` immediately.
   - She ran `judge_trace(93)` → **FAIL / unsupported_document** (correctly reflects the real, uncorrected state at that point).
   - Her first retry attempt used an inline `PYTHONPATH=...` prefix that the executor's command allowlist rejected.
2. Corrected the command form (no `PYTHONPATH=` prefix — `executor_run` already sets it; call the venv python directly, same pattern as the earlier successful `classify_scan.py` call).
   - `parse_statement_scan.py` succeeded: **52 transactions parsed**, Fifth Third Bank, account 6285, period 2025-03-15 → 2025-04-15.
   - `store_statement_transactions.py` ran: 18 credits skipped (deposits, correct — never expenses), 1 stored (expense_id 1530), 0 duplicates, **33 rows blocked** on `no category mapping for statement vendor` (fail-closed, per Rule 3 — correct behavior, not a bug).
   - She sent a dashboard callback, recorded a new trace (94), called `judge_trace(94)` → FAIL (accurately reflects the 33 unresolved rows), and filed `propose_improvement` (73) capturing the interpreter/rule-skip lesson.
3. Flagged that her first dashboard callback used placeholder single-transaction fields (`expense_date`, `amount:"0"`) that misrepresent a 52-row statement, and that several of the 33 "failed" rows are **not** unresolvable checks but truncated real vendor names (`AT&T U-VER-`, `AMERICAN E-`, `DINERS CLU-`, `PRIORITY H-`, `GUIDING LI-`, `CONSUMERS-`, `GFL ENVIRO-`, `BARCLAYCARD`) that her wrapper's Rule 10 (alias lookup) exists for but isn't matching.
   - She filed a **separate, correctly-typed** `propose_improvement` (74, `missed_vendor_key`) naming the exact failing strings and a suggested prefix/alias mapping.
   - She sent a corrected dashboard callback with `parsed=52, stored=1, skipped_credits=18, failed=33` and a problems summary instead of the earlier misleading single-row fields.

## Verification checklist

| Step | Status | Evidence |
|---|---|---|
| `load_wrapper_revision` | ✅ | wrap-v022 loaded |
| Classification | ✅ | `classify_scan.py` → bank_statement, Fifth Third, conf 1.0 |
| Statement parse (Rule 1/6) | ✅ (after coaching) | `parse_statement_scan.py` → 52 transactions |
| Statement store | ✅ (after coaching) | `store_statement_transactions.py` → stored=1, failed=33 fail-closed, skipped_credits=18 |
| Duplicate check | ✅ | ran inside `store_statement_transactions.py`, 0 duplicates |
| Category fail-closed guard | ✅ | 33 rows correctly blocked, no bad inserts |
| `record_trace` (`task_name=document-intake`) | ✅ | trace 93 (uncorrected), trace 94 (corrected) |
| `judge_trace` | ✅ (after coaching) | trace 93 FAIL, trace 94 FAIL (accurately reflects 33 unresolved rows — this is expected until vendor-alias coverage improves, not a false verdict) |
| `propose_improvement` on FAIL | ✅ | proposal 71 (superseded/incorrect premise), 73 (interpreter/rule-skip lesson), 74 (vendor-alias gap, correctly typed `missed_vendor_key`) |
| Dashboard notify (`/api/expense-stored`) | ✅ (corrected) | first callback misrepresented outcome; second callback accurately reports parsed/stored/skipped/failed counts |

## For a human to look at

- **Proposal 71** (`unsupported_document`, false premise from the interpreter bug) should be reviewed and rejected/superseded in favor of proposal 73 — it will poison future statement runs if approved as-is.
- **Real, unresolved data issue**: 32 of 33 failing rows in this statement have genuine or plausible vendor identity (checks aside) that the current vendor-alias resolver can't match because Fifth Third truncates merchant names (e.g. `AT&T U-VER-`, `AMERICAN E-`). Proposal 74 targets this; consider prioritizing it since it affects every Fifth Third statement, not just this one.
- 17 of the 33 failures are `Check ####` rows with no merchant text at all — these are not fixable via alias matching and may need a policy decision (e.g., a default "uncategorized check" review category) rather than a resolver fix.

## Summary

Mazda initially failed this PDF-statement intake by ignoring her own wrapper's explicit instruction to run `parse_statement_scan.py`/`store_statement_transactions.py`, misdiagnosing a real bank statement as unsupported due to a self-inflicted wrong-interpreter bug, and skipping the mandatory `judge_trace` call — all fixed by three rounds of in-conversation coaching. After correction, the statement was correctly parsed (52 transactions) and stored fail-closed (1 stored, 33 correctly blocked on missing category mappings, 18 deposits correctly skipped, 0 duplicates), with two properly-scoped improvement proposals filed (the interpreter/rule-skip lesson and a genuine vendor-alias resolution gap affecting truncated Fifth Third statement descriptors) and an accurate dashboard callback sent as the final action.
