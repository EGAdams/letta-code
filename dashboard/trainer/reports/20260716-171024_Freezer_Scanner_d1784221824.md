# Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Dispatch:** 2026-07-16T17:10:24Z
- **Conversation:** `conv-9b5d9eff-9a55-4626-b657-17c62b19f50e`
- **Verdict: PASS** (after Mazda's own self-improvement loop healed an initial FAIL, and an external
  correction fixed an OCR/vendor misread)

## Timeline / checklist

1. `load_wrapper_revision` — ✅ loaded wrap-v018.
2. STEP 0a `classify_scan.py` (facade was `doc_kind=unknown`) — ✅ `doc_type=receipt`, confidence
   0.78, merchant "Rosemary" (handwritten note: "2 paid from Rod for Kim wood to get two books
   from Sharon Buck... Rosemary ('love offering')").
3. STEP 0b `parse_and_categorize.py --json` — ✅ ran, but `party.merchant_name` came back `null`
   (Pydantic validation error on the primary engine, gemini fallback to chatgpt-oauth also
   returned `merchant_name: null` — expected for a handwritten note with no store name). Mazda
   reasonably derived `vendor_key="rosemary"` from context instead of stopping.
4. STEP 2a `check_vendor_key` — ✅ ran, correctly reported "rosemary" unrecognized.
5. STEP 2b `check_duplicates` — ✅ ran, not a duplicate.
6. STEP 3 categorize — ⚠️ **defect**: Mazda first called the MCP verification tool
   `check_category(category_id=0)` — a fabricated placeholder — instead of running
   `categorizer_main.py` first. She then did run `categorizer_main.py` correctly, which returned
   `category_id: null` (vendor research failed — "gemini CLI is not installed or not on PATH").
7. STEP 4 store — ❌ **defect**: instructions require *omitting* `--category-id` when it's null;
   Mazda instead passed `--category-id=0` (the fabricated placeholder from step 6), which hit a
   MySQL foreign-key constraint (`category_id 0` doesn't exist) → `stored: false`, no expense_id.
8. STEP 5 `record_trace` (trace 85) — ✅.
9. STEP 6 `judge_trace` — ✅ verdict **FAIL**, `failure_type=wrong_category`.
10. STEP 7 `propose_improvement` (proposal 65) → `apply_proposal` — ✅ Mazda correctly diagnosed
    the root cause in wrapper terms ("store step was attempted with category_id=0, which
    triggered a foreign key failure instead of storing uncategorized") and installed a durable
    rule: *"When categorizer returns null category_id for a receipt or invoice, omit
    --category-id entirely during save instead of passing 0."* Activated as **wrap-v019**, gates
    all `allow`.
11. STEP 8 dashboard notify — ✅ fired even on the failed store (`stored:0, expense_id:null`),
    confirmed in `/api/expense-stored-events`.

**No coaching from the Trainer was needed for the FAIL** — Mazda ran the entire self-improvement
loop (trace → judge → propose → apply) correctly and unassisted before I could intervene.

## External correction (not sent by the Trainer)

At 17:14:12Z — before any Trainer message was sent — a `user_message` arrived in this
conversation from an outside source providing visual-review ground truth: date 2025-03-15, total
**29.00** (not the OCR'd 29.08), counterparty **Sharon Buck** (Rosemary is only the signer/note
author), category **190 Gifts & Love Offerings**, plus a note that a durable
`sharon_buck -> 190` vendor mapping was installed. Mazda re-ran
`parse_and_categorize.py --save --category-id 190 --engine auto --merchant-name-override
"Sharon Buck" --transaction-date-override 2025-03-15 --total-amount-override 29.00`, then:

- `record_trace` (trace 86) — ✅
- `judge_trace` — ✅ verdict **PASS**, "all deterministic intake checks passed"
- Dashboard notify — ✅ `expense_id 1518`, `amount 29.00`, `vendor_key sharon_buck`

Confirmed against `/api/expense-stored-events`: event at `stored_at=1784222122` matches exactly
(expense_id 1518, amount 29.00, vendor_key sharon_buck, stored:1).

## Anomaly for human review (NOT part of this run — flagged, not acted on)

`/api/expense-stored-events` also shows a later event at `stored_at=1784222226.9` (~105s after
this conversation's last activity) for the **same** `document_path`
(`scan_freezer.jpg`): `expense_id 1520`, `amount 29.08` (the wrong OCR figure, not the corrected
29.00), `vendor_key "null"` (literal string), `description "Sharon Bues"` (typo of Sharon Buck).
This did **not** come from any tool call in `conv-9b5d9eff-9a55-4626-b657-17c62b19f50e` — I
polled this conversation through 17:15:25Z and it went quiet with no further activity. Per the
instructions on reused scanner filenames, I have not attributed this to Mazda's handling of this
document. It looks like a possibly bad/duplicate store (wrong amount, broken vendor_key, typo'd
name) for the same physical receipt from a run I was not watching — worth a human checking
whether expense 1520 should be removed/corrected, and why it wasn't caught by duplicate detection
against the just-stored 1518 (same date, near-identical amount).

## Wrapper defect diagnosed

Same as Mazda's own proposal 65: passing a fabricated `category_id=0` (from an earlier
verification-tool misuse) into the store command instead of omitting `--category-id` per the
STEP 4 instruction. Already fixed and activated as wrap-v019 — future runs should no longer
reproduce this. No further coaching sent.
