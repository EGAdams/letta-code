# Trainer Report — Freezer Scanner

**Dispatch:** 2026-07-31T01:28:14Z (unix 1785461294)
**Document:** `scan_freezer_1785461289096518671_f1a670d7c79b.jpg`
**Mazda conversation:** `conv-d8d1ee02-82a6-434c-a19a-5358f567041f`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Document
Facade returned `doc_kind=unknown` (JPEG scan, no extractable text) — STEP 0 vision
classification required and performed. Classified as **invoice** (confidence 0.98):
Priority Health, $55.00, health premium bill due 2025-03-01, `$0.00 balance` correctly
read as reflecting *prior* payment, not this invoice being paid — unpaid invoice, not a
receipt.

## Checklist (all with successful tool returns, verified in transcript after dispatch ts)

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | `wrap-v069`, found=true |
| STEP 0 classify | `classify_scan.py` → `doc_type=invoice, confidence=0.98, merchant=Priority Health` |
| Parse | `parse_and_categorize.py --json --engine=gemini` → full parsed JSON, `expense_scope=full_receipt`, single line item |
| `check_vendor_key` | normalized `priority_health` → `priority_health_9392` (adopted) |
| `check_duplicates` | `is_exact_duplicate=false`, `in_database=false` |
| Categorize | `categorizer_main.py --provider=auto` → `category_id=243` (nonzero, valid) |
| Store | `parse_and_categorize.py --save --invoice --category-id=243` → `expense_id=1972`, `expense_status=WAITING_FOR_PAYMENT_COUNTERPART`, `receipt_url` set, archived to `readable_documents/receipts/2025/march/march_01/priority_health_03_01_25_55_00.jpg` |
| Itemization | `itemize_existing_expense` → `itemizable:false`, reason "only one line item; a single line equal to the charge is standalone" — correct fail-closed outcome, remained STANDALONE |
| `record_trace` | `task_name="document-intake"`, trace_id=326 |
| `judge_trace` | **PASS**, `failure_type=none`, "all deterministic intake checks passed" |
| Dashboard callback | `POST /api/expense-stored` → `{"ok": true}`; confirmed via `/api/expense-stored-events` — `expense_id=1972`, `conversation_id` and `dispatched_at` match this run exactly |

## Independent verification (not from Mazda's prose)
- `/api/expense-stored-events?since=1785461294` shows the event for expense 1972 with matching `conversation_id` and `dispatched_at` — genuine evidence for this run, not a stale reuse.
- Red-box annotation confirmed: `GET /supporting-document/1972/receipt` served **672,928 bytes** vs the raw archived file's **658,146 bytes** — the annotated copy was generated, so the receipt is verifiable in the dashboard (View Receipt will show the box).
- Merchant/category are non-blank/non-placeholder; vendor_key is the normalized form, not the raw guess.

## Wrapper defect noticed (non-blocking, worth a look)
Mazda's own summary flagged a warning surfaced during the save step:
`Source-counterpart lookup failed: not enough arguments for format string`
This did not block storage or any downstream step, and Mazda correctly recorded it in
the trace's `problems` rather than silently discarding it. It looks like a Python
string-formatting bug in a source-counterpart lookup helper inside the receipt save
path (`rol_finances`, not this repo). No coaching sent — the run itself is fully
compliant and this is an infrastructure bug, not a Mazda wrapper defect. Recommend
someone check that code path directly rather than asking Mazda to "fix" it.

## Coaching sent
None — no defects requiring correction. `propose_improvement` was correctly not called.

## Summary
Clean, fast (~2 minutes), fully contract-compliant single-line invoice intake: correct
STEP 0 vision classification, vendor normalization, duplicate check, categorization,
fail-closed correct single-line-item itemization refusal, invoice save with proper
`WAITING_FOR_PAYMENT_COUNTERPART` status, trace/judge/callback all present and verified
against independent dashboard evidence, and confirmed red-box annotation on the receipt
viewer. One non-blocking format-string warning was correctly logged by Mazda and is
flagged above for a human to look at in `rol_finances`, but does not affect this run's
verdict.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
