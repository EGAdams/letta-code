# Trainer Report — Freezer Scanner

**Document:** `scan_freezer_1784572554747751100_89912efdcf65.jpg` (MB Markets / BP-branded gas station receipt, $35.00, 2025-03-20, expense_id 1518)
**Dispatch:** 2026-07-20T18:35:58Z
**Verdict: CORRECTED**

## Checklist (evidence = successful tool returns in this run's transcript, `conv-9021f7a4...`)

1. `load_wrapper_revision` — wrap-v030 loaded. ✅
2. STEP 0 (facade returned `doc_kind=unknown`) — `classify_scan.py` → `receipt`, confidence 1.0; `parse_and_categorize.py` (no `--save`) → parsed MB Markets / $35.00 / 2025-03-20. ✅
3. Investigate — `check_vendor_key` → `mb_markets`, unrecognized; `check_duplicates` → not a duplicate. ✅
4. Categorize — `categorizer_main.py` vendor research failed: **"gemini CLI is not installed or not on PATH"** on the executor → `category_id: null`. This is a recurring infra defect (also hit a BJ's receipt per prior sessions).
5. Store — **contract violation**: `parse_and_categorize.py --save` stored expense_id 1518 with `category_id NULL`, `expense_status=NEEDS_VENDOR_KEY`, `pending_vendor_review=true` despite the fail-closed rule (a null category must block storage, not degrade to "pending review").
6. `record_trace` (task_name=`document-intake`) — correctly reported `stored:true, category_id:null` and the problems list. ✅
7. `judge_trace` → **FAIL** (`wrong_category`) — correct verdict; matches the Trainer contract's rule that a stored row with null category is never PASS.
8. `propose_improvement` + `apply_proposal` → **wrap-v031** activated: future runs will fail closed (stop, don't store a pending-review row) instead of storing with null category. Good wrapper-level fix, but did not fix *this* document — flagged in coaching.
9. Dashboard notify — sent once with the broken state.

## Coaching sent

Sent one corrective message requiring her to fix expense_id 1518 *now* (not just propose a future fix): resolve the category deterministically from receipt content, update the stored row, re-verify with `check_category`, and re-notify the dashboard.

## Re-grade after coaching — CORRECTED

- She searched `vendor_category.yaml`, found the pre-existing vendor entry `bp_2836900_leo_gas` → category 374 (`Leo Gas`), matching the BP-branded gas-station receipt.
- Updated expense_id 1518's `category_id` to 374 via a direct SQL `UPDATE` (hand-built, but a plain field correction, not the forbidden itemization parent/child SQL — no dedicated "update existing expense category" tool was available).
- `check_category` re-verified: `category_id: 374, category_name: "Leo Gas", matches_vendor: true`.
- Re-sent `POST /api/expense-stored` with the corrected vendor_key/category state.
- Filed `propose_memory_note` documenting the missing-gemini-CLI infra defect (recorded as an approved proposal only; "no live-memory applier wired" — expected, not an error).

## Wrapper defect diagnosed

Categorize step lacked a fail-closed guard when vendor research errors out (vs. legitimately returning "no category") — the store path let a null-category row through as "pending review." Fixed by wrap-v031 for future runs.

## For a human

- **Standing infra defect, ≥3rd occurrence:** `gemini CLI is not installed or not on PATH` on the executor breaks vendor-research categorization. Needs a real fix (install/PATH-config gemini CLI on the executor host), not another wrapper patch — the wrapper can only make the failure fail-closed, not make research succeed.
- expense_id 1518 is now correctly categorized (374 / Leo Gas); no further action needed on this document.
