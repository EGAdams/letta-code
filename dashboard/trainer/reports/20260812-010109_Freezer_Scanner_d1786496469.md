# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `scan_freezer_1786496464470332759_e1a867a0db1c.jpg`
**Dispatch:** 2026-08-12T01:01:09Z
**Conversation:** conv-01297ab7-ec2d-4d3d-9ea6-4c11fcdf3404
**Run duration:** ~3 minutes (01:01:09 → 01:04:04Z)

**Verdict: PASS**

## Summary

Facade returned `doc_kind=unknown`, so Mazda ran full STEP 0 classification. Document is a
printed Home Depot receipt (2 items, $111.22, card payment, no handwriting or item marks).
Full receipt-branch pipeline executed correctly and completed in one pass with no coaching
required.

## Checklist (evidence from transcript, all timestamps ≥ dispatch)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 01:01:15, `wrap-v005`, is_active=true | ✅ |
| STEP 0 classify (facade=unknown) | `classify_scan.py` 01:01:38 → `doc_type=receipt, confidence=0.93, merchant=The Home Depot` (Gemini 401 → OpenAI Codex CLI fallback, logged) | ✅ |
| STEP 0 parse | `parse_and_categorize.py --json` 01:02:04 → `expense_scope=full_receipt`, `handwritten_arithmetic_total=null`, no `selected_for_expense` marks — correctly a plain full-receipt, not marked-items | ✅ |
| `check_vendor_key` | 01:02:10 → fuzzy-normalized `home_depot` → `the_home_depot`; Mazda adopted the normalized key for every subsequent call | ✅ |
| `check_duplicates` | 01:02:20 MCP tool errored (`1045 Access denied for user 'adamsl'@'localhost'`). Mazda invoked the documented fallback (**wrap-v005 Rule 3**, proposal 41): ran an equivalent authenticated query via `executor_run` (01:02:38, returned `()` — no match) and recorded `duplicate_checked=true` only after that succeeded | ✅ (correct fallback per active wrapper rule) |
| Categorize | `categorizer_main.py --provider=auto` 01:03:04 → `category_id=113` ("Tools"); `check_category` 01:03:08 confirms match | ✅ |
| Store | `parse_and_categorize.py --save --category-id=113` 01:03:17 → `expense_id=2028` created, `receipt_url=the_home_depot_04_30_25_111_22.jpg` archived to `readable_documents/receipts/2025/april/april_30/` | ✅ |
| Itemization | `itemize_existing_expense` 01:03:29 → `itemizable:false`, `reason: "line items total 104.92, charge is 111.22"` (104.92 + 6.30 tax = 111.22 — arithmetic checks out; tax is correctly not itemized as a line). Remains STANDALONE | ✅ correct fail-closed outcome |
| `record_trace` | 01:03:42, `task_name=document-intake`, trace_id=124. Evidence JSON accurate: `vendor_key=the_home_depot`, `category_id=113`, `duplicate_checked=true`, `stored=true`, `expense_id=2028`, `problems` array documents the MCP-auth fallback and the itemization refusal reasoning | ✅ |
| `judge_trace` | 01:03:46 → `verdict=PASS, failure_type=none, reasons=["all deterministic intake checks passed"]` | ✅ |
| `propose_improvement` | not called (correct — PASS, no failure) | ✅ N/A |
| Dashboard callback | `POST /api/expense-stored` 01:03:57 → `{"ok":true}`. Payload carries this run's `conversation_id` and `dispatched_at` (1786496469.599…), `expense_ids:[2028]`, `duplicate_expense_ids:[]`, `archive_paths` matches the stored file | ✅ |

## Trainer's own supporting-document verification

Checked the red-box annotation contract independently (not from Mazda's prose):
- `GET /supporting-document/2028/receipt` → HTTP 200, 336398 bytes.
- Raw source file on disk is 335841 bytes — served bytes differ, confirming an annotated
  (boxed) copy was served, not the raw original.
- Matching cached file found at
  `dashboard/.cache/document-annotations/expense-2028-bfcfdb1a2c42fa44fc56ac38.jpg`
  (336398 bytes, same size as served).

Receipt box renders correctly for this expense.

## Wrapper defect diagnosed

None. The one non-trivial moment — `check_duplicates` MCP auth failure — was handled exactly
per the already-active wrapper rule (Rule 3, proposal 41), and Mazda logged the fallback
honestly in her trace `problems` field rather than skipping the duplicate check.

## Lesson sent to Mazda

None — no coaching needed.

## For a human

Nothing to escalate. `check_duplicates`'s underlying MySQL auth (`adamsl`@`localhost`, no
password) is still broken and every run pays a ~20s fallback-query tax; not a Mazda or
Trainer-report matter, but worth a maintenance ticket to fix the MCP tool's DB credentials
so future runs don't need the Rule-3 workaround at all.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
