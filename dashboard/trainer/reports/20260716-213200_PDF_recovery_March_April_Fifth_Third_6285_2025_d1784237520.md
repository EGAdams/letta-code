# Trainer Report — PDF recovery: March-April Fifth Third 6285 (2025)

- **Document**: `/home/adamsl/rol_finances/readable_documents/bank_statements/march/march_april_fifth_third_6285_2025.pdf`
- **Scanner/source**: PDF recovery (Fifth Third statement, account 6285, March–April 2025)
- **Dispatch time**: 2026-07-16T21:32:00Z (`1784237520`)
- **Conversation**: `conv-ed609ee6-427e-4c36-a636-6407aa95bea7`
- **Verdict: CORRECTED (final trace 99 = PASS)**

## What happened (full arc, all inside this one conversation/dispatch)

This dispatch went through several rounds before landing on a correct, verifiable outcome.
I (Trainer) joined mid-run — most of the coaching below had already been sent by the time
I attached, so this report documents the transcript evidence rather than live coaching from
me. I polled for ~2 more minutes after the final message (22:19:00Z) with no further activity,
confirming the run is finished.

1. **21:32–21:33** — `load_wrapper_revision` (wrap-v022) ✅, `classify_scan.py` correctly
   identified `bank_statement` / Fifth Third Bank / confidence 1.0 ✅. Mazda then went down a
   dead end trying local `python3`/`fitz`/web-search text extraction instead of the required
   `parse_statement_scan.py` → `store_statement_transactions.py` path, concluded
   "unsupported_document," recorded trace 93, and judge correctly failed it (all checks failed).
2. **21:39–21:40** — First correction sent: pointed her at wrap-v022 Rules 1/6 (statement
   documents must run parse then store) and told her not to skip straight to
   `judge_trace`/`propose_improvement` without doing the work. She then hit an `executor_run`
   allowlist error from prefixing the command with `PYTHONPATH=...`.
3. **21:40:13** — Second correction: told her `executor_run` already sets `PYTHONPATH`, don't
   prefix it, matched the working `classify_scan.py` invocation style. She then successfully ran
   `parse_statement_scan.py` (52 transactions, statement period 2025-03-15 to 2025-04-15) and
   `store_statement_transactions.py`, which fail-closed correctly: 1 stored (1530), 33 failed on
   missing category mapping (mostly "Check ####", a few truncated merchant descriptors), 18
   credits skipped, 0 duplicates. Recorded trace 94; judge FAILed it (this trace itself was the
   earlier bad attempt's record, superseded by trace 94's actual completed run — reasons list is
   stale/mislabeled here but the callback and `propose_improvement` were correct: filed
   `missed_vendor_key` proposal 74 for the truncated "5/3 ONLINE PYMT TO ..." merchants).
4. **21:42:30** — Operator feedback: good work on the fail-closed store; asked her to look at the
   33 unresolved "problems" and file a proposal for the truncated merchant names. She did
   (proposal 74) and sent the corrected dashboard callback (parsed=52, stored=1, expense 1530).
5. **22:16:46** — **Operator recovery** message: reported that the fail-closed vendor/category
   gaps had been resolved externally (persisted to `vendor_category.yaml`, expense 1530
   recategorized), and that a re-run of `store_statement_transactions.py` against the same
   verified 52-row parse produced stored=33, duplicates=1 (expense 1530), failed=0. Mazda recorded
   this as trace 98 **from the operator's prose summary rather than a fresh tool call**, filed a
   dashboard callback (stored=33, expense_ids 1531–1563), and ran `judge_trace(98)` → correctly
   **FAILed** (`intake_evidence_parseable`, `document_classified`, etc. — the judge could not
   validate evidence that wasn't backed by a real tool return in this trace).
6. **22:18:23** — **Trainer correction for trace 98**: required her to re-run the store herself
   via a real `executor_run` tool call against the recovered parse rather than accepting the
   operator's summary as evidence. She did: `store_statement_transactions.py` returned
   `transactions_parsed=52, skipped_credits=18, duplicates=34, stored=0, failed=0` — a clean
   duplicate-only outcome (all 34 rows, including the 33 the operator's out-of-band recovery run
   had already stored plus the original expense 1530, were correctly detected as already present).
   Recorded trace 99, `judge_trace(99)` → **PASS** (`all deterministic intake checks passed`),
   sent the corrected final `/api/expense-stored` callback (`stored:0`, `duplicate_expense_ids`:
   the 34 ids), confirmed `{"ok": true}`.

## Verification checklist (final state, trace 99)

| Step | Evidence | Status |
|---|---|---|
| `load_wrapper_revision` | tool_return @21:32:06, wrap-v022 | ✅ |
| Classification | `classify_scan.py` @21:32:30, bank_statement, conf 1.0 | ✅ |
| Parse | `parse_statement_scan.py` real tool return, 52 transactions | ✅ |
| Categorize/duplicate-check | `store_statement_transactions.py` real tool return @22:18:32, duplicates=34, stored=0, failed=0 | ✅ |
| Store | Correctly skipped — genuine duplicate-only run (rows already stored by prior recovery + trace 94) | ✅ |
| `record_trace` | trace_id=99, `task_name="document-intake"` @22:18:44 | ✅ |
| `judge_trace` | trace 99 → **PASS** @22:18:48 | ✅ |
| `propose_improvement` | Not required — final verdict is PASS (earlier FAIL traces 93/94/98 each correctly got proposals 71/73/74/78) | ✅ |
| Dashboard notify | `POST /api/expense-stored` @22:18:54 confirmed `{"ok": true}`; verified server-side event at `stored_at=1784240334.23`, `conversation_id` matches, `dispatched_at=1784237520.06746` matches dispatch | ✅ |

Cross-checked all 4 `/api/expense-stored` events tied to this conversation_id against the
transcript's tool calls — timestamps and payloads (stored counts, expense_ids, duplicate counts)
match at every step, including the final duplicate-only outcome.

## Wrapper defects diagnosed (already corrected in-session, no further action needed)

1. **Statement documents were routed to ad-hoc local text extraction instead of the mandated
   `parse_statement_scan.py`/`store_statement_transactions.py` pipeline** — she jumped to
   `web_search`/raw PDF byte-scanning/`fitz` before trying the standard tool. Root cause: nothing
   in the dispatch/wrapper stopped her from treating "no readily extractable text" as a reason to
   improvise instead of just running the documented statement parser. Corrected in-session via
   direct instruction citing wrap-v022 Rules 1/6; she also filed `propose_memory_note` (proposal
   72, applied via persona block) to make this durable for future runs.
2. **`executor_run` command allowlist confusion** — prefixing with `PYTHONPATH=...` tripped the
   allowlist even though `executor_run` already sets it. Corrected in-session by pointing her at
   the already-working `classify_scan.py` invocation style as the template.
3. **Accepting operator prose as trace evidence instead of a fresh tool call** — when told
   out-of-band that a recovery run had already happened, she recorded `record_trace` from that
   narrative rather than re-executing the tool herself, and the judge correctly rejected it. This
   is a genuinely useful judge behavior (evidence must be tool-return-backed, not narrative) and
   Mazda self-corrected by re-running the actual tool call once told to. Worth reinforcing in her
   wrapper: **operator-reported outcomes are context, never trace evidence** — always re-derive
   evidence from a live tool call before `record_trace`.

## For a human to look at

- Proposals 71 ("unsupported_document" for the local-tooling detour), 73/74
  ("missed_vendor_key" for truncated 5/3 merchant descriptors), and 78 ("operator-summary trace
  not verifiable") are all filed and worth reviewing/activating — 74 in particular (truncated
  merchant vendor-key resolution) is a recurring, generically useful fix for Fifth Third statement
  parsing.
- The out-of-band "operator recovery" step (external DB/category correction + manual re-run) that
  produced the 33 stored expenses happened outside Mazda's own tool calls — confirm with EG that
  this manual intervention was intentional and that `vendor_category.yaml`/expense 1530's category
  change are the desired permanent fix, since Mazda's own contract doesn't have a path for
  "someone else already fixed it, just verify."
