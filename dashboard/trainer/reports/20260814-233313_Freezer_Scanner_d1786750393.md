# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `scan_freezer_1786750389826085790_8550cbd1b4e4.jpg`
**Dispatch time:** 2026-08-14T23:33:13Z
**Mazda conversation:** conv-bb25e906-9f0d-406a-a3b3-0bebd01b0aa0
**Facade result:** `ok:true, doc_kind:unknown, confidence:0.0, recommended_action:reject` → STEP 0 required.

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Why the Trainer was summoned
`GET /api/expense-stored-events?since=1786750393` shows the STEP 8 callback reported
`stored:0`, `expense_ids:[]`, `duplicate_expense_ids:[]` — a "zero persisted outcome."
`trainer_escalation_reason: "callback parsed records but reported no persisted outcome"`
correctly fired the watcher per the escalation design in
`intake/trainer_escalation.py`; this is intended behavior, not a defect, and exactly
the case this Trainer exists to adjudicate.

## Step-by-step checklist (all with successful tool returns, transcript timestamps ≥ dispatch)

1. `load_wrapper_revision(agent_name="Mazda")` @ 23:33:17 → `wrap-v080` loaded (63 learned rules, including today's Rule 58, see below). ✔
2. **STEP 0** (facade `doc_kind=unknown`) — `executor_run classify_scan.py` @ 23:33:41 →
   `doc_type=invoice, confidence=0.89, merchant="AT&T Small Business Services"`. ✔
3. STEP 0 receipt/invoice parse — `parse_and_categorize.py --json --engine=gemini
   --write-parsed-json /tmp/mazda_receipt_845d0d9055f7.json` @ 23:34:01. Gemini hit its
   free-tier 429 quota and the tool itself fell back to `chatgpt-oauth`
   (`model_provider:"chatgpt-oauth"`, `model_name:"gpt-5.4"`) and returned a full parse:
   `transaction_date:2025-04-23`, one line item "U-verse" `-13.71`, `totals.total_amount:
   -13.71`, `expense_scope:full_receipt`, `account_number_hint:"Account ending in 8457"`,
   raw OCR text literally shows `"Bill Amount -$13.71"`. This is a genuine AT&T *credit
   balance* notice, not an amount-due invoice. ✔ correct fallback behavior, no wrapper defect.
4. `check_vendor_key` @ 23:34:06 → `vendor_key=at_t_small_business_services, present:true,
   recognized:false` (new vendor, not yet in map — informational, doesn't block since
   categorizer still resolved a category). ✔
5. `check_duplicates` @ 23:34:11 (real extracted date `2025-04-23`, amount `-13.71`) →
   `is_exact_duplicate:false, in_database:false`. ✔
6. `categorizer_main.py --provider=auto` @ 23:34:25 → `category_id:124` (resolved via
   gemini provider tier). ✔
7. **Store decision** — Mazda did **not** run `parse_and_categorize.py --save --invoice`.
   She cited the already-active **Rule 58** (proposal 246, filed *earlier the same day*,
   2026-08-14): *"For doc_kind=invoice, when the validated parse total is zero or
   negative and evidence shows a credit balance rather than an amount due, stop before
   storage, record the credit-balance blocker, and treat stored=false as a correct
   fail-closed outcome."* The parsed total here (`-13.71`) matches that rule exactly —
   this is not a workaround Mazda invented, it is a pre-existing wrapper rule correctly
   applied. Storing a negative "expense" for an account credit would misrepresent the
   ledger; declining to store is the right call. ✔
8. `record_trace(task_name="document-intake", ...)` @ 23:34:36 → trace **423** saved.
   Evidence JSON present and accurate: `doc_kind:invoice`, `classification_confidence:0.89`,
   `vendor_key`, `vendor_key_recognized:false`, `category_id:124`, `duplicate_checked:true`,
   `is_duplicate:false`, `stored:false`, `expense_id:null`, `intake_halted:false`,
   `problems:[...]` naming the credit-balance blocker and the unrecognized vendor key. ✔
9. `judge_trace(423)` @ 23:34:41 → `verdict:FAIL, reasons:["failed: expense_stored"]`.
   This is a **judge rubric gap**, not a Mazda error: the deterministic judge's default
   invoice rubric (Rule 5: invoices should proceed through save) hasn't yet learned the
   negative-total carve-out that Rule 58 already taught Mazda's own instructions.
10. `propose_improvement(trace_id=423, failure_type=other, ...)` @ 23:34:48 → proposal
    **257** correctly diagnoses exactly this gap ("judge incorrectly required expense
    storage" for a credit-balance invoice) and proposes teaching the judge the same
    carve-out. ✔ — this is the self-improvement loop functioning exactly as designed
    (Ch. 5): a judge/wrapper defect detected and proposed for repair, not a model excuse.
11. `apply_proposal(257, ...)` @ 23:34:52 → all four gates (safety/cost/regression/
    usefulness) returned `allow`; activated as **wrap-v081** (learned rule #63). This
    activation happened without an explicit human-approval step visible in the
    transcript (`pending_approval:false`) — noted below for a human, not treated as a
    defect since gate-chain-approved auto-activation appears to be how this framework is
    built to operate (Ch. 5 describes human approval as the norm, but the gate chain
    itself is the safety mechanism here).
12. Dashboard notify — `POST /api/expense-stored` @ 23:35:03 with accurate non-stored
    evidence (`stored:0, expense_id:null, expense_ids:[], duplicate_expense_ids:[]`,
    `doc_kind:invoice`, correct `conversation_id`/`dispatched_at`) → `{"ok":true}`. ✔

## Wrapper defect diagnosed
None requiring Trainer coaching. The only gap found (judge rubric not yet covering
negative-total invoices) was correctly self-diagnosed and self-corrected by Mazda within
this same run via `propose_improvement`/`apply_proposal`. No corrective message was sent
to Mazda — none was needed.

## Lesson sent to Mazda
None.

## For a human
- The AT&T account (last 4: 8457, vendor_key `at_t_small_business_services`) shows a
  **-$13.71 credit balance**, not a bill due. Correctly left unstored; nothing to act on.
- `apply_proposal` auto-activated wrap-v081 with `pending_approval:false` after all four
  gates passed. Worth confirming this is intended (gate-chain-approved auto-activation)
  rather than a bypassed human-approval step, though nothing in this run suggests
  malfunction — the resulting rule (Rule 58 duplicate/generalization as rule #63) is
  sound and matches what this Trainer independently verified as correct.

No escalation filed — no application defect found.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
