# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-13T22:48:39Z
- Verdict: FAIL

## Checklist

- `load_wrapper_revision`: observed in the earlier Mazda run before the retry, but it is not present in the corrected run evidence I used for the final grade.
- STEP 0 classification/parsing: present and successful. `classify_scan.py` returned `doc_type=receipt`, `merchant=AT&T`, `confidence=0.95`; `parse_and_categorize.py --json` returned a parsed AT&T bill.
- `check_vendor_key`: present and successful, but wrong for this document. Mazda sent `vendor_key="at_t"` and the tool returned `recognized=false` with `vendor_key 'at_t' is not in the known-vendor map`.
- `check_duplicates`: present and successful. Mazda passed the AT&T bill context plus the candidate dates; the tool returned `is_exact_duplicate=false`, `fuzzy_duplicate_id_light=null`, `in_database=false`.
- `categorizer_main.py` via `executor_run`: not shown in the final retry evidence I used for grading.
- Store step: not performed in the final retry. Mazda recorded `stored:false` and did not call `parse_and_categorize.py --save`.
- `record_trace`: present and successful. Trace `64` saved with `task_name="document-intake"`.
- `judge_trace`: present and successful. Verdict was `NEEDS_REVIEW`, not `PASS`.
- `propose_improvement`: not required because the final verdict was not `FAIL`, but the run still did not satisfy the intake contract.
- Dashboard notify: correctly omitted because the final retry did not store a new expense.

## Diagnosis

Wrapper defect: the vendor-resolution path is still too permissive/under-specified. Mazda accepted a non-canonical vendor key (`at_t`) instead of normalizing to a known key for this document, which left the run in `NEEDS_REVIEW` rather than a clean duplicate PASS or a clean store PASS.

## Lesson Sent

No new corrective message was needed after the retry because the transcript already contained the trainer correction for the duplicate/AT&T mismatch. The remaining fix belongs in the wrapper: require Mazda to adopt the normalized key returned by `check_vendor_key`, and if it is not recognized, treat that as a hard blocker for a clean duplicate verdict.

## Human Review

- The transcript contains an earlier successful store (`expense_id=1500`) from the first pass, but the final retried run did not produce a valid PASS and did not reconcile the vendor key correctly.
- The expense-stored callback stream for this dispatch was empty, which matches the final retry not storing anything.
