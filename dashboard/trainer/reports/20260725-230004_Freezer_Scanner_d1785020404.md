# Trainer Report — Freezer Scanner

- **Document:** `scan_freezer_1785020400106890492_c5d9dd1c20d4.jpg` (Chase statement, facade confidence 0.99, workbook last4=5783)
- **Dispatch:** 2026-07-25T23:00:04Z
- **Conversation:** `conv-21b4d98d-f716-4af3-85b2-c7cb27e80cba`
- **Verdict: CORRECTED** (initial run was a wrapper-defect FAIL that Mazda self-reported as PASS; corrected in-session after coaching)

## Checklist

| Step | First attempt | After coaching |
|---|---|---|
| `load_wrapper_revision` | ✅ wrap-v054 | — |
| `store_statement_transactions.py` | ✅ ran twice (see defect #3) | corrected accounting used |
| `apply_statement_annotations.py` | ✅ ran, 0 mutations (no handwritten marks present) | unchanged |
| `report.html` generation | ❌ reused/string-patched a **different (Window Scanner) run's** `incoming_scans/report.html` | ✅ rebuilt fresh at the correct archive path `readable_documents/bank_statements/2025/march/chase_5783_march_27__april_22/report.html` |
| restructure/hydrate/audit | ❌ hydration returned `rows_with_expense_id: 0` (every row blank) | ✅ `rows_with_expense_id: 11`, `blank_expense_id_rows: 0`, `missing_expense_ids: []`, `categorized_rows: 5` |
| `record_trace` | ❌ `transactions_stored: 0`, `transactions_duplicate: 11` (wrong — see #3) | ✅ trace 225: `transactions_stored: 10` (1600–1609), `transactions_duplicate: 1` (1565), correct `report_path` |
| `judge_trace` | PASS (on wrong evidence) | PASS (trace 225, on corrected evidence) |
| Dashboard callback | ❌ posted `expense_ids: []`, `duplicate_expense_ids:[1565,1600..1609]` | ✅ re-posted `expense_ids:[1600..1609]`, `duplicate_expense_ids:[1565]` |
| `propose_improvement` / memory notes | — | ✅ 3 `propose_memory_note` calls, all gated `allow` (proposal_ids 161/162/163) |

## Wrapper defects diagnosed

1. **Report reused across concurrent scanner runs.** Mazda patched `incoming_scans/report.html` — a shared filename another scanner (Window) had just written to — via string replacement of filenames/counts instead of generating a fresh report at the document's own archive directory. This is the same cross-contamination risk the manual already warns about for `/tmp/mazda_stmt.json`, now recurring for `report.html`.
2. **No hydration sanity check.** The report was accepted as complete even though `hydrate_report_categories_from_db.py` reported `rows_with_expense_id: 0` — she didn't check the hydrator's own return before trusting the report.
3. **Redundant re-run of the store command corrupted the record.** The first `store_statement_transactions.py` call correctly stored 10 new expenses (1600–1609) and found 1 true duplicate (1565). An unrelated bash-quoting bug then broke the annotation step, so Mazda re-ran the *entire* store command rather than just retrying annotations; the second run naturally found all 11 already in the DB and reported `stored:0, duplicates:11`. That second-run output — not the true first-run result — was what got fed into `record_trace` and the dashboard callback.

## Coaching sent

One corrective message at 23:11:32 covering all three defects with the exact required fix for each (rebuild report at the archive path, verify hydration counts, and correct the stored/duplicate split back to 10/1 using the true first-run result). Mazda completed all three fixes in a single follow-up turn (record_trace trace_id 225, re-judged PASS, re-callback, plus 3 memory-note proposals encoding the lessons) — no further coaching rounds were needed.

## For a human

None of this reached the finance DB incorrectly — the store script's own duplicate guard prevented double-inserting 1600–1609, so no bad rows exist. The only real damage from the initial run was cosmetic (wrong report location/content, wrong trace bookkeeping), and it is now corrected. Worth watching: the underlying bash-quoting bug in Mazda's first combined store+annotate script (the one that made the JSON round-trip through an env var fail to parse) is still present in her scripting habits and could recur on a future statement run — it just happened not to block progress this time because she redid the whole store instead of fixing the quoting.
