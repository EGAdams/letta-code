# Mazda Trainer Report — Window Scanner

- **Document:** `scan_1784834363249985969_5232a94fd282.jpg` (Window Scanner)
- **Dispatch:** 2026-07-23T19:19:26Z, conversation `conv-dc7e96d6-3995-4e57-bc98-29f9a2ec2c8e`
- **Facade result:** `doc_kind=unknown, confidence=0.0` (JPEG scan, no extractable text) → STEP 0 vision classification required
- **Verdict: PASS**

## Checklist (all confirmed via successful tool returns)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v039` — 19:19:30 ✓
2. **STEP 0** — `executor_run classify_scan.py` → `doc_type=receipt, confidence=0.98, merchant="MR BURGER RESTAURANT 1"` — 19:19:45 ✓
   Then `executor_run parse_and_categorize.py --json` → full structured receipt (date `2025-03-14`, amount `29.57`, merchant `MR BURGER RESTAURANT I`) — 19:20:06 ✓
   Both calls logged Gemini `429` quota errors in stderr but self-healed via the script's internal ChatGPT-OAuth fallback and returned success (`returncode: 0`) — correct behavior, not a failure.
3. **Investigate** — `check_vendor_key("mr_burger_restaurant_i")` → normalized to `mr_burger` (fuzzy match); Mazda adopted this normalized key in every later call — 19:20:14 ✓
   `check_duplicates(date=2025-03-14, amount=29.57, description="MR BURGER RESTAURANT I")` → not a duplicate — 19:20:18 ✓
4. **Categorize** — `executor_run categorizer_main.py --provider=auto` → `category_id=137` (via gemini provider per stderr) — 19:20:27 ✓ Valid positive category, no fail-closed block needed.
5. **Store** — `executor_run parse_and_categorize.py --save --category-id=137` → success; expense stored as `expense_id=1545` (confirmed independently by `/api/expense-stored-events`) — 19:20:47 ✓
   `itemize_existing_expense` → `itemizable:false` ("only one line item; a single line equal to the charge is standalone, not itemized") — correct fail-closed/standalone outcome, not a guess — 19:21:02 ✓
6. `record_trace(task_name="document-intake", trace_id=163)` — evidence JSON complete: doc_kind, classification_confidence, vendor_key (normalized), vendor_key_recognized, category_id, duplicate_checked, is_duplicate, stored, expense_id, and a `problems` list correctly noting the two Gemini quota fallbacks plus a non-blocking store warning ("Could not update source_file to final receipt path") — 19:21:09 ✓
7. `judge_trace(163)` → **PASS**, `failure_type=none`, "all deterministic intake checks passed" — 19:21:13 ✓
8. `propose_improvement` — not called; correctly skipped (no FAIL verdict).
9. Dashboard notify — `curl POST /api/expense-stored` → `{"ok": true}` — 19:21:21 ✓, cross-verified via `GET /api/expense-stored-events?since=1784834366` (matching `conversation_id` and `dispatched_at`).

## Wrapper defect diagnosed

None. Gemini hit its free-tier daily quota mid-run on two separate calls; the existing
ChatGPT-OAuth fallback inside `classify_scan.py` / `parse_and_categorize.py` /
`categorizer_main.py --provider=auto` absorbed both without any Mazda intervention, and she
transparently logged both fallbacks in the trace's `problems` field rather than hiding them.
No coaching message was sent.

## For a human

Nothing actionable. Gemini quota exhaustion is already covered by the categorizer/vision
fallback chain (see `mazda_categorizer_provider_fallback_2026_07_20` project memory) — this
run is a live confirmation that the fallback path works end-to-end, not a new problem.

## Summary

Mazda correctly ran the full receipt-intake contract for this Window Scanner document end to
end: vision classification, parsing, vendor-key normalization, duplicate check, categorization,
storage (expense_id 1545), a correct fail-closed itemization skip, trace recording, a PASS
judge verdict, and the dashboard callback — all despite Gemini hitting its quota twice, which
the existing multi-provider fallback chain absorbed transparently. No wrapper defect found and
no coaching was necessary; verdict is **PASS**.
