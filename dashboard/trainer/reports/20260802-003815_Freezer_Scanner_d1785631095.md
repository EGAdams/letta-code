# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1785631090572579320_667dc53d61ef.jpg`
- **Dispatch time**: 2026-08-02T00:38:15Z
- **Mazda conversation**: `conv-cefdb734-bf78-43b7-a8f2-f1e5812bd02e`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Facade result
Deterministic facade returned `doc_kind: unknown, confidence: 0.0` (JPEG scan, no
extractable text), correctly triggering STEP 0 vision classification.

## Step-by-step checklist (all evidence = successful tool returns in-transcript, ≥ dispatch time)

| Step | Evidence |
|---|---|
| 1. `load_wrapper_revision` | 00:38:19 — `wrap-v070` loaded, learned rules present |
| 2. STEP 0 classify | `executor_run classify_scan.py` (00:38:43) → `doc_type=receipt, confidence=1.0, merchant=Priority Health, reason="Invoice is already paid: handwritten date '2/28/25' and circled initials/signature next to the circled 'Total amount due' of $55.00"`. Correctly applied the manual's rule that a visibly **paid** invoice is a receipt, not an unpaid-invoice branch. |
| STEP 0 parse | `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_0543f1462a1b.json` (00:39:09) → date `2025-03-01`, amount `$55.00`. Circled mark is the payment confirmation on the total (full-document amount), not a partial item selection — `marked_items` scope does not apply here. |
| 3. Investigate | `check_vendor_key` (00:39:13) → normalized `priority_health` → `priority_health_9392`, adopted by Mazda in all subsequent calls. `check_duplicates` (00:39:17) → exact duplicate, `expense_id=1972`. |
| 4. Categorize | `categorizer_main.py --provider=auto` (00:39:26) → `category_id=243` (provider: gemini) |
| 5. Store | `parse_and_categorize.py --save --category-id=243 --parsed-json /tmp/mazda_receipt_0543f1462a1b.json` (00:39:33, **no `--allow-duplicate`**, reloaded the exact STEP-0 artifact) → `success:true, duplicate:true, idempotent:true, expense_id:1972` — duplicate-safe save ran exactly once, no second row inserted. |
| 6. `record_trace` | 00:39:44 — `task_name="document-intake"`, `stored:false, expense_id:null, is_duplicate:true, duplicate_checked:true`, `problems` explicitly notes idempotent save + parse-artifact verification. Matches contract for a duplicate-only run. |
| 7. `judge_trace` | 00:39:47 — `trace_id=335`, verdict **PASS**, `failure_type=none` |
| 8. `propose_improvement` | not needed (PASS) |
| 9. Dashboard callback | `curl POST /api/expense-stored` (00:39:56) → `{"ok":true}`. Confirmed independently via `GET /api/expense-stored-events?since=1785631095`: event present with matching `conversation_id`, `dispatched_at`, `outcome:"EVIDENCE_ATTACHED"`, `duplicate_expense_ids:[1972]`, `stored:0`. |

## Supporting-document / red-box check
Attempted independent verification via `POST /api/supporting-documents {"expense_id":1972}`
from this Trainer's environment — it returned `DB error: No module named 'pymysql'`
(local box lacks MySQL client library, not a Mazda or dashboard defect). Could not directly
confirm the red-box annotation for this expense. This is an environment limitation of this
Trainer run, not evidence against Mazda; the callback confirmation above is the authoritative
dashboard-side success signal.

## Wrapper defect
None. No coaching was necessary — every required step appears with a successful tool
return, in order, matching the intake contract exactly.

## For a human
None required. Clean PASS, duplicate correctly detected and idempotently reconciled.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1972`, metadata: DB error: No module named 'pymysql'

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
