# Trainer Report — Window Scanner

- **Document:** Wells Fargo statement, account ...4884 (scan `window_scan_1785186447546854038_6e86c96559cd.jpg`)
- **Dispatch:** 2026-07-27T21:07:32Z (Mazda conv `conv-cfb4ad7b-0de5-4b36-9d0a-3a18d9b99c04`)
- **Verdict: CORRECTED** (initial run FAIL on report generation; fixed after one coaching message)

## Checklist (evidence-based)

| Step | Status | Evidence |
|---|---|---|
| `load_wrapper_revision` | ✅ | returned `wrap-v059` |
| Facade routing | ✅ | preflight identified `statement`, confidence 1.0 — STEP 0 vision correctly skipped |
| Statement store (`store_statement_transactions.py` via preflight `.statement.json`, no re-parse) | ✅ | `transactions_parsed:5, skipped_credits:2, duplicates:2 → [1366,1390], stored:1 → [1674], uncategorized:1 (fail-closed, allowed for statements)` |
| `archive_paths` / cross-year check | ✅ | `.../bank_statements/2025/july/wells_fargo_4884_july_31__august_15/...jpg`, range correctly excludes the 08/16 payment and 08/22 $0 interest row |
| `account_last4_source` | ✅ | `"operator"` (explicit `--account-last4 4884`, matches preflight value) |
| `apply_statement_annotations.py` | ✅ | applied EG's handwritten note ("1/2 = Rol travel") → category 160 on the one uncategorized row, `annotation_provider: gemini` |
| **`report.html` generation** | ❌→✅ **(coached)** | **Initial run**: hand-wrote HTML via inline Python instead of the canonical generator, saved to the **shared** `incoming_scans/report.html` (tool output said "updated", proving a pre-existing file). Self-check after restructure/hydrate: `data-vendor-key` count = 0, `rows_with_expense_id: 0` — the Set Category dialog would have resolved zero rows. Mazda logged this in her own trace `problems` but still returned "Done" without fixing it. **After coaching**: reran using the real `restructure_verified_transactions` function directly against the archived directory (`.../wells_fargo_4884_july_31__august_15/report.html`), producing `rows_with_expense_id:3, blank_expense_id_rows:0, categorized_rows:3`. |
| `record_trace` (`document-intake`) | ✅ (corrected version) | trace 273 includes `report_path` pointing at the archived report; `problems: []` |
| `judge_trace` | ✅ PASS both times | deterministic checks passed (judge_trace does not itself check report structure — that's why the Trainer had to check it independently) |
| `/api/expense-stored` callback | ✅ (corrected version) | second callback includes `report_path` at the corrected location |

## Wrapper defect diagnosed

Mazda's statement-report step has no enforced contract for *how* to build `report.html` — she improvised a hand-rolled HTML generator rather than reusing the existing `restructure_verified_transactions.py`-compatible generator used by prior successful reports, and defaulted to the shared `incoming_scans/` scratch path instead of the archived per-statement directory returned in her own `archive_paths`. This is exactly the "shared scratch file across concurrent runs" failure mode already called out for `/tmp/mazda_stmt.json` in her instructions, just recurring for `report.html`. Also notable: she recognized the defect (wrote it into her own trace's `problems` list) but did not treat it as blocking and still said "Done" — a completion-criteria gap, not just a generation gap.

## Lesson sent to Mazda (verbatim, abridged)

Explained both defects concretely (path + generation method), pointed her at her own `archive_paths` output as the correct destination, required her to redo report generation now (not as a future proposal), and to record a memory note that `report.html` for statements must always be produced by the canonical generator and saved under the archived `bank_statements/<year>/<month>/<bank>_<last4>_<range>/` directory, never the shared `incoming_scans/` folder.

Mazda's response: called `propose_memory_note` (a persona-block note matching the lesson, gates all `allow`), discovered via `grep`/reading `REPORT_OUTPUT_CONTRACT.md` and an existing good report (`wells_fargo_7580_december_23__january_17/report.html`) how the real generator is structured, imported `restructure_verified_transactions` directly, rebuilt the report at the correct path, re-ran hydrate + audit, re-recorded trace 273, re-judged (PASS), and resent the dashboard callback with `report_path`.

## Remaining non-blocking note

`report_audit_status` still reads `"WARN: no source PDF found in report directory"` on the corrected report (the source is a JPEG scan, not a PDF, so no PDF is expected to exist there) — a WARN, not a FAIL, and does not block per the report contract. Nothing further required.

## For a human

No action needed. The pattern (hand-rolled report + wrong scratch path) is worth watching for on other Window/Freezer statement scans in case wrap-v059's persona note doesn't fully prevent recurrence — the `propose_memory_note` from this run (proposal id 190, gates all `allow`) is pending review in the normal wrapper-revision approval flow.
