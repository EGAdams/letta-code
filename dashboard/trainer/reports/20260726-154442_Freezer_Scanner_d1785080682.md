# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-26T15:44:42Z
- Document: statement from `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785080678068788864_d2ff30fa878e.jpg`
- Verdict: PASS

## Checklist

- `load_wrapper_revision` succeeded with `wrap-v059`.
- Statement branch was followed for the preflight-backed statement, with `bank_name=Fubo`, `account_last4=4851`, `transactions_parsed=9`, and `duplicate_expense_ids=[1652-1659]`.
- `apply_statement_annotations.py` ran with `annotation_provider="codex-cli"` and applied/confirmed the handwritten note handling.
- `report.html` was generated, then restructured and hydrated.
- The first report hydration pass failed because rows had blank `data-expense-id`; Mazda corrected the report and reran hydration successfully.
- Final report verification showed `verified_id True`, `data_vendor_key_count 8`, and `picker_marker True`.
- `record_trace(task_name="document-intake")` succeeded:
  - final accepted trace: `trace_id=261`
  - evidence included `stored=0`, `duplicate_expense_ids`, `report_generated=true`, and `report_path=/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/report.html`
- `judge_trace(261)` returned `PASS`.
- Dashboard callback posted successfully with `{"ok": true}`.

## Wrapper Defect Diagnosed

- Initial `executor_run` usage sent a raw `set -euo pipefail` token stream, which the executor treated as an invalid command. That was a transport/wrapper defect, not a finance defect.
- The first report build also emitted blank `data-expense-id` rows; Mazda recovered by regenerating and rehydrating the report before closing the run.

## Lesson Sent

- I told Mazda to:
  - finish the statement intake now,
  - use `apply_statement_annotations.py` with the expense and duplicate ids from the successful store,
  - generate and hydrate the report at the exact `report.html` path,
  - then call `record_trace`, `judge_trace`, and the dashboard callback,
  - and to wrap shell work as a single `bash -lc "..."` command instead of passing `set` directly to `executor_run`.

## Human Notes

- Final intake state is complete and judged PASS.
- `report_audit_status` was `WARN` (`no source PDF found in report directory`), but it did not block completion.
- No further action is required unless someone wants to inspect the trainer recovery steps or the intermediate report fix.
