# Trainer Report — Window Scanner

- **Document**: `scan_1784570833771811625_6d1b4ba11c02.jpg`
- **Scanner**: Window Scanner
- **Dispatch**: 2026-07-20T18:07:16Z
- **Facade result**: `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` → STEP 0 required
- **Verdict: PASS**

## Checklist (all with successful tool returns)

1. `load_wrapper_revision` — wrap-v030 loaded (18:07:19). ✅
2. **STEP 0** — facade returned `unknown`, so Mazda self-classified via `executor_run classify_scan.py`
   → `doc_type=receipt`, confidence 1.0, merchant "MR BURGER RESTAURANT 1" (18:07:52). Correctly
   routed to the receipt branch. ✅
3. Parsed via `parse_and_categorize.py --json --engine=gemini` (18:08:07) → date 2025-03-06, total
   $29.43, no line items. ✅
4. `check_vendor_key` (18:08:12) → normalized `mr_burger_restaurant_1` to known `mr_burger`; Mazda
   adopted the normalized key for all subsequent calls. ✅
5. `check_duplicates` (18:08:16) → not a duplicate, real date used (not placeholder). ✅
6. `check_category` (18:08:20) → no category yet, `expected_category_id=137`. Mazda then ran
   `categorizer_main.py --provider=gemini` (18:08:26) which confirmed `category_id=137`. Fail-closed
   category resolution honored — she did not store with a null/zero category. ✅
7. **Store** — `parse_and_categorize.py --save --category-id=137 --engine=gemini` (18:08:44) →
   `expense_id=1517`. Single line item (no itemizable items in the parsed receipt), so
   `itemize_existing_expense` was correctly skipped (`itemization_attempted:false`). ✅
8. `record_trace` (18:08:50, trace_id=117), `task_name="document-intake"` exactly, evidence JSON
   matches reality (`stored:true`, `expense_id:1517`, `vendor_key:"mr_burger"`, `category_id:137`,
   `is_duplicate:false`). ✅
9. `judge_trace(117)` (18:08:53) → verdict PASS, failure_type none. ✅
10. Dashboard notify — `curl POST /api/expense-stored` (18:09:00) with matching
    `conversation_id`/`dispatched_at`. Verified independently via
    `GET /api/expense-stored-events?since=1784570836` — event present, `expense_id=1517`,
    `conversation_id` and `dispatched_at` match this dispatch exactly. ✅

## Wrapper defect diagnosed

None. Full contract satisfied end-to-end on the first pass, no coaching required.

## Lesson sent to Mazda

None — no corrective message was needed.

## Notes for a human

None. Clean run: unknown-facade receipt correctly self-classified, vendor normalized, category
fail-closed-resolved before store, single-item receipt correctly left un-itemized, trace/judge/
dashboard-notify all present and independently verified.
