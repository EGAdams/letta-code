# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-25T22:34:32Z
- Document path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785018866813155347_44fe9390d51d.jpg`
- Verdict: FAIL

## Checklist

1. `load_wrapper_revision` ran successfully.
   - Evidence: `wrap-v051` returned at `2026-07-25T22:34:37Z`.
2. STEP 0 receipt parsing ran, but failed before a validated parse artifact could be written.
   - Evidence: `parse_and_categorize.py --json` returned `Could not write validated parse artifact: marked_items total mismatch: selected rows sum to 4.99, but total_amount is 47.67`.
3. `check_vendor_key` / `check_duplicates` / categorizer / store did not run.
   - Evidence: the transcript stops at the parse blocker; no successful tool returns for those steps appear.
4. `record_trace` ran.
   - Evidence: trace `212` saved, `task_name="document-intake"`.
5. `judge_trace` ran.
   - Evidence: verdict `FAIL`, `failure_type="missed_vendor_key"`, trace `212`.
6. `propose_improvement` ran.
   - Evidence: proposal `154` filed for trace `212`.
7. Dashboard callback ran.
   - Evidence: `POST /api/expense-stored` returned `{"ok": true}`.

## Diagnosis

The failure is a wrapper defect in STEP 0 marked-items parsing / validation, not a finance-db or storage problem. The parse layer detected marked items, but the selected rows summed to `$4.99` while the receipt total was `$47.67`, so Mazda could not write the required source-bound parse artifact and correctly stopped before downstream receipt stages.

The judge labeled the run `missed_vendor_key`, but the actual blocker was parse validation. Mazda later self-filed and activated a wrapper revision during the run (`wrap-v052`), which is the right kind of fix for this class of failure.

## Lesson Sent

- None from the trainer. Mazda already surfaced the parse blocker, filed proposal `154`, and activated the updated wrapper revision herself.

## Human Follow-Up

- Inspect the receipt image for whether the marked-items selection was real or a parser false positive.
- If the marked-items heuristic is expected to accept this image, repair STEP 0 parsing so it writes the validated artifact and preserves the receipt pipeline.
