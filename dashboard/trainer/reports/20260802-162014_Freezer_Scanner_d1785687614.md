# Trainer Report — Freezer Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785687610942975057_f490dfbb794d.jpg`
**Dispatch:** 2026-08-02T16:20:14Z
**Conversation:** conv-e867a9de-b674-4a70-9432-4542f348ae95
**Run duration:** ~3.5 minutes (16:20:14 → 16:23:54), well within the 15-minute window.

**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

This is **not a Mazda/wrapper failure** — she executed every required step correctly and
honestly. The FAIL reflects two application-level defects (categorizer provider chain,
document-annotation service) documented under Escalation below. No coaching message was
sent; there is nothing Mazda's instructions, tools, or memory can fix here.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v071`, active | ✅ |
| STEP 0 classify (facade returned `doc_kind:unknown`) | `classify_scan.py` → `doc_type=receipt, confidence=1.0, merchant=DTE` (utility bill, "No Payment Due") | ✅ |
| STEP 0 parse | `parse_and_categorize.py --json --engine=gemini` → parse artifact written to `/tmp/mazda_receipt_84f59b1b5ac9.json`; store later reports `parse_artifact_verified: true` | ✅ |
| `check_vendor_key` | `vendor_key="dte"`, `recognized=false` — adopted as-is (no different normalized key returned) | ✅ |
| `check_duplicates` | real extracted date `2025-04-10`, amount `53.06` → `is_exact_duplicate=false` | ✅ |
| Categorize | `categorizer_main.py --provider=auto` → **all three providers failed**: gemini timed out, chatgpt-oauth errored (`Could not resolve authentication method`), anthropic returned nothing usable → `category_id: null` | ❌ (application defect, see below) |
| Store | `parse_and_categorize.py --save` → `success:true, expense_id:1985`, `expense_status="NEEDS_VENDOR_KEY"`, `pending_vendor_review:true`, `category_id: null` | ⚠️ stored with null category — contract requires categorization to block storage for the receipt/invoice branch |
| Itemization | single line item → `itemize_existing_expense` correctly returned `itemizable:false` ("only one line item... standalone"), row left standalone | ✅ |
| `record_trace` | `task_name="document-intake"`, evidence honestly includes `category_id:null` and both failure problems in `problems[]` | ✅ |
| `judge_trace` | `NEEDS_REVIEW`, reason "vendor_key resolved but not in the known-vendor map" (did not separately flag the null category) | ✅ (evidence matches) |
| `propose_improvement` | not called — correctly skipped since judge did not return FAIL | ✅ |
| Dashboard callback | `POST /api/expense-stored` with correct `expense_ids:[1985]`, matching `conversation_id`/`dispatched_at`, `status="awaiting_vendor_review"` → `{"ok":true}` | ✅ |

## Supporting-document / dialog-visibility check (independent verification)

`POST /api/supporting-documents {"expense_id":1985}` → `receipt_url` present, `documents[0]`
(`View Receipt`) → `available:true`; the other three fields correctly `available:false`
(no source doc/statement/ledger exists for a lone receipt). **Dialog visibility: PASS.**

**Red box: FAILED.** `GET /supporting-document/1985/receipt` served 1,137,811 bytes —
byte-identical in size to the raw archived file
(`readable_documents/receipts/2025/april/april_10/dte_04_10_25_53_06.jpg`, also 1,137,811
bytes). The dashboard log confirms:
```
[supporting-document] opened without highlight expense_id=1985 type=receipt reason=No high-confidence expense row was found in the image.
```
This receipt is a clean, single-line utility-bill scan (not the handwritten-note hard
case) — the annotator's OCR/matching failed to find its own just-parsed row on an
otherwise straightforward document. This is the dashboard's job
(`dashboard/document_annotation.py`), not Mazda's; per instructions I am not coaching her
or suggesting a re-store to "fix" it.

## Escalation

```
repo_path: /home/adamsl/letta-code (document annotation) and /home/adamsl/rol_finances (categorizer)
bug_description: |
  Two independent application defects surfaced on this run, both against expense 1985
  (DTE utility bill, $53.06, 2025-04-10):

  1. Categorizer LLM fallback chain failed on ALL THREE providers for a single
     `categorizer_main.py --provider=auto` call:
       - gemini: "Research failed for vendor_key='dte': gemini CLI timed out"
       - chatgpt-oauth: "Research failed for vendor_key='dte': \"Could not resolve
         authentication method. Expected one of api_key, auth_token, or credentials to
         be set. Or for one of the `X-Api-Key` or `Authorization` headers to be
         explicitly omitted\""  <- looks like a real code/config bug: the OAuth provider
         fell through to an OpenAI-SDK client expecting a raw API key instead of using
         the stored OAuth token (~/.codex/auth.json or ~/.codex-moms/auth.json per
         CLAUDE.md's documented chatgpt-oauth provider design).
       - anthropic: printed "Trying provider: anthropic" then returned category_id:null
         with no logged failure reason — a silent failure that should have logged
         something through IProviderHealthRecorder.
     Result: the receipt was stored with `category_id: null`, `expense_status:
     "NEEDS_VENDOR_KEY"`, `pending_vendor_review: true`. The Trainer's written contract
     ("Intake is fail-closed: a null, zero, or invalid category_id blocks storage until
     Mazda resolves a valid category" — receipt/invoice branch, explicitly NOT the
     statement exception) says storage should have been blocked; instead
     `parse_and_categorize.py --save` persisted it anyway. Either the contract text is
     stale (the store script may have been intentionally extended to accept a
     pending_vendor_review fallback, mirroring the statement NEEDS_VENDOR_KEY case) or
     the store script has a real fail-closed-enforcement gap — worth reconciling either
     way. Check whether this run already appears in ~/.mazda/provider_health.json /
     the "LLM Provider Fallbacks (Categorizer)" dashboard tile.

  2. document_annotation.py failed to draw the red box for expense 1985's receipt
     viewer. Confirmed independently: `GET /supporting-document/1985/receipt` served
     exactly the same byte count (1,137,811) as the raw archived file, and the server
     log recorded "[supporting-document] opened without highlight expense_id=1985
     type=receipt reason=No high-confidence expense row was found in the image." This
     is a single-line, machine-printed utility bill (not a handwritten-note hard case),
     so the OCR/row-matching failure here is unexpected and likely a distinct bug from
     the known cross-out-notes case documented in CLAUDE.md.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785687610942975057_f490dfbb794d.jpg
  expense_id: 1985
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/categorizer/categorizer_main.py -i /tmp/mazda_cat_input.json --provider=auto
```

## For a human

- Expense 1985 (DTE, $53.06, 2025-04-10) is parked `NEEDS_VENDOR_KEY` /
  `pending_vendor_review` — add `dte` to the known-vendor map (or resolve manually) to
  clear it, once the categorizer provider chain is working again.
- The receipt's viewer opens without its verification red box — treat any "did the box
  render" checks on this expense as informational only until the annotator bug above is
  fixed.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- expense `1985`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
