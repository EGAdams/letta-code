# Mazda Trainer Report

- Scanner: Window Scanner
- Dispatch time: 2026-07-16T16:08:47Z
- Verdict: FAIL

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded at `2026-07-16T16:08:52+00:00` and returned `wrap-v007` with learned rules 1-6.
- `executor_run` classification succeeded at `2026-07-16T16:09:01+00:00`:
  - `doc_type`: `invoice`
  - `confidence`: `0.99`
  - `merchant`: `Jacob Menninga`
- No evidence appeared for the required invoice follow-through steps:
  - no `check_vendor_key`
  - no `check_duplicates`
  - no categorizer run
  - no `parse_and_categorize.py --save --invoice`
- `record_trace` succeeded at `2026-07-16T16:09:11+00:00` with `trace_id=75`, but the evidence JSON showed:
  - `doc_kind`: `invoice`
  - `vendor_key`: `jacob_menninga`
  - `duplicate_checked`: `false`
  - `stored`: `false`
  - `expense_id`: `null`
- `judge_trace(75)` succeeded at `2026-07-16T16:09:14+00:00` and returned:
  - `verdict`: `FAIL`
  - `failure_type`: `wrong_category`
  - reasons: missing category resolution, duplicate check, and expense storage
- `GET /api/expense-stored-events?since=1784218127` returned `[]`.
- Mazda did not call `propose_improvement` until after I sent a corrective message; that follow-up succeeded and activated `wrap-v008` from proposal `54`.

## Wrapper Defect

The invoice branch was not enforced in practice. Mazda classified the document as an invoice, then stopped after `record_trace` and `judge_trace` instead of continuing through vendor lookup, duplicate checking, categorization, and `--invoice` storage.

## Lesson Sent

I sent Mazda this corrective instruction:

> You stopped after classifying this scan as an invoice and you never completed the invoice branch. For invoice documents, continue through vendor check, duplicate check, categorization, and save with `--invoice` unless a tool return explicitly blocks the stage. For this run, call `propose_improvement(trace_id=75, failure_type="wrong_category", summary="Invoice intake stopped after classification; vendor lookup, duplicate check, categorization, and --invoice save were skipped.", expected_benefit="Invoice scans complete end-to-end and record a learnable failure pattern.")`, then `apply_proposal` with the rule: for `doc_type=invoice`, continue through vendor check, duplicate check, categorization, and save with `--invoice` unless blocked. Record the rule in memory.

## Human Follow-Up

- Verify the next invoice scan actually runs through investigate/categorize/save under `wrap-v008`.
- If the same stop recurs, inspect the invoice routing logic in the trainer prompt or Mazda memory, because the current wrapper now contains the corrected rule.
