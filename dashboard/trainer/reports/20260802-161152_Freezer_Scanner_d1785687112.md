# Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

- **Document**: `scan_freezer_1785687108539638784_ac4622c9e0cf.jpg` (Consumers Energy utility bill, handwritten-paid)
- **Dispatch**: 2026-08-02T16:11:52Z, conversation `conv-9fee8919-95b2-4f98-b1c7-c021feebfe71`
- **Facade result**: `doc_kind=unknown`, confidence 0 → correctly routed to STEP 0 vision classification.

## Checklist (all steps observed with successful tool returns)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v071` loaded. ✅
2. STEP 0 — `executor_run` → `classify_scan.py`: `doc_type=receipt`, confidence 1.0, merchant Consumers Energy, `payment_evidenced=true` — handwritten "3/20" + "rec KV" circled with a line through "Amount Due"/"Please pay by", i.e. a paid-invoice-treated-as-receipt call. Correct: no item-selection marks present (single line item, whole-bill payment evidence, not `marked_items` scope). ✅
3. STEP 0 parse — `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_2d1a4d504e20.json`. Same artifact path reused later at save time (`--parsed-json /tmp/mazda_receipt_2d1a4d504e20.json`) — parse-artifact consistency held. ✅
4. Investigate — `check_vendor_key` (`consumers_energy` → normalized `consumer_energy`, adopted) then `check_duplicates` (date 2025-03-20, amount 231.05 → no duplicate). ✅
5. Categorize — `categorizer_main.py` via `executor_run` → `category_id=3` (nonzero/nonnull). ✅
6. Store — `parse_and_categorize.py --save --category-id=3 --parsed-json <same artifact>` → `expense_id=1983`. ✅
7. Itemization — `itemize_existing_expense` → `itemizable:false`, reason "only one line item; a single line equal to the charge is standalone, not itemized", `difference:"0.00"`. Correct fail-closed outcome (single line item, no handwritten-arithmetic mismatch to override it). ✅
8. `record_trace(task_name="document-intake")` — evidence JSON complete (`doc_kind`, `vendor_key`, `vendor_key_recognized`, `category_id`, `duplicate_checked`, `is_duplicate`, `stored`, `expense_id`, `problems`). `problems` honestly notes the classifier's paid-invoice-as-receipt routing and the itemization skip. ✅
9. `judge_trace(342)` → **PASS**, `failure_type=none`. ✅
10. Dashboard notify — `POST /api/expense-stored` succeeded (`{"ok":true}`); confirmed via `/api/expense-stored-events?since=1785687112` with matching `conversation_id` and `dispatched_at`. ✅

No wrapper defect diagnosed. No corrective message was sent to Mazda — none was needed.

## Supplementary check: red-box/dialog visibility (my own verification)

Opened `/supporting-document/1983/receipt`: served bytes are **byte-identical** to the raw incoming scan (no annotation applied). Dashboard log confirms:

```
[supporting-document] opened without highlight expense_id=1983 type=receipt reason=No high-confidence expense row was found in the image.
```

This is the documented hard case (CLAUDE.md: "Scans EG has written on are the hard case") — the handwritten "3/20"/"rec KV" circle sits near the amount region and the OCR-based row matcher couldn't find a high-confidence row to box. This is **not** a Mazda failure (she never touches annotation) and does not change the top-line verdict, but the receipt is currently unverifiable via the red box in the dashboard.

## Escalation

repo_path: /home/adamsl/letta-code
bug_description: |
  document_annotation.py's receipt-row matcher returned "No high-confidence expense row was
  found in the image" for expense 1983 (Consumers Energy, $231.05, 2025-03-20), so
  /supporting-document/1983/receipt serves the unannotated original scan instead of a red-boxed
  copy. The receipt has handwritten payment-confirmation marks ("3/20" circled, "rec KV" circled,
  a line through "Amount Due"/"Please pay by") near the amount column — consistent with the
  known handwriting-over-amount-column failure mode already described in CLAUDE.md for this
  module. Filing per that doc's instruction to report every unboxed row rather than have Mazda
  "fix" it.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785687108539638784_ac4622c9e0cf.jpg
  failing_command: GET /supporting-document/1983/receipt

## For a human

Expense 1983 (Consumers Energy, $231.05, 2025-03-20) is correctly stored and categorized, but its receipt viewer currently shows no red box (see Escalation above). No action needed on the intake itself.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1983`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
