**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

Document: Window Scanner / /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786496061403878371_450bfeea1206.jpg
Dispatch: 2026-08-12T00:54:25Z

Checklist:
- `executor_run` evidence showed Mazda retried `check_duplicates`, then treated the result as unresolved instead of trusted success.
- Transcript shows the run initially inserted duplicate expense `2026`, then corrected to canonical expense `1452` via `SupportingDocumentService` consolidation.
- `executor_run` return at 2026-08-12T01:06:03Z shows dashboard callback posted with `stored:0`, `expense_id:1452`, `duplicate_expense_ids:[1452,2026]`, and receipt archive path `/home/adamsl/rol_finances/readable_documents/receipts/2025/april/april_08/new_2_you_04_08_25_40_28.jpg`.
- `judge_trace` returned `FAIL` with `failure_type: missed_duplicate`, which is consistent with the original run having inserted a duplicate before correction.
- `propose_improvement` was called with `trace_id:126` and the duplicate-handling lesson was recorded via `propose_memory_note` / `apply_proposal`.
- No application-defect escalation was needed; this was a wrapper/memory defect in duplicate handling, not a broken downstream tool.

Wrapper defect diagnosed:
- Mazda failed to keep duplicate checking fail-closed after `check_duplicates` authentication failures. The wrapper allowed an ad-hoc recovery path that inserted a duplicate before consolidation.

Lesson sent to Mazda:
- When `check_duplicates` fails, retry exactly once. If it still fails, keep `duplicate_checked=false`, do not insert a new receipt expense, and use only the normal SupportingDocumentService attachment/consolidation path after explicit review.

For a human:
- The final canonical state appears corrected on expense `1452`, but the judge still records the original duplicate insertion as a failure event. The correction was completed and reported back to the dashboard.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- expense `2026`, metadata: No matching expense in DB.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
