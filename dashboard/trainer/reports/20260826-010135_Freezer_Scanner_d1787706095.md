# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1787706092754872299_e7c420849d8d.jpg`
**Dispatch time:** 2026-08-26T01:01:35Z (unix 1787706095)
**Conversation:** conv-8717d517-b67c-48a7-be9b-52c297030f8f

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Facade result
`doc_kind=unknown`, confidence 0.0 — expected for a JPEG scan, requires Mazda-driven STEP 0 classification.

## What I observed (own image inspection)
Read the scanned image directly. It is a small cut-out fragment pasted onto an otherwise
blank page: printed totals-section text (`37.48 +`, `0.00 *`) with a handwritten annotation
`37,48 / AO2 / July 2025` beside it. No vendor name, no itemized lines, no store letterhead,
nothing identifying what was purchased or from whom. This genuinely has no usable
receipt/invoice/statement/ledger content — `doc_type=other` is the correct classification,
not a missed detection.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision(agent_name="Mazda")` | returned `wrap-v082`, active | ✅ |
| STEP 0 classification (`classify_scan.py`) | `executor_run` → Gemini hit 429 quota error, fell back to OpenAI Codex CLI (`"Classified via OpenAI Codex CLI fallback."`) → `{"doc_type":"other","confidence":0.88,"merchant":null,"reason":"isolated handwritten amount/date on a mostly blank paper fragment without identifiable vendor or clear purchase documentation"}` | ✅ genuine fallback classification, not an error default |
| Investigate/categorize/store branches | correctly skipped — `doc_type=other` is the one unsupported classification per the contract | ✅ |
| `record_trace(task_name="document-intake")` | trace_id 479; evidence JSON: `doc_kind:"other"`, `vendor_key:null`, `category_id:null`, `duplicate_checked:false`, `stored:false`, `expense_id:null`, `problems:["Unsupported document..."]` — internally consistent with a no-store outcome | ✅ |
| `judge_trace(479)` | `{"verdict":"PASS","failure_type":"none","reasons":["all deterministic intake checks passed"]}` | ✅ |
| `propose_improvement` | not required (PASS) | n/a |
| Dashboard notify | `curl POST /api/expense-stored` with `stored:0, expense_id:null, doc_kind:"other"` → `{"ok":true}` | ✅ |

## Wrapper defect
None. Every required step for the `other`/unsupported branch is present with a successful
tool return, the evidence JSON is internally consistent, and independent inspection of the
image confirms the document really is unclassifiable (no vendor, no items — just a totals
fragment and a handwritten note). No coaching message was sent.

## For a human
Nothing to flag. This looks like a stray scrap (possibly meant to be stapled to another
receipt) rather than a standalone expense document — no action needed unless EG intended
this fragment to be matched to a specific receipt/invoice, in which case it should be
rescanned alongside its counterpart page.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
