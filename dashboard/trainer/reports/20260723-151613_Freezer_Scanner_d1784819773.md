# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-23T15:16:13Z
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1784819770289220468_c426eb1b716d.jpg`
- Verdict: PASS

## Checklist

- `load_wrapper_revision` succeeded at `2026-07-23T15:16:17Z` with active `wrap-v034`.
- Vision classification ran via `executor_run`; `classify_scan.py` returned `doc_type=invoice`, merchant `Consumers Energy`, confidence `0.96`.
- `check_vendor_key` succeeded; normalized `consumers_energy` to `consumers_7996`.
- `check_duplicates` succeeded; `is_exact_duplicate=false`.
- Storage succeeded in `executor_run`; `parse_and_categorize.py --save --invoice` returned `success=true` with `expense_id=1529`.
- `record_trace` succeeded at trace id `136` with `task_name="document-intake"`.
- `judge_trace(136)` returned `PASS`.
- Dashboard callback arrived in `/api/expense-stored-events?since=1784819773` with `stored=1`, `expense_id=1529`, `vendor_key=consumers_7996`, and matching `dispatched_at`.

## Notes

- No wrapper defect was observed.
- No corrective lesson was sent.
- The run completed despite an initial Claude CLI failure in the trainer harness; Codex fallback finished the intake successfully.
- A Pydantic date validation warning appeared during parsing, but it did not block the successful store.

