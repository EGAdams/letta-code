# Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

- Scanner: Freezer
- Document: `scan_freezer_1786131943641676882_f3e0e40b7271.jpg` — handwritten receipt, "I received 200.00 from Robert K/83, Rosemary Barnes, River of Life Ministries for vehicle work. Jose Garcia"
- Dispatch time: 2026-08-07T19:45:46Z
- Mazda conversation: `conv-b457be38-8461-4125-a09e-e2c5365c8884`
- Facade result: `doc_kind=unknown, confidence=0.0` → correctly routed to STEP 0 self-classification

## Checklist (first attempt, trace 86)

| Step | Evidence | Result |
|---|---|---|
| load_wrapper_revision | wrap-v004 returned | OK |
| STEP 0 classify_scan.py | Gemini 401 → Codex CLI fallback → `doc_type=receipt, confidence=0.97, merchant="River of Life Ministries"` | OK, fallback worked |
| STEP 0 parse_and_categorize.py --json | Gemini 401 → chatgpt-oauth fallback → merchant_name="Jose Garcia", item "vehicle work" $200, `transaction_date=2028-08-07`, `handwritten_arithmetic_total=73126.0` | Ran, but date wrong (see below) |
| check_vendor_key | description "River of Life Ministries" → fuzzy-normalized to `mel_trotter_ministries` | **Suspect — see Escalation** |
| categorizer_main.py | vendor_key `mel_trotter_ministries` → category_id 216 "Mel Trotter" | Wrong vendor in, wrong category out |
| check_duplicates | `Access denied for user 'adamsl'@'localhost'` | **Tool auth broken — see Escalation**; Mazda followed existing wrapper Rule 3 (proposal 41) and ran an authenticated equivalent SQL check via `executor_run` |
| parse_and_categorize.py --save | `expense_id=2005` stored, category_id=216, `receipt_url=jose_garcia_08_07_28_200_00.jpg`, archived under `.../receipts/2028/...` | Stored with wrong category and wrong (future) year |
| record_trace / judge_trace | `task_name=document-intake`; judge returned PASS | **Judge PASS was wrong** — mechanically complete but semantically broken (see below) |
| dashboard callback | `POST /api/expense-stored` fired, confirmed via `/api/expense-stored-events` (conversation_id + dispatched_at match) | OK |

**Two real defects in trace 86**, both self-flagged by Mazda in her own `problems[]` but not acted on:
1. **Vendor/category mismatch.** "River of Life Ministries" is the *organization itself* (the payer), not a vendor — the actual payee for the $200 "vehicle work" is Jose Garcia. Vendor/category resolution ran off the wrong party name and landed on an unrelated fuzzy match ("Mel Trotter", category 216).
2. **Future-dated expense.** Stored `expense_date=2028-08-07` and archived into a `2028/` folder. The scan was dispatched today (2026-08-07); "8-7-28" is almost certainly a misread of "8-7-26".

The judge's PASS on trace 86 is a rubric gap: it only confirmed the deterministic steps ran, not that the vendor/category/date were internally consistent — the same "internal consistency isn't evidence" trap called out in the Trainer instructions.

## Coaching sent

Sent a corrective message identifying both defects, requiring Mazda to (a) re-resolve vendor/category from "Jose Garcia" not the org's own name, correcting or blanking the category rather than leaving the wrong one, and (b) verify/correct the transaction date and re-archive the file, then re-record/judge/callback.

## Checklist (corrected attempt, trace 88)

- Re-ran `check_vendor_key` with `jose_garcia` → correctly reported **not recognized** (no false-positive fuzzy match this time).
- Re-ran `categorizer_main.py` for `jose_garcia` — Gemini 401 (known provider issue), no other provider matched → correctly left **category_id NULL** and set `pending_vendor_review=true` rather than keeping the wrong category. This is the correct fail-closed outcome for a genuinely unresolvable vendor on a real, already-stored transaction (deleting the row would lose a real $200 expense; NEEDS_REVIEW is the honest state).
- Corrected `expense_date` 2028→2026 in the DB, moved the receipt file from the `2028/` archive folder to `2026/august/august_07/jose_garcia_08_07_26_200_00.jpg`, updated `receipt_url` and added an explanatory `notes` field on expense 2005.
- `record_trace` (trace 88) + `judge_trace` → **NEEDS_REVIEW** (`vendor_key resolved but not in the known-vendor map`) — correct verdict, not a Mazda failure.
- Re-POSTed `/api/expense-stored` with corrected `expense_date=2026-08-07`, `vendor_key=jose_garcia`, `vendor=Jose Garcia`, `status=awaiting_vendor_review`, corrected `archive_paths` — confirmed via `/api/expense-stored-events` (second event, `dispatched_at` matches, correct values).
- `propose_memory_note` recorded the lesson ("never use the organization's own name as vendor/category subject") — passed all four gates, applied as an approved proposal (no live-memory applier wired, which is a known infra limitation, not a run failure).

All corrected values confirmed consistent between DB update, dashboard callback event, and trace evidence.

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  Two tool-level defects surfaced during Freezer scan intake (conversation
  conv-b457be38-8461-4125-a09e-e2c5365c8884, expense_id 2005):

  1. check_duplicates fails with "Access denied for user 'adamsl'@'localhost'
     (using password: NO)" -- the MySQL credentials wired into this MCP tool
     are broken/missing a password. Mazda is currently working around this via
     an existing wrapper rule (Rule 3 / proposal 41) that runs an equivalent
     authenticated raw SQL query through executor_run with a hardcoded
     password embedded in the shell command. That workaround should not be
     the permanent fix -- it exposes a DB credential in tool-call args/logs
     on every run. Fix check_duplicates' own DB connection config.

  2. check_vendor_key's fuzzy matching normalized "river_of_life_ministries"
     to the unrelated known vendor "mel_trotter_ministries" with no lexical
     similarity between the two names. This produced a wrong category
     (216, "Mel Trotter") on a real stored expense in this run. The fuzzy
     match threshold/algorithm likely needs tightening -- these two vendor
     names should never be confusable.

  3. (Minor, no tool exists) There is no tool to correct an existing expense's
     expense_date/receipt_url/notes -- only update_expense_category.py exists
     (category_id only). Mazda had to fall back to raw SQL with a hardcoded
     password to fix the 2028->2026 date and receipt_url on expense 2005.
     Consider adding an update_expense_field.py (or equivalent) tool covering
     date/receipt_url/notes so corrections don't require ad-hoc SQL.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786131943641676882_f3e0e40b7271.jpg
  failing_command: check_duplicates MCP tool call (id_light=scan, expense_date=2028-08-07, amount=200.00)
```

## For a human

- My own supplementary red-box check (`GET /supporting-document/2005/receipt`) returned a plain 404 from this dashboard instance — could not confirm the annotated-viewer/red-box behavior for expense 2005 independently. This is a gap in *my own verification tooling this run*, not evidence against Mazda's work (the corrected `/api/expense-stored` callback and DB record both look right), so it did not affect the top-line verdict. Worth a human spot-check of `View Receipt` on expense 2005.
- Expense 2005 is now correctly `NEEDS_REVIEW` / `awaiting_vendor_review` with `category_id=NULL` — needs a human to assign "Jose Garcia" a real vendor/category (likely a one-off contractor payment for vehicle work, not a ministry donation).
- The `gemini` CLI/API leg is failing with 401 across classify_scan, receipt parsing, and categorizer — consistent with the already-tracked "LLM Provider Fallbacks (Categorizer)" dashboard tile; fallbacks (Codex CLI, chatgpt-oauth) are working, so this is informational only.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- expense `2005`, metadata: No receipt/source viewer is available for this expense.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
