# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1786042553843962793_dcf4e2c739ec.jpg`
- Dispatch: 2026-08-06T18:55:57Z
- Mazda conversation: `conv-0e568a21-dcf8-4f42-b3fd-64a30a993c1f`
- Facade result: `doc_kind=unknown, confidence=0.0, recommended_action=reject` → STEP 0 self-classification required.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 18:56:00, returned wrap-v003 | ✅ |
| STEP 0 classify (`classify_scan.py`) | 18:56:29 executor_run → `doc_type=receipt, confidence=0.95, merchant="River of Life Ministries Inc."`. Gemini 401'd, correctly fell back to Codex CLI ("Classified via OpenAI Codex CLI fallback") | ✅ |
| Parse (`parse_and_categorize.py --json`) | 18:56:51 executor_run → single line item, $125.00, `expense_scope=full_receipt` (correct — no marked-item evidence on this document; it's a bank check-posting notice). Gemini 401'd again, fell back to `chatgpt-oauth`/gpt-5.4 | ✅ |
| `check_vendor_key` | 18:56:55 → returned `vendor_key=mel_trotter_ministries` via **fuzzy match** on `river_of_life_ministries_inc` | ⚠️ see Escalation — Mazda correctly adopted the tool's normalized key per contract, but the match itself looks wrong |
| `check_duplicates` | 18:56:59 → **tool error**: `Access denied for user 'adamsl'@'localhost' (using password: NO)` | ❌ infra/credentials failure, see Escalation |
| `categorizer_main.py` | 18:57:13 → `category_id=216` ("Mel Trotter"), positive/valid | ✅ (category itself is valid, but inherits the vendor-key error above) |
| Store (`parse_and_categorize.py --save --category-id=216`) | 18:57:24 → `{"ok": true}`, archived to `readable_documents/receipts/2025/september/september_08/...`, `expense_id=2004` | ✅ |
| `itemize_existing_expense` | 18:57:42 → `itemizable:false`, "only one line item; a single line equal to the charge is standalone" | ✅ correct fail-closed, single-line receipt |
| `record_trace` | 18:57:50, `task_name="document-intake"`, evidence JSON accurately reflects `duplicate_checked:false` and disclosed the check_duplicates tool failure in `problems` | ✅ honest evidence |
| `judge_trace` | 18:57:52 → `FAIL`, `failure_type=missed_duplicate`, reason `failed: duplicate_check_ran` | ✅ verdict consistent with the observed broken stage |
| `propose_improvement` / `apply_proposal` | 18:57:57 / 18:58:00 → proposal 41 filed and activated as wrap-v004 (adds a rule to attempt an authenticated duplicate check via `executor_run` when `check_duplicates` can't auth) | ✅ but see note below — this self-proposed fix can't work without the underlying DB credentials fix |
| Dashboard callback | 18:57:35 `curl POST /api/expense-stored`, correct payload (`expense_id:2004, stored:1, expense_ids:[2004]`) | ⚠️ **fired BEFORE `record_trace`/`judge_trace`**, violating the required STEP 9 ordering (callback must run after trace/judge) |

## Wrapper defect diagnosed & coached

**Callback-ordering violation.** Mazda POSTed `/api/expense-stored` at 18:57:35, two record_trace/judge_trace calls before those steps ran (18:57:50 / 18:57:52). The contract requires the notify step to always run after trace/judge. Sent a corrective message in-conversation at 18:59; Mazda called `propose_memory_note` (approved, proposal 42) recording the correct order — `store → itemize → record_trace → judge_trace → propose/apply improvement → dashboard callback` — and confirmed she will follow it going forward. No further intake work was needed for this document since the callback data itself was correct, just early.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  Two independent tool-level defects surfaced during a normal receipt intake, neither
  fixable by Mazda retrying or by a wrapper/instruction change:

  1. `check_duplicates` MCP tool failed with a MySQL authentication error:
     `Access denied for user 'adamsl'@'localhost' (using password: NO)`. This is the
     documented "needs DB creds" limitation (Mazda's manual, Table 2.2) actually firing
     on this host — the executor environment is missing/has invalid MySQL credentials
     for this tool. This caused `duplicate_checked=false` and the judge to (correctly)
     FAIL the run as `missed_duplicate`. Mazda's own self-proposed fix (wrap-v004,
     "run an equivalent authenticated duplicate check via executor_run") cannot actually
     work until the underlying DB credentials are fixed for this host/tool — the same
     auth gap would block any equivalent query too.

  2. `check_vendor_key` returned a highly questionable fuzzy match: it normalized the
     scanned document's actual vendor name `river_of_life_ministries_inc` to
     `mel_trotter_ministries` — two unrelated organization names that only share the
     word "ministries". Per the intake contract Mazda is required to adopt whatever
     normalized key the tool returns, so she correctly used `mel_trotter_ministries`
     and the expense (id 2004, $125.00, 2026-09-08... actually 2025-09-08, "RIVER OF
     LIFE MINISTRIES INC.") is now filed under category 216 ("Mel Trotter"), which
     looks like a miscategorization risk. Suspect the vendor-key fuzzy-matching
     threshold/algorithm in the vendor_category lookup (likely under
     tools/receipt_scanning_tools or tools/categorizer) is too permissive and needs a
     tighter similarity threshold or exact-token requirement before accepting a fuzzy
     match, especially for nonprofit/ministry names that share common words.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786042553843962793_dcf4e2c739ec.jpg
  failing_command: "MCP tool check_duplicates (invoked with id_light=scan, expense_date=2025-09-08, amount=125.00)"
  expense_id: 2004
  vendor_key_assigned: mel_trotter_ministries
  vendor_key_expected_review: river_of_life_ministries_inc (or a new unrecognized-vendor entry)

## For a human

- Expense 2004 ($125.00, River of Life Ministries Inc. check to John Roark, 2025-09-08)
  is filed under vendor_key `mel_trotter_ministries` / category "Mel Trotter" — worth a
  manual review/recategorization once the vendor lookup bug above is confirmed.
- The MySQL credentials gap for `check_duplicates` should be fixed at the infra/MCP
  config level; until then every receipt intake on this host will FAIL the
  `missed_duplicate` judge rule even when Mazda behaves correctly.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- expense `2004`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
