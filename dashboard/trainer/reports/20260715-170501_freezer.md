# Trainer Report — Freezer Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
**Dispatch:** 2026-07-15T16:57:14Z
**Verdict: PASS**

Facade returned `doc_kind=unknown, confidence=0.0` (expected for a JPEG scan — no
extractable text). Mazda correctly ran STEP 0 vision fallback and completed the full
contract within ~2m19s (16:57:15 → 16:59:33), with no stall and no coaching needed.

## Step-by-step evidence

| Step | Evidence | Result |
|---|---|---|
| 1. `load_wrapper_revision` | 16:57:26, tool return success | `wrap-v004` loaded |
| 2. STEP 0 vision fallback | `classify_scan.py` (16:57:45) → `receipt`, conf 0.95, merchant "Fifth Third Bank"; `parse_and_categorize.py --json` (16:58:08) → structured fields | doc_kind resolved, no literal "unknown" passed downstream |
| 3. `check_vendor_key` | 16:58:20, `vendor_key: fifth_third_bank` → normalized to `first_bank_8416` (fuzzy match), adopted by Mazda in all later calls | recognized:true |
| 3. `check_duplicates` | 16:58:33, `is_exact_duplicate:false, in_database:false` | **false negative** — see note below |
| 4. Categorize | `categorizer_main.py` via `executor_run` (16:58:43) → `category_id: 3` | non-null, used |
| 5. Store | `parse_and_categorize.py --save --category-id=3` (16:59:03) → `returncode:1`, `{"success":false,"duplicate":true,"existing":{"expense_id":531,...}}` | correctly NOT stored — real duplicate caught at save time |
| 6. `record_trace` | 16:59:15, `task_name:"document-intake"`, full IntakeVerificationEvidence with `is_duplicate:true, stored:false, expense_id:531, duplicate_checked:true`, honest `problems` note | trace_id 67 saved |
| 7. `judge_trace` | 16:59:22 → `PASS`, "all deterministic intake checks passed" | consistent with observed reality |
| 8. `propose_improvement` | not called | correct — no FAIL to propose against |
| 9. Dashboard notify | `curl POST /api/expense-stored` (16:59:28) with `stored:0, duplicate_expense_ids:[531]` | confirmed via `GET /api/expense-stored-events?since=1784134634` — event present, fires-on-duplicate as required |

## Note for a human (not a Mazda defect, no coaching sent)

The `check_duplicates` MCP tool (step 3) reported **no duplicate**
(`in_database:false`) for a transaction that the store step's own duplicate check
(the real, authoritative DB-backed check inside `parse_and_categorize.py --save`)
correctly caught seconds later (`expense_id 531`). The two checks likely key off
different fields — `check_duplicates` was given a long free-text description
("Fifth Third Bank confirmation of a single scheduled payment transaction
MB51Z5H2...") while the store step computed its own `id_light` from
date+amount+vendor. This is a false-negative in the deterministic
`check_duplicates` verifier, not a Mazda wrapper defect — she used its output
correctly and the save-time check still caught the real duplicate before
anything bad happened, so no lesson was sent. Worth tightening
`check_duplicates`' matching (or having Mazda pass it the same normalized
id_light the store step uses) so the earlier check isn't misleadingly "clean."

## Summary

Mazda handled this run cleanly: correct STEP 0 vision fallback, correct vendor
normalization, correct categorization, and — critically — she did not treat the
store-time duplicate rejection as a failure; she recorded it honestly
(`stored:false, is_duplicate:true, expense_id:531`) and still fired the
dashboard callback with `stored:0`, exactly per the "always fire, even on
duplicate" rule. Judge verdict PASS is correct. No corrective message was sent
to Mazda.
