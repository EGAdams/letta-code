# Trainer Report — Freezer Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Scanner:** Freezer Scanner
- **Dispatch time:** 2026-07-10T21:55:58Z (unix 1783720558)
- **Facade result:** `ok:true, doc_kind:unknown, confidence:0.0, recommended_action:reject` (expected for JPEG scans — no extractable text)
- **Verdict: PASS** (Mazda's handling was correct; `judge_trace` itself returned FAIL, but that FAIL is a known rubric gap, not a Mazda defect — see below)

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| 1. `load_wrapper_revision` | 21:56:05 → `wrap-v001`, `is_active:true` | ✅ |
| 2. STEP 0 self-classification (facade `doc_kind=unknown`) | 21:56:50 `executor_run` → `classify_scan.py` → `{"doc_type":"bank_statement","confidence":1.0,"merchant":"Chase","reason":"...multiple transactions, payments, and interest charges..."}` | ✅ correctly identified as a **non-receipt** document via vision, did not pass "unknown" downstream |
| 3. Investigate / routing | Determined this is a bank statement, not a receipt — correctly diverted away from the receipt store/categorize path rather than forcing it through `check_vendor_key`/`check_duplicates`/`categorizer_main.py` | ✅ (no receipt-store path applies to a statement) |
| 4. `record_trace` | 21:57:00 → `trace_id=53`, `task_name="document-intake"`, evidence JSON: `doc_kind:"statement"`, `vendor_key:null`, `stored:false`, `expense_id:null`, `problems:["classified as bank statement (Chase), not a receipt - routed to statement-verification instead of receipt storage"]` | ✅ honest evidence, no fabricated `stored:true` |
| 5. `judge_trace` | 21:57:04 → `trace_id=53` → `FAIL / missed_vendor_key` (reasons: `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored` all "failed") | Judge FAILed, but see wrapper defect below — this is expected given the current rubric, not a Mazda error |
| 6. `propose_improvement` (required on FAIL) | 21:57:10 → `proposal_id=39`, `failure_type:missed_vendor_key`, correctly diagnoses the rubric gap and proposes a "statement-routed" flag so the judge stops applying receipt-only checks to statements | ✅ |
| 7. Dashboard notify | N/A — no store occurred, correctly no `POST /api/expense-stored` | ✅ (confirmed `GET /api/expense-stored-events?since=1783720558` → `[]`) |

Total run time: dispatch → close-out in ~76 seconds. No stalls, no prose-only claims — every step above has a matching successful tool return.

## Wrapper defect diagnosed

**The intake judge rubric has no "statement, correctly non-receipt" outcome.** It only knows the
receipt checklist (`vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`,
`expense_stored`), so any document that is legitimately *not* a receipt — like this Chase bank
statement — always FAILs with `missed_vendor_key`, even when Mazda's routing decision was exactly
correct. This is now the **third consecutive run** on this same image (traces 51, 52, 53 /
proposals 36, 37, 38, 39) producing the identical false FAIL. This matches the manual's
Principle 5.1 failure taxonomy: the defect is in the judge/evaluator, not in Mazda's behavior —
she has correctly classified and routed the document all three times.

## Lesson sent to Mazda

None sent this run — her behavior was fully correct and she already re-diagnosed the same rubric
gap on her own in `propose_improvement` (proposal 39), consistent with proposals 36-38 from the
two prior identical dispatches. Sending another corrective message would be redundant; the
proposal contains an actionable suggested_patch (`add a statement-routed flag; teach the judge to
treat it as a successful non-receipt outcome`).

## For a human to look at

- **This same scan_freezer.jpg has now been dispatched three times** (17:53, 21:43, 21:56) and
  processed identically each time, each producing a new FAIL trace + new proposal (36, 37, 38, 39
  — four proposals total, since the second run at 17:56 double-submitted `judge_trace`/
  `propose_improvement`). Consider: (a) applying the fix Mazda has now proposed four times
  (statement-routed flag in the judge), and (b) checking why the same image keeps getting
  re-dispatched — if `incoming_scans/scan_freezer.jpg` isn't being cleared/archived after
  processing, every future scan on the Freezer scanner will keep re-triggering this exact loop.
- Earlier in the transcript (11:17-11:20, an older/unrelated run against
  `walgreens_2025-01-22.json`) shows the same `missed_vendor_key` FAIL pattern on a genuine
  receipt fixture where vendor_key/category truly were never resolved — that one is a real defect
  and already has its own proposal (35), separate from this statement-rubric issue.
