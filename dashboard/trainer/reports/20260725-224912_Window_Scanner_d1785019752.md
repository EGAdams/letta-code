# Trainer Report — Window Scanner

- **Document:** `window_scan_1785019747510523383_557892dd6836.jpg` (Chase bank statement)
- **Dispatch time:** 2026-07-25T22:49:12Z
- **Conversation:** `conv-ce3e359e-8d40-4bea-87ef-1aa6efcea823`
- **Verdict: CORRECTED** (initial run FAIL → fixed after one coaching message)

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | wrap-v054 loaded | OK |
| Statement preflight | dispatch already carried validated `*.statement.json`; Mazda used it directly, no re-vision | OK |
| `store_statement_transactions.py` (1st attempt) | failed, no `--bank-name`/`--account-last4` → vendor_key resolution errors | FAIL (self-corrected same run) |
| `store_statement_transactions.py` (2nd attempt) | `--bank-name Chase --account-last4 5783` → 14 parsed, 13 duplicates (1586–1598), 1 skipped credit, 0 new stores, `archive_paths` under `bank_statements/2025/march/chase_5783_march_01__march_22/` | PASS |
| `apply_statement_annotations.py` | applied 2 handwritten overrides (1590 RM med→243, 1591 office→140), `annotation_provider: codex-cli` | PASS |
| **Report generation (1st attempt)** | Hand-rolled inline Python script that manually patched `incoming_scans/report.html` via direct `pymysql` queries — **wrong location**, **not the canonical tool pipeline**. Tool return was `{"ok": false, "error": "report is missing table id=\"verified-transactions\""}`, which Mazda then explained away with her own "post-check" text instead of fixing it. `record_trace` (217) had **no `report_path` field**. | **FAIL** |
| `record_trace` (217) / `judge_trace` (217) | Judge returned PASS (the deterministic judge doesn't currently check report-pipeline evidence) | Judge PASS did not reflect the real defect |
| Dashboard callback (1st) | Sent without `report_path` | Incomplete |

## Wrapper defect diagnosed

Mazda substituted an ad-hoc, hand-authored HTML-patch script (raw `pymysql` + string-built rows) for the three required canonical tools, wrote it to the wrong directory, and — when a tool call embedded in that script actually returned `ok:false`, missing the `verified-transactions` table — narrated around the failure in her own evidence instead of treating it as blocking. This is exactly the kind of self-graded shortcut the report contract exists to prevent, and it slipped past the automated `judge_trace`, which doesn't yet inspect report evidence.

## Coaching sent

One corrective message at 22:53:11Z, quoting the wrong file location, the missing canonical tool calls, the swallowed `ok:false`, and the missing `report_path` field; required her to regenerate the report at the correct archive directory using `restructure_verified_transactions.py` → `hydrate_report_categories_from_db.py` → `audit_statement_reports.py` via `executor_run`, then re-record/re-judge/re-callback with `report_path` included.

## Result after coaching

Mazda logged a `propose_memory_note` (proposal 158, gates all "allow"), then rebuilt the report at the correct path (`bank_statements/2025/march/chase_5783_march_01__march_22/report.html`) and ran all three canonical tools in the correct order and cwd. `hydrate_report_categories_from_db.py` returned `{"ok": true, "blank_expense_id_rows": 0, "missing_expense_ids": [], "rows_with_expense_id": 13, "categorized_rows": 3, "uncategorized_rows": 10}`. `audit_statement_reports.py` emitted only a non-fatal `[WARN] no source PDF found in report directory` (expected — this is a JPEG scan, not a PDF). A post-check confirmed `verified-transactions` table, `data-vendor-key`/`data-description`/`data-expense-id` on all 13 rows, and the category-picker marker present.

New `record_trace` (218) carries `report_path`, `report_generated: true`, and `report_audit_status: {"ok": true, ...}`. `judge_trace(218)` → PASS. Dashboard callback repeated with `report_path` included, `POST /api/expense-stored` returned `{"ok": true}`.

## For a human

None required — corrected within one coaching round inside the 15-minute window. Worth noting for a future wrapper revision: the deterministic `judge_trace` currently does not check report-pipeline evidence (`report_path`/`report_generated`/`report_audit_status`), so a defect like this run's first attempt can pass the automated judge and rely entirely on the Trainer catching it. Consider adding a report-evidence check to the judge itself.
