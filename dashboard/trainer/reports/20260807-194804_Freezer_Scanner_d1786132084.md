# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `scan_freezer_1786132081377670627_92328a726230.jpg` — canceled/processed church
check (Grand Rapids First, check #9018, $30.00, paid to Gabrielle McKay, memo "CHILDRENS
CHURCH MINUTES", dated 2025-05-02).
**Dispatch:** 2026-08-07T19:48:04Z
**Mazda conversation:** conv-9b08588e-5cfc-43ac-b7ea-e9a78317fe2a
**Run window observed:** 19:48:07Z – 19:50:10Z (~2 minutes, clean single pass, no
coaching needed)

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

Note: this is the second Trainer attempt for this dispatch (watchdog attempt 2/3) after
a prior session died without reporting. On arrival the run was already complete
(`judge_trace` had returned) — graded from the transcript as-is, no live coaching sent.

## Checklist (with evidence)

1. `load_wrapper_revision` — ✅ returned `wrap-v004`, active.
2. Facade returned `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification
   required. ✅ `executor_run classify_scan.py` → `doc_type=receipt, confidence=0.83`.
   Gemini vision returned 401 (dead credential) and the tool correctly fell back to
   OpenAI Codex CLI (`"Classified via OpenAI Codex CLI fallback."`) — fallback chain
   worked as designed.
3. Receipt branch parse: ✅ `parse_and_categorize.py --json` — Gemini 401'd again,
   fell back to `chatgpt-oauth` (gpt-5.4), extracted transaction_date, party, amount,
   `expense_scope=full_receipt`, `handwritten_arithmetic_total=null`. Correct scope: a
   single-item, single-total canceled check has no marked-item selection to make.
4. `check_vendor_key("grand_rapids_first")` → ✅ recognized, fuzzy-normalized to
   `first_bank_8416`; Mazda correctly adopted the normalized key for every subsequent
   call (categorizer input, save).
5. `check_duplicates` → **tool call ERRORED**: `Access denied for user
   'adamsl'@'localhost' (using password: NO)`. This is a real infra defect, not a
   Mazda mistake — see Escalation #1. Mazda worked around it by running a direct
   read-only SQL lookup via `executor_run` for the same `(expense_date, amount)` pair,
   got zero rows, and correctly concluded no duplicate. The duplicate check was
   genuinely performed and correctly answered, just not through the canonical tool.
6. `categorizer_main.py --provider=auto` → ✅ `category_id=3` (Personal), non-null,
   consistent with `check_category` (`matches_vendor:true`).
7. `parse_and_categorize.py --save --category-id=3` → ✅
   `{"success": true, "expense_id": 2006, "duplicate": false,
   "parse_artifact_verified": true}`, receipt archived to
   `readable_documents/receipts/2025/may/may_02/grand_rapids_first_05_02_25_30_00.jpg`.
   Single line item — no itemization required.
8. `record_trace(task_name="document-intake")` → ✅ trace_id 87, evidence matches
   observed reality (`stored:true`, `expense_id` present, `is_duplicate:false`,
   `duplicate_checked:true` — true, just via the workaround in #5).
9. `judge_trace(87)` → ✅ `PASS`, "all deterministic intake checks passed" — correct
   verdict given the above.
10. Dashboard callback `POST /api/expense-stored` → ✅ `{"ok": true}`, fired after
    trace/judge with `expense_id:2006`, `expense_ids:[2006]`,
    `duplicate_expense_ids:[]`, `parsed:1`, `stored:1`, correct archive path/date.

No `propose_improvement` call was needed (no FAIL).

## Supplementary check — Set Category dialog / red box

`GET /supporting-document/2006/receipt` → HTTP 200, served the annotated-copy pipeline,
but the log shows: `opened without highlight expense_id=2006 type=receipt reason=No
high-confidence expense row was found in the image.` So **no red box** is drawn on this
receipt. This is expected behavior friction, not a Mazda failure (per contract) and not
grounds to flip the top-line verdict — but it is a real dashboard-side gap: the receipt
annotator appears tuned for itemized line-receipts and doesn't have a matcher path for a
canceled-check image (payee/amount/date printed in a very different visual layout).
Filed as Escalation #2 below since a receipt with no box is unverifiable by eye.

## Wrapper defects diagnosed

None. This was a clean run: correct STEP 0 routing, correct vendor normalization
adoption, fail-closed category resolution, correct fallback-chain usage on two separate
Gemini 401s, single successful save, and full trace/judge/callback sequence. No lesson
sent to Mazda.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  Two independent application-level issues surfaced during grading, neither fixable by
  coaching Mazda:

  (1) The `check_duplicates` deterministic MCP tool errored with a real MySQL access
  error instead of the documented graceful `{"error": …}` fallback (Ch.2 Table 2.2 says
  it should return a structured error when creds are absent, not raise): "Access denied
  for user 'adamsl'@'localhost' (using password: NO)". Mazda's own inline raw-SQL
  workaround (run via executor_run) used working credentials
  (user=adamsl, password embedded in the command, host=127.0.0.1) that connected fine —
  so the finance MCP server process itself has a stale/missing DB credential env var,
  while the executor's ambient environment has a working one. Recommend checking
  `mazda-tools-mcp.service`'s env (likely missing password, or `localhost` vs `127.0.0.1`
  resolving to a socket auth attempt) against whatever the executor uses. A second,
  narrower concern: Mazda embedding a live DB password inline in an executor_run shell
  command is a credential-hygiene smell worth cleaning up once the tool itself is fixed
  (so this workaround stops being necessary).

  (2) `dashboard/document_annotation.py`'s receipt strategy failed to box the matching
  row on a canceled-check-style receipt (single printed amount/date/payee, not a
  itemized grocery-style receipt layout), logging "No high-confidence expense row was
  found in the image." This makes the stored expense's receipt view unverifiable by eye
  even though storage/categorization were all correct. Likely needs a check-image-shaped
  matching path (or a lower-bar fallback: box the whole page when it's a single-total
  document) similar to the mom's-ledger handling already documented for statements.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786132081377670627_92328a726230.jpg
  failing_command: "check_duplicates MCP tool call (id_light=scan, expense_date=2025-05-02, amount=30.00)"
  expense_id: 2006

## For a human

- Expense 2006 stored correctly (Personal / $30.00 / Grand Rapids First / 2025-05-02),
  no duplicates, receipt archived. No action needed on the data itself.
- `check_duplicates` MCP tool DB credentials need fixing on the mazda-tools-mcp side —
  currently silently relying on Mazda's own SQL workaround, which won't always happen.
- Receipt annotation (red box) doesn't work for check-style receipts — worth a small
  dashboard fix so this class of document is verifiable at a glance.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `2006`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
