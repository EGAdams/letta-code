# Trainer Report — Freezer Scanner intake

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Scanner**: Freezer Scanner
- **Dispatch**: 2026-07-10T21:43:18Z
- **Verdict: PASS** (contract compliance for this dispatch)

## Context

This is a repeat scan of the same `scan_freezer.jpg` image that was already dispatched twice
earlier today (17:53Z and its follow-up) and classified both times as a **Chase bank
statement**, not a receipt (trace 51). This run (trace 52) reproduces that same classification.

## Checklist (evidence from transcript, 21:43:23–21:44:45Z)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | tool_return: `wrap-v001`, `is_active: true` | ✅ |
| STEP 0 classify | `executor_run` → `classify_scan.py` → `{"doc_type":"bank_statement","confidence":1.0,"merchant":"Chase",...}` | ✅ correct, high-confidence |
| STEP 0 parse (receipt only) | N/A — doc is not a receipt, correctly skipped | ✅ |
| STEP 2 investigate | Not run — correctly N/A for a non-receipt (no merchant/vendor_key to check) | ✅ |
| STEP 5 `record_trace` | `task_name="document-intake"`, `doc_kind:"statement"`, `vendor_key:null`, `stored:false`, `expense_id:null`, `problems:[...]` → trace_id=52 | ✅ accurate evidence, not stored as receipt (correct) |
| STEP 6 `judge_trace` | verdict **FAIL**, `failure_type: missed_vendor_key`, reasons: vendor_key/category/duplicate/expense_stored all "failed" | rubric artifact, see below |
| STEP 7 `propose_improvement` (FAIL path) | proposal_id=38 filed, summary correctly explains this is a statement misjudged by the receipt rubric | ✅ |
| STEP 8 dashboard notify | correctly skipped (nothing was stored) | ✅ |

No expense was stored (`/api/expense-stored-events?since=1783719798` → `[]`), which is correct —
this document should never enter the finance DB as a receipt.

## Wrapper defect (already diagnosed by Mazda, not new)

The `document-intake` judge rubric only scores receipt fields (`vendor_key_resolved`,
`category_resolved`, `duplicate_check_ran`, `expense_stored`). It has no branch for
`doc_kind:"statement"`, so a **correctly-identified, correctly-not-stored** bank statement is
always scored FAIL under `missed_vendor_key` — a false negative. Mazda has now filed three
near-identical proposals for this exact gap: **proposal 36, 37 (trace 51, same image, earlier
today), and proposal 38 (trace 52, this run)**. She is doing her part (Principle: propose a
wrapper fix, don't blame the model); the proposals are piling up ungated.

**Recommendation for a human**: gate/activate one of proposals 36-38 (or write a fresh
statement-aware branch in the intake rubric — PASS when `doc_kind:"statement"` and
`stored:false` with no problems other than "not a receipt") so this stops manufacturing a new
FAIL+proposal every time the Freezer scanner (or any scanner) picks up a bank statement instead
of a receipt. Until fixed, expect one new duplicate proposal per non-receipt scan.

## Coaching sent to Mazda

None this run — she executed the documented STEP 0/5/6/7 contract correctly for a non-receipt
document. (Separately, in live human chat earlier today at 17:56-17:58Z, EG asked her to also
continue into an actual statement-verification path after classifying a document as a
statement — that request is outside this dispatch's receipt-intake contract and outside my
scope to enforce; flagging for human awareness only, since she has not yet done that follow-on
statement processing in either run.)

## Summary

PASS. Correct classification, correct refusal to store a bank statement as a receipt, correct
trace/judge/propose closeout. The only issue is systemic (rubric gap), already self-diagnosed
by Mazda across three proposals — needs a human to gate one of them rather than more coaching.
