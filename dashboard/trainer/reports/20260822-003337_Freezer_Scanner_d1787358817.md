# Mazda Trainer Report — Freezer Scanner

**Verdict: CORRECTED**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1787358813937326317_f4ef61a09e52.jpg` (Walgreens pharmacy receipt, badly OCR-garbled)
- Dispatch: 2026-08-22T00:33:37Z
- Mazda conversation: `conv-9087f969-3b2e-4891-8f72-075e61b850f7`
- Facade: `doc_kind=unknown` (JPEG, expected) → STEP 0 self-classification required

## Checklist, with evidence

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 00:33:45, `wrap-v081` loaded | ✓ |
| STEP 0 classify | `classify_scan.py` → `doc_type=receipt`, confidence 1.0, merchant "Walgreens" | ✓ |
| STEP 0 parse | `parse_and_categorize.py --json --engine=gemini --write-parsed-json` → gemini rejected (no transaction_date), fell back to `local-fallback`; artifact written to `/tmp/mazda_receipt_7ad5cacfa317.json` with `total_amount=41.23`, `transaction_date=null` | ✓ (fail-closed date handling correct) |
| `check_vendor_key` | `vendor_key="walgreens"`, `recognized:true` | ✓ |
| `check_duplicates` | placeholder date `1970-01-01` used since none extracted; `is_exact_duplicate:false` | ✓ |
| Categorize | `categorizer_main.py --provider=auto` → `category_id:243` (non-null/non-zero) | ✓ |
| Store | `parse_and_categorize.py --save --category-id=243` → `expense_id=2216`, `receipt_url='apress_pay_08_21_26_41_23.jpg'` | ✓ |
| Itemization | **Initially skipped** (`itemization_attempted:false` despite a 2-item parse artifact) — see coaching below | Corrected |
| `record_trace` | trace_id 436 (initial), 437 (post-coaching) | ✓ |
| `judge_trace` | Both trace 436 and 437 → **PASS** | ✓ |
| Dashboard notify | Two `/api/expense-stored` POSTs, both confirmed via `/api/expense-stored-events?since=1787358817` with `conversation_id` and `dispatched_at` matching this dispatch | ✓ |

## Wrapper defect diagnosed

The STEP 0 parse artifact returned an `items` array with two entries (`TOTAL $41.23`, `APPRUVED FSA/HRA AMUUNI $0.08`) — an OCR/structural artifact of a garbled single-line pharmacy receipt, not real distinct purchased products. Mazda judged on her own that these weren't "real" line items and skipped `itemize_existing_expense` entirely, recording `itemization_attempted:false`. The contract is explicit that any parsed items array with >1 entries requires the call — the tool's own cent-exact reconciliation is the fail-closed gate, not Mazda's judgment about whether the items look genuine. This is a **missing-guard wrapper defect**: nothing in her instructions told her it's never her call to skip the itemization attempt.

## Coaching sent

Sent a corrective message in the same conversation instructing her to call `itemize_existing_expense(expense_id=2216, ...)` now, record the real outcome, and re-run record_trace/judge_trace/callback. Also required a durable memory rule.

## Re-grade after coaching

Mazda responded correctly and completely:
- Read the parse artifact, called `itemize_existing_expense(expense_id=2216, ...)`.
- Tool returned `itemizable:false`, `reason:"line items total 41.31, charge is 41.23"`, `difference:-0.08` — a correct fail-closed outcome (items 41.23+0.08=41.31 ≠ 41.23 charge). Expense 2216 correctly remains STANDALONE.
- Recorded a fresh trace (id 437) with `itemization_attempted:true`, `itemized:false`, `itemization_reconciled:false`, and the reconciliation reason in `problems`.
- `judge_trace(437)` → PASS.
- Repeated the `/api/expense-stored` dashboard callback (confirmed above, second event at `stored_at=1787360182.7`).
- Saved a durable memory note (`propose_memory_note`, proposal_id=264, gates all `allow`): always call `itemize_existing_expense` when parsed items.length > 1, regardless of whether items look genuine; `itemizable:false` is a valid outcome, never a reason to have skipped the call.

## For a human

- Minor cosmetic inconsistency in both dashboard callbacks: `archive_years:[1970]` (derived from the placeholder `expense_date`) doesn't match the actual archive path, which is filed under `readable_documents/receipts/2026/august/august_21/...` (today's date, used because the transaction date was unrecoverable). Not contract-relevant (no checklist item grades `archive_years` for receipts), but worth a look if `archive_years` feeds any receipt-side reporting.
- I attempted to independently verify red-box/dialog-visibility evidence per the contract's "DIALOG VISIBILITY / RED BOX" requirements. `GET /supporting-document/2216/receipt` returned `200` with a real 430KB JPEG, which is consistent with a served (and likely annotated) receipt image. I could **not** fully confirm annotation (served bytes vs. raw file) or check `/api/supporting-documents` because that endpoint requires a POST with a JSON body, and my Trainer Bash allowlist restricts POST to only the Mazda conversation's `/messages` endpoint. This is a gap in my own verification tooling, not evidence of an intake or dashboard defect — flagging per the "don't flip Verdict on your own tooling gaps" rule. If a human wants full box confirmation, check `/api/supporting-documents` for expense 2216 directly.
- Garbled OCR (`APRESS PAY \\` instead of the vendor name, all-caps corruption throughout) is a known Gemini/local-fallback quality issue on this particular scan, not a new class of failure — classification correctly recovered "Walgreens" from context (`THANK YUU FUK SHOPPING AT WALGREENS` in raw OCR text) and vendor/category resolution were unaffected.

No escalation filed — no application-level defect observed; the gap was a coachable wrapper instruction gap, now corrected and reinforced in Mazda's memory.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
