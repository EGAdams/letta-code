# Trainer Report — Window Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785468134512955367_9055dc8d11ac.jpg`
- **Dispatch time:** 2026-07-31T03:22:18Z
- **Mazda conversation:** `conv-33936787-09ef-402a-af37-38f3d0da51d1`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Summary

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), correctly routing
Mazda through STEP 0 vision classification. Document turned out to be a donation
acknowledgment receipt from Tikkun International ($24.25, 2025-03-24). Mazda completed
every required step with successful tool returns; judge verdict was `NEEDS_REVIEW`
(not FAIL), so no `propose_improvement` was required or expected.

## Step-by-step checklist

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | ✅ `wrap-v069`, found |
| STEP 0 vision classify | ✅ `classify_scan.py` → `doc_type=receipt, confidence=0.98, merchant=Tikkun International` (Gemini 429'd first, fell back successfully per stderr) |
| Receipt parse (`--json`) | ✅ `parse_and_categorize.py --json --engine=gemini` succeeded, wrote artifact to `/tmp/mazda_receipt_3f051d6017d6.json` |
| `check_vendor_key` | ✅ called, `tikkun_international` not in known-vendor map — Mazda correctly adopted the returned normalized key anyway |
| `check_duplicates` | ✅ called with date/amount/description — `is_exact_duplicate=false`, not in DB |
| Categorize | ✅ `categorizer_main.py --provider=auto` → `category_id=356` (nonzero, valid) |
| Store | ✅ `parse_and_categorize.py --save --category-id=356` → `success:true, expense_id=1973, parse_artifact_verified=true, duplicate=false` |
| Itemization | ✅ `itemize_existing_expense` called → correctly refused (`itemizable:false`, "only one line item; a single line equal to the charge is standalone, not itemized") — correct fail-closed outcome per contract |
| `record_trace` | ✅ `task_name="document-intake"`, full IntakeVerificationEvidence, trace_id=330 |
| `judge_trace` | ✅ verdict `NEEDS_REVIEW`, `failure_type=none`, reason: "vendor_key resolved but not in the known-vendor map" — a reasonable, non-FAIL verdict, so no proposal call was required |
| Dashboard callback | ✅ `POST /api/expense-stored` → `{"ok": true}`, `expense_id=1973`, `expense_ids=[1973]`, `duplicate_expense_ids=[]`, `parsed=1, stored=1` |

## Supporting-document / dialog visibility check

`POST /api/supporting-documents {"expense_id":1973}` confirms `receipt_url` is set
(`tikkun_international_03_24_25_24_25.jpg`) and correctly produces `View Receipt`
(`available:true`); `document_url`/`scanned_statement_url`/`moms_ledger` correctly
`available:false` (none of those evidence types apply here). This part of the contract
is satisfied.

## Defects found — NOT Mazda's fault, for a human to look at

1. **Missing red-box highlight on the receipt viewer.** `/supporting-document/1973/receipt`
   served bytes identical to the raw archived file (both 384705 bytes, `cmp` identical).
   Dashboard log: `[supporting-document] opened without highlight expense_id=1973
   type=receipt reason=No high-confidence expense row was found in the image.` This is a
   single-line donation-acknowledgment receipt, not a tabular/itemized scan, and the
   annotator's row-matching heuristic apparently found nothing to box. Per the Trainer
   contract this is a dashboard (`document_annotation.py`) defect, not something to coach
   Mazda to fix or re-store around.
2. **Latent tool warning in `parse_and_categorize.py`'s save path**: stderr contained
   `WARNING:__main__:Source-counterpart lookup failed: not enough arguments for format
   string` during the `--save` call. It did not block storage (save succeeded, correct
   `expense_id`/`receipt_url`/archive path), but indicates a Python `%`-style format-string
   bug in that lookup's error/logging path worth a look when someone is next in that file.

## Lesson sent to Mazda

None — no correctable wrapper defect found in this run. Nothing to coach.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
