# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer.jpg` (Chase statement, Jan 2025, facade: doc_kind=statement, vendor=Amazon Chase, confidence=0.99)
- **Scanner**: Freezer Scanner
- **Dispatch**: 2026-07-16T17:22:30Z
- **Conversation**: `conv-e16ac815-1bb8-4453-bbb4-1df8ccc09677`
- **Trace**: 90 · **Proposal**: 68 (activated → `wrap-v021`)
- **Verdict: FAIL** (matches Mazda's own `judge_trace` verdict — consistent with observed evidence, not a contract violation)

## Checklist (all steps present with successful tool returns)

1. ✅ `load_wrapper_revision` → `wrap-v020` active, 17:22:33.
2. ✅ Statement branch taken directly (facade already `doc_kind=statement`, confidence 0.99 — STEP 0 correctly skipped per dispatch instruction "Do not classify it again").
3. ✅ `executor_run parse_statement_scan.py` → 17:22:49, `ok:true`, 7 transactions parsed, period 2025-01-01→2025-01-31.
4. ✅ `executor_run store_statement_transactions.py` → 17:22:59. Internally handles vendor-key resolution + duplicate check for the statement branch (no separate `check_vendor_key`/`check_duplicates` calls needed here — consistent with the statement-branch pattern). Result: `parsed:7, skipped_credits:3, duplicates:0, stored:1, failed:3`. 3 outflow rows failed with `no category mapping for statement vendor ... (resolved vendor_key=None)`:
   - `AMAZON MKTPL*ZP35E5Z21 Amzn.com/bill WA` (2025-01-04, -$60.00)
   - `AMZN Digital*ZDSSO7DNO 888-802-3080 WA` (2025-01-10, -$1.99)
   - `OPENAI *CHATGPT SUBSCR HTTPSOPENAI.C CA` (2025-01-22, -$20.00)
   - Only `APPLE.COM/BILL 866-712-7753 CA` (-$10.59) resolved and stored as expense_id 1522.
5. ✅ `record_trace` → trace_id 90, `task_name="document-intake"` (exact match), evidence JSON accurately reflects the store result (`transactions_stored:1`, `transactions_skipped_credits:3`, problems array matches actual failures).
6. ✅ `judge_trace(90)` → `FAIL`, `failure_type: other`, reason `failed: statement_transactions_stored` — correct verdict given 3/4 non-credit rows failed to store.
7. ✅ `propose_improvement` → proposal 68 filed, sensible: alias-lookup fallback for `resolve_vendor_key()` on statement vendor strings.
8. ✅ `apply_proposal(68)` → all 4 gates (safety/cost/regression/usefulness) allowed, activated `wrap-v021` (was `wrap-v020`), snapshot 18 taken for rollback.
9. ✅ Dashboard notify → `POST /api/expense-stored` returned `{"ok":true}`, `expense_ids:[1522]`, `duplicate_expense_ids:[]`, `parsed:7, stored:1`, correct `conversation_id`/`dispatched_at`.

## Wrapper defect diagnosed

None in Mazda's own behavior — every stage executed correctly and in order, and she followed the FAIL-path contract exactly (record → judge → propose). The underlying failure is a **data gap in `vendor_category.yaml`**: three recurring billers (Amazon Marketplace, Amazon Digital, OpenAI ChatGPT subscription) have no vendor_key mapping. Mazda's proposal 68 was auto-activated as a *prose* instruction in `wrap-v021` ("attempt a normalized alias lookup ... before failing the row") — this does not itself add the missing YAML entries, so the same three vendors will likely fail again on the next statement unless the alias logic or the YAML map is actually implemented/extended.

## Lesson sent to Mazda

None — no coaching needed this run; the contract was fulfilled correctly.

## For a human

Add explicit vendor_key mappings (or implement the alias-normalization logic wrap-v021 now instructs) for:
- `AMAZON MKTPL*...` → Amazon Marketplace
- `AMZN Digital*...` → Amazon Digital
- `OPENAI *CHATGPT SUBSCR...` → OpenAI/ChatGPT subscription

in `vendor_category.yaml`, otherwise every future Chase statement with these recurring charges will keep failing to store and keep generating FAIL verdicts / proposals for the same root cause.
