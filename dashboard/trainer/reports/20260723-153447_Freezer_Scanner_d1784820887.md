# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-23T15:34:47.000Z
- Document path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1784820884191547746_8a42b65c64ca.jpg`
- Verdict: FAIL

## Checklist

1. `load_wrapper_revision` succeeded at `15:34:51Z` with `wrapper_revision=wrap-v035`.
2. `classify_scan.py` succeeded at `15:35:05Z`; doc type was `receipt` with confidence `0.99`.
3. `parse_and_categorize.py --json` succeeded at `15:35:38Z`; parsed merchant `Menards`, date `2025-03-27`, total `30.40`.
4. `check_vendor_key` succeeded at `15:35:43Z`; returned `vendor_key=menards`, `recognized=false`.
5. `check_duplicates` succeeded at `15:35:51Z`; no exact or fuzzy duplicate.
6. `categorizer_main.py` succeeded at `15:37:51Z`; returned `category_id=null`.
7. `parse_and_categorize.py --save` succeeded at `15:38:16Z`; created `expense_id=1531` with `expense_status=NEEDS_VENDOR_KEY` and `pending_vendor_review=true`.
8. `itemize_existing_expense` succeeded at `15:38:26Z`; correctly refused itemization (`itemizable=false`) because the receipt had one line item only.
9. `record_trace` succeeded at `15:38:34Z`; `task_name=document-intake`, `trace_id=141`, evidence matched the run.
10. `judge_trace` succeeded at `15:38:39Z`; verdict was `NEEDS_REVIEW` with reason `vendor_key resolved but not in the known-vendor map`.
11. Dashboard callback succeeded at `15:38:47Z` via `POST /api/expense-stored`.
12. After coaching, `propose_improvement` succeeded at `15:40:19Z` with `trace_id=141`, `failure_type=missed_vendor_key` and proposal `106`.
13. Memory-note proposal also succeeded at `15:40:26Z` with proposal `107`.

## Defect

This was not a clean receipt pass. The run ended stored, but `category_id` was null and the receipt remained `NEEDS_VENDOR_KEY` / `pending_vendor_review=true`. Under the trainer rubric, that is a FAIL even though the save itself succeeded.

## Lesson Sent

“This run is a FAIL for training purposes even though the store call succeeded: the receipt was left with `category_id null` and vendor_key `menards` was not recognized. The contract says a newly stored receipt with category_id null/zero or placeholder merchant is not a PASS. Keep the intake evidence you already recorded, but file the learning-loop step now: call `propose_improvement(trace_id=141, failure_type="missed_vendor_key")` and record that future receipt runs must not end as a PASS when the vendor key is unrecognized and the receipt is still pending vendor review. Do not change the stored expense; just close the loop and record the lesson in memory.”

## Human Follow-Up

- Review whether `menards` should be added to the vendor map or handled by a stronger normalization rule.
- Investigate the save warning about failing to update `source_file` to the archived receipt path.
