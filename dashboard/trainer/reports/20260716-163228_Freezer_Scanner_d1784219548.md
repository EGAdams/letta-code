# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-16T16:32:28Z
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: `FAIL`

## Checklist

- `load_wrapper_revision` succeeded at `16:32:35Z`, returning `wrap-v010`.
- STEP 0 classification/parsing ran via `executor_run` and identified the image as `bank_statement` with `confidence: 1.0`.
- `check_vendor_key` succeeded and normalized `amazon_chase` to `chase_mc_9915`.
- `check_duplicates` succeeded and returned `is_exact_duplicate: false`.
- `check_category` returned `category_id: null` with `expected_category_id: 3` and `expected_category_name: Personal`.
- Mazda then ran receipt-style save logic on the statement image instead of the statement branch.
- `record_trace` succeeded with `task_name: "document-intake"` and trace `78`.
- `judge_trace(78)` returned `FAIL` with `failure_type: other` and reason `failed: statement_transactions_stored`.
- No dashboard `expense-stored` event was recorded for this dispatch.
- After coaching, Mazda filed `propose_improvement(trace_id=78, failure_type=other)` as proposal `56` and activated `wrap-v011`, but she still did not complete statement extraction/storage for this document.

## Defect

Wrapper defect: statement routing was not enforced after classification. A `bank_statement` image was allowed to proceed through receipt parsing/save logic, which left the required statement storage path incomplete.

## Lesson Sent

I told Mazda that this scan classified as `bank_statement`, so she must not use receipt parsing/storage. I instructed her to run `parse_statement_scan.py`, then `store_statement_transactions.py`, then `record_trace` with the statement evidence JSON, then `judge_trace` again, and to call `propose_improvement(trace_id=78, failure_type=other)`. I also asked her to record the lesson in memory: bank_statement docs use the statement branch, and receipt save is not a substitute for statement storage.

## Human Review Notes

- The run is not correct for this document because statement storage never happened.
- The receipt-path save produced an expense-like result, but the judge still failed the run due missing statement transactions.
- `wrap-v011` was activated after the failure, so the next dispatch should load the corrected rule, but this run remains a FAIL.
