# Mazda Trainer Report

- Scanner: Window Scanner
- Dispatched at: 2026-07-25T22:26:26Z
- Document: receipt, Walgreens, $15.07
- Verdict: PASS

## Checklist

- `load_wrapper_revision` returned active wrapper `wrap-v051`.
- STEP 0 classification ran via `executor_run`; Gemini hit quota, Codex CLI fallback classified the scan as `receipt` with `merchant=Walgreens` and confidence `0.99`.
- `check_vendor_key` returned `vendor_key=walgreens`.
- `check_duplicates` completed without a duplicate.
- Receipt parse/save returned success for `expense_id=1580` with `parse_artifact_verified=true`, `expense_scope=marked_items`, `parsed_amount=15.07`, and selected item `FSA RX 3134457`.
- The parser output matched the marked-item evidence: one localized circle selected the single $15.07 line.
- `itemize_existing_expense` ran and correctly returned `itemizable=false` because there was only one line item, so no parent/child reconciliation was needed.
- `record_trace` saved trace `210` with `task_name=document-intake`.
- `judge_trace(210)` returned `PASS` with `failure_type=none`.
- Dashboard callback evidence exists for this dispatch with `stored=1`, `expense_id=1580`, `vendor_key=walgreens`, and `expense_date=2025-03-11`.

## Notes

- No coaching message was needed. Mazda completed the intake contract successfully and stopped at the correct terminal steps.
