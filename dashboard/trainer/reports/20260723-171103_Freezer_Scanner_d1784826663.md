# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Image:** `scan_freezer_1784826660167288231_eef557391e02.jpg`
- **Dispatch:** 2026-07-23T17:11:03Z (conv-b366b0c4-6b8c-4b80-80ba-e31a58d35d6e)
- **Verdict: PASS** (judge_trace, trace_id 156, failure_type=none)

## Facade result
`doc_kind=unknown, confidence=0.0, recommended_action=reject` — expected for a JPEG scan with no extractable text; correctly routed Mazda to STEP 0 self-classification.

## Step-by-step evidence

| Step | Tool call | Result |
|---|---|---|
| STEP 1 | `load_wrapper_revision` | wrap-v037 loaded, learned rules applied |
| STEP 0 classify | `executor_run classify_scan.py` | `doc_type=receipt, confidence=0.98, merchant=Meijer` |
| STEP 0 parse | `executor_run parse_and_categorize.py --json --engine=gemini` | Gemini hit 429 quota, **fell back to chatgpt-oauth** automatically, parsed 2 line items, total 11.92 (later corrected, see below) |
| STEP 2a | `check_vendor_key` | `vendor_key=meijer, recognized=true` |
| STEP 2b | `check_duplicates` (date 2025-03-11, amount 11.92) | `is_exact_duplicate=false, in_database=false` |
| STEP 3 | `executor_run categorizer_main.py --provider=auto` | `vendor_key=meijer, category_id=3` |
| STEP 4 | `executor_run parse_and_categorize.py --save --category-id=3` | Gemini 429 again → chatgpt-oauth fallback; **re-OCR corrected total to 22.89** (subtotal 29.16 − discount 6.27); `success=true, expense_id=1540, duplicate=false` |
| STEP 4B | `itemize_existing_expense` | Correctly refused: `itemizable=false, itemized=false` — line items (8.49+20.67=29.16) don't reconcile cent-exactly to the stored charge (22.89); left STANDALONE |
| STEP 5 | `record_trace` (task_name=document-intake) | All fields correct; `problems` array **explicitly documented** the pre-save vs save-time total discrepancy (11.92 → 22.89) and the itemization refusal reason |
| STEP 6 | `judge_trace` | **PASS** — "all deterministic intake checks passed" |
| STEP 7 | n/a | Not required (verdict PASS) |
| STEP 8 | `curl POST /api/expense-stored` | `{"ok": true}` |

## Notable but non-blocking observation

The amount used for the STEP 2b duplicate check (11.92, from the `--json` preview parse) differed from the amount actually stored at STEP 4 (22.89, from a second independent OCR pass during `--save`). Per the dispatch contract, the store path's own internal final duplicate guard is authoritative ("the save path performs a final duplicate guard using its final parsed/overridden date, amount, and merchant... treat the existing expense as the result") — and that guard reported `duplicate:false` at the corrected amount, so no duplicate slipped through. Mazda also proactively logged the discrepancy in `problems`, and appropriately let the itemization check fail closed rather than force a reconciliation. No coaching was needed.

## Wrapper defect

None found. No corrective message was sent to Mazda.

## Summary

Clean PASS. Mazda correctly self-classified the unknown-facade JPEG as a Meijer receipt, transparently navigated two Gemini 429 quota failures via the existing chatgpt-oauth fallback, caught and reported an OCR total-amount discrepancy between the preview and save parses instead of silently using the wrong figure, correctly refused itemization when the line items didn't reconcile to the final charge, and completed record_trace/judge_trace/dashboard-notify in full. No wrapper changes or coaching required.
