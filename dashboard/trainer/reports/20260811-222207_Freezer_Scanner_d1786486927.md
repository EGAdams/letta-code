Verdict: PASS

## Run
- Scanner: Freezer Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786486923001092093_3a9a921df7c0.jpg`
- Dispatched: 2026-08-11T22:22:07Z
- Mazda conversation: `conv-af44b54f-d07c-407d-9aa3-99466bbcc332`
- Facade: `doc_kind=unknown, confidence=0.0` (expected for JPEG scans) → routed to STEP 0 self-classification.

## Document identity
Consumers Energy utility bill, Amount Due $244.69, due 2025-04-03, with a "Thank You — we received your last payment of $217.75 on Feb 28, 2025" line. Gemini vision auth failed (401) at both classification and parse stages; OpenAI Codex/chatgpt-oauth fallback completed both. Classifier correctly routed this as **receipt** (paid-invoice precedence: the "last payment received" line proves this specific $244.69 balance is the current unpaid one, single line item, no marked-item selection present) rather than invoice.

## Checklist (all with successful tool returns, all timestamps after dispatch)

1. `load_wrapper_revision` — 22:22:13 ✓ (wrap-v004)
2. STEP 0 classify (`classify_scan.py` via `executor_run`) — 22:22:39 ✓ → `doc_kind=receipt, confidence=0.99`
3. Parse (`parse_and_categorize.py --json`) — 22:23:11 ✓ → single line item, total $244.69, `expense_scope` effectively full_receipt (one selected line, no marks)
4. `check_vendor_key` — 22:23:16 ✓ → normalized to `consumers_7996`, adopted
5. `check_duplicates` — 22:23:24 **tool error** (`Access denied for user 'adamsl'@'localhost'`, no password). Per wrapper Rule 3 (proposal 41), Mazda immediately ran an authenticated equivalent duplicate query via `executor_run` (22:23:35, credentialed `pymysql.connect`) → `[]`, no existing expense for that date/amount. This is the documented fallback path, correctly applied — not a defect.
6. `categorizer_main.py` via `executor_run` — 22:23:44 ✓ → `category_id=3` (Personal)
7. `check_category` — 22:23:48 ✓ → confirms category_id 3 matches vendor's expected category
8. Store (`parse_and_categorize.py --save --category-id=3`) — 22:24:02 ✓ → `{"ok": true}`, expense_id 2017
9. Dashboard callback `POST /api/expense-stored` — 22:24:15 ✓, confirmed present in `/api/expense-stored-events` with matching `conversation_id`/`dispatched_at`
10. `record_trace` (task_name=`document-intake`) — 22:24:33 ✓, trace_id 105, evidence JSON matches reality (stored=true, expense_id=2017, vendor_key_recognized=true, category_id=3, parse_artifact_verified=true)
11. `judge_trace(105)` — 22:24:37 ✓ → **PASS**, "all deterministic intake checks passed"

Single line item ("Amount Due") — itemization correctly not attempted (nothing to itemize).

## Supporting-document / dialog-visibility check (Trainer's own verification, independent of judge)
- Expense 2017's only applicable evidence field is `receipt_url` (a scanned receipt, not a statement/ledger) — correctly the only one populated; `document_url`/`scanned_statement_url`/`moms_ledger` untouched, as Mazda's own evidence noted.
- `GET /supporting-document/2017/receipt` → HTTP 200, and the served bytes **differ** from the raw archived file on disk — confirms the red-box annotator ran and produced a highlighted copy, not a pass-through of the unannotated original. Dialog visibility contract satisfied.

## Wrapper defect
None. The one tool-level hiccup (`check_duplicates` DB auth error) was already anticipated and handled by an existing wrapper rule (Rule 3), executed correctly and without any coaching needed.

## Lesson sent to Mazda
None — no coaching required this run.

## For a human
Nothing outstanding. Gemini vision auth (401) is still down for both classify and parse stages on this run; the Codex/chatgpt-oauth fallback is absorbing it without operator impact, consistent with prior reports — no new escalation needed.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
