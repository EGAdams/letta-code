# Mazda Trainer Report — Freezer Scanner

**Dispatch:** 2026-08-11T22:31:44Z
**Conversation:** conv-9f9e2e1f-be4e-4271-b6d1-2b5b981e6dd5
**Trace ID:** 107
**Document:** `scan_freezer_1786487501093579358_68bd6761aef4.jpg` — dental explanation of benefits / claim summary (merchant "DR DANIEL ROBERTS PLLC"), `doc_type=other`, classifier confidence 0.97.

Verdict: CORRECTED

## What happened

Facade returned `doc_kind=unknown`, routing Mazda to STEP 0 vision classification. `classify_scan.py` (Gemini failed 401 → OpenAI Codex CLI fallback) correctly identified the document as `doc_type=other` (a dental EOB — not a receipt, invoice, bank statement, tax form, or Mom's ledger), which per the contract is explicitly unsupported and out of scope for intake.

Mazda handled this exactly right:
- `record_trace` (trace 107) with `stored:false`, `is_duplicate:false`, `vendor_key:null`, `category_id:null`, `duplicate_checked:false`, and a `problems` entry stating the document is unsupported. No vendor/category/duplicate/store calls were attempted — correct, since none apply to an out-of-scope document.
- `judge_trace(107)` — but the judge returned **FAIL** with `failure_type=missed_vendor_key`, reasons `failed: vendor_key_resolved / category_resolved / duplicate_check_ran / expense_stored`.
- `propose_improvement` — correctly called on FAIL.
- `apply_proposal(57, ...)` — returned `"Proposal 57 is already satisfied by wrap-v004; no duplicate rule or wrapper revision was created."` (no-op).
- `curl POST /api/expense-stored` — dashboard callback sent with `outcome:EVIDENCE_ATTACHED`, `stored:0`, `parsed:0`, `doc_kind:other`. Callback returned `{"ok": true}`.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| load_wrapper_revision | tool_return `wrap-v004` | OK |
| STEP 0 vision classify | `classify_scan.py` → `doc_type=other`, confidence 0.97 | OK, correctly out of scope |
| Investigate/categorize/store | correctly skipped — not applicable to `doc_type=other` | OK |
| record_trace | trace 107 saved, task_name=`document-intake`, accurate fields | OK |
| judge_trace | verdict FAIL, wrong reasons (see below) | **judge defect, not Mazda's fault** |
| propose_improvement | filed on FAIL, correct trigger | OK |
| dashboard notify | `/api/expense-stored` → `{"ok":true}` | OK |

## The real defect — do not coach Mazda

This is **not a wrapper-prose gap**. Mazda's active wrapper already contains, verbatim, from a **prior run five days ago (2026-08-06, proposal 40)**:

> **Rule 2 (proposal 40, missed_vendor_key, 2026-08-06)**
> When classify_scan.py returns doc_type=other with a clear unsupported-document reason, record the unsupported result and ensure the intake rubric does not require vendor/category/duplicate/storage checks.

Mazda followed Rule 2 correctly this run. The judge still failed the trace on exactly the fields Rule 2 says shouldn't be required. Her fresh `propose_improvement`/`apply_proposal` this run (proposal 57) came back as a no-op ("already satisfied by wrap-v004") — because the fix that's actually needed lives in the **deterministic verdict-judge/evaluator code** (the rule-based rubric in the self-improvement framework's evaluation layer — `IEvaluationFactory`/`verdict_judge`), not in more wrapper instruction text. Wrapper prose cannot patch code-level rubric logic; that's the design boundary the whole framework is built on (Ch. 3–5 of the manual). This is a **recurring application defect** — the same failure class was supposedly proposed-away on 2026-08-06 and has now resurfaced identically.

No coaching message was sent to Mazda — there is nothing for her to do differently; she executed the contract exactly as written.

## For a human / dashboard

None beyond the escalation below — the dashboard callback succeeded and no expense should have been (and was not) created for this out-of-scope document.

## Escalation
repo_path: /home/adamsl/rol_finances (self-improving-agent evaluation layer, likely `tools/self_improving_agent/implementations/evaluation/` or wherever the intake `verdict_judge`/rubric checks `vendor_key_resolved`/`category_resolved`/`duplicate_check_ran`/`expense_stored`)
bug_description: |
  The deterministic intake judge (verdict_judge for task_name="document-intake") unconditionally
  requires vendor_key_resolved, category_resolved, duplicate_check_ran, and expense_stored to
  pass, even when the trace's agent_output carries doc_kind="other" (an explicitly unsupported
  document type per the intake contract). Mazda's wrapper already documents this exemption
  (Rule 2, proposal 40, added 2026-08-06) and she complies with it correctly (record_trace with
  stored:false, duplicate_checked:false, vendor_key:null, and a "Unsupported document" problems
  entry) — but the judge still returns FAIL with reasons "failed: vendor_key_resolved / failed:
  category_resolved / failed: duplicate_check_ran / failed: expense_stored" (trace_id 107, this
  run, 2026-08-11T22:32:50Z).
  A fresh propose_improvement/apply_proposal cycle this run (proposal 57) was a no-op — apply_proposal
  returned "Proposal 57 is already satisfied by wrap-v004; no duplicate rule or wrapper revision was
  created" — because the wrapper text Mazda would add already exists verbatim. The rubric that
  actually needs the doc_kind=="other" exemption is implemented in code (the rule-based evaluator/
  verdict_judge), not in wrapper prose, so no amount of proposal/apply cycling on the wrapper can
  fix it. This is a recurring defect: the same failure_type (missed_vendor_key) on the same
  doc_kind=="other" condition was already proposed against on 2026-08-06 (proposal 40) and has now
  resurfaced identically five days later, suggesting either that proposal 40's fix was itself only
  ever a wrapper-text patch, or the evaluator ignores doc_kind entirely.
  Fix: add a doc_kind=="other" (or more generally, "unsupported document") short-circuit to the
  intake verdict_judge/rubric so it does not check vendor_key_resolved/category_resolved/
  duplicate_check_ran/expense_stored on trace records where the agent_output reports the document
  as out of scope, and treats a `stored:false, problems:["Unsupported document..."]` record as
  PASS (or a distinct NOT_APPLICABLE/CORRECTLY_REJECTED verdict) rather than FAIL.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786487501093579358_68bd6761aef4.jpg
  failing_command: judge_trace(trace_id=107)
  trace_id: 107
  proposal_ids: [40 (2026-08-06, original), 57 (2026-08-11, this run, no-op)]

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
