# Trainer Report — Freezer Scanner intake

- **Document/scanner**: Freezer Scanner, `scan_freezer.jpg` → classified as receipt, merchant MR BURGER RESTAURANT
- **Dispatch time**: 2026-07-16T12:51:22Z
- **Run window observed**: 2026-07-16T12:51:33Z – 12:54:06Z (~2.5 min, clean single pass, no stall)
- **Verdict: PASS** (trace_id=71, judge_trace verdict PASS, failure_type=none)

## Step-by-step checklist

| Step | Contract requirement | Evidence | Result |
|---|---|---|---|
| STEP 1 | `load_wrapper_revision(agent_name="Mazda")` first | **not called** — first tool call was `executor_run classify_scan.py` at 12:51:33 | **SKIPPED** |
| STEP 0 | Classify + parse (facade returned `doc_kind=unknown`) | `classify_scan.py` → receipt, MR BURGER RESTAURANT, conf 0.98; `parse_and_categorize.py --json` → date 2025-09-25, amount 39.15 | OK |
| STEP 2a | `check_vendor_key` | called with raw vendor_key, tool normalized to `mr_burger`, adopted downstream | OK |
| STEP 2b | `check_duplicates` | returned `is_exact_duplicate:false`, `in_database:false` — no error this time (contrast with trace 69's SQL error on the previous run) | OK |
| STEP 3 | Categorize | `categorizer_main.py` → `category_id: 137` (non-null) | OK |
| STEP 4 | Store | `parse_and_categorize.py --save --category-id=137` → `expense_id: 1503` success | OK |
| STEP 5 | `record_trace`, `task_name="document-intake"` | evidence JSON: doc_kind=receipt, vendor_key=mr_burger, category_id=137, duplicate_checked=true, is_duplicate=false, stored=true, expense_id=1503, problems=[] — matches reality | OK |
| STEP 6 | `judge_trace` always | trace_id=71 → PASS | OK |
| STEP 7 | `propose_improvement` on FAIL only | not applicable (PASS) | N/A |
| STEP 8 | Dashboard notify | `POST /api/expense-stored` → `{"ok":true}`; confirmed present in `/api/expense-stored-events?since=1784206282` with `expense_id:1503, stored:1` | OK |

## Wrapper defect diagnosed

Mazda skipped **STEP 1** (`load_wrapper_revision`) entirely — she went straight from the
dispatch message to STEP 0 classification. This run happened to PASS because no rule in
the active wrapper (`wrap-v005`) actually applied to a plain receipt, but the omission
means she never read her own accumulated learned rules before acting. That is a real
process gap: a future document that *does* trip a learned rule (e.g. a statement-branch
rule, or a stale rule like the now-outdated "receipt storage blocked" note from wrap-v005)
could be silently mishandled because she never looked.

Root cause is likely instruction-salience: the dispatch message lists STEP 1 before STEP 0,
but STEP 0 is the block most directly answering "the facade said unknown, go classify it,"
which may pull attention first. No tool-level guard currently forces `load_wrapper_revision`
before the other intake tools are callable.

## Lesson sent to Mazda

Sent as a corrective Letta message referencing trace_id=71: PASS overall, but STEP 1 was
skipped; instructed her to always call `load_wrapper_revision(agent_name="Mazda")` as her
first tool call on every intake run, before any classification/parsing, with no exceptions,
and to record this in her memory. She acknowledged at 12:58:07Z: *"Recorded. Going forward,
STEP 1 `load_wrapper_revision(agent_name="Mazda")` will be my first tool call on every
intake run, before any classification or parsing."* (via `propose_memory_note` to
`system/persona`).

## For a human to look at

- Note: `propose_memory_note` calls in past runs have shown up with `"applied": false ...
  "no live-memory applier wired; note recorded as an approved proposal only"` — worth
  confirming whether this run's note actually persists to Mazda's live memory block, or
  whether (as seen in trace 69's aftermath) the STEP-1 lesson will again silently fail to
  take effect and need re-teaching next scan.
- No data-integrity concerns: single receipt, no near-duplicate/OCR anomaly detected,
  amounts reconcile, extension matches content.

## Summary

Trace 71 (MR BURGER RESTAURANT receipt via the Freezer Scanner) judged **PASS** — classification, vendor-key normalization, categorization, storage (expense_id 1503), tracing, judging, and dashboard notification all completed correctly in a clean ~2.5-minute run. The one defect was Mazda skipping the mandatory STEP 1 `load_wrapper_revision` call before acting, which didn't affect this particular document but risks silently missing a future applicable learned rule; a corrective lesson was sent and acknowledged, with a flag for a human to verify the memory-note actually persists this time given past instances where the applier wasn't wired up.
