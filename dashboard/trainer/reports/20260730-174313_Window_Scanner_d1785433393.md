# Mazda Trainer Report — Window Scanner

- **Document**: `window_scan_1785433389930956991_0ddaebb16a55.jpg`
- **Dispatch**: 2026-07-30T17:43:13Z (unix 1785433393)
- **Conversation**: `conv-66a6154f-a202-432a-bfd1-3a29b6fbd360`
- **Facade result**: `doc_kind=unknown, confidence=0.0` → correctly routed to STEP 0 self-classification.
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

Total run time: ~2 minutes (17:43:17 → 17:45:21). No coaching required.

## Checklist

| Step | Evidence | Result |
|---|---|---|
| 1. `load_wrapper_revision` | 17:43:17, returned `wrap-v067` | ✅ |
| 2. STEP 0 self-classify (facade was `unknown`) | `classify_scan.py` → `doc_type=receipt, confidence=0.95`, merchant "Intercessors for America" (a paid-donation gift statement) | ✅ |
| 2b. Parse | `parse_and_categorize.py --json --engine=gemini` → date `2025-03-18`, amount `$30.50`, `beneficiary_hint="Rosemary Barnes"` | ✅ |
| 3. Investigate | `check_vendor_key` → `intercessors_for_america` recognized. `check_duplicates` → no exact dup, fuzzy dup: existing expense **1547** (`check_11051_03_18_25_30_50`), same date/amount | ✅ |
| 4. Categorize | `categorizer_main.py` (gemini) → `category_id=190` (Gifts & Love Offerings) — matches the existing row's category | ✅ |
| 5. Store | **Skipped correctly** — duplicate detected, per contract "skip store, still trace + judge." `stored=false`, `expense_id=null` | ✅ |
| 6. `record_trace` | `task_name="document-intake"`, all required fields present (`vendor_key`, `vendor_key_recognized`, `category_id`, `duplicate_checked`, `is_duplicate`, `stored`, `expense_id`, `problems`) — trace_id 311 | ✅ |
| 7. `judge_trace` | `PASS`, `failure_type=none`, "all deterministic intake checks passed" | ✅ |
| 8. `propose_improvement` | N/A — verdict was PASS | ✅ (correctly skipped) |
| 9. Dashboard notify | `POST /api/expense-stored`, confirmed landed via `/api/expense-stored-events` with matching `conversation_id` + `dispatched_at` | ✅ |

Beneficiary hint (`Rosemary Barnes`) was passed through unmodified per Rule 18 (Robert/Rosemary shared-vendor guard) — vendor `intercessors_for_america` isn't on that shared-vendor list anyway, so it's moot here, but the pass-through was correct regardless.

## Supporting-document handling (observation, not a failure)

Expense 1547 already carries a non-empty `receipt_url` and `document_url`. Mazda correctly identified that attaching this new scan would be a same-type `receipt_url` conflict and **preserved the existing reference** — no overwrite, no second expense created, matching the manual's fail-closed requirement.

Two things worth a human look, not urgent:

1. The dashboard callback sent `"outcome":"EVIDENCE_ATTACHED"`, but nothing was actually attached — the dispatch template defines that value specifically for "a scan only fortified existing rows," and says to omit the key otherwise. Here the correct action was `omit`. Minor mislabeling; could make the Recent Report page describe this run inaccurately.
2. Mazda reasoned about the same-type conflict entirely in prose (recorded faithfully in the trace's `problems` field) rather than through an actual `SupportingDocumentService`-backed attach attempt that would log the conflict via the manual's "review-note/log route." She has no directly exposed supporting-document tool — only `executor_run` — so it's unclear whether a CLI path exists for her to invoke that service at all. No harm resulted here (the rescan added no new information), but if a future rescan legitimately carries better evidence than what's on file, this same self-reasoning shortcut would leave it undiscoverable to EG. Worth a wrapper/tooling review, not a retroactive fix to this run.

I did not coach Mazda or ask her to redo anything — every mandatory contract item for a duplicate-receipt run was satisfied, judge_trace's PASS was correct, and forcing action on an ambiguous edge case not covered by the dispatch's own `outcome` enum would be manufacturing work rather than correcting a real defect.

## Spot-check

Confirmed `GET /supporting-document/1547/receipt` returns a real image (200, `image/jpeg`, 573KB) — the View Receipt action for the matched duplicate still resolves correctly.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
