# Mazda Trainer Report — Freezer Scanner

- **Document:** `/tmp/fake.jpg` (Freezer Scanner)
- **Dispatch time:** 1785009406 (2026-07-25T19:56:46Z)
- **Mazda conversation ID given:** `conv-debug-test`
- **Verdict: STALLED (infrastructure defect, not a Mazda wrapper defect)**

## What happened

The very first step — reading Mazda's transcript for this run — failed permanently:

```
curl -s "http://100.80.49.10:8283/v1/conversations/conv-debug-test/messages?limit=60"
```

returned a schema-validation error, not empty results or a timeout:

```
"msg": "String should match pattern '^(default|conv-[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|agent-...)$'",
"input": "conv-debug-test"
```

`conv-debug-test` is not a valid Letta conversation id (it isn't a UUID-shaped
`conv-########-####-4###-[89ab]###-############` string), so the server rejects the
request outright — this is not a transient error that a retry or a 15-minute poll
window would resolve.

As a secondary check, I looked at Mazda's default agent transcript
(`/v1/agents/agent-6b536cf4-.../messages`) to see if any run happened to line up. The
most recent completed run there (`conv-bf874a8f-08ec-494c-81c6-c0b8af708cce`, trace_id
196, judge verdict `NEEDS_REVIEW`, a Burlington receipt duplicate) finished at
**19:35:00Z — before** this dispatch's timestamp of **19:56:46Z**. It is unrelated to
this dispatch and must not be credited to it (per the "same filename ≠ same document"
and conversation-id-must-match rules in the Trainer instructions).

`GET /api/expense-stored-events?since=1785009406` also returned `[]` — no dashboard
callback has landed for this dispatch.

The **deterministic facade result** supplied for this run was `{}` — not even a
`doc_kind` key — which is itself inconsistent with a real scan dispatch (every real
scan facade result carries at least `ok`/`doc_kind`/`confidence`). Combined with the
placeholder image path `/tmp/fake.jpg` and the non-UUID conversation id, this dispatch
appears to be synthetic/test data rather than a genuine scanner run.

## Verdict rationale

There is no evidence Mazda ever received this dispatch, because there is no valid
conversation to have received it in. This cannot be diagnosed as a Mazda wrapper
defect (prompt, tool, memory) — the defect, if any, is upstream of Mazda, in whatever
generated this dispatch's conversation id / facade payload.

## Action taken

None. Per the Trainer's write-only-to-Mazda's-conversation rule, I did not send a
corrective message anywhere, since there is no valid conversation id to send one to,
and I did not touch any finance tooling or the database.

## What a human should look at

- Confirm whether this was an intentional test/debug dispatch (the id
  `conv-debug-test` and path `/tmp/fake.jpg` strongly suggest so). If so, no fix is
  needed — the Trainer correctly identified it as non-actionable.
- If this was meant to be a real dispatch, the bug is in whatever code path built
  `MAZDA_CONVERSATION_ID`/`document_path` for it — it must produce a real
  `conv-<uuid>` id and a real facade result before spawning a Trainer.
