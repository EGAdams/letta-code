# Mazda Trainer Report

- Scanner: Window Scanner
- Dispatch time: 2026-07-23T15:56:36.595Z
- Document: statement, `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_1784822192990888128_e36611d6739f.jpg`
- Verdict: PASS

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded and loaded `wrap-v036`.
- Statement parsing ran and returned 7 parsed transactions, 3 skipped payments, Chase, last4 `5783`.
- Initial store run produced the wrong archive token: `chase_5783_january_04__january_28`.
- I coached Mazda to derive the archive range from expense/debit rows only and exclude payment/credit/refund/deposit dates from both endpoints.
- Mazda patched `tools/receipt_scanning_tools/statement_archive.py`, ran the targeted archive tests (`18 passed`), and reran `store_statement_transactions.py`.
- Corrected store output returned the expected archive path: `chase_5783_january_04__january_22`.
- Mazda ran `apply_statement_annotations.py` on duplicate IDs `1504,1505,1503,1506`; output showed `office` unchanged for `1503` and `1506`, and `Dental` unrecognized for `1504`.
- Mazda recorded trace `150` with `task_name="document-intake"` and refreshed `judge_trace(trace_id=150)`; verdict was `PASS`.
- Mazda sent the required dashboard callback to `/api/expense-stored`; response was `{"ok": true}`.

## Defect Diagnosed

The statement archive planner initially used the last payment date as the folder-range endpoint. The contract requires the range to be derived from expense/debit rows only, so the correct token for this Chase statement is `January_04__January_22`, not `January_04__January_28`.

## Lesson Sent

I instructed Mazda to:
- rerun the statement storage path with the corrected archive-range rule,
- refresh `record_trace(task_name="document-intake")` and `judge_trace`,
- record the lesson in memory,
- and send the dashboard callback with the duplicate-only evidence for this document.

## Human Follow-up

- The corrected archive path is now right, but the old incorrect archive copy still exists at `/home/adamsl/rol_finances/readable_documents/bank_statements/2025/january/chase_5783_january_04__january_28/chase_5783_january_04__january_28.jpg`.
- Mazda recorded that residual path in trace problems; a human may want to clean it up if the archive should contain only the corrected copy.
