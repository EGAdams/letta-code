# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatched at: `2026-07-16T17:02:09.000Z` (`1784221329`)
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: `FAIL`

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded at `17:02:16Z` and returned `wrap-v017`.
- Statement parse was attempted with `parse_statement_scan.py`, but the tool return failed with `ResourceExhausted: 429` quota exhaustion.
- `store_statement_transactions.py` was attempted with the parse failure payload and returned `ok: false`.
- `record_trace` succeeded with `task_name="document-intake"` and `trace_id=82`.
- `judge_trace(82)` succeeded and returned `FAIL` with `failure_type="unsupported_document"`.
- `propose_improvement(82, ...)` succeeded and filed proposal `64`.
- `apply_proposal(64)` succeeded and activated `wrap-v018`.
- Dashboard notify landed: `GET /api/expense-stored-events?since=1784221329` shows a statement event for this conversation with `parsed=0`, `stored=0`, and `document_path` matching this scan.

## Wrapper Defect

The statement flow depends on a single Gemini-backed parser path and does not recover from quota exhaustion. That is a wrapper problem, not a model problem: the intake instructions need a fallback or retry branch for statement parsing failures before storage is attempted.

## Lesson Sent

No additional coaching message was sent in this trainer pass because Mazda already completed the end-of-run loop and filed/activated the improvement herself.

## Human Review

- The run completed its trace/judge/callback obligations, but the statement intake itself did not complete because no transactions were parsed or stored.
- The judge labeled the failure `unsupported_document`, but the underlying cause in the transcript was quota exhaustion on `parse_statement_scan.py`.
- The activated wrapper revision is now `wrap-v018`; verify the next statement dispatch actually retries or falls back when the primary parser hits 429.
