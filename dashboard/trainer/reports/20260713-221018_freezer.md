# Mazda Trainer Report — Freezer Scanner

- Dispatch: `2026-07-13T22:06:22Z` (`1783980382`)
- Document: LEO GAS AND FOOD LLC receipt, 2025-01-21, receipt 023525, subtotal $7.98 + tax $0.48 = total $8.46
- Source: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: **FAIL**

## Contract evidence

- PASS — `load_wrapper_revision(agent_name="Mazda")` returned active `wrap-v002`.
- PASS — STEP 0 `classify_scan.py` returned receipt/confidence 1.0; `parse_and_categorize.py --json` returned LEO GAS AND FOOD LLC, 2025-01-21, $8.46. Image inspection confirms merchant, date, receipt number, and arithmetic.
- FAIL — `check_vendor_key(... vendor_key="leo_gas_and_food_llc")` falsely fuzzy-normalized the merchant to `harvest_health_foods_grand_rapids_mi`. The repository already maps exact `leo_gas` to category 374 (Fuel & Maintenance); Harvest maps to category 3 (Personal).
- PASS — `check_duplicates` returned no duplicate. No same-merchant/date near-amount artifact was found; saved file is a real JPEG.
- FAIL — categorizer returned category 3 from the false Harvest key.
- PARTIAL — store succeeded as expense 1500, but with wrong category 3. It also warned that `source_file` could not be updated. Recent Report confirms expense 1500 as LEO GAS, $8.46, 2025-01-21, Personal.
- PASS — trace 60 used exact task name `document-intake`, then `judge_trace(60)` returned PASS. Its evidence was inaccurate (`problems: []`) because the vendor/category conflict was missed.
- PASS — dashboard callback succeeded and the event/pointer confirm expense 1500.
- COACHED — exact `check_vendor_key(... vendor_key="leo_gas")` then returned recognized `leo_gas`. Mazda recorded trace 61 with the false normalization and wrong category in `problems`.
- FAIL — `judge_trace(61)` still returned PASS despite the explicit problem. No `propose_improvement` followed because the judge did not produce FAIL.

## Wrapper defects

1. Vendor-key fuzzy matching can substitute a semantically different known merchant even when an exact canonical key exists.
2. The intake judge ignores non-empty `problems` and does not verify that stored category matches the canonical vendor mapping.

## Lesson sent to Mazda

> Correction for this Freezer Scanner run: the receipt visibly says LEO GAS AND FOOD LLC, store 2836900, receipt 023525, 2025-01-21, subtotal 7.98 + tax 0.48 = total 8.46. check_vendor_key incorrectly fuzzy-normalized leo_gas_and_food_llc to harvest_health_foods_grand_rapids_mi even though vendor_category.yaml already contains the canonical leo_gas mapping; that caused expense 1500 to be stored as category 3 Personal instead of the Leo Gas Fuel & Maintenance mapping. The contract requires normalized keys, but a fuzzy normalization that changes the merchant identity is not trustworthy. For this document NOW: run check_vendor_key again with vendor_key="leo_gas" and description="LEO GAS AND FOOD LLC"; do not create a second expense. Record a fresh document-intake trace whose problems explicitly report the false fuzzy vendor normalization and wrong stored category (do not claim a clean intake), run judge_trace, and on FAIL call propose_improvement with the returned trace_id and a vendor-resolution/categorization failure_type. Apply/record a durable memory rule: when a fuzzy normalized vendor names a different merchant, reject it; search/use an exact canonical merchant key such as leo_gas, and never mark problems=[] while the stored category conflicts with that canonical mapping. Also flag expense 1500 for human correction rather than silently treating it as correct.

## Human follow-up

Correct expense 1500 from category 3 to category 374, verify its `source_file`, and repair the vendor matcher/judge guards. Do not create another expense.
