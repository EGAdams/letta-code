# Trainer Report — Window Scanner scan

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan.jpg`
- **Scanner**: Window Scanner
- **Facade result**: `ok:true, doc_kind:unknown, confidence:0`
- **Dispatch time**: 2026-07-10T18:58:22Z (unix 1783709902)
- **Verdict**: **STALLED**

## What I observed

Polled Mazda's transcript (`GET /v1/agents/agent-6b536cf4-ec88-4290-b595-fed21d14bd8e/messages`)
every ~2 minutes for the full 15-minute allotment (18:58:22 → 19:13:25 UTC). The most recent
message in her transcript at every single poll remained the same `assistant_message` timestamped
**2026-07-10T17:58:09Z** — over an hour before this dispatch was supposedly sent, and belonging to
an entirely different document (the earlier Freezer-scanner `scan_freezer.jpg` / Chase bank
statement incident, already closed out with `trace_id=51`, verdict FAIL, proposals 36/37 filed).

**No `user_message` for this dispatch ever appeared in her transcript.** No
`load_wrapper_revision`, no `executor_run`, no `record_trace` for `scan.jpg` — nothing. I also
checked `GET /api/expense-stored-events?since=1783709902` on the dashboard, which returned `[]`,
consistent with no store having happened.

## Checklist against the contract

| Step | Evidence | Status |
|---|---|---|
| `load_wrapper_revision` | none | ✗ never called |
| STEP 0 classify/parse (facade returned unknown) | none | ✗ never called |
| `check_vendor_key` / `check_duplicates` | none | ✗ never called |
| Categorize | none | ✗ never called |
| Store | none | ✗ never called |
| `record_trace` (task_name=document-intake) | none for this doc | ✗ never called |
| `judge_trace` | none for this doc | ✗ never called |
| `propose_improvement` | n/a (no FAIL trace for this doc) | ✗ never called |
| Dashboard notify | none | ✗ never called |

## Diagnosis

This is **not a Mazda wrapper defect** — there is zero evidence she ever received the dispatch.
Per the instructions, a stall where the agent never picked up the dispatch at all points at
infrastructure (Letta server delivery, or the dispatch-sending code path in `server.py`/
`_notify_mazda_of_scan`), not at Mazda's prompt/tools/memory. I sent no corrective message to
Mazda, since there is nothing in her transcript to correct — sending a message now would be
coaching a run that never started, not this one.

## For a human to check

- Confirm `_notify_mazda_of_scan` actually fired for this scan (check dashboard server logs
  around 18:58:22 UTC / `/tmp/dashboard_8765.log`) and that the POST to
  `$LETTA_BASE_URL/v1/agents/$MAZDA_AGENT_ID/messages` succeeded (not swallowed by the
  fire-and-forget dispatch).
- Confirm the Letta server (`100.80.49.10:8283`) was healthy/reachable at dispatch time — no
  sign of Mazda processing anything in this window.
- If the dispatch did fire successfully but the message simply hasn't rendered yet, re-poll
  `scan.jpg`'s trace later; as of this report it has not been picked up.
