# Mazda Trainer Report — Window Scanner

- **Document**: `scan_1784826738491047682_000ab25b2241.jpg` (Chase statement, per facade, confidence 0.96)
- **Dispatch**: 2026-07-23T17:12:22Z
- **Conversation**: `conv-1c74fb36-41ad-4f14-97d3-a8b17cc37139`
- **Verdict**: **PASS**

## Checklist (all steps confirmed via successful tool returns)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v037` loaded. ✓
2. STEP 0 skipped correctly — dispatch stated facade already classified `doc_kind=statement`
   (confidence 0.96); not re-classified. ✓
3. `parse_statement_scan.py --bank-name Chase --account-last4 5783` → returned 1 parsed row,
   but `unreadable_count: 1` — the single Microsoft*365 row came back with `date: null`. ✓ (ran
   exactly as literally specified in the dispatch)
4. `store_statement_transactions.py -f /tmp/mazda_statement.json ...` → **rejected** (returncode 2):
   `missing=["date"]`, `row_errors[0]` for the unreadable row, wrote a `needs_review_path` sidecar
   under `bank_statements/_needs_review/`. This is the contractually-correct fail-closed outcome
   for a genuinely unreadable row (manual §"One unreadable row rejects the whole statement"). ✓
5. `apply_statement_annotations.py --expense-ids ""` → `{"ok": true, "applied": [], "note": "no
   expense rows to annotate"}` — correct, since nothing was stored. ✓
6. `record_trace(task_name="document-intake", ...)` → evidence matches reality: `transactions_parsed:1,
   transactions_stored:0`, `problems` names the unreadable row, the needs_review_path, and the
   annotation no-op. ✓
7. `judge_trace(trace_id=157)` → **FAIL** (`reasons: ["failed: statement_transactions_stored"]`). ✓ called
8. `propose_improvement` (FAIL path) → proposal 113: the deterministic judge penalizes
   `transactions_stored=0` without distinguishing a genuine fail-closed quarantine (row_errors +
   needs_review_path present) from an actual storage defect. Well-scoped, matches manual's own
   "correct fail-closed rejection, never permission to guess" language. ✓
9. `apply_proposal(113, ...)` → all four gates (`safety/cost/regression/usefulness`) returned
   `allow`; activated as `wrap-v038`, snapshot 33 taken (rollback-safe). ✓
10. Dashboard notify — `curl POST /api/expense-stored` with `{expense_ids:[], duplicate_expense_ids:[],
    parsed:1, stored:0, doc_kind:"statement", vendor:"Chase", conversation_id, dispatched_at}` →
    `{"ok": true}`. ✓ (initially appeared to be skipped mid-poll; a later poll showed she completed
    it ~8s after `apply_proposal`, then ended her turn with a summary — nothing was actually missing)

## Wrapper defect diagnosed

**Judge miscalibration**, not a Mazda mistake: `judge_trace` was scoring
`statement_transactions_stored` as a hard pass/fail signal with no allowance for the documented
"correct fail-closed quarantine" outcome. Mazda's proposal 113 / wrap-v038 teaches the wrapper to
treat a `store_statement_transactions` rejection carrying `needs_review_path`/`row_errors` as a
correct outcome rather than a storage failure. This is exactly the intended self-improvement loop
(judge → propose → gate → activate) and needed no coaching from me.

## Something a human should look at (not a Mazda defect, no message sent)

The dispatch's deterministic facade (`statement_preflight`) had **already** parsed this same image
with a real date (`2025-09-15`) for the one Microsoft*365 transaction — but the dispatch message
built for Mazda never surfaces that preflight data, only the doc_kind/vendor/confidence. Mazda's
own `parse_statement_scan.py` re-derivation (a fresh Gemini vision call, per Rule 1 of her wrapper)
came back with `date: null`/unreadable on the same row, causing the quarantine. Mazda had no way to
notice or reconcile this discrepancy since she was never given the preflight's transaction data.
Worth checking on the `rol_finances` side whether `build_mazda_scan_message()` should pass along
`statement_preflight.transactions` so a disagreement between the deterministic OCR pass and the
vision reparse can be caught rather than silently trusting whichever ran last. Not fixable via a
message to Mazda — it's dispatch construction, outside her wrapper — so no corrective message was
sent and this did not affect the PASS verdict.

## Coaching sent

None. No defect in Mazda's execution.
