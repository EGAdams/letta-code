# Mazda Trainer Report

- **Document:** PDF intake — `diners_0587_year_2025.pdf` (BMO/Diners Club 2025 Annual Summary Statement, account 0587)
- **Path:** `/home/adamsl/rol_finances/readable_documents/bank_statements/january/diners_0587_whole_year_2025/diners_0587_year_2025.pdf`
- **Dispatch:** 2026-07-30T23:05:53Z (unix 1785452753)
- **Mazda conversation:** `conv-30814ee3-9279-46d5-87fd-6c2a679fc9f7`
**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

## What kind of document this was

Not a normal itemized monthly statement — a BMO Diners Club *Annual Summary Statement*: five
pages of month×category tally tables followed by a category-grouped "Transaction Detail"
section (Restaurants, Medical Expenses, Retail Purchases, etc., each with its own `Total:` line
and list of dated rows), plus a separate "Payments" category. `classify_scan.py`'s Gemini PDF
vision leg hit a `429` quota error and the ChatGPT/OpenAI vision fallbacks can't take PDF input,
so Mazda correctly fell back to `pdftotext` + manual reconstruction rather than stalling.

## Run 1 (trace 320) — FAIL, self-reported as success

- `load_wrapper_revision` ✅, wrapper `wrap-v069`.
- `classify_scan.py` ✅ ran, hit Gemini 429 as noted above — acceptable fail-closed-to-text-extraction.
- Built a parse artifact by hand (`/tmp/diners_0587_year_2025_parse.json`) after two
  executor quoting failures (`set -e` rejected, heredoc with embedded quotes rejected — both
  self-corrected via a `cat <<'EOF'` heredoc, no coaching needed there).
- `store_statement_transactions.py` returned `ok:true`, `transactions_parsed:48`,
  `skipped_credits:40`, **`stored:8`** — only the 13-row Medical Expenses category actually
  landed (as 8 expense rows; category resolution details unclear), leaving **~$1,337 of real
  charges (Restaurants, Retail Purchases, Other Services, Gas/Auto, Miscellaneous) silently
  dropped**, counted internally as "skipped credits" though they were real purchases.
- Archive path computed as `diners_club_0587_february_14__november_11` — that's the first/last
  **Payments** dates, not expense/debit dates, a direct violation of the archive full-range rule
  and a symptom of the same charge/payment mislabeling.
- **No `report.html` was generated** (contract requires this for every statement run).
- **`judge_trace` was never called** on trace 320 (required always, PASS or FAIL).
- **No `propose_improvement`** despite Mazda's own assistant summary flagging the 40-skip
  result as "suspicious."
- Sent `POST /api/expense-stored` reporting `stored:8` as if this were a normal, complete
  result — a false-positive callback to the dashboard.

Mazda's own final summary for this run *did* flag the anomaly in prose, which is good
self-awareness, but she stopped there instead of investigating or fixing it, and had already
told the dashboard the run succeeded.

## Coaching #1

Sent a corrective message identifying: (a) the ~39 real charge rows vs. 8 stored — a
data-loss defect, not a valid fail-closed outcome; (b) the archive range using payment dates
instead of expense dates; (c) the missing `report.html`; (d) the missing `judge_trace`; and
required her to rebuild the parse data, re-run storage, generate the report via the three
`verification_lib` scripts, record + judge a new trace, file `propose_improvement`, and
correct the dashboard callback.

## Run 2 (trace 322) — correctly re-graded as FAIL

- Used `run_claude_code_sdk` to have a Claude session re-read the PDF and reconstruct a clean
  transaction list: 39 charges, 1 true credit, 7 payments — matching the document's own
  category totals.
- Filed a memory note (`propose_memory_note`, proposal 224, gates passed) capturing the
  "annual summary" document-shape rule for future runs.
- Re-ran `store_statement_transactions.py` with the corrected data — **it reproduced the exact
  same defect**: `stored:0`, `duplicates:8`, `skipped_credits:40`. Read the tool source
  (`split_expenses_and_credits`) and confirmed the real root cause: when all amounts are
  encoded as positive (no mixed sign), the script falls back to a description regex
  (`payment|autopay|thank you|credit|deposit|refund|cr`) to decide expense vs. credit, and that
  path is misclassifying most real charge rows as credits for this statement shape — a genuine
  tool defect, not something Mazda's data could route around.
- Generated `report.html` at the correct path and ran `restructure_verified_transactions.py`
  (success) → `hydrate_report_categories_from_db.py` (correctly reported failure: 40 blank
  `data-expense-id` rows, since those rows don't exist in the DB) → `audit_statement_reports.py`
  (structural PASS).
- `record_trace` (trace 322) and `judge_trace(322)` → **verdict FAIL** (judge's
  `failure_type:"unsupported_document"` is a mislabel of the true cause, but the FAIL call
  itself and the honest evidence are correct).
- `propose_improvement` (proposal 226) with an accurate root-cause summary pointing at
  `store_statement_transactions.py`'s `split_expenses_and_credits`.
- Correctly refused to send a "corrected success" callback with fabricated `expense_ids`,
  since no corrected rows actually exist in the DB — good judgment.

## Coaching #2

The refusal to fabricate success was right, but she over-applied it to mean "send nothing,"
leaving the dashboard's last word on this document as the false `stored:8` success from run 1.
Sent a second, narrow correction: the contract requires a callback reflecting the *true* current
state always, including failures — silence lets a stale false-positive stand. Instructed her to
send one more `/api/expense-stored` POST honestly reporting `stored:0`,
`duplicate_expense_ids:[1733..1740]`, and a failure description referencing trace 322 /
proposal 226.

She complied immediately and correctly — final callback sent with `stored:0`,
`duplicate_expense_ids:[1733,1734,1735,1736,1737,1738,1739,1740]`, `parsed:48`, and a
description flagging the store defect and citing trace 322 / proposal 226.

## Wrapper defect diagnosed

`rol_finances/tools/receipt_scanning_tools/store_statement_transactions.py`,
`split_expenses_and_credits()`: when all parsed transaction amounts share the same sign
(all-positive, which is how a manually reconstructed "category `Total:` block" statement like
this BMO/Diners Club annual summary naturally comes out), the function falls back to a
description regex to separate credits from charges. That regex path is swallowing real charge
rows (Restaurants, Retail Purchases, Other Services, Gas/Auto, Miscellaneous) as
"skipped_credits" for this document shape, on both the original and the corrected reconstruction.
This is a tool-layer bug, confirmed reproducible, and is now tracked in improvement proposal 226
(trace 322) — not something fixable by better prompting alone. A human should look at
`split_expenses_and_credits`'s fallback heuristic before the next annual-summary-style statement
is processed.

## Human follow-up needed

1. **Fix `store_statement_transactions.py`'s `split_expenses_and_credits` fallback** — the
   description-regex path is unsound for all-positive-amount annual-summary reconstructions.
   Proposal 226 / trace 322 has the details.
2. **This document (diners_0587_year_2025.pdf / account 0587) is NOT actually stored** — the 8
   DB rows created in run 1 (ids 1733-1740) are stray/incorrect artifacts of the original bad
   run and were left as `duplicate_expense_ids` in the final callback rather than being deleted;
   a human should decide whether to delete/relabel them once the store script is fixed and this
   document can be reprocessed cleanly.
3. The report.html at
   `/home/adamsl/rol_finances/readable_documents/bank_statements/january/diners_0587_whole_year_2025/report.html`
   is a **failure report** (blank expense IDs on 40 rows) — do not treat its presence as
   evidence of a completed intake.

## Summary

This run FAILED due to a genuine data-loss defect in `store_statement_transactions.py` for
annual-summary-shaped statements (40 of 48 real transactions silently dropped as "credits").
Mazda's first attempt reported false success to the dashboard and skipped `judge_trace`,
`report.html`, and `propose_improvement` entirely. After coaching, she correctly diagnosed the
tool-layer root cause (not a prompting issue), reproduced it, generated the required report,
recorded and judged a FAIL trace, and filed an accurate improvement proposal. A second coaching
pass was needed to get her to send an honest failure callback rather than staying silent, which
she then did correctly. Net: FAIL verdict is accurate and durable; a human needs to patch the
store script before this statement can be reprocessed to completion.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
