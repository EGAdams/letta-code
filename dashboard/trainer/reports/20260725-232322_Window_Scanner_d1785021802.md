# Trainer Report — Window Scanner intake

- **Document:** `window_scan_1785021798325066756_3466c4c84a1b.jpg`
- **Scanner:** Window Scanner
- **Dispatch:** 2026-07-25T23:23:22Z (`1785021802`)
- **Mazda conversation:** `conv-52454e73-d55e-4af5-95a4-e621ccbd1697`
- **Trace ID:** 230
- **Verdict: CORRECTED (with an open infrastructure blocker)**

## What happened

1. `load_wrapper_revision` → wrap-v054. ✅
2. Facade returned `doc_kind=unknown` (expected for JPEG). Mazda ran STEP 0
   `classify_scan.py` via `executor_run` → `doc_type=moms_ledger`, confidence 0.93
   ("credit card statement heavily annotated with handwritten notes, circles, and
   markup"). Correct routing per Rule 24/28/29/32/37: Mom-ledger reconciliation
   branch, not receipt/statement storage. ✅
3. Mazda ran `moms_ledger_reconciler.py --image <scan>` via `executor_run`.
   - Attempt 1 (`timeout_sec=240`): executor HTTP 408, "Command timed out after 240s".
   - She sanity-checked with `ps -ef | grep moms_ledger_reconciler.py` → not running
     (returncode 1), correctly ruling out "still running in background."
   - Attempt 2 (`timeout_sec=480`): MCP error -32001, "Request timed out."
   - Both timeouts are a script/infrastructure problem, not a Mazda mistake — she
     used a sensible escalating-timeout retry and verified process state before
     giving up. ✅
4. `record_trace` (task_name=`document-intake`) with an accurate, fail-closed
   evidence JSON: `doc_kind=moms_ledger`, `stored=false`, `expense_id=null`,
   `matched_expense_ids=[]`, `duplicate_checked=false`, problems naming the
   reconciler timeout. Matches contract Rule 28 (moms_ledger evidence shape). ✅
5. `judge_trace(230)` → **FAIL** (`missed_duplicate`; ledger_rows_matched /
   ledger_categories_reconciled both failed). Correct — no rows were actually
   reconciled. ✅
6. `propose_improvement` (trace 230, `missed_duplicate`) → proposal 166,
   then `apply_proposal(166)` → activated **wrap-v055** (Rule 38: retry once
   with a bounded fallback on reconciler timeout). ✅ Learning loop closed correctly.
7. **Missing step:** she stopped here without the mandatory dashboard notify
   (`POST /api/expense-stored`), which the contract requires for every outcome
   including no-store/fail-closed runs.

## Coaching

Sent corrective message #1 (23:41:42Z) naming the missing callback step and
giving a concrete `curl` sketch. Mazda responded correctly in spirit — reloaded
the (now current) wrapper, attempted the callback via `executor_run`, and also
filed a durable memory-note proposal (168, activated) stating "a FAIL verdict
or applied proposal does not end an intake run — the dashboard callback is
still mandatory." Good, durable lesson, properly recorded. ✅

The callback attempt itself failed: `executor_run` returned `Not connected`.
`GET /api/server-health` showed `executor: up` at the time, so I sent
corrective message #2 telling her infra looked healthy and to retry
immediately. She retried at once and got the identical `Not connected` result
again.

## Open issue — infrastructure, not a Mazda defect

Two consecutive `executor_run` calls for a simple `curl POST` both failed with
`Not connected`, while the dashboard's own `/api/server-health` reported the
Executor Server as `up`. This suggests either:
- a stale/broken connection specific to this Letta conversation's executor
  binding (vs. the dashboard's own health probe path), or
- a brief real outage that outlasted the health-check's cache/poll interval.

I did not execute the callback myself (out of scope per Trainer rules — the
Trainer must never call `/api/expense-stored`). Mazda's trace/judge/proposal
work for this document is complete and correct; only the final dashboard
notification is unconfirmed. **A human should check whether
`/api/expense-stored` for document
`window_scan_1785021798325066756_3466c4c84a1b.jpg` (trace 230, moms_ledger,
stored=0) ever landed**, and if not, investigate the executor connectivity
Mazda's tool actually uses (as distinct from the dashboard's health-check
path).

## Wrapper changes this run produced

- wrap-v055 (proposal 166): moms_ledger reconciler timeout → retry once with a
  bounded fallback before judging missing matches.
- wrap-v056 / memory proposal 168 (activated): dashboard callback is mandatory
  even after a FAIL verdict and an applied proposal.

Both are sound, durable wrapper improvements and should help future moms_ledger
timeout cases self-correct without Trainer intervention.
