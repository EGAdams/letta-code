# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

## Run details

- **Scanner:** Freezer Scanner
- **Scanned image:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1787601669066129970_33d8503a4b75.jpg`
- **Dispatch timestamp:** 1787601672 (2026-08-24T20:01:12Z)
- **Mazda conversation:** `conv-20012966-76fb-4051-98bd-09e01f5202ed`
- **Deterministic facade result:** `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` — this run required STEP 0 (Mazda self-classifies via `classify_scan.py`).

## What was observed

Polled `GET /v1/conversations/conv-20012966-76fb-4051-98bd-09e01f5202ed/messages?limit=60`
roughly every 30 seconds from dispatch time through **905 seconds (~15.1 minutes)** elapsed.
At every poll the conversation contained exactly **one message**: the `system_message`
dispatch itself, timestamped `2026-08-24T20:01:12Z`.

`GET /v1/conversations/conv-20012966-76fb-4051-98bd-09e01f5202ed` confirms this directly:

```
created_at:      2026-08-24T20:01:12.577213Z
updated_at:      2026-08-24T20:01:13.028887Z
last_message_at: 2026-08-24T20:01:13.010868Z
in_context_message_ids: ["message-124f7440-cae1-4751-b556-3be4bae125e7"]  (just the dispatch)
```

The conversation was never touched again after the dispatch was written (`updated_at` is
~0.4s after `created_at`). There is no evidence Mazda ever picked up this dispatch:
no `load_wrapper_revision`, no `classify_scan.py` call, no tool call of any kind.

## Checklist against the intake contract

Every required step is **absent** because Mazda never began the run:

1. `load_wrapper_revision(agent_name="Mazda")` — not observed
2. STEP 0 classification (`classify_scan.py`) — not observed
3. Investigate (`check_vendor_key`, `check_duplicates`) — not observed
4. Categorize — not observed
5. Store/file — not observed
6. `record_trace` — not observed
7. `judge_trace` — not observed
8. `propose_improvement` — n/a, no FAIL trace exists to propose against
9. Dashboard notify (`/api/expense-stored`) — not observed

## Wrapper defect diagnosis

**None to diagnose against Mazda's wrapper.** This is not a case of a missed step, a
malformed tool argument, or a misread document — there is zero transcript evidence she
ever began processing. Per the Trainer instructions, "if she stalled entirely and never
picked up the dispatch, note that as an infrastructure problem (Letta server / executor),
not a Mazda lesson." No corrective message was sent to Mazda's conversation, since there
is nothing in her behavior to coach — the dispatch itself appears not to have been
delivered as an actionable turn, or the agent's step loop never fired.

## For a human

- The dispatch message landed in the conversation (it's `in_context_message_ids[0]`) but
  produced no subsequent agent turn — worth checking whether the Letta server or the
  process responsible for triggering Mazda's step on new-message events is healthy, and
  whether this conversation is stuck rather than merely slow.
- The freezer scan `scan_freezer_1787601669066129970_33d8503a4b75.jpg` has not been
  processed at all as of this report. It should be redispatched (or this run
  investigated and redispatched) once the underlying trigger/infrastructure issue is
  understood.
- No expense was stored, no trace was recorded, and no dashboard callback fired for this
  document — there is nothing to verify further because nothing happened.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
