# Mazda Trainer Report — Window Scanner

**Verdict: PASS**

- Scanner: Window Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785695190038684750_a3ab7c9e4045.jpg`
- Dispatch: 2026-08-02T18:26:33Z
- Mazda conversation: `conv-09326dcf-606c-48fa-a713-e9e674d810c8`
- Run duration: ~2m10s (18:26:33 → 18:28:43 UTC) — clean, no coaching needed

## Document identity

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), so Mazda correctly
routed to STEP 0 self-classification. `classify_scan.py` hit a Gemini 429 quota error and
fell back to the OpenAI Codex CLI fallback path, returning `doc_type=receipt, confidence=0.99,
merchant=Priority Health`, reason: "Invoice is already paid: Account Summary shows
'02/27/2025 Payment BGO-ACH ($55.00)' and 'Outstanding balance as of 03/12/2025: $0.00'."
Correctly routed a visibly-paid invoice as a receipt per the manual's rule.

## Checklist (all steps confirmed via successful tool returns)

1. `load_wrapper_revision` → `wrap-v071` ✓
2. STEP 0 `classify_scan.py` (executor_run) → receipt, 0.99 confidence ✓ (Gemini→Codex-CLI fallback worked)
3. `parse_and_categorize.py --json` → single line item "Current invoice amount" $55.00,
   `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` — correct scope for a
   single-line paid invoice, no marked-item or itemization signal present.
4. `check_vendor_key("priority_health")` → normalized to `priority_health_9392`; Mazda adopted
   the normalized key for all subsequent calls ✓
5. `check_duplicates` → exact duplicate, `exact_duplicate_expense_id=1988` ✓
6. Categorizer (`categorizer_main.py --provider=auto` via executor_run) → `category_id=243`.
   Correctly passed the parsed `beneficiary_hint="BARNES, ROSEMARY L"` through unchanged per
   Rule 18, which redirected this dual-owner vendor (Priority Health) to Rosemary's category
   (243) instead of defaulting to Robert's. ✓
7. `parse_and_categorize.py --save --category-id=243` (duplicate-safe save, no
   `--allow-duplicate`) → `success:true, duplicate:true, idempotent:true, expense_id:1988,
   parse_artifact_verified:true` — filed/confirmed receipt evidence on the existing expense
   without creating a second row. ✓
8. `record_trace` (`task_name="document-intake"`) → evidence correctly shows `stored:false,
   expense_id:null, is_duplicate:true`, with the canonical existing id (1988) named in
   `problems`. ✓
9. `judge_trace` → **PASS**, `trace_id=348`, "all deterministic intake checks passed" ✓
10. Dashboard callback (`POST /api/expense-stored`) → `duplicate_expense_ids:[1988],
    parsed:1, stored:0, doc_kind:receipt` ✓

## Red-box / dialog-visibility check (independent verification)

Fetched `GET /supporting-document/1988/receipt` → HTTP 200, 587KB. Cross-checked the live
dashboard log (`/tmp/dashboard_8765.log`) for the `[supporting-document] opened without
highlight` warning that `document_annotation.py` emits on an unannotated open — **no such
warning appears for expense_id=1988** (it does appear for several *other* expense ids in the
same log window, confirming the log is working and would have caught this). This confirms the
served receipt is the annotated/boxed copy, not a raw pass-through — the dialog-visibility
contract is satisfied for this expense.

(Note: `GET /api/supporting-documents?expense_id=1988` returned 404 and the batch
`POST /api/supporting-documents` returned `"No matching expense in DB"` for my own ad-hoc
probe — that's a mismatch in *my* verification call, not evidence of a defect; the direct
`/supporting-document/1988/receipt` fetch plus the absence of a highlight warning is the
authoritative check per the contract, and it passed.)

## Wrapper defect

None. No coaching message was sent — the run was correct on the first pass.

## For a human

Nothing to flag. Clean duplicate-detected run: Priority Health receipt already stored as
expense 1988 (Rosemary's insurance), Mazda correctly re-confirmed/attached the new scan's
receipt evidence without creating a duplicate expense, and the red-box annotation renders
correctly on that receipt.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
