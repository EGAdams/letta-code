# Trainer Report — Window Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1787709579280258152_528d8448ba95.jpg`
**Dispatch:** 2026-08-26T01:59:42Z (unix 1787709582)
**Mazda conversation:** conv-242abaff-2118-42cb-b190-8c4edfbd428a
**Run window observed:** 01:59:42Z → 02:01:34Z (all inside 15-min deadline)

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## What the document actually is
A Priority Health "Account Summary" billing page: prior ACH payments of **$80.00** (2025-04-17) and
**$85.00** (2025-05-09), a current invoice amount of **$79.30**, and a printed
**Total amount due: ($7.10) — "Information Only, Do Not Pay."** This is a credit-balance
informational summary, not a payable purchase.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 01:59:47 return `wrap-v081`, `is_active:true` | ✅ |
| Facade `doc_kind=unknown` → STEP 0 required | dispatch facade confirms | ✅ triggered |
| `classify_scan.py` | 02:00:14 return: `doc_type=receipt`, `confidence=0.99`, reason cites the negative total + "Do Not Pay" + two prior payments | ✅ successful tool return |
| `parse_and_categorize.py --json` | 02:00:46 return, `returncode:0`. Gemini exhausted quota on all 3 models, **fell back to `chatgpt-oauth` (gpt-5.4)** and returned a full valid parse: `total_amount:-7.1`, 3 line items (both ACH payments + current invoice, all `selected_for_expense:true`), `expense_scope:full_receipt`, `handwritten_arithmetic_total:null` | ✅ genuine parse artifact, not hallucinated |
| Negative-total fail-closed stop | `record_trace` evidence: `vendor_key:null`, `category_id:null`, `duplicate_checked:false`, `stored:false`, `expense_id:null`, `problems` cites the exact -$7.10/Information-Only blocker | ✅ **matches Mazda's own already-active wrapper rules** — wrap-v081's loaded instructions already contain Rule 55 (proposal 236, 2026-08-13: *"receipt parse with a negative total... stop before storage... never attempt to save a non-positive expense"*), Rule 58 and Rule 63 (proposals 246/257: *"credit balance rather than an amount due... stored=false as a correct fail-closed outcome"*). Skipping vendor/duplicate/categorize/save here is the **documented correct behavior**, not a missed step. |
| `record_trace` | 02:00:59, `task_name:"document-intake"`, idempotent:false, trace_id 483 | ✅ |
| `judge_trace` | 02:01:05 → **FAIL**, `reasons:["failed: vendor_key_resolved","failed: duplicate_check_ran"]` | See below — judge-rubric defect, not a Mazda error |
| `propose_improvement` | 02:01:13, proposal 271, `failure_type:missed_vendor_key`, correctly diagnoses: *"negative-total fail-closed branch intentionally stopped... current rubric did not recognize that blocker"* | ✅ accurate diagnosis |
| Dashboard notify | 02:01:26 `curl POST /api/expense-stored`, body: `stored:0, parsed:3, expense_id:null, doc_kind:"receipt", vendor:"Priority Health", amount:"-7.10", conversation_id` matches, `dispatched_at:1787709582.549` matches this dispatch | ✅ |

## Why the top-line verdict is PASS despite `judge_trace` saying FAIL

Mazda's own wrapper (loaded at `wrap-v081`, active *before* this run) already contains three
independent learned rules — Rule 55 (2026-08-13), Rule 58 and Rule 63 (2026-08-14) — that all say the
same thing: a receipt/invoice with a non-positive total and explicit credit-balance language must
stop before vendor/duplicate/category/save, and `stored=false` is the *correct* fail-closed outcome.
Mazda followed that instruction exactly, using a genuine parse artifact (not prose-only inference).
The deterministic `judge_trace` rubric still unconditionally checks `vendor_key_resolved` and
`duplicate_check_ran`, so it fails a run the wrapper's own prose already tells her to handle this
way. That is a judge/rubric defect, not a wrapper-instruction gap Mazda can coach around — three
separate prior proposals already tried to fix this via wrapper prose and the judge still didn't
change. No corrective message was sent to Mazda; nothing about her handling of this document was
wrong.

## Escalation (application defect — judge rubric, not wrapper prose)

```
## Escalation
repo_path: /home/adamsl/letta-code (tools/self_improving_agent, agent_self_improvement evaluation layer)
bug_description: |
  The deterministic verdict judge (rule-based ScoreCard evaluator, Chapter 5 "Judge" station,
  FinanceEvaluationFactory's verdict judge) unconditionally requires vendor_key_resolved=true and
  duplicate_check_ran=true to pass a document-intake trace. Mazda's wrapper (wrap-v081, active
  before this run) already contains three merged learned rules — Rule 55/proposal 236
  (2026-08-13), Rule 58/proposal 246 and Rule 63/proposal 257 (both 2026-08-14) — that correctly
  instruct her to stop BEFORE vendor/duplicate/category/save whenever a receipt or invoice parses
  to a non-positive total with an explicit credit-balance/"Information Only, Do Not Pay" blocker,
  and to record stored=false as a correct fail-closed outcome. Trace 483 (this run) followed that
  documented rule exactly and still got judge_trace verdict FAIL with
  reasons ["failed: vendor_key_resolved","failed: duplicate_check_ran"]. Because three prior
  proposals already patched the WRAPPER PROSE for this exact scenario and the judge's scoring code
  was never updated to match, this produces a permanent mismatch: every negative-total document
  that follows the documented-correct procedure will keep failing judge_trace, and each run will
  keep filing yet another propose_improvement (this run filed a fourth: proposal 271) instead of
  the actual defect — the rule-based evaluator/ScoreCard logic — ever getting fixed. This needs a
  source change to the verdict-judge/rule scorer to treat a "payment-credit blocker" / documented
  non-positive-total stop (already recognized in problems) as satisfying its checklist instead of
  unconditionally requiring vendor_key_resolved and duplicate_check_ran.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1787709579280258152_528d8448ba95.jpg
  failing_command: judge_trace(trace_id=483)
  related_proposals: "236, 246, 257, 271"
```

## For a human — worth a look, not scored into the verdict

Mazda called `apply_proposal(proposal_id=271, ...)` immediately after `propose_improvement`,
inside the same intake run, and it **activated automatically** (`activated:true`,
`pending_approval:false`, all four gates — safety/cost/regression/usefulness — returned `allow`),
bumping the active wrapper from `wrap-v081` to `wrap-v083`. `apply_proposal` is not part of the
document-intake contract this Trainer grades (the contract only requires `propose_improvement` on
FAIL), and the manual describes activation as normally passing through "a human approval gateway"
(Ch. 5, station 6). This may be the fleet's normal automated fast-path for gate-clean proposals
(wrap-v081's instructions already show 63+ merged rules from this same proposal→apply pattern), but
an agent self-activating a change to the very rubric that just failed it, in the same run and with
no visible human step, is worth someone confirming is intended rather than a missing guardrail.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
