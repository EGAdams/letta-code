# Trainer Report — Freezer Scanner

- **Scanner**: Freezer Scanner
- **Document**: `scan_freezer_1784320482902525344_26483feabb19.jpg`
- **Dispatch**: 2026-07-17T20:34:46Z
- **Mazda conversation**: `conv-85a1d908-3880-469e-99a6-6ded5d7b9c4d`
- **Verdict: PASS** (procedurally correct run; underlying store failed due to a real infrastructure/schema bug, correctly caught and reported by Mazda — not a wrapper coaching issue)

## Facade result
`doc_kind=statement`, `vendor=Chase`, `confidence=0.95` → routed straight to the statement branch (no STEP 0 vision fallback needed).

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 20:34:52 return, `wrapper_revision=wrap-v022`, `is_active=true` | ✅ |
| Statement parse (`parse_statement_scan.py` via `executor_run`) | 20:36:23 return, `ok:true`, Chase statement, period 2025-01-01→2025-01-31, 7 transactions parsed | ✅ |
| Statement store (`store_statement_transactions.py` via `executor_run`) | 20:36:34 return, `ok:false`, `stored:0`, `failed:4`, all 4 rows errored: `OperationalError: (1054, "Unknown column 'expense_status' in 'where clause'")` | ❌ genuine infra bug (see below) |
| `record_trace` | 20:36:40, `task_name="document-intake"`, trace_id 100, agent_output correctly reflects `transactions_stored:0`, `duplicate_checked:true` | ✅ |
| `judge_trace` | 20:36:46, `verdict=FAIL`, `failure_type=other`, reason `failed: statement_transactions_stored` — consistent with the broken store stage | ✅ |
| `propose_improvement` (required on FAIL) | 20:36:52, trace_id 100, accurate summary naming the exact `OperationalError 1054` root cause | ✅ |
| Dashboard notify (`POST /api/expense-stored`) | 20:37:04, fired even though `stored:0`, includes `expense_ids:[]`, `duplicate_expense_ids:[]`, `problems:[...]`, correct `conversation_id`/`dispatched_at` | ✅ (always-fire rule honored) |

Mazda also called `apply_proposal` (20:36:57) on her own initiative, which auto-activated wrapper revision `wrap-v023` (all 4 gates `allow`). This is not in the required contract but is not prohibited either, and the gate chain approved it. Note: the newly-added wrapper instruction ("only reference expense_status when the column exists...") **cannot actually fix this bug** — the defect is in `store_statement_transactions.py`'s SQL, a real code file outside wrapper scope, not a prompt/instruction issue. A future statement intake will hit the identical failure until a human patches the query.

## Wrapper defect diagnosed
None on Mazda's side. She followed the statement branch correctly (Rule 1 of her learned instructions: parse → store → record_trace, all before judging), reported the true `stored:0` outcome instead of hallucinating success, and completed the full evidence/judge/propose/notify chain even on failure.

## Lesson sent to Mazda
None — no coaching needed, run was procedurally correct throughout.

## For a human
**Real bug, not a Mazda issue**: `rol_finances/tools/receipt_scanning_tools/store_statement_transactions.py` (or a helper it calls for duplicate/counterpart checks) references a column `expense_status` in a `WHERE` clause that does not exist in the current `expenses` table schema (`OperationalError 1054`). This blocked storage of all 7 parsed Chase statement transactions (4 non-credit rows failed; 3 credit rows were correctly skipped). Every future statement intake will fail identically until the query is fixed to match the live schema — this needs an actual code fix, not another wrapper proposal. Proposal 79 / wrapper `wrap-v023` (auto-activated by Mazda) only adds advisory instruction text and will not resolve it.
