# Mazda Trainer Report — Freezer Scanner

Verdict: PASS

- Scanner: Freezer
- Document: `scan_freezer_1786481754568035552_1dee6ab6707e.jpg` (USPS receipt, 2 line items: 1st Class $5.80 + US Intl Fee $4.39 = $10.19)
- Dispatch: 2026-08-11T20:55:58Z
- Mazda conversation: `conv-8c696274-c062-4424-9d13-9ccf9051b175`
- Run duration: ~2m41s (20:56:04 load_wrapper_revision → 20:58:39 dashboard callback)

## Checklist (all with successful tool returns unless noted)

1. `load_wrapper_revision` — wrap-v004 loaded. ✅
2. STEP 0 (facade returned `doc_kind=unknown`) — `classify_scan.py`: doc_type=receipt, confidence 0.97 (Gemini 401'd, fell back to Codex CLI automatically). ✅
3. `parse_and_categorize.py --json --write-parsed-json` — parsed 2 items, subtotal $10.19, `expense_scope=full_receipt`. ✅
4. `check_vendor_key` — recognized, normalized `united_states_postal_service`. ✅
5. `check_duplicates` — **tool errored**: `Access denied for user 'adamsl'@'localhost' (using password: NO)` (MySQL 1045). Mazda correctly applied her own learned Rule 3: ran an equivalent authenticated duplicate query via `executor_run` (explicit password), found no `1970-01-01`/$10.19 match. ✅ (documented honestly in trace `problems`)
6. `categorizer_main.py --provider=auto` → category_id 144 (Postage), confirmed valid via `check_category`. ✅
7. `parse_and_categorize.py --save --category-id=144` → `expense_id=2011`, `parse_artifact_verified=true`, `receipt_url=united_states_postal_service_08_11_26_10_19.jpg`, archived to `readable_documents/receipts/2026/august/august_11/...`. ✅
8. `itemize_existing_expense` — **tool errored**, same MySQL 1045 "no password" failure as step 5's tool. Mazda correctly did **not** hand-build parent/child SQL and left the row standalone (`itemization_attempted=true, itemized=false`), documented honestly in trace `problems`. This is fail-closed-correct behavior on her part.
9. `record_trace(task_name="document-intake")` → trace_id 99, full honest evidence including both tool failures above. ✅
10. `judge_trace(99)` → PASS, failure_type=none. ✅
11. Dashboard callback `POST /api/expense-stored` → `{"ok": true}`. ✅

## Independent dashboard verification (Trainer's own checks)

- `POST /api/supporting-documents` for expense 2011: `receipt_url` present, `available:true` → **View Receipt** correctly shown; `document_url`/`scanned_statement_url`/`moms_ledger` all correctly absent (single-document receipt intake, nothing else should be attached).
- `GET /supporting-document/2011/receipt`: served bytes (329722 B) differ from the raw archived file (329159 B, different md5) → the red-box annotator successfully drew a highlight box. No dashboard-annotation defect on this run.
- `expense-stored-events` confirms `conversation_id` matches this dispatch and `dispatched_at` matches — genuine evidence for this run, not a stale event.

## Wrapper defect diagnosed

None. Mazda followed the contract exactly, including two previously-learned rules (Rule 3 duplicate fallback) and the "never hand-build itemization SQL" rule. No coaching message was sent.

## Lesson sent to Mazda

None needed.

## For a human

Two low-severity items, both environment/application bugs, not Mazda mistakes:

1. `itemize_existing_expense` and `check_duplicates` are both failing with the identical MySQL error `Access denied for user 'adamsl'@'localhost' (using password: NO)`, while Mazda's own manual `executor_run` pymysql connection (with an explicit password) succeeds. This looks like a missing/blank DB password in whatever backs those two Letta custom tools specifically — worth fixing so future runs don't need the Rule-3 workaround and so itemization (which should have succeeded here — items sum cent-exactly to the total) can actually complete instead of leaving a row standalone.
2. The receipt store return reports `archive_years: [1970]` even though the real archive path is `2026/august/august_11/...`. This is because `archive_years` is derived from the `expense_date` sentinel (`1970-01-01`, used when the transaction date is unreadable) rather than from the actual file archive date. Harmless here (single receipt, no cross-year logic depends on it) but worth fixing in `parse_and_categorize.py`'s save path.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  Two related MySQL-auth application defects, both hit in this single run:

  (a) `check_duplicates` and `itemize_existing_expense` (Letta custom tools backing
  Mazda) both fail with `(1045, "Access denied for user 'adamsl'@'localhost'
  (using password: NO)")`. A manual `executor_run` pymysql connection using an
  explicit password against the same DB/host succeeds immediately, so the fix is
  almost certainly a missing/blank password in whichever module builds the DB
  connection for these two tools specifically (other tools like check_vendor_key/
  check_category/categorizer_main.py are unaffected). This is not something Mazda
  can fix by retrying — she cannot supply a password to a server-side tool, and
  the contract explicitly forbids her from hand-building itemization SQL as a
  workaround. Effect: itemization is currently broken for every multi-line
  receipt/invoice until this is fixed (this run's 2-item USPS receipt, which sums
  cent-exactly to the total and should have itemized, was left standalone instead).

  (b) `parse_and_categorize.py --save` returns `archive_years` computed from the
  expense's `expense_date` field rather than from the actual archived-file date.
  When the transaction date is unreadable and stored as the `1970-01-01` sentinel,
  `archive_years` reports `[1970]` while `archive_paths` correctly points under
  `readable_documents/receipts/2026/august/august_11/...`. Low severity (metadata
  inconsistency only, no misfiled data), but should be fixed to derive the year
  from the archive path/timestamp instead of the expense_date sentinel.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786481754568035552_1dee6ab6707e.jpg
  failing_command: itemize_existing_expense (Letta tool call, expense_id=2011); check_duplicates (Letta tool call)
  expense_id: 2011

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
