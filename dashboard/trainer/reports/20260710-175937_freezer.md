# Trainer Report — Freezer Scanner intake

- **Scanner:** Freezer Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Dispatch:** 2026-07-10T17:53:21Z
- **Facade result:** `doc_kind=unknown, confidence=0.0` (JPEG, no extractable text) → routed to STEP 0 self-classification per contract.
- **Verdict: PASS (after one coaching intervention)**

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | tool_call/return 17:53:26, `wrap-v001` | ✅ |
| STEP 0a — classify via `classify_scan.py` (executor_run) | tool_call/return 17:54:05 → `{"doc_type":"bank_statement","confidence":1.0,"merchant":"Chase"}` | ✅ — correctly identified this scan as a Chase **bank statement**, not a receipt |
| STEP 0b — parse (receipt only) | N/A | correctly skipped — document is not a receipt |
| STEP 2 investigate / STEP 3 categorize / STEP 4 store | N/A | correctly skipped — not applicable to a non-receipt document |
| STEP 5 `record_trace` | **Initially missing.** Only appeared after coaching, 17:56:37 → `trace_id=51` | ⚠️ then ✅ |
| STEP 6 `judge_trace` | after coaching, 17:56:42 → `FAIL` (`missed_vendor_key`; expected — the intake rubric scores vendor_key/category/duplicate/stored, all N/A for a statement) | ✅ |
| STEP 7 `propose_improvement` on FAIL | after coaching, called **twice** (proposal_id 36 and 37, same trace_id) | ✅ (with minor duplicate-call inefficiency) |
| STEP 8 dashboard notify | N/A — nothing stored (correct, confirmed `GET /api/expense-stored-events?since=1783706001` → `[]`) | ✅ |

## What went wrong (first pass)

After classifying the scan as a bank statement at 17:54:11, Mazda stopped the turn entirely:

> "I'm stopping here because the dispatch you gave was specifically the receipt-intake back
> half... Since this is a statement, I should route it to the statement verification path
> instead."

She never called `record_trace` or `judge_trace`. This is a genuine wrapper-instruction gap,
not a "the model was dumb" failure: the dispatch's STEP 5/6 say record_trace + judge_trace run
"ALWAYS, on success or failure," and the evidence-JSON schema explicitly lists `"statement"` as
a valid `doc_kind` — but nothing in the dispatch text spells out that a **non-receipt
classification is itself an outcome that must still be traced**, so the instruction to
"complete the AGENTIC back half" read (to the cheap model) as scoped only to receipts, licensing
an early exit. Without a trace, this run would have been invisible to the self-improvement loop
— no evidence at all, not even a FAIL.

An unrelated user (not the Trainer) then asked her "Did you route it?" in the same window,
confirming independently that she had stalled ("I didn't run the statement-processing path
after that").

## Coaching sent

One corrective message at 17:56:37 telling her to (1) call `record_trace` now with
`doc_kind:"statement", stored:false`, an explicit `problems` note, (2) `judge_trace` the
resulting trace regardless of outcome, (3) `propose_improvement` on FAIL, and (4) record a
durable memory: *"receipt-intake dispatches can legitimately turn out to be bank statements
after classification — record_trace + judge_trace are NOT conditional on doc_kind being a
receipt."*

She complied immediately and fully (trace 51 → judge FAIL → propose_improvement), closing the
loop correctly. `judge_trace` was called twice and `propose_improvement` was called twice for
the same trace — redundant but harmless (both proposals reference the same trace_id and a
similar summary).

## For a human

- Recommend tightening the dispatch template itself (not just Mazda's memory) so this class of
  cheap-model early-exit doesn't recur on the next non-receipt scan: state explicitly, near
  STEP 0, that a `doc_kind` of `"statement"` still requires completing STEP 5/6 with `stored:
  false` before ending the turn.
- The Chase bank-statement scan itself is still unprocessed — no statement-verification path was
  dispatched or run in this session. That is out of scope for the receipt-intake contract this
  dispatch covers, but a human should confirm the statement gets picked up by whatever normally
  handles statement scans.
- `judge_trace`/`propose_improvement` double-calls are a minor inefficiency worth trimming from
  Mazda's memory note (call once, check the return, don't retry on a normal success return).
