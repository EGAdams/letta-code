# Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785692691999337970_3ffad25ef743.jpg`
- Dispatch: 2026-08-02T17:44:56Z (unix 1785692696)
- Mazda conversation: `conv-3444b9dc-666a-4fcf-9d93-a50754be8d21`
- Facade: `doc_kind=unknown, confidence=0.0` (JPEG scan, expected) → STEP 0 self-classification required.

## Checklist (all with successful tool returns)

1. `load_wrapper_revision(Mazda)` → `wrap-v071` — ✅
2. STEP 0 `classify_scan.py` (executor_run) → `doc_type=receipt, confidence=0.95`, merchant "Children's Vision International, Inc." — ✅
3. `parse_and_categorize.py --json` → date `2025-02-14`, total `$300.50`, `items:[]`, `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` — correct scope for a no-line-item donation-acknowledgment letter (no marks/circles to misjudge). — ✅
4. `check_vendor_key("childrens_vision_int_inc")` → fuzzy-normalized to `vision_for_israel`; Mazda adopted the returned key downstream. — ✅
5. `check_duplicates` → not a duplicate. — ✅
6. `categorizer_main.py --provider=auto` → `category_id=355` (nonzero, valid). — ✅
7. `parse_and_categorize.py --save --category-id=355` → `expense_id=1987`, receipt archived to `readable_documents/receipts/2025/february/february_14/childrens_vision_int_inc_02_14_25_300_50.jpg`. — ✅
8. `record_trace(task_name="document-intake")` → evidence matches reality (`stored:true, expense_id:1987, vendor_key:vision_for_israel, category_id:355`), `problems` correctly notes the fuzzy vendor-key normalization. — ✅
9. `judge_trace(346)` → **PASS**, `failure_type: none`. — ✅
10. Dashboard notify `POST /api/expense-stored` — **first attempt failed** (inline bash single-quote escaping of `Children's` broke the JSON payload → `returncode 3, {"error":"Invalid JSON"}`). Mazda **self-corrected in the same turn**, switching to a `python3 subprocess` call with `json.dumps(payload)`, which succeeded (`{"ok": true}`). Confirmed durable via `GET /api/expense-stored-events?since=1785692696`: an event for `expense_id=1987`, `conversation_id=conv-3444b9dc-...` (this run) is present. — ✅ (no coaching needed; she recovered unaided and the contract was ultimately fulfilled)

Itemization: not applicable — `items:[]`, single total, `itemization_attempted=false` correctly.

## Dialog visibility (verified via `/api/supporting-documents`)

`POST {"expense_id":1987}` → `receipt_url` present, `documents[0] = {type:receipt, available:true}`; the other three fields (`document_url`, `scanned_statement_url`, `moms_ledger`) correctly `available:false` (none of those evidence types exist for this expense). Contract satisfied.

## Red-box check — unboxed (dashboard defect, not Mazda's)

`GET /supporting-document/1987/receipt` served **byte-identical** to the raw archived file (`cmp` — no diff), and the dashboard log shows:
```
[supporting-document] opened without highlight expense_id=1987 type=receipt reason=No high-confidence expense row was found in the image.
```
This document is a printed donation-acknowledgment letter with no itemized line rows (`items:[]` in the parse) — the annotator's row-matching logic (`document_annotation.py`) appears built around itemized/tabular receipts and finds nothing to box on a single-total letter-style receipt. Not coaching Mazda to "fix" this per instructions.

## Escalation
repo_path: /home/adamsl/letta-code
bug_description: |
  document_annotation.py's receipt-line matcher returns "No high-confidence expense
  row was found in the image" for a single-total, non-itemized receipt (a printed
  donation/payment acknowledgment letter with no tabular line items — parse_and_categorize.py
  reported items:[]). The View Receipt dialog action correctly shows available:true, but the
  served bytes at /supporting-document/1987/receipt are byte-identical to the unannotated
  original (no red box), making the expense visually unverifiable against its source. Likely
  the OCR-line matcher only scores tabular/itemized rows and has no fallback for a
  single-amount letter-style receipt (should probably box the printed total/date region
  instead when there are zero item rows).
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/february/february_14/childrens_vision_int_inc_02_14_25_300_50.jpg
  failing_command: GET /supporting-document/1987/receipt (via document_annotation.py image strategy)

## For a human
Everything in Mazda's intake contract passed cleanly on the first real attempt; the only
hiccup (a shell-quoting JSON bug in her own callback command) was self-corrected within the
same turn with no Trainer intervention. The one open item is the unboxed receipt above —
cosmetic/verification-only, does not affect the stored expense data.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1987`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
