# Mazda Trainer Report

- **Scanner:** Bank 6285 PDF 2 report repair
- **Dispatch:** 2026-07-23T22:54:24Z
- **Document:** `/home/adamsl/rol_finances/readable_documents/bank_statements/march/business_january_february_6285/march_april_fifth_third_6285_2025.pdf`
- **Verdict:** PASS

## Checklist

- `load_wrapper_revision` succeeded with `wrap-v039`.
- `classify_scan.py` returned `bank_statement` with confidence `0.95`.
- `parse_statement_scan.py` / statement parsing succeeded: `52` transactions, `0` unreadable.
- `store_statement_transactions.py` succeeded with `0` new expenses, `34` duplicates, `18` credits skipped, `bank_name=Fifth Third Bank`, `account_last4=6285`, and `account_last4_source=statement`.
- `apply_statement_annotations.py` succeeded with `annotation_provider=codex-cli` and `annotations_applied=0`.
- `record_trace` and `judge_trace` were called; the final corrected trace judged `PASS`.
- `report.html` was initially missing, then Mazda generated `/home/adamsl/rol_finances/readable_documents/bank_statements/march/business_january_february_6285/report.html`, ran `restructure_verified_transactions.py`, ran `audit_statement_reports.py`, and updated the evidence with `report_path`, `report_generated=true`, and `report_audit_status=PASS`.
- Dashboard callback was resent with `report_path`.

## Wrapper Defect

Mazda initially stopped at parse/store/judge and missed the required statement report loop. The first report attempt also failed because the HTML template used raw CSS braces in a `.format()`-style render path, causing a `KeyError: ' font-family'`.

## Coaching Sent

- Generate `<source statement directory>/report.html`.
- Run `restructure_verified_transactions.py` on the report.
- Run `audit_statement_reports.py` on the statement directory.
- Record evidence including `report_path`, `report_generated=true`, and `report_audit_status`.
- Resend the dashboard callback with `report_path`.
- Fix the template by escaping CSS braces or using a template mechanism that does not treat CSS braces as placeholders.

## Notes

- The final report passed audit.
- The wrapper gap was recorded back to Mazda as a memory-note proposal so future statement runs do not omit the report step.
