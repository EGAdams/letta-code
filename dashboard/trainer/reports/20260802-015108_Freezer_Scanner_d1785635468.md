# Trainer Report — Freezer Scanner

**Verdict: PASS**

- Scanner: Freezer Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785635465696060372_c9903a92a762.jpg`
- Dispatch: 2026-08-02T01:51:08Z
- Mazda conversation: `conv-49e8b2b7-3e87-4136-8c4a-63a3cbd6380b`
- Facade result: `doc_kind=unknown, confidence=0.0` → correctly routed to STEP 0 self-classification.

## Step-by-step checklist (all evidence = successful tool returns in this run's transcript, ≥01:51:08Z)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v070` — ✅
2. STEP 0 `classify_scan.py` (executor_run) → `doc_type=receipt, confidence=1.0, merchant="Dermatology Associates of West Michigan"` — ✅
3. `parse_and_categorize.py --json` (executor_run) → parsed `transaction_date=2025-04-08, total_amount=150.0, items=[], expense_scope=full_receipt, handwritten_arithmetic_total=null`. Single-line payment receipt, no marked-item/handwritten-arithmetic ambiguity — scope correctly defaulted to `full_receipt`. — ✅
4. `check_vendor_key` → `vendor_key=dermatology_associates_of_west_michigan_dawm, recognized=false` (new vendor, not a blocker). Mazda adopted the normalized key consistently through categorization/save/trace. — ✅
5. `check_duplicates` → `is_exact_duplicate=false, in_database=false` — ✅
6. `categorizer_main.py --provider=auto` (executor_run) → gemini timed out, fell back to `chatgpt-oauth`, resolved `category_id=393` (nonzero, valid) — ✅ fallback chain worked as designed
7. `parse_and_categorize.py --save --category-id=393` (executor_run) → `{"success":true,"expense_id":1980,"parse_artifact_verified":true,"expense_scope":"full_receipt","duplicate":false}`. Receipt archived to `readable_documents/receipts/2025/april/april_08/dermatology_associates_of_west_michigan_dawm_04_08_25_150_00.jpg`; `receipt_url` set. No items to itemize (single line, `items:[]`) — itemization correctly skipped (`itemization_attempted:false`). — ✅
8. `record_trace(task_name="document-intake", ...)` → `trace_id=339`, evidence JSON matches reality (stored=true, expense_id=1980, is_duplicate=false, category_id=393) — ✅
9. `judge_trace(339)` → `verdict=NEEDS_REVIEW, failure_type=none, reason="vendor_key resolved but not in the known-vendor map"` — consistent with observation: a first-time vendor with a valid resolved category is expected to need review, not a wrapper defect. `propose_improvement` correctly **not** called (only required on FAIL). — ✅
10. Dashboard callback `POST /api/expense-stored` with `expense_id:1980, expense_ids:[1980], parsed:1, stored:1, archive_paths:[...]` — ✅

No coaching was needed — every stage produced correct, verifiable evidence and the NEEDS_REVIEW verdict reflects an expected new-vendor state, not a defect.

## Independent verification (dashboard side)

`GET /supporting-document/1980/receipt` → `200`, 330262 bytes served. Dashboard log for this request:
```
[supporting-document] opened without highlight expense_id=1980 type=receipt reason=No high-confidence expense row was found in the image.
```
The receipt viewer is reachable but serves the **unannotated** original — no red box. This is a dashboard-side annotator limitation, not a Mazda failure (see Escalation below); the top-line verdict stays PASS per Trainer instructions since it reflects Mazda's intake contract, not this supplementary check.

Also noted in `record_trace`'s `problems` and confirmed in the `--save` stderr:
```
WARNING:__main__:Source-counterpart lookup failed: not enough arguments for format string
```
Non-fatal (storage still succeeded), but indicates a real Python string-formatting bug in the store path's source-counterpart lookup — filed below.

## For a human

- New vendor `dermatology_associates_of_west_michigan_dawm` is unmapped; NEEDS_REVIEW is expected and just needs a human to add it to the known-vendor map if desired.
- Two dashboard/rol_finances application defects filed below — neither blocked this intake.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  During `parse_and_categorize.py --save` for expense_id 1980, stderr logged
  `WARNING:__main__:Source-counterpart lookup failed: not enough arguments for
  format string`. This is a Python string-formatting bug (a `%`/`.format()`
  call passed fewer arguments than its format string expects) inside the
  receipt save path's source-counterpart lookup (used to correlate a new
  receipt against an existing statement/document reference per the
  supporting-document contract). It did not block storage — the receipt saved
  and archived successfully — but the lookup itself silently failed, which
  means any legitimate source-counterpart match for this receipt was skipped.
  Likely file: `tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py`
  (or a helper it calls for source-counterpart resolution). Needs a code fix,
  not a wrapper/prompt change — Mazda has no visibility into or control over
  this internal logging call.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785635465696060372_c9903a92a762.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=393 --parsed-json /tmp/mazda_receipt_ad90d654486a.json --engine=gemini

## Escalation

repo_path: /home/adamsl/letta-code
bug_description: |
  `GET /supporting-document/1980/receipt` served the raw, unannotated receipt
  image instead of a red-boxed copy. Dashboard log:
  `[supporting-document] opened without highlight expense_id=1980
  type=receipt reason=No high-confidence expense row was found in the
  image.` The receipt is a simple payment-confirmation slip (merchant name,
  transaction date, single $150.00 total, no itemized line table), which may
  be outside what the receipt-image annotation strategy in
  `dashboard/document_annotation.py` can currently box (it appears tuned for
  line-item tables). Expenses stored from this class of receipt are
  unverifiable via the red-box highlight even though the underlying data is
  correct. Suspected file: `dashboard/document_annotation.py` (OCR-image
  annotation strategy / `_line_score` matching, per project notes on how it
  currently expects a matchable line row).
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/april/april_08/dermatology_associates_of_west_michigan_dawm_04_08_25_150_00.jpg
  failing_command: GET /supporting-document/1980/receipt

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
