# Mazda Trainer Report — Window Scanner

- **Document**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_1784597760399702323_e9bf85361ad9.jpg`
- **Scanner**: Window Scanner
- **Dispatch time**: 2026-07-21T01:36:03Z (unix 1784597763)
- **Mazda conversation**: `conv-c419f787-2e58-4138-beb4-8670ed833a36`
- **Facade result**: `doc_kind=statement`, vendor=Chase, confidence=1.0 (STEP 0 vision fallback correctly skipped since facade already identified it)

## Verdict: **PASS**

## Checklist (all confirmed via successful tool returns in this run's transcript)

1. **`load_wrapper_revision`** (01:36:07) → `wrap-v032`, active. ✅
2. **STEP 0 skipped correctly** — facade already returned `doc_kind=statement` with confidence 1.0, so no self-classification needed. ✅
3. **Parse (statement branch)** — `executor_run` → `parse_statement_scan.py` (01:36:57): bank=Chase, 7 transactions extracted. ✅
4. **Investigate + Store (statement branch, combined)** — `executor_run` → `store_statement_transactions.py` (01:37:02): `transactions_parsed=7`, `skipped_credits=3` (correctly excluded — statements only expense debits), `duplicates=4`, `duplicate_expense_ids=[1504,1505,1503,1506]`, `stored=0`, `expense_ids=[]`. All 4 non-credit transactions matched existing duplicates; nothing new to store. ✅
5. **`record_trace`** (01:37:09) → `trace_id=121`, `task_name="document-intake"` (exact match). Evidence JSON includes document_path, doc_kind=statement, classification_confidence=1.0, vendor, `duplicate_checked=true`, parsed/stored/duplicate counts, `duplicate_expense_ids`, `problems: []`. ✅
6. **`judge_trace(121)`** (01:37:12) → verdict **PASS**, failure_type=none, reason "all deterministic intake checks passed". ✅
7. **`propose_improvement`** — not called, correctly, since verdict was PASS. ✅
8. **Dashboard notify** — `curl POST /api/expense-stored` via `executor_run` (01:37:20) → `{"ok": true}`. Confirmed independently via `GET /api/expense-stored-events?since=1784597763`: event present with matching `conversation_id=conv-c419f787-...` and `dispatched_at=1784597763.72...`, `duplicate_expense_ids=[1504,1505,1503,1506]`, `stored=0`. ✅
9. Mazda also recorded a second internal duplicate-only trace (`trace_id=122`, `judge_trace` → PASS) summarizing the same duplicate-only outcome before her final assistant message. Not required by the contract but consistent/harmless.

## Wrapper defect diagnosed
None. Clean run.

## Lesson sent to Mazda
None — no coaching needed.

## Notes for a human
None. Statement correctly recognized as a re-scan of already-stored transactions (4 duplicates, 3 credits properly skipped, 0 new rows) — expected, correct behavior for a repeat statement scan.
