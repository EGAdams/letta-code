# Trainer Report — Window Scanner

- **Document**: `window_scan_1785630992057327460_b2a7108e2394.jpg`
- **Dispatch**: 2026-08-02T00:36:35Z (unix 1785630995)
- **Mazda conversation**: `conv-0623a4af-3f87-42fe-9010-cac4367af3a6`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Summary

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), correctly triggering
STEP 0. Mazda classified the image herself, ran the receipt branch end-to-end, correctly
detected an exact duplicate, filed the duplicate-safe save, and closed the loop with
trace/judge/callback. No coaching was required.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 00:36:40, returned `wrap-v070` (active) | ✓ |
| STEP 0 classify (doc_kind=unknown) | `classify_scan.py` → `doc_type=receipt, confidence=0.95, merchant=Tikkun International` ("donation acknowledgment letter confirming a payment already received") | ✓ |
| STEP 0 parse | `parse_and_categorize.py --json --engine=gemini`, single line item "Restoration from Zion" $24.25, total $24.25 | ✓ |
| `check_vendor_key` | `vendor_key=tikkun_international`, `recognized:true` | ✓ |
| `check_duplicates` | `is_exact_duplicate:true, exact_duplicate_expense_id:1973` | ✓ |
| Categorize | `categorizer_main.py --provider=auto` → `category_id=356` (non-null, valid) | ✓ |
| Duplicate-safe save | `parse_and_categorize.py --save --category-id=356` (no `--allow-duplicate`) → `success:true, duplicate:true, idempotent:true, expense_id:1973` | ✓ |
| `record_trace` | `task_name=document-intake`, `is_duplicate:true, stored:false, expense_id:null`, duplicate evidence correctly shaped per contract | ✓ trace_id=334 |
| `judge_trace` | verdict `PASS`, `failure_type:none`, "all deterministic intake checks passed" | ✓ |
| Dashboard callback | `POST /api/expense-stored` → `{"ok": true}`, `duplicate_expense_ids:[1973]`, `stored:0, parsed:1` | ✓ — confirmed independently via `/api/expense-stored-events?since=1785630995`, matching `conversation_id` and `dispatched_at` |

Total run time: ~1.5 minutes (00:36:36 dispatch → 00:38:07 callback).

## Notes

- Single-item receipt, no marked-items scope or handwritten-arithmetic ambiguity to adjudicate.
- Could not independently confirm the red-box/dialog-visibility invariant for expense 1973
  (`/api/supporting-documents` and `/supporting-document/1973/receipt`) — this Trainer's local
  dashboard instance has no `pymysql` installed, so the DB-backed lookup fails with
  `DB error: No module named 'pymysql'` before it can even attempt annotation. This is a
  verification-environment gap on this box, not something observed in Mazda's transcript, so it
  does not affect the verdict. Worth fixing pymysql on this host if red-box spot-checks are
  expected to run routinely from here.

## Escalation

None — no application defect observed this run.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1973`, metadata: DB error: No module named 'pymysql'

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
