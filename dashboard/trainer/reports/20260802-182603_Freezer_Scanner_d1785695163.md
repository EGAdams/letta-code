# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1785695160451600795_c81723e80a2b.jpg` — "Children's Vision Int. Inc." year-end donation summary
- Dispatch: 2026-08-02T18:26:03Z
- Mazda conversation: `conv-916e1855-3d5e-46b4-b426-f0dddcca97c9`
- Facade result: `doc_kind=unknown, confidence=0.0` → STEP 0 required

## Checklist (all with successful tool returns observed in-transcript, ≥ dispatch ts)

1. `load_wrapper_revision` — wrap-v071. ✅
2. STEP 0 — `classify_scan.py` via `executor_run` → `doc_type=receipt, confidence=0.95, merchant="Children's Vision Int. Inc"`. Then `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_a9c3e599788d.json` (run-scoped path, not a shared tmp name). ✅
3. `check_vendor_key` → normalized `vendor_key=vision_for_israel` (Mazda adopted the normalized key, not her raw guess). `check_duplicates` → not a duplicate. ✅
4. Categorizer (`categorizer_main.py` via `executor_run`, provider `gemini`) → `category_id=355` (nonzero/valid). ✅
5. Store: `parse_and_categorize.py --save --category-id=355 --parsed-json <same run-scoped file>` (no `--allow-duplicate`) → `expense_id=1989`, archived to `readable_documents/receipts/2026/january/january_06/childrens_vision_int_inc_01_06_26_3047_00.jpg`.
   Then `itemize_existing_expense` — 10 line items (checks dated 2025-01-14 through 2025-12-24, one 2023-08-16 outlier) reconciled **exactly** ($3047.00, difference 0.00) → `itemization_reconciled=true`, parent 1989, children 1990–1999, each pre-supplied with the verified `category_id=355`. Each child's own `line_date` was passed distinctly in the parse payload (not the parent's `transaction_date=2026-01-06`). ✅
6. `record_trace` — `task_name="document-intake"`, full IntakeVerificationEvidence including `intake_halted:false`, itemization fields, and an honest `problems` note about the classifier calling a donation summary a "receipt" and the date mismatch between the letter's stated date and the underlying donation dates. ✅
7. `judge_trace(349)` → **PASS**, failure_type none. ✅
8. `propose_improvement` — not required (PASS). ✅
9. Dashboard callback — `/api/expense-stored-events` confirms an event for this exact `conversation_id`/`dispatched_at` with `stored=1`, `expense_ids=[1989]`, `archive_paths` matching. ✅

## Independent verification (Trainer's own checks, not Mazda's prose)

- **Per-child red-box annotation**: fetched `/supporting-document/<id>/receipt` for children 1990, 1991, 1992, 1993, 1994, 1999 — each returned bytes with a **distinct md5**, confirming each child is boxed to its own line on the page (not all serving the same unannotated original).
- **Parent (1989)** served byte-identical to the raw archived file (`md5 147f56c0...`), with dashboard log `opened without highlight expense_id=1989 ... reason=No high-confidence expense row was found in the image`. This is expected, not a defect: the parent is the reconciliation anchor for the aggregate $3047.00 total, which is not itself a single printed line on the page for the annotator to box — the 10 individually-boxed children are the real per-row evidence. Noting it here for completeness, not filing as a bug.
- No duplicate/`linked_counterpart` confusion, no `intake_halted` signal — this run is unrelated to the 2026-08-02 Vision-For-Israel double-count incident (`expense_ids 1534/1981`) despite the same vendor family name; this is a new, correctly-deduplicated donation-summary receipt.

## Wrapper defect diagnosed

None. No coaching message was sent — the run was clean end-to-end on the first pass.

## For a human

None — clean PASS, itemization reconciled exactly, callback and per-line annotations verified independently.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1989`, receipt: No high-confidence expense row was found in the image.
- expense `1996`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
