# Trainer Report — Window Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785636135370824020_e42e49feb5c8.jpg`
**Dispatch:** 2026-08-02T02:02:19Z
**Conversation:** conv-6deaeca1-e7a3-4f31-9022-8d8793258efb
**Facade result:** `doc_kind=unknown, confidence=0` → STEP 0 self-classification required.

**Verdict: FAIL**

## Step-by-step checklist

1. `load_wrapper_revision` — ✅ wrap-v071 loaded (02:02:24).
2. STEP 0 classification (facade unknown) — ✅ `classify_scan.py` → `receipt`, confidence 1.0, merchant "VISION for ISRAEL" (02:02:43).
3. Receipt parse — ✅ `parse_and_categorize.py --json` → check-payment donation receipt, $100.00, date 2025-03-17, `expense_scope=full_receipt` (02:03:01).
4. `check_vendor_key("vision_for_israel")` — tool fuzzy-normalized this to an **unrelated** known key `michigan_corporations_division`. Mazda correctly rejected the bogus normalization (documented reasoning in her trace `problems`) and kept the derived key. Good judgment, but flags a real bug in `check_vendor_key`'s fuzzy matcher (see Escalation).
5. `check_duplicates` — ✅ returned a **fuzzy duplicate**: existing expense `1534` (`check_2101_03_17_25_100_00`), same date+amount, `is_exact_duplicate=false`, note explicitly flags the existing row is identified only by a payment instrument with no payee — i.e. this receipt is almost certainly the same $100 gift already recorded from the bank statement.
6. Categorization — ✅ `categorizer_main.py` → category_id 355 via Gemini.
7. Save — `parse_and_categorize.py --save --category-id=355` **created a brand-new expense `1981`** instead of attaching the receipt to the matched expense `1534`. Its stderr contained: `Source-counterpart lookup failed: not enough arguments for format string` — the counterpart-attach code path threw and silently fell back to inserting a new row.
8. `record_trace` — ✅ called, `task_name="document-intake"`, and Mazda **herself documented both defects** in the `problems` array (vendor-key bug and the wrong-branch duplicate save). Commendable self-report.
9. `judge_trace(340)` — returned **PASS** ("all deterministic intake checks passed"), which is wrong given the documented problems — the judge does not appear to weigh the `problems` field.
10. Dashboard callback `/api/expense-stored` — ✅ sent, confirmed via `/api/expense-stored-events`: `expense_id:1981, duplicate_expense_ids:[1534]`.

## Why this is a FAIL, not a PASS

Per the supporting-document contract, a receipt that fuzzy-duplicates an existing check-payment expense (same date/amount, existing row identified only by payment instrument) must have its `receipt_url` **attached to the existing expense**, not be stored as a second expense. This run now has **two DB rows for the same $100 donation to Vision for Israel on 2025-03-17**: expense 1534 (from the bank statement, `document_url` set, category 190 "Gifts & Love Offerings") and the newly created expense 1981 (this receipt, `receipt_url` set, category 355). That is a real double-count in the finance database.

## Classification: application defect (not a Mazda wrapper fix)

The root cause is a code bug in `rol_finances`' receipt save/attach path (`parse_and_categorize.py --save`, or the counterpart-lookup helper it calls), which threw a Python string-formatting error (`not enough arguments for format string`) while attempting to attach this receipt to the matched counterpart row, then fell through to creating a new expense instead of failing closed. Mazda cannot fix this by retrying, using a different flag, or changing her prompt — she ran the documented correct sequence and the tool itself broke. No corrective message was sent to Mazda; she already surfaced both anomalies honestly in her trace, which is the correct wrapper-side behavior.

The judge's PASS despite these documented `problems` is a second, related defect in the deterministic verdict logic (part of the same `rol_finances`/`agent_self_improvement` code, not Mazda's wrapper).

## For a human

- Expenses **1534** and **1981** likely represent the same $100 gift to Vision for Israel (2025-03-17) and should be reconciled/merged manually; do not let both stand as separate expenses.
- The `check_vendor_key` fuzzy matcher mapped `vision_for_israel` → `michigan_corporations_division`, an unrelated key — this fuzzy-match algorithm needs a tighter similarity threshold.

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  parse_and_categorize.py --save (receipt intake path), when check_duplicates has
  already returned a fuzzy (same date+amount) duplicate against an existing expense
  identified only by a payment instrument (e.g. a check number, no payee), is
  expected to attach the new receipt's receipt_url to that existing expense rather
  than insert a second expense. Instead, the save's counterpart-lookup helper threw
  `Source-counterpart lookup failed: not enough arguments for format string` and the
  code fell through to creating a brand-new expense row (1981) alongside the
  original (1534), duplicating a $100 transaction (Vision for Israel, 2025-03-17) in
  the finance DB. Separately, check_vendor_key's fuzzy vendor-key matcher normalized
  "vision_for_israel" to the unrelated known key "michigan_corporations_division",
  which Mazda correctly rejected but which indicates an overly loose fuzzy-match
  threshold in that tool. Also, judge_trace returned PASS for a trace whose
  `problems` array documented both of these defects — the deterministic judge does
  not appear to inspect `problems` when scoring a verdict.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785636135370824020_e42e49feb5c8.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=355 --parsed-json /tmp/mazda_receipt_0e1c79007806.json
  new_expense_id: 1981
  existing_counterpart_expense_id: 1534
  trace_id: 340
```

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
