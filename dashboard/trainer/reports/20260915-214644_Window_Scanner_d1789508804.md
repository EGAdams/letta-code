# Mazda Trainer Report — Window Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1789508798754026746_2fdf6d3f3ab6.jpg`
**Dispatch:** 2026-09-15T21:46:44Z · conversation `conv-b894a6aa-550d-4606-843c-85be7cef8490`
**Facade result:** `doc_kind=unknown`, confidence 0.0 → STEP 0 required.

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

Run completed end-to-end in ~97 seconds (21:46:44 → 21:48:21). Every required step present with successful tool returns.

## Checklist

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | Loaded `wrap-v115`, 95 accumulated learned rules |
| STEP 0 classify | `classify_scan.py` → `doc_type=receipt` (0.99 conf), merchant "Priority Health", reason: handwritten "pd E 8-29" linked to circled "Total due: $73.60" → paid invoice = receipt (correct per manual) |
| STEP 0b parse | `parse_and_categorize.py --json --engine=gemini` — all three Gemini models failed (503×2 high-demand, 429 quota), ChatGPT OAuth failed ("gpt-5.4" unsupported), OpenAI not configured → local fallback engaged |
| Fallback consistency check | Local fallback: subtotal $79.30, total $73.60, no discount/tax recorded → internally inconsistent per its own schema. Correctly identified as untrustworthy: raw OCR text shows a `Previous total balance: ($5.70)` credit that the fallback never bound into a discount line ($79.30 − $5.70 = $73.60 reconciles, but the *parser* didn't capture it) |
| Vendor/duplicate/category/store | Correctly skipped — not applicable under the `parse_blocked=true` fail-closed branch |
| `record_trace` | `task_name="document-intake"`, `parse_blocked:true`, `stored:false`, `expense_id:null`, `vendor_key:null`, problems array names exhausted providers + concrete contradiction |
| `judge_trace` | trace_id 565 → `NEEDS_REVIEW`, `failure_type=none` — correct per current judge rubric for parse-blocked evidence |
| `propose_improvement` | Correctly **not called** (verdict is not FAIL) |
| Dashboard callback | `POST /api/expense-stored` with `stored:0`, matching `conversation_id`, `dispatched_at=1789508804.3428419`, `document_path` — confirmed via `/api/expense-stored-events?since=...` returning this exact event |

## Analysis

This is a clean fail-closed run matching wrapper Rules 68–95 (parse-blocked handling), which were built from many prior identical scenarios. Mazda did not fabricate a merchant, amount, or vendor key from her own reading of the raw OCR text — correctly leaving that to a human, per "never turn your own visual estimate into an expense." No coaching needed; no wrapper defect found.

## Note for a human

All three AI parse providers failed on this attempt: Gemini (quota/demand), ChatGPT OAuth (rejected model `gpt-5.4` as unsupported for Codex/ChatGPT accounts), and OpenAI (`OPENAI_API_KEY` not set). The pipeline's fail-closed design absorbed this correctly (NEEDS_REVIEW, not a bad store), so this did **not** cause an incorrect verdict and I am not filing a full escalation. But the `gpt-5.4` rejection looks like the same class of stale/unsupported-model defect already documented for `gemini-2.5-flash-lite` elsewhere in the contract — worth a human check of whatever config names `gpt-5.4` as the ChatGPT-OAuth fallback model for `parse_and_categorize.py`, since if Gemini quota is exhausted again with that fallback still broken, more receipts will land in NEEDS_REVIEW than necessary. This is a resilience/config observation, not a Mazda wrapper defect.

No escalation block filed — no application defect blocked or corrupted this run.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
