# Mazda Trainer Report — Freezer Scanner

- **Document:** `scan_freezer_1784976616134245343_959158a8041b.jpg`
- **Scanner:** Freezer Scanner
- **Dispatch:** 2026-07-25T10:50:20Z
- **Mazda conversation:** `conv-18bc30f3-1d06-49d5-872a-b23a4bcdd8c5`
- **Facade result:** `doc_kind=unknown, confidence=0.0` → routed to STEP 0 self-classification.

## Verdict: **CORRECTED** (PASS after coaching — legitimate fail-closed outcome)

The document is Mom's check-payment ledger (Fifth Third Bank, handwritten annotations).
Mazda correctly classified it, correctly routed to the Mom-ledger reconciliation branch, and
correctly recorded a FAIL/no-store outcome once the reconciler tool itself proved broken. No
finance data was mis-stored. This is a genuine tool defect in `moms_ledger_reconciler.py`, not
a Mazda mistake, and it now has a filed proposal for a human/dev to fix.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 10:50:28 — returned `wrap-v045`, active | ✅ |
| STEP 0 classification (facade said `unknown`) | 10:50:40 — `classify_scan.py` → `{"doc_type":"moms_ledger","confidence":1.0,"merchant":"Fifth Third Bank"}` | ✅ correct routing decision |
| Mom-ledger reconciliation (`moms_ledger_reconciler.py`) | 1st attempt 10:54:50 — **HTTP 408, timed out at 240s**. 2nd attempt (after coaching) 11:10:59 with `timeout_sec=600` — ran to completion but returned `ok:false`, `OperationalError (1054): Unknown column 'expense_role' in 'where clause'` | ❌ tool defect, not recoverable by retry |
| `record_trace` | 11:16:45 — `trace_id=189`, `task_name="document-intake"`, `doc_kind="moms_ledger"`, `stored=false` | ✅ (after 2nd coaching) |
| `judge_trace` | 11:16:48 — verdict **FAIL** (`failed: ledger_rows_matched`, `failed: ledger_categories_reconciled`) | ✅ consistent with observed failure |
| `propose_improvement` | 11:16:54 — `proposal_id=141`, names the exact SQL defect (`expenses.expense_role` doesn't exist) | ✅ (after 2nd coaching) |
| Dashboard `/api/expense-stored` callback | 11:26:40 — `stored:0`, `expense_ids:[]`, `duplicate_expense_ids:[]`, `doc_kind:moms_ledger`, note cites `trace_id=189`/`proposal_id=141`; dashboard returned `{"ok":true}` | ✅ (after 3rd coaching) |

No storage occurred anywhere (correct — the reconciler never returned matched rows), so there
is nothing to cross-check against `expenses` table category/duplicate state.

## Wrapper defect diagnosed

**Tool bug, not a Mazda/model failure:** `tools/categorizer/moms_ledger_reconciler.py` queries a
nonexistent `expenses.expense_role` column, causing every `moms_ledger` document to fail
reconciliation with `OperationalError 1054`. This blocks the entire Mom-ledger branch until a
developer fixes the query (either the column was renamed/removed, or the reconciler was written
against a schema that never shipped). Mazda's `propose_improvement` (id 141) already names this.

**Secondary wrapper gap (now coached, should be made durable):** Mazda's instructions had no
guidance for what to do after an `executor_run` timeout or tool-level error — she stalled twice
(after the 240s timeout, and again after the SQL error) instead of retrying-once-then-failing-
closed. Recommend adding an explicit rule to her wrapper/memory: *"On an executor_run timeout,
retry once with a longer timeout_sec before treating it as a failure. On a tool return with
`ok:false`/a real error (not a timeout), do not retry the same command — proceed straight to
record_trace (stored=false) → judge_trace → propose_improvement → dashboard callback."*

## Coaching sent (3 messages, all in-conversation)

1. **10:56 (after ~15 min stall #1):** told her to retry `moms_ledger_reconciler.py` with
   `timeout_sec=600` instead of abandoning the run after the 240s timeout.
2. **11:16 (after stall #2, ~5 min post-SQL-error):** told her the retry hit a real (non-timeout)
   tool bug, not to retry again, and to complete `record_trace` → `judge_trace` →
   `propose_improvement` with a FAIL verdict for this document now.
3. **11:26 (after stall #3, ~7 min post-proposal):** told her the dashboard callback (step 9,
   required even on no-store/failed outcomes) was still missing and gave her the exact curl.

All three were addressed successfully; Mazda performed the real work herself in every case —
the Trainer never touched `rol_finances`, MySQL, or the dashboard callback endpoint directly.

## For a human to look at

- **Fix `moms_ledger_reconciler.py`**: remove/correct the `expense_role` column reference (see
  proposal 141, trace 189). Until fixed, every Mom-ledger scan will FAIL identically.
- Consider whether Mazda's wrapper should encode the "one retry on timeout, no retry on real
  error, always finish trace/judge/callback" rule directly rather than relying on Trainer
  coaching each time — this is the second/third time similar stalls have needed manual coaching.
