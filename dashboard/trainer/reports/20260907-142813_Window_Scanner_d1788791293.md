# Mazda Trainer report

Document: Window Scanner scan at `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1788791289993383420_3bdb8aaa3c58.jpg`  
Dispatch: 2026-09-07 14:28:13 UTC (`1788791293`)  
**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

## Evidence

- PASS — `load_wrapper_revision(agent_name="Mazda")` returned at 14:28:18; active revision was `wrap-v089` and its parse-blocked rules applied.
- PASS — STEP 0 classification returned `doc_type=receipt`, Walgreens, confidence `0.95` at 14:28:34.
- Parse blocked — receipt parsing returned `transaction_date=null`, merchant `,ebuey`, no items, `total_amount=null`, and `engine_failure.kind=no_receipt_identity`; the local fallback was internally inconsistent with the Walgreens classification. Mazda correctly stopped before vendor, duplicate, category, and storage instead of inventing financial data.
- PASS — `record_trace` returned trace `518` with `task_name="document-intake"` and truthful `stored=false`, `expense_id=null`, `duplicate_checked=false`, and parse-blocker problems.
- PASS — `judge_trace(518)` returned, but incorrectly reported `FAIL (failure_type=missed_vendor_key)` by requiring downstream stages that the active parse-blocked rules mark not applicable.
- PASS — `propose_improvement(trace_id=518)` returned proposal `282`; `apply_proposal` activated `wrap-v090`.
- PASS — dashboard callback returned `{"ok":true}` at 14:30:04 with this conversation/dispatch, `parsed=0`, `stored=0`, and no expense IDs. No expense or duplicate was proven.

## Diagnosis

No corrective Mazda message was sent. Her wrapper followed the fail-closed parse-blocker rule and the document did not yield trustworthy date, merchant, amount, or line-item evidence. The application judge/rubric is stale: it converted this expected parse-blocked outcome into `missed_vendor_key` instead of a review/non-applicable downstream verdict. The report is therefore FAIL because this intake did not produce a completed stored/duplicate receipt, while the failure attribution belongs to the judging application rather than Mazda.

## Escalation
repo_path: /home/adamsl/rol_finances
bug_description: |
  Trace 518 recorded a source parse blocker: Gemini could not extract a transaction date,
  and the local fallback returned merchant ",ebuey", null date, null total, and no items.
  Mazda's active wrap-v089 rules correctly stopped before vendor resolution, duplicate
  checking, categorization, and storage. However, judge_trace(518) returned
  "FAIL (failure_type=missed_vendor_key)" with failures for vendor, category, duplicate,
  and storage instead of treating those stages as not applicable and returning the
  parse-blocked review outcome. Expected behavior is a non-Mazda parse-blocked verdict
  (NEEDS_REVIEW or equivalent), with no demand to store unverified financial data.
  Suspected area: the document-intake judge/rubric in tools/self_improving_agent.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1788791289993383420_3bdb8aaa3c58.jpg
  failing_command: judge_trace(trace_id=518)

For a human: the callback was accepted but the dashboard event remains `status=processing`; verify the application status transition when the escalation is repaired. No finance database write was attempted by the Trainer.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
