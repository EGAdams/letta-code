# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786653208905239715_fe3f2f2bd59b.jpg`
**Dispatch time:** 2026-08-13T20:33:32Z (unix 1786653212)
**Conversation:** conv-5d6c6ea9-cd43-47ab-8223-4e35e9fa6533
**Run duration:** ~55s (20:33:32 → 20:34:27), no coaching needed

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Summary

Facade returned `doc_kind=unknown, confidence=0.0`, correctly triggering STEP 0. Mazda
classified the image herself and it turned out to be a California Secretary of State
government PIN access letter — not a receipt, invoice, statement, or tax document. Per
the contract's explicit routing rule, `doc_type=other` is unsupported, so she correctly
skipped receipt parsing, vendor/duplicate checks, categorization, and storage, then
completed trace/judge/callback anyway.

## Step-by-step checklist with evidence

1. **`load_wrapper_revision`** (20:33:36) — returned `wrapper_revision=wrap-v073`,
   `is_active=true`. ✅
2. **STEP 0 classification** — `executor_run` (20:33:58) ran
   `tools/classify_scan.py <image>`, returned `{"doc_type":"other","confidence":0.99,
   "merchant":"California Secretary of State","reason":"...PIN access letter for entity
   record online access, not a receipt, invoice, statement, or tax form."}`. Note
   Gemini vision quota was exhausted and classification succeeded via the OpenAI Codex
   CLI fallback — expected/allowed degraded path, correctly reported in trace
   `problems`. ✅
3. **Investigate/Categorize/Store** — correctly **not run**, per the contract's
   `doc_type=other` → unsupported routing (STEP 0 branch only requires
   parse/vendor/categorize/store for `receipt`/`invoice`). No receipt/statement/ledger
   evidence was misrouted. ✅
4. **`record_trace`** (20:34:08) — `task_name="document-intake"` exactly. Evidence JSON
   correctly reflects reality: `doc_kind:"other"`, `classification_confidence:0.99`,
   `vendor_key:null`, `category_id:null`, `duplicate_checked:false`, `stored:false`,
   `expense_id:null`, `unsupported_document:true`, `problems` names both the
   unsupported-document reasoning and the Gemini→Codex CLI fallback. Returned
   `trace_id: 373`. ✅
5. **`judge_trace(373)`** (20:34:12) — returned `verdict:"PASS"`, `failure_type:"none"`,
   `reasons:["all deterministic intake checks passed"]`. Consistent with what was
   observed — this is a correctly-handled non-storable document, not a broken stage. ✅
6. **Dashboard notify** (20:34:22) — `POST /api/expense-stored` with `stored:0,
   parsed:0, expense_id:null, doc_kind:"other", document_path` matching the dispatch,
   `conversation_id` matching. Returned `{"ok": true}`. ✅ Confirmed independently via
   `GET /api/expense-stored-events?since=1786653212`: one event, `conversation_id` and
   `dispatched_at` match this dispatch exactly, `stored:0`, `parsed:0`, `doc_kind:"other"`.
7. **`propose_improvement`** — correctly not called; verdict was PASS, no failure to
   propose against.

## Wrapper defect diagnosed

None. No coaching sent — the run needed none.

## Note for a human (informational only, not a failure)

The dashboard's own escalation watcher (`intake/trainer_escalation.py`) summoned this
Trainer run because the callback reported `stored:0`/`parsed:0` — its
`trainer_escalation_reason` field reads "callback reported no parsed or persisted
records." That heuristic is doing its job as designed (a zero/incomplete callback is the
correct trigger for Trainer review), and this particular case resolves as a legitimate
PASS: the document genuinely had nothing to store. No action needed beyond this
verdict being recorded.

No escalation filed — no application defect observed in this run.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
