# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1784742397999146531_d5aa8158f48a.jpg`
- **Dispatch**: 2026-07-22T17:46:41Z
- **Conversation**: conv-d630737a-a990-4a06-bb40-0fd5c05ab1c3
- **Verdict: CORRECTED**

## What the document was

MB MARKETS receipt (gas station/market), Grand Rapids MI, $35.00, 2025-03-20 — a genuine
re-scan of a document already stored earlier as `expense_id 1518`.

## Checklist

| Step | Evidence | Result |
|---|---|---|
| load_wrapper_revision | `wrap-v032` loaded 17:46:45 | ✅ |
| STEP 0 classify (facade returned `doc_kind=unknown`) | `classify_scan.py` → `doc_type=receipt`, confidence 1.0, merchant MB MARKETS | ✅ |
| Parse | `parse_and_categorize.py --json` → date 2025-03-20, amount $35.00, vendor_key derived `mb_markets` | ✅ |
| check_vendor_key | returned `recognized:false` (not in known-vendor map), no renormalized key to adopt | ✅ |
| check_duplicates | `in_database:false` at check time (placeholder id_light `scan`) | ✅ (see note below) |
| Categorize | `categorizer_main.py` — **all three fallback providers failed** (gemini timeout, chatgpt-oauth, anthropic) → `category_id: null` | ⚠️ infra issue |
| Store attempt | `parse_and_categorize.py --save` → `success:false, duplicate:true`, existing `expense_id 1518` — correctly refused to store | ✅ fail-closed |
| record_trace | `task_name=document-intake`; evidence correctly shows `is_duplicate:true, stored:false, expense_id:null`, problems list names vendor-unrecognized, null category, and the duplicate `expense_id 1518` | ✅ |
| judge_trace | `trace_id 124` → `NEEDS_REVIEW`, `failure_type:none`, reason "vendor_key resolved but not in known-vendor map" | ✅ (not FAIL, so no propose_improvement required) |
| Dashboard notify | first POST sent `duplicate_expense_ids:[]` (gap) → **coached, re-posted with `duplicate_expense_ids:[1518]`**, confirmed present in `/api/expense-stored-events` | ✅ after correction |

## Wrapper defects found & coached

1. **Dashboard callback dropped the duplicate's expense_id.** Trace evidence correctly
   identified the duplicate (`existing expense_id 1518`), but the `/api/expense-stored`
   callback sent `duplicate_expense_ids:[]`, which would make a correctly-detected duplicate
   look like an unexplained no-store on the Recent Report page. Sent a corrective message;
   Mazda re-posted the callback with `duplicate_expense_ids:[1518]` and confirmed on-record
   the rule for future runs (always forward the existing expense_id from a duplicate save
   result into `duplicate_expense_ids`).
2. **Attempted `--save` with an unresolved (`null`) category.** The standalone
   `categorizer_main.py` call failed across the entire fallback chain (gemini → chatgpt-oauth
   → anthropic all failed), returning `category_id: null`. Per the fail-closed contract, Mazda
   should have retried or escalated before attempting to store — instead she proceeded
   straight to `--save`. No bad data resulted only because the independent duplicate check
   also refused the save; this was luck, not correctness. Coached her to retry/escalate on a
   null category before attempting save in future runs.

## Infrastructure note (not a Mazda lesson)

All three categorizer providers (gemini, chatgpt-oauth, anthropic) failed in this run —
worth checking the "LLM Provider Fallbacks (Categorizer)" dashboard tile / `provider_health.json`
for a live outage, independent of this document's correct duplicate-refusal outcome.

## Human follow-up

None required — the document correctly remains un-stored (real duplicate of expense_id 1518),
and the dashboard record is now complete. Worth a glance at the categorizer provider-health
tile given all three providers failed on this run.
