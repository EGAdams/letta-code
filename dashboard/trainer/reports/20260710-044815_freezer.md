# Trainer Report — Freezer Scanner intake, 2026-07-10

- **Scanner**: Freezer Scanner
- **Scanned image**: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- **Dispatch time**: 2026-07-10T04:43:13Z (unix 1783658593)
- **Facade result**: `doc_kind=unknown, confidence=0.0, recommended_action=reject` — vision fallback (STEP 0) required.
- **Verdict: FAIL** (judged correctly by Mazda as `missed_vendor_key`, trace_id=41)

## Checklist vs. contract

| Step | Result | Evidence |
|---|---|---|
| 1. `load_wrapper_revision` | ✅ | 04:43:18 return: `wrap-v001`, `is_active: true` |
| 0. Vision classify+parse (`run_claude_code_sdk`) | ❌ | 4 attempts (04:43:51, 04:44:14, 04:46:46, 04:47:05), all returned `ERROR: Claude Code SDK exited 1: Claude Code CLI exited with code 1`, no stdout/stderr surfaced |
| 2. `check_vendor_key` / `check_duplicates` | not reached | blocked by STEP 0 failure |
| 3. `categorizer_main.py` | not reached | blocked |
| 4. store (`parse_and_categorize.py --save`) | not reached | blocked |
| 5. `record_trace` (`task_name="document-intake"`) | ✅ | trace_id=41 |
| 6. `judge_trace` | ✅ | verdict FAIL, failure_type `missed_vendor_key` |
| 7. `propose_improvement` (FAIL path) | ✅ | proposal_id=30 |
| 8. dashboard notify | n/a (correctly skipped — nothing stored) | — |

## What happened

Mazda picked up the dispatch immediately (4s after send) and logged the active wrapper
correctly. STEP 0 then failed on her first attempt at 04:43:51 with an opaque
`Claude Code SDK exited 1` error and empty stdout/stderr. She retried once on her own
(04:44:14, switching to `model: "haiku"`), got the identical error, and — correctly —
stopped and reported the failure in prose rather than fabricating a classification or
vendor key.

**Trainer's first hypothesis was wrong and worth recording**: her first two
`run_claude_code_sdk` calls sent a paraphrased natural-language task ("Classify and
parse the scanned JPEG receipt... Return JSON only") instead of the literal
`classify_scan.py`/`parse_and_categorize.py` commands the dispatch specified, and both
omitted the required `env={"PYTHONPATH": "/home/adamsl/rol_finances"}`. I coached her on
this (message at 04:46:23). She corrected course immediately and precisely — her next
two calls (04:46:46, 04:47:05) passed the exact literal commands plus the PYTHONPATH
instruction, in full compliance with the coaching. **Both still failed identically.**
This confirms the real cause is an executor/Claude-Code-SDK infrastructure problem, not
a wrapper-instruction-following defect — Mazda's original prose-paraphrasing was a real,
minor issue (now corrected and should persist via her memory), but it was not the root
cause of this particular run's failure.

I then instructed her to stop retrying and close the loop for this document (second
coaching message, 04:47:xx). She complied exactly: `record_trace` → `judge_trace`
(FAIL, `missed_vendor_key`) → `propose_improvement` (proposal_id=30), all in one clean
sequence with correct arguments. This is good, disciplined behavior under a real
infrastructure outage.

## Wrapper defect diagnosed

1. **(Minor, coached, self-corrected)** STEP 0 tasks sent to `run_claude_code_sdk`
   should carry the dispatch's literal shell command plus the exact `PYTHONPATH` env
   requirement, not a paraphrased natural-language ask. Mazda adopted this immediately
   when told; she should record it in her own memory (I asked her to) so it survives
   future scans without a human reminder every time.
2. **(Major, infrastructure, human action needed)** The Claude-Code-SDK executor at
   `:8799` is failing every `run_claude_code_sdk` invocation with a bare
   `Claude Code SDK exited 1` and zero stdout/stderr, regardless of task content or
   model (`sonnet` and `haiku` both failed). The lightweight `/claude_sdk_status` probe
   reports `ok:true, ready:true, sdk_present:true, claude_present:true,
   creds_present:true` — so the shallow health check is misleading; per the manual's
   §2.3 note, only an actual `reply PONG` round-trip proves real execution, and that
   round-trip is currently failing. I could not run that smoke test directly (missing
   bearer token) — **a human should SSH/check the executor logs on `100.80.49.10:8799`
   (or its `172.17.0.1:8799` fallback) to find the underlying CLI crash reason** (auth
   token expiry, `IS_SANDBOX`/permissions regression, or a broken venv/creds mount).

## Lessons sent to Mazda

1. (04:46:23) Pass literal dispatch commands + required env verbatim into
   `run_claude_code_sdk`, don't paraphrase; retry STEP 0 with exact commands.
2. (04:47:xx) Once confirmed the failure persists even with exact commands, stop
   retrying, and close the loop for this document via `record_trace` (documenting the
   executor failure in `problems`) → `judge_trace` → `propose_improvement`.

Both lessons were followed correctly and immediately.

## For a human

- **`scan_freezer.jpg` is unstored** and will need reprocessing once the executor is
  healthy — it currently sits in `incoming_scans/`, unprocessed.
- **Root-cause the executor's `Claude Code SDK exited 1` crash** at `100.80.49.10:8799`
  — the status endpoint is not catching this; consider tightening
  `frita_executor_health`/`claude_sdk_status` to include (or link to) a real
  invocation-level smoke test rather than only presence flags.
- Proposal 30 (trace 41, failure_type `missed_vendor_key`) is queued in the
  self-improvement loop for review — note its failure_type is a slight mismatch (the
  real cause was an executor outage, not a vendor-key miss); this may be worth a rubric
  refinement so infra outages get their own failure_type rather than borrowing
  `missed_vendor_key`.
