# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1784830091475517008_18c5bb6e9a0c.jpg`
- **Dispatch**: 2026-07-23T18:08:14Z
- **Mazda conversation**: `conv-434c5bec-72c5-4bc2-a390-1f32a04eaba1`
- **Verdict**: **CORRECTED** (initial run was solid but incomplete on one checklist item; coached and fixed within the same run)

## What happened

Deterministic facade returned `doc_kind: unknown`, so Mazda correctly went to STEP 0.

1. `load_wrapper_revision` → `wrap-v039` loaded. ✓
2. `classify_scan.py` → `doc_type: receipt`, confidence `0.99`, merchant `Burlington`. (Gemini vision hit a 429 quota error but the script still returned a usable classification.) ✓
3. `parse_and_categorize.py --json --engine=gemini` → Gemini 429'd, fell back to `chatgpt-oauth` (gpt-5.4) and parsed the receipt: Burlington, total `$13.77`, date `2022-03-27`. ✓
4. `check_vendor_key("burlington")` → not recognized, key adopted as-is. ✓
5. `check_duplicates(date=2022-03-27, amount=13.77, "Burlington")` → not a duplicate. ✓
6. `categorizer_main.py --provider=auto` → tried all three providers (gemini, chatgpt-oauth, anthropic); all three failed (gemini: internal `'query'` error; chatgpt-oauth: "Could not resolve authentication method"). Returned `category_id: null`. This matches the known **Categorizer LLM fallback chain** infra issue (see project memory `mazda_categorizer_provider_fallback_2026_07_20`) — not a Mazda defect.
7. `parse_and_categorize.py --save --engine=gemini` → ran a **second, independent** parse (Gemini 429'd again, fell back to chatgpt-oauth again). This second parse disagreed with the first on several low-confidence fields — date `2022-03-26` (vs `2022-03-27`), phone last digit, card last-4, item description — but totals matched (`$13.77`). Stored successfully: `expense_id 1542`, `id_light burlington_03_26_22_13_77`, `category_id: null`, `expense_status: NEEDS_VENDOR_KEY`, `pending_vendor_review: true`. This is the store script's own fail-closed pending-review path for an unrecognized vendor, not a bypass Mazda invented.
8. `itemize_existing_expense` → correctly declined: single line item equal to the charge is standalone, not itemized. ✓
9. `record_trace(task_name="document-intake", ...)` → trace 160 saved, fields consistent with the run. ✓
10. `judge_trace(160)` → **NEEDS_REVIEW** (reason: "vendor_key resolved but not in the known-vendor map"). Correctly *not* graded PASS given the null category_id, per the contract's rule that a stored receipt with null category_id must never be awarded PASS.
11. `curl POST /api/expense-stored` → confirmed `{"ok": true}`. ✓

## Defect found and coached

The duplicate check in step 5 used the **first** parse's date (`2022-03-27`), but the value that actually got stored (from the second, independent `--save` parse) was `2022-03-26` — a different date from a second non-deterministic LLM read of the same image. The contract requires a duplicate recheck against the *final* stored values whenever the parsed values change between the check and the store. Mazda had not done this before finishing her run.

**Lesson sent** (in-conversation, 18:12:37Z): re-run `check_duplicates` with the actual stored values (`id_light=burlington_03_26_22_13_77`, `date=2022-03-26`, `amount=13.77`, `Burlington`) and confirm no conflicting duplicate; recorded as a durable rule — always treat the *last* parse as ground truth and re-verify against it before trusting storage is final, rather than relying on an earlier parse's duplicate check.

**Mazda's correction** (18:12:44Z): re-ran `check_duplicates` on the final values → `is_exact_duplicate: true`, `exact_duplicate_expense_id: 1542` — resolves to the same row she just stored, so no conflict. She also called `propose_memory_note` to record the lesson (proposal 116, gates all `allow`).

## For a human

Nothing blocking. Two pre-existing infra issues surfaced again and are already tracked elsewhere, not new:
- Gemini free-tier quota (20 req/day) is being exhausted early in the day, forcing every vision/parse call onto the chatgpt-oauth fallback.
- The categorizer's chatgpt-oauth path is failing with "Could not resolve authentication method" — worth checking if this is the same OAuth-token issue documented in `mazda_categorizer_provider_fallback_2026_07_20`.

Expense 1542 sits `NEEDS_VENDOR_KEY` / `pending_vendor_review` — normal outcome for an unrecognized vendor, no action required unless someone wants to add "burlington" to the known-vendor map.
