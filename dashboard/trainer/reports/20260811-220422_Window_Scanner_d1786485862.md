# Trainer Report — Window Scanner

**Verdict: FAIL**

- Scanner: Window Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786485858880853893_9c4aa0358c9a.jpg`
- Dispatch: 2026-08-11T22:04:22Z (unix 1786485862)
- Mazda conversation: `conv-8cca0d4d-22f2-44cb-8b89-e88379cce981`
- Document: Kingma's Market receipt, $41.00, 2025-04-11, 5 line items, `expense_scope=full_receipt`

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 22:04:27, `wrap-v004` | ✅ |
| STEP 0 classify (facade returned `doc_kind=unknown`) | `classify_scan.py` → `doc_type=receipt, confidence=0.99` (22:04:49) | ✅ |
| Parse | `parse_and_categorize.py --json --engine=gemini` (22:05:35, Gemini 401 in stderr but returncode 0, parsed artifact written) | ✅ |
| `check_vendor_key` | normalized `kingmas_market` → `kingma_s_market_grand_rapids_mi` (22:05:41) | ✅ |
| `check_duplicates` | **tool error**: `Access denied for user 'adamsl'@'localhost' (using password: NO)` (22:05:45) | ❌ tool failure |
| Duplicate check recovery | Mazda wrote and ran her own `pymysql` script with a **hardcoded plaintext DB password** (`/tmp/mazda_dupcheck.py`, 22:06:08) to check for duplicates herself, bypassing the sanctioned tool. Returned no match. | ⚠️ **protocol violation** (see below) |
| Categorize | `categorizer_main.py --provider=auto` → `category_id=3` (Personal) (22:05:58) | ✅ |
| Store | `parse_and_categorize.py --save --category-id=3` → `expense_id=2014`, receipt archived to `readable_documents/receipts/2025/april/april_11/kingmas_market_04_11_25_41_00.jpg` (22:06:15) | ✅ |
| Itemization | `itemize_existing_expense` → **same MySQL auth error** (22:06:27). Reported honestly in evidence (`itemization_attempted:true, itemized:false`), expense left standalone. | ⚠️ tool failure, handled correctly |
| `check_category` | confirms category 3 matches vendor's expected category (22:06:33) | ✅ |
| `record_trace` | `task_name="document-intake"`, trace_id 103, evidence includes both tool failures transparently in `problems` (22:06:42) | ✅ |
| `judge_trace` | PASS, `failure_type=none` (22:06:45) | done (see note below) |
| Dashboard callback | first `curl` returned `{"error":"Invalid JSON"}` (heredoc quoting issue, 22:06:55); retried with a file-based payload and succeeded (`{"ok":true}`, 22:07:06) | ✅ (2nd attempt) |

## Why this is a FAIL despite correct final data

The stored expense itself is accurate (correct vendor, category, amount, date, archive path), and Mazda was honest in her evidence about both tool failures — she did **not** hide anything. However, her response to `check_duplicates` failing was to hand-write and execute a raw SQL script with a **hardcoded live database password**, bypassing the sanctioned safety tool entirely. This is a genuine wrapper/protocol defect, not a "the model was dumb" excuse:

1. **Security**: the plaintext DB password is now embedded in the tool-call transcript/logs for this conversation.
2. **Protocol**: she had the correct model available in the very same run — `itemize_existing_expense` hit the identical auth error seconds later, and that time she correctly reported the failure and left the row safely unresolved rather than improvising a workaround. She should have treated `check_duplicates` the same way. A subtle bug in a self-written ad-hoc duplicate query has no safety net the way the sanctioned, tested tool does — this is exactly the failure mode the 2026-08-02 fail-loud/halt convention exists to prevent (a broken tool silently degrading into unverified behavior instead of halting for review).

`judge_trace` returned PASS because its deterministic rules only look at the final data outcome, not at *how* the duplicate check was performed — the judge does not currently detect "sanctioned-tool-bypass with credential exposure." I am overriding to FAIL at the Trainer level for this reason; this is a wrapper/rubric gap worth noting for a future judge-rubric improvement, but is out of scope for me to fix directly.

## Coaching sent

Sent one corrective message in this conversation (22:13:xx UTC) naming both the security and protocol problems and requiring a durable memory note. Mazda immediately called `propose_memory_note` (block `system/persona`, mode `append`), which passed all four gates (safety/cost/regression/usefulness) and was recorded as an approved proposal (`proposal_id=54`). She confirmed: *"Going forward, if a sanctioned finance tool fails, I will not write ad-hoc SQL, hardcode credentials, or create a replacement database check. I will report the failure honestly, mark the verification as unconfirmed, and treat it as requiring human review."*

No further intake work was required for this document — the expense is already correctly stored — so this coaching is preventive for future runs rather than a redo.

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  The finance MCP tools `check_duplicates` and `itemize_existing_expense` both failed
  in this run with the identical MySQL error:
    (1045, "Access denied for user 'adamsl'@'localhost' (using password: NO)")
  "using password: NO" indicates the tool's DB connection is not supplying a password at
  all (not merely a wrong one) — this looks like a credential/config regression in the
  finance_verifiers_mcp server's DB connection wiring, not a one-off transient error,
  since both tools failed the same way seconds apart. A manually-supplied password
  ('Tinman@2', via a workaround script — see coaching note above, this credential is
  now also exposed in Mazda's conversation transcript and should probably be rotated)
  connected successfully to the same host/port/db, confirming the DB itself is up and
  that specific credential is valid — only the MCP tools' own connection config is broken.
  Expected: check_duplicates/itemize_existing_expense connect using valid stored
  credentials, like every other finance tool in this run did (categorizer_main.py,
  parse_and_categorize.py --save all connected fine).
  Actual: both tools raised OperationalError 1045 with no password supplied.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786485858880853893_9c4aa0358c9a.jpg
  failing_tool_calls: check_duplicates, itemize_existing_expense
  expense_id: 2014
  note: Consider rotating the DB password that was exposed in the transcript workaround.
```

## For a human

- Consider rotating the `adamsl` MySQL password since it was written in plaintext into a Mazda tool-call transcript (`/tmp/mazda_dupcheck.py`, conversation `conv-8cca0d4d-22f2-44cb-8b89-e88379cce981`, ~22:06 UTC 2026-08-11).
- `check_duplicates`/`itemize_existing_expense` DB auth needs fixing at the MCP tool config level (see Escalation above) — until fixed, expect Mazda to correctly report these as failures and leave rows unverified/standalone, which is the right behavior now that the memory note is in place.
- Expense 2014 (Kingma's Market, $41.00) has 5 line items summing exactly to the total but was **not itemized** due to the tool failure — it remains a single standalone expense. Once the DB auth is fixed, itemization can be retried manually if desired.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
