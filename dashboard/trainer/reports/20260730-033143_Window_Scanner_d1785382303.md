# Trainer Report — Window Scanner

**Scanned image:** `window_scan_1785382300364792143_7bf5fd8357a3.jpg`
**Dispatch:** 2026-07-30T03:31:43Z
**Conversation:** `conv-6ab36ef5-1867-4b9a-a0ba-0d1fdb667cfc`
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Facade result
`doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required.

## Checklist (all evidence from successful tool returns, timestamps ≥ dispatch)

1. `load_wrapper_revision(Mazda)` → 03:31:50, `wrap-v066` loaded. ✅
2. STEP 0 `classify_scan.py` → 03:32:16, `doc_type=receipt, confidence=0.98, merchant="Intercessors for America"` (Gemini quota-exhausted 429 on first attempt, tool self-recovered/retried and returned a valid classification anyway — no fallback-provider field surfaced, but the call still returned a real, high-confidence result rather than erroring). ✅
3. Receipt parse `parse_and_categorize.py --json` → 03:32:40. `transaction_date=2025-03-18, total_amount=30.50, expense_scope=full_receipt, handwritten_arithmetic_total=null` — no marked-item or handwritten-math scope issue on this document; full-receipt scope is correct. ✅
4. `check_vendor_key(intercessors_for_america)` → 03:32:44, `recognized=true`. ✅
5. `check_duplicates(2025-03-18, 30.50, INTERCESSORS FOR AMERICA)` → 03:32:49, fuzzy duplicate against `check_11051_03_18_25_30_50` / expense id 1547 (same date+amount, existing row identified only by a check number, no payee — correctly flagged as needing confirmation rather than auto-treated as exact). ✅
6. Mazda queried the existing expense 1547 directly (id 1547, category_id 190 "Gifts & Love Offerings", `receipt_url` + `document_url` already populated from an earlier scan/statement) before deciding not to store — good diligence. ✅
7. Categorizer (`categorizer_main.py --provider=auto`) → 03:33:37, `category_id=190`, consistent with the existing expense's category (not used to store anything here, but confirms no conflicting category signal). ✅
8. `record_trace(task_name="document-intake")` → 03:33:47, trace_id 300. Fields correct for a duplicate-only run: `is_duplicate=true, stored=false, expense_id=null`, `duplicate` evidence names expense 1547 and states the reason (check-number-only existing row, no vendor evidence against the match) plus that the new scan wasn't attached as an alternate receipt without human verification. ✅
9. `judge_trace(300)` → 03:33:52, **PASS**, `failure_type=none`. ✅
10. Dashboard callback → 03:34:02, `POST /api/expense-stored` returned `{"ok": true}`, `duplicate_expense_ids:[1547]`, `stored:0`. ✅ (fired even though nothing was stored, as required.)

## Independent spot-check (Trainer, not Mazda)
Fetched `http://localhost:8765/supporting-document/1547/receipt` — served 573,533 bytes vs. 553,299 bytes for the raw scan on disk, confirming the annotator actually boxed the matching line rather than serving the file unchanged. Receipt dialog visibility for expense 1547 is intact.

## Wrapper defects found
None. No coaching sent.

## Notes for a human
- The callback's `outcome` field was hardcoded to `"EVIDENCE_ATTACHED"` even though nothing was attached this run (it was a no-op duplicate). Cosmetic/naming-only — the `stored:0` and `duplicate_expense_ids` fields carry the real signal — but worth tightening if that field is ever surfaced to EG directly.
- Gemini vision hit a 429 quota error on the first `classify_scan.py` call but still returned a usable classification in the same tool return (unclear whether from a fallback provider or a successful retry within the same call) — worth confirming which, so a future truly-exhausted-quota run doesn't silently degrade to an unvalidated guess.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
