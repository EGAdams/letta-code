# Mazda Trainer Report — Window Scanner

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

- Scanner: Window Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786726688673995245_7315e6db6343.jpg`
- Dispatch: 2026-08-14T16:58:12Z (unix 1786726692)
- Conversation: `conv-37edf766-810a-4259-8256-d926b9c16ccd`
- Facade result: `doc_kind=unknown`, `confidence=0.0` → STEP 0 required.
- Total run time: ~81s (16:58:16 → 16:59:33).

## Step-by-step evidence

1. **`load_wrapper_revision`** (16:58:16) → `wrap-v075`, active. ✅
2. **STEP 0 classification** (16:59:08) — `executor_run` of `classify_scan.py`:
   ```
   {"doc_type": "other", "confidence": 0.98, "merchant": "Children's Vision Int. Inc.",
    "reason": "It is a charitable giving acknowledgement listing donations, not a
    purchase receipt, invoice, bank statement, or tax form."}
   ```
   stderr shows a Gemini 429 quota hit; classifier fell through to its next provider
   and still returned a clean, high-confidence result. Per the contract, `doc_type=other`
   is the one explicitly unsupported classification — no receipt/invoice/statement/ledger
   branch should run, and none did. ✅
3. **`record_trace`** (16:59:18) — `task_name="document-intake"`, evidence JSON has
   `doc_kind:"other"`, `stored:false`, `expense_id:null`, `duplicate_checked:false`,
   `category_id:null`, and `problems` correctly stating *"Unsupported document... Receipt,
   invoice, statement, and ledger storage branches were intentionally not run"* plus the
   Gemini→fallback note. Accurate to what actually happened. ✅
4. **`judge_trace(396)`** (16:59:23) → `PASS`, `failure_type:none`, "all deterministic
   intake checks passed". ✅ Consistent with the observed evidence — a correctly-detected
   unsupported document is a PASS outcome, not a failure.
5. **Dashboard notify** (16:59:33) — `POST /api/expense-stored` with `stored:0`,
   `expense_id:null`, `doc_kind:"other"`, correct `conversation_id`/`dispatched_at`. ✅
   Confirmed via `GET /api/expense-stored-events?since=1786726692`: one event, same
   `conversation_id` and `dispatched_at` as this dispatch.
6. **`propose_improvement`** — not called, correctly, since the verdict was PASS.

No expense was stored, so the supporting-document/dialog-visibility and red-box
annotation checks do not apply to this run (nothing to view).

## Wrapper defect

None found. Mazda followed the full contract in order, made the correct fail-closed
classification call, and reported honestly. No coaching message was needed or sent.

## For a human

The dashboard's own escalation logic (`trainer_escalation_reason: "callback reported no
parsed or persisted records"`) is what summoned this Trainer session — that trigger fires
on any `stored:0` callback, which correctly includes legitimate no-store outcomes like this
one. Nothing to act on here; document was, in fact, not intake-relevant (a nonprofit
donation acknowledgement letter, not a receipt/invoice/statement).

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
