# Trainer Report — Window Scanner

**Document:** `window_scan_1786494483499994837_4caec35fd6f9.jpg` (Walgreens receipt, 2025-04-18, $151.61)
**Dispatch:** 2026-08-12T00:28:06Z
**Conversation:** conv-9d3d0fef-a7a8-47b8-b051-07b05187ff4e

**Verdict: PASS**

## Checklist / evidence

1. `load_wrapper_revision` — `wrap-v005`, 00:28:10. ✅
2. Facade returned `doc_kind=unknown`, so STEP 0 required. `executor_run classify_scan.py` (00:28:33) → `doc_type=receipt, confidence=0.99` (Gemini 401'd, fell back to OpenAI Codex CLI cleanly). ✅
3. `parse_and_categorize.py --json --engine=gemini` (00:28:58) → Gemini 401'd, fell back to `chatgpt-oauth`/gpt-5.4. Two line items ($9.85 + $141.76 = $151.61), `expense_scope=full_receipt`, no marks/handwritten arithmetic on this document — correct scope, `parse_artifact_verified=true` carried into the later save call. ✅
4. `check_vendor_key` → `walgreens`, recognized. ✅
5. `check_duplicates` **errored** (`1045 Access denied for user 'adamsl'@'localhost'`). Per wrapper Rule 3 (proposal 41), Mazda ran an equivalent authenticated duplicate check via `executor_run` (raw `pymysql` SELECT) and found existing expense **id 982**, same date/amount/id_light (`walgreens_04_18_25_151_61`), already carrying `receipt_url=walgreens_04_18_25_151_61.jpg`, `category_id=378`. `duplicate_checked=true` recorded only after this succeeded. ✅
6. `categorizer_main.py --provider=auto` → `category_id=243` ("RM — Priority Health"). `check_category` flagged the mismatch (`243 != expected 378 for vendor 'walgreens'`). Mazda correctly did **not** trust the categorizer output — she used the vendor-map's expected `category_id=378` (which also matches the existing row's category) in the save attempt. ✅
7. `parse_and_categorize.py --save --category-id=378` → **crashed**: `(1062, "Duplicate entry 'walgreens_04_18_25_151_61' for key 'expenses.ux_expenses_id_light'")`, after moving the incoming scan into the canonical `readable_documents/receipts/2025/april/april_18/` path. Mazda then read back expense 982 via a read-only SQL SELECT and confirmed the canonical file and `receipt_url` were already correctly in place from a prior run — nothing needed to change. No second row was inserted. See Escalation below.
8. `record_trace` (`task_name=document-intake`, trace 117) — honest evidence: `stored=false`, `is_duplicate=true`, `expense_id=982`, `duplicate_checked=true`, `intake_halted=false`, and a `problems` list that transparently documents both tool defects (auth failure + save-crash) and how she worked around each. ✅
9. `judge_trace(117)` → **PASS**, "all deterministic intake checks passed". ✅
10. Dashboard callback `POST /api/expense-stored` → `{"ok": true}`, `expense_id:982, duplicate_expense_ids:[982], stored:0, outcome:EVIDENCE_ATTACHED`. ✅

## Supplementary check (red-box / dialog visibility)

`GET /supporting-document/982/receipt` → HTTP 200, 413,851 bytes vs. 409,100 bytes for the raw file on disk — confirms the annotator produced a highlighted copy, so **View Receipt** is genuinely available for expense 982.

## Wrapper defect diagnosed

None requiring coaching this run — Mazda followed the wrapper's own Rule 3 fallback correctly for the `check_duplicates` auth failure, and independently declined to trust a categorizer result that conflicted with the vendor map (good judgment, not covered by any explicit rule but consistent with "never invent... database matches").

No corrective message was sent to Mazda.

## Escalation

`repo_path: /home/adamsl/rol_finances`
`bug_description:` `parse_and_categorize.py --save` does not handle the case where the target `id_light` already exists. Instead of returning a graceful duplicate-safe result (create-if-missing / attach-or-upgrade `receipt_url` on the matched expense, per the intake contract's "a duplicate result... is success" language), it lets an unhandled MySQL `IntegrityError` (`1062, Duplicate entry ... for key 'expenses.ux_expenses_id_light'`) propagate after already moving the incoming scan file into the canonical archive path. In this run the net effect was harmless (the file/`receipt_url` were already correctly in place from an earlier run), but in a case where the *existing* row was missing a `receipt_url` or needed upgrading, this crash would silently skip that upgrade — Mazda has no tool path to complete it without ad-hoc SQL, which she is not supposed to write. Suggest catching the unique-constraint violation on `id_light` and returning something like `{"success": true, "duplicate": true, "linked_counterpart": true}` (attaching/upgrading `receipt_url` on the matched row) instead of raising.
`metadata:`
  `document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786494483499994837_4caec35fd6f9.jpg`
  `failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=378 --parsed-json /tmp/mazda_receipt_bfbba2f8b1ac.json --engine=gemini`

## For a human

Also note `check_duplicates`'s generic DB credentials (`adamsl`@`localhost`, no password) are still broken — this is the second consecutive run needing the Rule-3 authenticated-fallback workaround; worth fixing the credential/tool directly rather than relying on Mazda's per-run bypass indefinitely.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
