# Trainer Report — Window Scanner

- **Scanner:** Window Scanner
- **Document:** `scan_1784570447373542162_10e1c22d21eb.jpg`
- **Dispatch:** 2026-07-20T18:00:50Z
- **Mazda conversation:** `conv-b8c377bb-a49a-443c-9bc9-3e8367031392`
- **Verdict: PASS**

## Document identity (from this run's evidence)

Annual donation-summary receipt from **Children's Vision Int. Inc.** (Michigan
Corporations Division / nonprofit licensing vendor), 10 line items, total
**$3,047.00**, dated 2026-01-06. Facade returned `doc_kind=unknown` (expected
for a JPEG scan with no extractable text) — Mazda correctly ran STEP 0 vision
classification herself.

## Checklist — evidence observed

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v030`, active | ✅ |
| STEP 0 classify | `classify_scan.py` → `doc_type=receipt`, confidence 0.95 | ✅ |
| STEP 0 parse | `parse_and_categorize.py --json` → merchant, date, 10 items | ✅ |
| STEP 2a `check_vendor_key` | supplied `childrens_vision_int_inc` → normalized to `michigan_corporations_division`; Mazda adopted the normalized key in all later calls | ✅ |
| STEP 2b `check_duplicates` | placeholder id_light returned `in_database:false` (expected — pre-store guard is coarse) | ✅ |
| STEP 3 categorize | `categorizer_main.py` → `category_id=357` (Licensing) | ✅ |
| STEP 4 store | `parse_and_categorize.py --save --category-id=357` → final duplicate guard caught it: `duplicate:true, existing.expense_id=1502`. Correctly treated as the result, no `--allow-duplicate` retry | ✅ |
| STEP 5 `record_trace` | `task_name=document-intake`, evidence JSON with `is_duplicate:true, stored:false, duplicate_checked:true` | ✅ (see note) |
| STEP 6 `judge_trace` | trace 116 → **PASS**, `failure_type=none` | ✅ |
| STEP 8 dashboard notify | first attempt failed (`HTTP 400: No closing quotation` — apostrophe in "Children's" broke out of a single-quoted curl `-d` payload); **Mazda self-corrected** on the next turn by rebuilding the payload with `python3 -c ... json.dumps(...)` piped to curl, which succeeded (`{"ok": true}`). Confirmed via `/api/expense-stored-events`: event present, `conversation_id`/`dispatched_at` match this run, `stored:0`, `duplicate_expense_ids:[1502]` | ✅ (self-healed, no coaching needed) |

## Minor observation (not corrected — cosmetic, not blocking)

Both `record_trace`'s evidence JSON and the STEP 8 callback set top-level
`expense_id: 1502` (the pre-existing duplicate's id) instead of `null`. The
Trainer contract calls for `expense_id=null` on a duplicate-only run, with the
existing id carried only in duplicate-evidence/callback fields — which she
also did correctly (`duplicate_expense_ids:[1502]`, `problems:[...]`,
`stored:false`). The duplicate id also appearing in the primary `expense_id`
field is a small deviation, not a data-correctness issue: nothing was
double-counted, and the dashboard event's `stored:0`/`expense_ids:[]` fields
are unambiguous. Not worth a corrective round for this run; worth folding
into the next wrapper-instruction pass if it recurs.

## Wrapper defect diagnosed

None requiring intervention. The STEP 8 quoting failure is a recurring class
of bug (apostrophes inside merchant/description strings breaking naive
single-quoted shell `-d` payloads) — Mazda's own fix (build the JSON in
Python and let `json.dumps` handle escaping, then `subprocess.run` with an
argv list) is exactly the durable pattern. Since she applied it unprompted
and it isn't yet captured as a learned rule, a human may want to seed this
as a proposal/instruction so future runs use the Python-payload pattern for
STEP 8 by default rather than rediscovering it after a failed attempt.

## Coaching sent

None. No corrective message was required — Mazda completed the full
contract, including self-recovering from the one tool-execution error.

## For a human

Nothing urgent. Consider proactively hardening the STEP 8 dispatch template
project-wide (build the curl payload via Python/`json.dumps` instead of an
inline single-quoted shell string) so future runs don't hit the same
avoidable 400 before self-correcting.
