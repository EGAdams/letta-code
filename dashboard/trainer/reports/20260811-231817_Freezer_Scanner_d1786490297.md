# Mazda Trainer Report — Freezer Scanner

Verdict: CORRECTED

- Scanner: Freezer Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786490294569530474_ec40dc9878fe.jpg`
- Dispatch: 2026-08-11T23:18:17Z
- Conversation: `conv-f197a37b-21b7-454a-aae9-0f98e6ebbb86`

## What the document is

A dental claim/EOB (Explanation of Benefits) summary from DR DANIEL ROBERTS PLLC — not a
purchase receipt, invoice, bank statement, or tax form. Facade returned `doc_kind=unknown,
confidence=0.0` (expected for a JPEG scan). Mazda correctly routed to STEP 0 vision
classification.

## Checklist (with evidence)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v004` loaded. ✅
2. STEP 0 classification: `executor_run` → `classify_scan.py` via `executor_run` (never
   `run_claude_code_sdk`), `PYTHONPATH` passed via `env` as required. Gemini failed with a
   401 (`ACCESS_TOKEN_TYPE_UNSUPPORTED`); tool correctly fell back to the OpenAI Codex CLI
   and returned `doc_type=other, confidence=0.98, merchant="DR DANIEL ROBERTS PLLC"`,
   reason: "dental claim/EOB summary ... not a purchase receipt, invoice, bank statement,
   or tax form." ✅ Correct classification and correct fallback chain.
3. Routing decision: `doc_type=other` → genuinely unsupported document, per the intake
   manual's explicit-routing rule. Vendor/category/duplicate/storage steps correctly
   skipped. ✅
4. `record_trace` (trace 110, `task_name="document-intake"`) — **defect**: logged
   `doc_kind="unknown"` instead of the actual `classify_scan.py` result `doc_type="other"`.
5. `judge_trace(110)` → **FAIL**, `failure_type=unsupported_document`, because the
   `doc_kind="unknown"` mislabel made the judge require
   vendor_key_resolved/category_resolved/duplicate_check_ran/expense_stored.
6. `propose_improvement` (proposal 59) → `apply_proposal` → **activated as `wrap-v005`**
   (Rule 4: always record the real `classify_scan.py` doc_type, never the facade's
   `unknown`, when unsupported). Self-improvement loop worked correctly and immediately.
7. Dashboard callback `POST /api/expense-stored` — already correct on the first attempt:
   `doc_kind:"other"`, `status:"unsupported_document"`, `stored:0`, `parsed:0`. ✅

## Coaching sent

Sent one corrective message: apply the newly-activated Rule 4 retroactively to *this*
document — re-`record_trace` with `doc_kind="other"` (all other fields unchanged,
`wrapper_revision="wrap-v005"`) and re-`judge_trace`, without resending the (already
correct) dashboard callback.

Mazda complied exactly: **trace 111** recorded with `doc_kind="other"`. But
`judge_trace(111)` **still returned FAIL** (`failure_type="missed_vendor_key"`, same four
reasons), proving the judge rubric itself never implements the "other is exempt" carve-out
that Rule 2 (proposal 40, 2026-08-06) assumed was already active. This is not a wrapper
defect fixable by coaching Mazda further — the fault is in the judge/evaluation code.

Mazda also filed `propose_memory_note` (proposal 60, approved but not live-applied — "no
live-memory applier wired") restating the doc_kind rule for her own memory. Fine as
belt-and-suspenders; the wrap-v005 activation is what actually matters going forward.

## Verdict rationale

Mazda's actual intake behavior was correct end-to-end: right classification tool, right
fallback chain, right routing decision (unsupported → skip vendor/category/duplicate/
storage), right dashboard callback. The only real problem — the stale `doc_kind` on the
first trace — was self-diagnosed and wrapper-fixed within this same run before I even
coached her. Grading this **CORRECTED**, not FAIL: the intake outcome for this document is
complete and correct; the residual FAIL verdict on trace 111 is a rubric bug outside her
control (escalated below), not evidence Mazda mishandled the document.

## Escalation

repo_path: /home/adamsl/rol_finances (tools/self_improving_agent — the judge/evaluation
factory backing `judge_trace`)
bug_description: |
  `judge_trace` unconditionally requires vendor_key_resolved, category_resolved,
  duplicate_check_ran, and expense_stored evidence, even when the trace's own `doc_kind`
  is "other" (a document explicitly and correctly classified as unsupported for intake).
  Reproduced twice on the same document in this run:
    - trace 110: doc_kind="unknown" (mislabeled) -> judge FAIL, failure_type=unsupported_document
    - trace 111: doc_kind="other" (corrected) -> judge STILL FAIL, failure_type=missed_vendor_key,
      same four reasons (vendor_key_resolved/category_resolved/duplicate_check_ran/expense_stored)
  Expected: when doc_kind="other" (or any unsupported classification), the judge should
  exempt vendor/category/duplicate/storage checks and score the trace as PASS (or a
  distinct "correctly rejected" verdict), matching the intent already written into Mazda's
  active wrapper as Rule 2 (proposal 40, applied 2026-08-06): "ensure the intake rubric
  does not require vendor/category/duplicate/storage checks" for doc_type=other. That rule
  has apparently never been true on the judge side — Rule 2 documents an intent that was
  never implemented in the evaluator/verdict-judge code, so every unsupported-document run
  has likely been misgraded FAIL since 2026-08-06.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786490294569530474_ec40dc9878fe.jpg
  failing_command: judge_trace(trace_id=111) via Mazda's Letta tool, doc_kind="other"
  trace_ids: [110, 111]
  proposal_ids: [59 (activated, wrap-v005, fixes the trace-recording side), 60 (memory note, approved not live-applied)]

## For a human

- The judge/evaluator rubric bug above should be fixed in `rol_finances` (likely
  `implementations/evaluation/` or the `IEvaluationFactory`/verdict-judge code in
  `tools/self_improving_agent`) so `doc_kind="other"` (or an explicit
  unsupported-classification flag) short-circuits the vendor/category/duplicate/storage
  requirements. Until fixed, every unsupported-document run (dental EOBs, non-finance
  paperwork, etc.) will keep scoring FAIL despite Mazda handling them correctly — this
  will pollute the self-improvement evidence store with false failures and may trigger
  spurious future proposals chasing a non-bug.
- No expense was stored and none was expected to be — this document is out of scope for
  finance intake by design. No action needed on the document itself.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
