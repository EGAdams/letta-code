# Trainer Report — Window Scanner

**Document:** `window_scan_1786645547824210427_e4259bcbbce9.jpg` (DTE utility bill payment coupon, 1854 Gratiot St NW, Grand Rapids MI)
**Dispatch:** 2026-08-13T18:25:52Z · conv `conv-3563c425-f863-434b-b90a-0a9f38269bf6`
**Facade:** `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required.

**Verdict: CORRECTED**

## Timeline / checklist

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | ✅ 18:25:58, wrap-v072 |
| STEP 0 classify (`classify_scan.py` via `executor_run`) | ✅ 18:26:26, `doc_type=receipt, confidence=0.89` (Gemini quota exhausted → Codex fallback) |
| `parse_and_categorize.py --json` | ✅ 18:27:01, but **artifact internally inconsistent**: items summed to -196.94, `total_amount` reported -413.73 (defect #1) |
| `check_vendor_key` + `check_duplicates` | ✅ 18:27:13, batched — `dte` unrecognized, no duplicate |
| `categorizer_main.py` | ✅ 18:29:38, returned `category_id=null` (all 3 providers failed to resolve generic `dte`) |
| Save attempt | ❌ 18:29:48, `parse_and_categorize.py --save` attempted **despite null category** (defect #2), failed on `chk_expense_amount_pos` (negative amount) — correctly fail-closed by luck, not by design |
| Quarantine | ✅ moved to `_needs_review/dte_05_13_25_-413_73.jpg` |
| `record_trace` (369) / `judge_trace` | ✅ FAIL (`category_resolved`, `expense_stored`) — genuine, accurately self-reported |
| `propose_improvement` → `apply_proposal` | ✅ proposal 236 activated as wrap-v073 |
| Dashboard callback | ✅ `stored:0` |

**Trainer coaching round 1** (18:34:55, landed): flagged (1) missing parse self-consistency check (item-sum vs. reported total), (2) the correct document model — only the current-period charge (Gas Commercial Heating $53.06, 2025-05-13) is a valid expense; the "Payment Received -$250.00" line is a stale prior-period payment, not part of this receipt, and (3) the save-before-category-resolved ordering bug.

Mazda's response (18:35–18:37): reparsed isolating the single $53.06 line, verified reconciliation with `verify_statement_totals` (Python-exact, `matches:true`), re-ran `check_vendor_key`/`check_duplicates` for the corrected amount (no duplicate), resolved category via `categorizer_main.py` **before** attempting save this time, saved successfully (`expense_id=2071`), recorded trace 370, `judge_trace` → **PASS**, dashboard callback `stored:1`. She also filed `propose_memory_note` (proposal 237) capturing both lessons.

**Trainer coaching round 2** (18:39:42, landed, distinct defect): her corrected run used `vendor_key=dte_energy_0020 → category_id=123 (Church Electric Bill)` with no supporting evidence — and her *own* trace-369 DB query already showed expense `id 489, "DTE - DTE for 1854 Gratiot", category_id 311` (same property address as this coupon), matching `dte_energy_0544 → category 311` in the vendor map, not `_0020`. This also violated the wrap-v073 rule she'd just had activated ("resolve an account-specific key only when the account is authoritative").

Mazda's response (18:40): re-queried the DB for `1854 Gratiot`/`dte_energy_0544` evidence, confirmed the match, corrected expense 2071 via the existing `update_expense_category.py` tool (123 → **311, "Housing Gas Bill"** — a materially better fit for a gas-heating charge than "Church Electric Bill"), re-verified the persisted row, recorded trace 372, `judge_trace` → **PASS**, dashboard callback resent with corrected vendor_key/category, and filed a second `propose_memory_note` (proposal 238) generalizing the lesson (cross-check property/address against existing categorized rows before picking among multiple known sub-account keys for the same vendor).

## Final stored state (verified)
`expense_id=2071`, date `2025-05-13`, amount `$53.06`, vendor_key `dte_energy_0544`, category `311 (Housing Gas Bill)`, `receipt_url=dte_05_13_25_53_06.jpg`, no duplicate, DB row independently re-read and confirmed by Mazda after the correction.

## Wrapper defects diagnosed (both fixed in this run, both memory-noted)
1. No self-consistency check between a parse artifact's own item sum and its reported total before treating it as verified.
2. Save was attempted while `category_id` was still null, relying on an unrelated DB constraint to fail closed instead of blocking explicitly.
3. Multi-account vendor key selection had no evidence-gathering step (property/address cross-check against existing categorized rows) before picking among known sub-account keys — led to a wrong initial category (123 instead of 311).

All three are captured in her `propose_improvement`(236)/`propose_memory_note`(237, 238) proposals for future runs.

## For a human — dashboard defect, not Mazda's fault
`GET /supporting-document/2071/receipt` returned bytes **byte-identical** (md5 `6ca3c5ce1ff7632f99628be1bec8f20a`) to the raw file on disk, and no matching file exists under `dashboard/.cache/document-annotations/` for expense 2071 — i.e. **View Receipt is unboxed** for this expense. This document is a printed utility-bill payment coupon (single-line letterhead/table layout), not a typical itemized POS receipt and not the check-style/handwritten layout the 2026-08-08 `CodexCliImageRegionFallbackMatcher` fix targeted, so this looks like a genuinely uncovered document layout for the red-box annotator rather than a recurrence of the fixed bug. I could not retrieve the annotator's exact failure `reason` string (no server-log access in the Trainer's read-only toolset). Per the contract, this does not affect the top-line Verdict.

## Escalation
repo_path: /home/adamsl/letta-code
bug_description: |
  ImageExpenseDocumentAnnotator (dashboard/document_annotation.py) failed to produce a
  red-box-annotated copy for expense_id 2071's receipt. GET /supporting-document/2071/receipt
  served bytes identical (md5 6ca3c5ce1ff7632f99628be1bec8f20a, size 776623) to the raw file
  at readable_documents/receipts/2025/may/may_13/dte_05_13_25_53_06.jpg, and no cache file
  exists under dashboard/.cache/document-annotations/ for expense 2071. Expected: an annotated
  copy boxing the matched row/amount ($53.06, Gas Commercial Heating, 2025-05-13). Actual:
  original file served unchanged. Suspected cause: the document is a printed utility-bill
  payment coupon (single line item, company letterhead/table layout) — a document class
  distinct from both the standard itemized POS receipt case and the check-style/handwritten
  case fixed 2026-08-08 by CodexCliImageRegionFallbackMatcher, so it may fall outside both
  matchers' coverage. Could not confirm the exact annotator failure reason/log line from the
  Trainer's read-only toolset.
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/may/may_13/dte_05_13_25_53_06.jpg
  failing_command: GET http://localhost:8765/supporting-document/2071/receipt

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
