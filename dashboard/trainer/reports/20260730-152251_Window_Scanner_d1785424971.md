# Trainer Report — Window Scanner

- **Document**: `window_scan_1785424967228605740_aacf92c32f7c.jpg` (Intercessors for America donation acknowledgment letter, $30.50, 2025-03-18)
- **Dispatch**: 2026-07-30T15:22:51Z
- **Conversation**: `conv-934e3a77-ec3f-44e5-819b-c6ff3db66e0a`
- **Verdict: CORRECTED** (initial run FAIL on supporting-document handling; corrected within the same conversation after one coaching message)

## Facade result
`doc_kind=unknown, confidence=0.0, recommended_action=reject` → STEP 0 (self-classification) required.

## Step-by-step checklist (initial run)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | wrap-v067 loaded | ✅ |
| STEP 0 classify | `classify_scan.py` → `doc_type=receipt, confidence=0.96, merchant=Intercessors for America` | ✅ |
| Parse | `parse_and_categorize.py --json` → date 2025-03-18, amount 30.50, `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` | ✅ |
| `check_vendor_key` | recognized, `intercessors_for_america` | ✅ |
| `check_duplicates` | fuzzy match on date+amount vs expense 1547 (`check_11051_03_18_25_30_50`); tool explicitly flagged "no vendor evidence against the match — CONFIRM before storing" | ✅ |
| Investigate duplicate | Mazda queried the DB row for 1547 directly (read-only, via `executor_run`) — found existing `receipt_url` = a **different** file (`window_scan_1785375180579246760_bf6820fdd860.jpg`, a check-stub photo), `document_url` = bank statement PDF, category 190 already set | ✅ |
| Categorize | `categorizer_main.py` → category 190 (Gifts & Love Offerings) — consistent with existing row | ✅ |
| Decision | Treated as same real-world payment; correctly skipped inserting a new expense row | ✅ |
| `record_trace` (trace 305) | `task_name=document-intake`, `is_duplicate=true`, `stored=false`, `expense_id=null` | ✅ |
| `judge_trace` | PASS | ✅ (but see defect below) |
| Dashboard callback | `duplicate_expense_ids:[1547]`, **`outcome:"EVIDENCE_ATTACHED"`** | ❌ nothing was actually attached |

## Defect diagnosed (initial run)

Expense 1547 already carried `receipt_url` pointing at a **different** image (the check-stub photo). This run's scan is itself a distinct receipt-type document (the charity's donation acknowledgment letter) for the same real-world payment — exactly the "Four Independent Supporting Documents" **same-type conflict** scenario in the manual's operational addendum: a different non-empty `receipt_url` candidate must never silently overwrite or be discarded; it must be routed through the review-note/log mechanism (`SupportingDocumentService`) with `requires_verification=true`, preserving the original reference.

Mazda never called any supporting-document association tool. She instead labeled the dashboard callback `outcome:"EVIDENCE_ATTACHED"` — a nonstandard status not in the documented `inserted`/`already_attached`/`conflicted` enum — while nothing was attached or logged. This is a wrapper gap: her duplicate-handling path stops at "skip store" and never checks whether the incoming document itself needs to be reconciled as a supporting-document reference against the matched expense.

## Coaching sent

One corrective message to the same conversation, explaining the same-type `receipt_url` conflict, requiring her to (1) call the correct supporting-document conflict/association tool for expense 1547 naming the new file, (2) update the trace's `problems` to state the conflict and human-verification requirement, (3) re-record/re-judge, (4) repeat the callback with a real status, and to save the rule to memory.

## Corrected run

- `SupportingDocumentService.attach(..., DocumentKind.RECEIPT, <new scan>)` → `{"status":"conflicted","expense_id":1547,"field":"receipt_url","requires_verification":true}`
- New trace `306`: `problems` now explicitly states the same-type `receipt_url` conflict, that the original reference was preserved, and that human verification is required.
- `judge_trace(306)` → PASS
- Dashboard callback re-sent with `outcome:"conflicted"` (correct enum value), `duplicate_expense_ids:[1547]`
- `propose_improvement` filed (id 214) recording the wrapper lesson

## Outcome

- No new expense row inserted; expense 1547 unchanged.
- New donation-letter scan correctly logged as a same-type supporting-document conflict on `receipt_url`, flagged for human verification.
- Nothing further for a human to act on beyond the flagged conflict itself (by design — that's the point of `requires_verification=true`).

**Final verdict: CORRECTED.**

## Deterministic red-box gate — PASS

2 available receipt/source viewer(s) returned `highlighted=true`.
