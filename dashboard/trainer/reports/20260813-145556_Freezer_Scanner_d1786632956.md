**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

**Document/scanner:** Freezer Scanner — `scan_freezer_1786632952110328653_c87436ee93e2.jpg`
**Dispatch:** 2026-08-13T14:55:56Z · **Conversation:** `conv-b64a5256-e264-485f-933b-bf409f6f6f02`
**Facade result:** `ok=true, doc_kind=unknown, confidence=0` → STEP 0 required.
**Run completed:** 2026-08-13T14:58:13Z (~2m17s after dispatch)

## Checklist with evidence

1. **`load_wrapper_revision`** (14:56:02) — `wrap-v071` loaded. ✅
2. **STEP 0 classification** — `classify_scan.py` (14:56:16, returncode 0):
   `{"doc_type":"receipt","confidence":1.0,"merchant":"Meijer",...}`. ✅ receipt/invoice branch entered correctly.
3. **STEP 0 parse** — `parse_and_categorize.py -f <scan> --json --engine=gemini --write-parsed-json ...` (14:57:07,
   returncode 1). Gemini call succeeded (2548/566 tokens), but the tool's own deterministic guard refused to write
   a validated parse artifact:
   `Could not write validated parse artifact: marked_items selection exceeds the printed total: selected rows sum
   to 38.07, but total_amount is 30.15.`
   This is the parse-validation fail-closed guard working as intended — a marked-item subset can never legitimately
   exceed the printed total, so the tool correctly refused rather than storing an internally inconsistent result.
4. **Investigate/Categorize/Store — correctly skipped.** With no validated parse artifact and no derivable vendor
   key, `check_vendor_key`, `check_duplicates`, `categorizer_main.py`, and `--save` were all appropriately not run.
   No `--allow-duplicate` or other bypass was attempted. ✅
5. **`record_trace`** (14:57:21, trace 363) — honest evidence: `doc_kind:receipt`, `classification_confidence:1.0`,
   `vendor_key:null`, `category_id:null`, `stored:false`, `expense_id:null`, `intake_halted:false` (correct — this
   was a validation refusal, not a runtime crash/halt), and a `problems` entry naming the exact mismatch
   (38.07 vs 30.15). `task_name` exactly `document-intake`. ✅
6. **`judge_trace`** (14:57:24) — returned `FAIL` / `missed_vendor_key` because the deterministic judge doesn't yet
   recognize "parse-blocked before artifact" as a valid terminal state and dinged vendor/category/duplicate/store
   as failed steps, even though skipping them was correct given no valid data existed. This is a judge rubric gap,
   not a Mazda mistake (analogous to the documented `intake_halted` rubric carve-out).
7. **`propose_improvement`** (14:57:51, proposal 234) — correctly triggered since verdict was FAIL. Summary and
   suggested patch accurately describe the rubric gap and ask the judge to treat parse-validation-blocked intakes
   as a valid terminal state without requiring vendor/duplicate/category/store. `apply_proposal` (14:57:55) passed
   all four gates (safety/cost/regression/usefulness) and activated `wrap-v072`. This is exactly the intended
   self-improvement loop: Mazda diagnosed a wrapper (judge-rubric) defect and fixed it herself, no Trainer
   coaching needed.
8. **Dashboard notify** — first attempt (14:57:41) errored with a tool-call malformation (the model merged the
   `executor_run` call's `env` argument with a `propose_improvement` call into one invalid JSON blob — a transient
   cheap-model glitch). Mazda recovered on her own: issued `propose_improvement` cleanly at 14:57:51, then retried
   the dashboard callback at 14:58:08, which succeeded (`{"ok": true}`). Final callback payload correctly reflects
   `stored:0, parsed:0, expense_id:null`, vendor "Meijer", correct `document_path`/`receipt_url`, correct
   `conversation_id`/`dispatched_at`. ✅

## Wrapper defect diagnosed (by Mazda, not the Trainer)

Judge rubric gap: no terminal-state handling for "parse blocked before writing a validated artifact." Mazda's own
`propose_improvement` → `apply_proposal` closed this gap (`wrap-v072`) within the run. No corrective message was
sent to Mazda — none was needed.

## For a human

- The receipt itself (Meijer, Freezer scan) was never stored and needs manual review/reparse — the printed total
  ($30.15) and the tool's computed marked-item selection ($38.07) are inconsistent, most likely an OCR/mark-detection
  misread on this scan (a genuinely mispriced or over-selected line). Unlike statements, there is no
  `_needs_review/`-style requeue mechanism for a parse-blocked receipt, so this scan will not resurface on its own —
  worth a manual look at `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786632952110328653_c87436ee93e2.jpg`.
- No application-defect escalation filed: the refusal was a clean, intentional validation message with no
  traceback, i.e. the guard working as designed, not a crash.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
