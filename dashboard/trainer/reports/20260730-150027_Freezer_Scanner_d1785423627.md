# Trainer Report — Freezer Scanner

- **Document:** `scan_freezer_1785423621469670042_376a1c462d2b.jpg` (Choice Privileges Mastercard statement, scanned)
- **Dispatch time:** 2026-07-30T15:00:27Z
- **Conversation:** `conv-8edf7a8e-1728-494a-ba64-58eeeeb4b7a7`
- **Verdict: CORRECTED** (initial run had 2 defects, both fixed in-run for this same document)

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | wrap-v067 loaded | OK |
| STEP 0 (vision classify) | Skipped — facade already returned `doc_kind=statement`, confidence 1.0 | OK (correctly exempt) |
| Workbook last4 resolution | Preflight already resolved `Choice Privileges Mastercard` / `7580` via `known_cards_workbook` (`workbook_matched_names: ["choice_7580"]`), overriding the raw "Wells Fargo" issuer name | OK |
| Used exact preflight `.statement.json` | Yes — did not rerun `parse_statement_scan.py` | OK |
| `store_statement_transactions.py` | `ok:false`, rolled back: `DocumentConflict: expense 1366 already has a different scanned_statement_url`. All 3 non-credit rows (KFC -6.24, MR BURGER -16.99, COUNTRY INN -179.08) accounted for via `duplicate_expense_ids:[1366,1390,1434]`; 2 credit/payment rows correctly skipped | Correct per Mazda's own learned **Rule 49** — review-required-conflict is the right outcome here, not a FAIL |
| `apply_statement_annotations.py` | Ran once for ids 1366,1390,1434; `applied:0, unchanged:1` (travel note already matched category 160), `annotation_provider:codex-cli` | OK |
| Report build (1st attempt) | `report.html` written to `tools/receipt_scanning_tools/incoming_scans/` — **wrong location** | **FAIL** (see below) |
| Report build (corrected) | `report.html` rebuilt at `readable_documents/scanned_statements/2025/report.html` (the actual `archive_paths` directory); restructure/hydrate/audit re-run | **Fixed** |
| Hydration | `rows_with_expense_id:3, blank_expense_id_rows:0, missing_expense_ids:[]` | OK |
| Audit | `WARN` — "no source PDF found in report directory" (expected: this is a JPEG scan, no PDF) | Acceptable (WARN, not FAIL) |
| `record_trace` (1st) | trace 303 — **missing `report_path`** | **FAIL** |
| `record_trace` (corrected) | trace 304 — includes `report_path` | **Fixed** |
| `judge_trace` | 303 → PASS; 304 → PASS | OK |
| Dashboard callback (1st) | `/api/expense-stored` POST succeeded but **missing `report_path`** | **FAIL** |
| Dashboard callback (corrected) | Re-POSTed with `report_path` included | **Fixed** |
| `propose_improvement` | Not applicable — final verdict PASS, no FAIL to propose against | N/A |

## Wrapper defects diagnosed

1. **Wrong report directory.** Mazda's own wrapper already contains **Rule 46** (proposal 200): after a statement store, resolve the report/annotation location from the store result's `archive_paths`, not `incoming_scans`. She violated her own rule — she wrote the initial `report.html` to the scratch `tools/receipt_scanning_tools/incoming_scans/` folder (a path that gets overwritten by the next scan and is not on the dashboard's report-resolution path) instead of `readable_documents/scanned_statements/2025/` (the directory named in `archive_paths`).
2. **Missing `report_path` field.** Neither the `record_trace` evidence JSON nor the `/api/expense-stored` callback payload named the report's path, despite the instructions requiring both to carry `report_path` alongside `report_generated`/`report_audit_status`.

## Lesson sent to Mazda (verbatim summary)

Coached her in-conversation to: rebuild `report.html` at the archive-path-derived directory, re-run restructure/hydrate/audit against that path, re-record a fresh trace with `report_path` included, re-judge, and re-POST the callback with `report_path` — all for this same document, not deferred to a future run. Also asked her to record the rule (report location derives from `archive_paths`, never `incoming_scans`; `report_path` is a required trace/callback field for every statement run) in memory.

## Result

Mazda executed the correction fully and promptly:
- Copied/rebuilt `report.html` to `/home/adamsl/rol_finances/readable_documents/scanned_statements/2025/report.html`.
- Re-ran restructure → hydrate (`categorized_rows:3`, `blank_expense_id_rows:0`) → audit (WARN, PDF-absence only).
- New trace `304` includes `report_path`; `judge_trace(304)` → PASS.
- Re-posted `/api/expense-stored` with `report_path` included; `{"ok": true}`.
- Filed a durable memory note (`propose_memory_note`, proposal 213, gates all `allow`) capturing this rule for future statement runs.

## For a human to look at

- The `WARN` on `audit_statement_reports.py` ("no source PDF found in report directory") is expected/harmless for a JPEG-scanned statement — the auditor's PDF-comparison logic simply has nothing to compare against. Not a defect, just noting it's not a `PASS`.
- Expense 1366's pre-existing conflicting `scanned_statement_url` still needs human review/resolution (this is exactly what the `conflicted`/`requires_verification=true` status is flagging) — that's expected human-in-the-loop work, not something for Mazda or the Trainer to resolve.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
