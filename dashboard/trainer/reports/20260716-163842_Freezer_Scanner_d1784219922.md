# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-16T16:38:42Z
- Scan path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: **FAIL**

## Evidence Checklist

- `load_wrapper_revision` succeeded at `16:38:45Z`, active revision `wrap-v011`.
- STEP 0 classification ran and succeeded at `16:39:49Z`: `executor_run` returned `doc_type=bank_statement`, `confidence=1.0`, merchant `Chase`.
- `check_vendor_key` succeeded at `16:39:56Z`, normalized `amazon_chase` to `chase_mc_9915`.
- `check_duplicates` succeeded at `16:40:00Z`, not a duplicate.
- Categorization succeeded at `16:40:14Z` with `vendor_key=chase_mc_9915`, `category_id=3`.
- Storage path was wrong for this doc type:
  - `record_trace` evidence says `doc_kind=statement`, `transactions_parsed=7`, `transactions_stored=0`, and the problem was that the statement was routed through the receipt save path instead of `store_statement_transactions.py`.
  - `judge_trace(79)` returned `FAIL` with `failure_type=other` and reason `statement_transactions_stored`.
- `record_trace` succeeded at `16:40:50Z` with `task_name="document-intake"` and trace `79`.
- `propose_improvement` succeeded at `16:41:00Z` for trace `79`.
- `apply_proposal` succeeded at `16:41:04Z`, activating `wrap-v012`.
- Dashboard callback was emitted: `expense-stored-events` contains one event for this dispatch with `doc_kind: statement`.

## Diagnosis

Wrapper defect: statement routing was not enforced strongly enough. Mazda classified the scan correctly as a bank statement, but the intake pipeline still used the receipt save path instead of the statement branch.

## Lesson Sent

I sent Mazda a corrective lesson: for `bank_statement` / `statement`, always run `parse_statement_scan.py` followed by `store_statement_transactions.py` before `record_trace`; never use `parse_and_categorize.py --save` as a substitute. The agent converted that into approved memory proposal `59` (`propose_memory_note`), though no live-memory applier is wired yet.

## Human Follow-Up

- The run is not corrected: the statement-specific storage step did not complete successfully.
- The wrapper has already been updated to `wrap-v012`; verify that future statement runs actually use the statement branch end-to-end.
