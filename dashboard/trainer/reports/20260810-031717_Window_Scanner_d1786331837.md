# Mazda Trainer Report — Window Scanner

**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786331833131632328_e45c5d2c69e1.jpg`
**Dispatch:** 2026-08-10T03:17:17Z
**Conversation:** conv-ca49bf7e-4065-412f-ba03-9530bff551d2
**Doc kind (final):** receipt — Samaritan's Purse 2025 annual contribution acknowledgment ($3,633.00), single line item, no marks/handwriting.

Verdict: PASS

## Checklist / evidence

1. `load_wrapper_revision` — wrap-v004 loaded. ✅
2. Facade returned `doc_kind=unknown`, so STEP 0 required. Mazda ran `classify_scan.py` via `executor_run` → `doc_type=receipt, confidence=0.98` (≥0.90, no manual override needed). ✅
3. STEP 0 parse: `parse_and_categorize.py --json --engine=gemini` — Gemini vision call **failed with 401** (OAuth/API-key auth error on `generativelanguage.googleapis.com`), and the tool **self-fell-back to the OpenAI Codex/ChatGPT-OAuth path** (`model_provider: chatgpt-oauth`, `model_name: gpt-5.4`) in the same call, producing a full valid parse artifact (`expense_scope: full_receipt`, single item, `$3,633.00`, no `handwritten_arithmetic_total`). No marks/circles on this document, so `full_receipt` scope is correct. ✅
4. `check_vendor_key("samaritans_purse")` → normalized/fuzzy-matched to the recognized key `5_3_online_pymt_to_samaritan_acct_8855`; Mazda adopted the normalized key downstream. ✅
5. `check_duplicates` **failed to authenticate** (`Access denied for user 'adamsl'@'localhost'`). Per her Rule 3 (learned 2026-08-06), Mazda ran an equivalent authenticated duplicate check herself via `executor_run` (direct `pymysql` query, date=2026-01-20, amount=3633.00) — returned no rows — and only then recorded `duplicate_checked=true, is_duplicate=false`. Exactly the corrective behavior the wrapper rule prescribes. ✅
6. Categorize: `categorizer_main.py --provider=auto` → `category_id=215` ("Samaritans Purse"). `check_category` confirmed `matches_vendor=true`. Non-null, non-placeholder, verified category. ✅
7. Store: `parse_and_categorize.py --save --category-id=215` → `expense_id=2009`, `receipt_url=samaritans_purse_01_20_26_3633_00.jpg`, archived under `readable_documents/receipts/2026/january/january_20/`. Single line item → itemization correctly not attempted (`itemization_attempted=false`). ✅
8. `record_trace(task_name="document-intake")` — evidence JSON matches the transcript exactly (vendor_key_recognized, duplicate_checked, category_id, stored, expense_id, intake_halted=false, and an honest `problems` list documenting both the Gemini auth failure and the check_duplicates credential gap). Trace 93 saved. ✅
9. `judge_trace(93)` → **PASS**, "all deterministic intake checks passed." ✅
10. Dashboard callback: first `curl` attempt to `/api/expense-stored` failed (`Invalid JSON`, bad shell quoting on the apostrophe in "Samaritan's"). Mazda caught this, rewrote the payload via a heredoc file (`/tmp/mazda_callback.json`) and retried — second attempt succeeded (`{"ok": true}`), carrying `expense_id`, `expense_ids`, `duplicate_expense_ids:[]`, `archive_paths`, `vendor_key`, `receipt_url`, `conversation_id`, `dispatched_at`. ✅ (self-corrected without coaching)

## Independent verification (Trainer-side, not from Mazda's prose)

- `POST /api/supporting-documents {expense_id:2009}` → `receipt_url` present, `documents[]` shows `receipt: available=true` ("View Receipt"), and `document_url`/`scanned_statement_url`/`moms_ledger` all correctly `available=false` (none of those evidence types apply to a fresh receipt). Dialog-visibility contract satisfied.
- `POST /api/open-supporting-document {expense_id:2009, document_type:"receipt"}` → `{"ok":true, "highlighted":true}`.
- Fetched `/supporting-document/2009/receipt` (654,561 bytes) vs. the raw archived file (641,093 bytes) — byte-for-byte **different**, confirming a real annotated/boxed copy is served, not a pass-through of the original. Red-box contract satisfied.

## Wrapper defects

None. Both anomalies in this run (Gemini vision 401, check_duplicates missing DB creds) were handled correctly by Mazda's *existing* learned rules/fallbacks without any need for coaching, and the one self-inflicted mistake (bad shell quoting in the first callback attempt) was self-corrected within the same run.

## Coaching sent

None required.

## For a human

Nothing actionable. Gemini's `generativelanguage.googleapis.com` OAuth/API credential is returning 401 again (same class of failure documented in `reference_provider_health_quick_lookup`/`gemini_key_403_unrestricted` memory) — the categorizer-fallback-health dashboard tile should already be tracking this; no new escalation needed since the fallback chain absorbed it cleanly end-to-end.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
