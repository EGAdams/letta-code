# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Document:** `scan_freezer_1785022033228022415_99a3198b9de8.jpg`
- **Dispatch:** 2026-07-25T23:27:16Z
- **Mazda conversation:** conv-b75a6321-a9eb-4fcf-88d8-8b7fc1550298
- **Verdict: CORRECTED** (agent-side work complete and correct; one required step blocked by executor infrastructure, not a Mazda defect)

## What happened

1. `load_wrapper_revision` — wrap-v054 loaded, 23:27:21Z. ✅
2. `classify_scan.py` (STEP 0, facade returned `doc_kind=unknown`) — Gemini hit a 429 quota error, fell back to OpenAI Codex CLI, returned `doc_type=moms_ledger`, `confidence=0.98`, merchant "Diners Club International" (credit-card statement covered in handwritten checkmarks/notes). Correct classification per the moms_ledger branch rules. ✅ (23:29:27Z)
3. `moms_ledger_reconciler.py --image <scan>` — **failed twice**, both transport-layer, not document-content errors:
   - 23:34:33Z: `MCP error -32001: Request timed out` (300s timeout)
   - 23:42:50Z (after Trainer coaching #1, 600s timeout): `Not connected`
   Mazda stalled ~8 minutes after the first failure with no retry and no trace/judge — this was the contract violation the Trainer coached.
4. **Trainer coaching #1** (23:42:50Z UTC send): retry once, then fail-closed with root-cause evidence in `problems`, still record_trace/judge_trace/callback, and propose an improvement for "MCP timeout with no retry."
5. Mazda responded correctly and completely:
   - Retried the reconciler once (still failed, `Not connected`).
   - `record_trace` (trace_id 231, `task_name="document-intake"`, `doc_kind="moms_ledger"`, `stored=false`, `expense_id=null`, root cause named verbatim in `problems` with both failure timestamps/messages). ✅
   - `judge_trace(231)` → `FAIL` / `missed_duplicate` (`ledger_rows_matched`, `ledger_categories_reconciled` failed) — correct verdict given the reconciler never ran successfully. ✅
   - `propose_improvement` (proposal 169, distinct wrapper defect: retry transient MCP/executor timeouts before giving up) ✅
   - `apply_proposal(169)` → activated, `wrap-v056`, all 4 gates passed. ✅
   - Attempted the STEP 8 dashboard callback (`/api/expense-stored`) — failed, `Not connected`.
6. **Trainer coaching #2** (23:44:34Z UTC send): retry the callback specifically; everything else already correct.
7. Mazda retried the callback **3 more times** (23:44:46, 23:44:54, 23:45:02Z), all `Not connected`, then reported a final, accurate summary and stopped.

## Checklist against the contract

| Step | Result |
|---|---|
| load_wrapper_revision | ✅ |
| STEP 0 classify_scan (facade=unknown) | ✅ moms_ledger, 0.98 |
| moms_ledger_reconciler.py | ❌ transport failure ×2 (root cause recorded, not swallowed) |
| record_trace (document-intake, moms_ledger fields) | ✅ |
| judge_trace | ✅ FAIL/missed_duplicate (correct given reconciler never succeeded) |
| propose_improvement (FAIL → required) | ✅ proposal 169 |
| apply_proposal | ✅ wrap-v056 activated |
| Dashboard callback (STEP 8/9, required always) | ❌ never delivered — 4 total attempts, all `Not connected` |

## Wrapper defect diagnosed and fixed

Original defect: Mazda had no learned rule to retry a transient MCP/executor transport timeout before giving up on the moms_ledger branch — she stalled silently for ~8 minutes after the first failure instead of retrying or recording anything. Fixed via proposal 169 → **wrap-v056**: retry once on a transient reconciler transport failure; if the retry also fails, record the exact transport failure as root cause and still complete record_trace/judge_trace, never falling back to receipt/statement storage.

## Remaining issue — infrastructure, not Mazda

The STEP 8 `/api/expense-stored` callback never reached the dashboard: **4 separate `executor_run` calls** (the callback itself plus surrounding tool calls) all returned `Not connected` in the 23:42–23:45Z window, while `record_trace`/`judge_trace`/`propose_improvement`/`apply_proposal` (also via the executor, moments earlier) succeeded. This looks like the Claude-SDK executor's connection dropped partway through this run rather than anything Mazda did wrong — she retried appropriately when coached. **A human should check the executor (`:8799`/`:8787`) and `mazda-tools-mcp` service health around 2026-07-25 23:34–23:45 UTC**; the dashboard will show no expense-stored event for this scan/conversation even though Mazda's reasoning and finance-side handling were correct.

## Lessons sent to Mazda

1. Retry a transient reconciler/MCP timeout once; if it still fails, record the transport failure verbatim as root cause and complete record_trace/judge_trace without falling back to receipt/statement storage (now codified as wrap-v056, rule #39).
2. Retry the STEP 8 dashboard callback several times before giving up, since it is required always — she complied (3 retries, all failed) but no additional wrapper rule was needed since the failure was purely transport-level, not a missing behavior.
