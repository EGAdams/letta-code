**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Run
- Scanner: Window Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786497027998041224_35002ffa4af3.jpg`
- Dispatch: 2026-08-12T01:10:31Z
- Conversation: `conv-5c64b8f5-c4dd-419a-ae13-818bc7fb7309`
- Facade: `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required.
- Actual document: single-item gas station receipt, "DOUBLE M FOOD / Azeele Marathon", 318 S Dale Mabry Hwy, Tampa FL, $16.01, 2019-04-14. Severely faded thermal print (barely legible even zoomed in). Confirmed by reading the archived source image directly.

## Checklist (all steps observed with successful tool returns)
1. `load_wrapper_revision` — ok, wrap-v006, 21:10:40Z.
2. STEP 0 `classify_scan.py` via `executor_run` — Gemini 401'd, fell back to OpenAI Codex CLI, returned `doc_type=receipt, confidence=0.97`. ✓.
3. STEP 0 parse `parse_and_categorize.py --json` — Gemini parse 401'd, fell back to chatgpt-oauth. Full-receipt scope, single line item ("FUEL SALE PLUS" $16.01), matches printed total exactly. No marked-item/handwriting scope issue — plain single-item receipt. ✓ correct scope.
4. `check_vendor_key("double_m_food")` → returned `vendor_key=harvest_health_foods_grand_rapids_mi` (fuzzy match), `recognized=true`. Mazda adopted it as required by contract. **See Escalation — this fuzzy match is wrong.**
5. `check_duplicates` → MCP auth error (`Access denied for user 'adamsl'@'localhost'`). Mazda fell back to a manual authenticated `executor_run` MySQL query (per wrapper Rule 3) matching on `(expense_date, amount)` — found zero existing rows, so genuinely no duplicate. Store proceeded. **See note below on a wrapper-memory conflict (Rule 3 vs Rule 5) — did not affect correctness this run, but is worth reconciling.**
6. `categorizer_main.py` → `category_id=3` ("Personal"), gemini succeeded this time.
7. `parse_and_categorize.py --save --category-id=3` → `success:true, expense_id:2030, parse_artifact_verified:true`, single-line receipt, no itemization needed (one item).
8. `check_category` → confirms `category_id=3` matches expected. ✓.
9. Dashboard notify — `curl POST /api/expense-stored` via `executor_run`, `{"ok":true}`, includes `expense_id`, `archive_paths`, `vendor_key`, etc. ✓.
10. `record_trace(task_name="document-intake", ...)` — evidence JSON complete and **honest**: `problems` field explicitly names the check_duplicates auth fallback, the OAuth parser fallback, and the archive-year mismatch (see below) — good transparency. trace_id 127.
11. `judge_trace(127)` → `PASS, failure_type=none`. Consistent with observed evidence.

No disqualifying condition applies (merchant is a real value "DOUBLE M FOOD", category_id is valid/nonzero).

## Independent verification (Trainer's own checks)
- **Red-box annotation missing.** `GET /supporting-document/2030/receipt` returned HTTP 200, 170971 bytes, but the served image is byte-for-byte visually identical to the raw archived file — no red box, i.e. served unannotated. This receipt is neither the handwritten/check-style case (fixed 2026-08-08) nor EG's handwritten-statement case; it's a plain single-item receipt whose print is extremely faded/low-contrast, which likely defeats OCR line-matching entirely (no readable line to box). This is a **dashboard defect** per the Trainer's standing instructions, not a Mazda failure — filed below.

## Wrapper-memory note (not coached this run — no live defect, but flagging for hygiene)
Mazda's loaded wrapper instructions contain two rules that directly conflict for the same trigger (`check_duplicates` auth failure):
- Rule 3 (proposal 41, 2026-08-06): "perform an equivalent authenticated duplicate check via executor_run before storage, and record duplicate_checked=true only after that check succeeds."
- Rule 5 (proposal 64, 2026-08-11, same day as this run): "If check_duplicates fails after one retry, never claim duplicate_checked=true or insert a new receipt expense; fail closed."

This run followed Rule 3 (single check_duplicates attempt, then a manual `executor_run` SQL fallback that succeeded and found no duplicate — so the stored data is correct). It did not literally trigger Rule 5's "fails after one retry" wording since check_duplicates itself was only attempted once. But Rule 5 was added the same day specifically to close this exact workaround pattern after a prior missed-duplicate incident, so the two rules are in practical tension and should be reconciled (e.g., Rule 5 should explicitly supersede/delete Rule 3, or clarify when the manual-query fallback is still allowed). No corrective message sent to Mazda this run because the underlying store is verified correct (zero real duplicate rows for this date+amount) and re-litigating a PASS run over ambiguous rule wording isn't warranted — but the next FAIL of this class should cite this conflict directly.

## For a human
- Expense 2030's `vendor_key` is wrong (`harvest_health_foods_grand_rapids_mi` for a Tampa FL gas station) — will misfile vendor/reporting rollups. Root cause is the `check_vendor_key` tool's fuzzy match, not Mazda (she is contractually required to adopt whatever it returns). Needs a manual vendor_key correction on expense_id 2030 once the matcher is fixed.
- Expense 2030's receipt has no red-box annotation in the dashboard viewer (faded thermal print, likely OCR-unreadable).

## Escalation
repo_path: /home/adamsl/rol_finances
bug_description: |
  Three related-but-distinct defects surfaced by this intake run, none of them a Mazda
  wrapper/prompting issue — all are in downstream deterministic tooling:

  1. check_vendor_key fuzzy-match false-positive. Input vendor_key guess "double_m_food"
     (merchant on receipt: "DOUBLE M FOOD" gas station/convenience store, Tampa FL) was
     normalized by the check_vendor_key MCP tool to "harvest_health_foods_grand_rapids_mi"
     (an unrelated health-food store in Grand Rapids, MI), returned with recognized=true
     and note "normalized supplied vendor_key 'double_m_food' to known
     'harvest_health_foods_grand_rapids_mi' (fuzzy match)". The two vendors share no
     meaningful identity (different business type, different state) beyond generic
     token overlap on "food"/"foods". Mazda's contract requires her to adopt whatever
     check_vendor_key returns, so this bad match was written straight into expense_id
     2030's vendor_key. Suspect the fuzzy-matching threshold/algorithm in the finance_mcp
     check_vendor_key implementation (likely near vendor_category.yaml lookup) is too
     permissive. Needs a tighter similarity threshold or token-based guard.

  2. parse_and_categorize.py --save archives to the wrong year folder. Transaction date
     was correctly parsed as 2019-04-14 (present in both the raw parse JSON and the
     final metadata row), but the tool moved/archived the receipt image to
     readable_documents/receipts/2025/april/april_14/... — year 2025, which matches
     neither the transaction year (2019) nor the actual run date (2026-08-12). Day/month
     ("april_14") were derived correctly; only the year component is wrong. Likely an
     off-by-something or fallback-year bug in the archive path builder specifically for
     old (2019-vintage) transaction dates. Full evidence: stdout log line
     "INFO:__main__:Moved receipt file .../double_m_food_04_14_19_16_01.jpg ->
     /home/adamsl/rol_finances/readable_documents/receipts/2025/april/april_14/double_m_food_04_14_19_16_01.jpg"
     alongside archive_years:[2019] reported in the same tool's JSON success payload —
     the tool's own two outputs (log path vs. archive_years field) disagree with each
     other, which should make this easy to reproduce and locate in code.

  3. Missing red-box annotation (dashboard, not rol_finances) for expense_id 2030's
     receipt — GET /supporting-document/2030/receipt (server.py /
     document_annotation.py, repo /home/adamsl/letta-code/dashboard) served bytes
     identical to the unannotated original. The source image is an extremely
     faded/low-contrast thermal receipt (barely legible to the human eye); likely OCR
     line-matching produced no usable line, and this document class (faded-but-printed,
     not handwritten/check-style) may not be covered by the 2026-08-08
     CodexCliImageRegionFallbackMatcher fix, which targeted handwritten/check-style
     receipts specifically.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786497027998041224_35002ffa4af3.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=3 --parsed-json /tmp/mazda_receipt_9d43f8f24455.json --engine=gemini
  expense_id: 2030
  wrong_vendor_key: harvest_health_foods_grand_rapids_mi
  correct_merchant: DOUBLE M FOOD (Azeele Marathon, Tampa FL)
  archived_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/april/april_14/double_m_food_04_14_19_16_01.jpg

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `2030`, receipt: Image fallback matching was unavailable: Command '['/usr/local/bin/codex', 'exec', '--ephemeral', '--ignore-user-config', '--ignore-rules', '--skip-git-repo-check', '--sandbox', 'read-only', '--model', 'gpt-5.4-mini', '--image', '/home/adamsl/rol_finances/readable_documents/receipts/2025/april/april_14/double_m_food_04_14_19_16_01.jpg', '-']' timed out after 120 seconds

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
