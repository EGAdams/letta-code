# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1788953569161781905_33bdd94975b7.jpg`  
Dispatch: `1788953572` (`2026-09-09T11:32:52Z`)  
Conversation: `conv-e94f7636-6235-4d66-98fd-ee174779f851`

## Evidence

- PASS — `load_wrapper_revision` returned active `wrap-v100`.
- PASS — `classify_scan.py` returned `doc_type=receipt`, merchant `AT&T`, confidence `0.95`.
- FAIL — `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_2d66fb3f012d.json` exhausted Gemini (`503`, `503`, `429`), ChatGPT OAuth (`401`), and OpenAI (key unavailable). Its local fallback had no merchant, items, or positive total, so no validated parse artifact existed.
- Correct fail-closed behavior — Mazda did not run vendor/category/duplicate/save stages or create an expense from contradictory evidence. Her trace recorded `parse_blocked=true`, `stored=false`, `expense_id=null`, and the provider failures.
- PASS — `record_trace` saved trace `536` with `task_name="document-intake"`; `judge_trace(536)` returned `FAIL/missed_vendor_key`.
- PASS — Mazda called `propose_improvement` for trace `536`; proposal `297` was applied as `wrap-v102`, teaching parse-blocked receipts to stop before downstream stages.
- PASS — Dashboard callback returned `{ok:true}` with `stored:0`; the matching event has this conversation and dispatch timestamp, with no expense or duplicate IDs.

The judge verdict is inconsistent with the observed safe behavior: it blamed skipped downstream stages even though parsing was explicitly unavailable. This run is not CORRECTED or PASS because no trustworthy receipt parse or stored/duplicate evidence exists.

No corrective message was sent: Mazda followed the safe wrapper behavior and cannot repair provider availability from this intake.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  The dashboard dispatched this scan after reporting vision availability, but the
  required receipt parse had no usable provider: Gemini returned 503/503/429,
  ChatGPT OAuth returned 401 Unauthorized, and the OpenAI fallback was unavailable
  because OPENAI_API_KEY was unset. The local fallback returned no merchant, items,
  or positive total. Expected behavior is either a usable source-bound parse or an
  explicit infrastructure/application rejection that the judge accepts as
  parse-blocked; actual judge_trace 536 returned FAIL/missed_vendor_key for not
  running vendor/category/duplicate/store. Suspect the provider fallback/configuration
  and parse-blocked judge path, not Mazda's intake reasoning.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1788953569161781905_33bdd94975b7.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1788953569161781905_33bdd94975b7.jpg --json --engine=gemini --write-parsed-json /tmp/mazda_receipt_2d66fb3f012d.json

For a human: no database write occurred. Review the provider health/configuration and ensure the judge recognizes this documented parse-blocked, fail-closed outcome instead of classifying it as a Mazda vendor-key failure.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
