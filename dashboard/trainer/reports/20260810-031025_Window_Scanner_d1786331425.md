# Trainer Report — Window Scanner

**Dispatch:** 2026-08-10T03:10:25Z
**Document:** `window_scan_1786331421964964318_44bbbf6f440f.jpg`
**Mazda conversation:** conv-b90d3b7f-8c61-4904-a8d4-b05bb95f2f8f

Verdict: PASS

## What happened

Facade returned `doc_kind=unknown`, correctly routing Mazda to STEP 0 self-classification.

1. `load_wrapper_revision` — wrap-v004, success. ✓
2. `executor_run classify_scan.py` (03:10:46) — Gemini vision failed (401, dead key), but the
   script's fallback returned a clean result: `doc_type="other", confidence=0.98, merchant="Samaritan's
   Purse", reason="This is a charitable contribution acknowledgment letter, not a receipt, invoice,
   bank statement, or tax form."` ✓
3. Per the contract's explicit rule — *"only doc_type=other is unsupported"* — Mazda correctly
   short-circuited: no receipt/invoice parse, no `check_vendor_key`/`check_duplicates`, no
   categorizer, no store. This is the required behavior for `other`, not a skipped step.
4. `record_trace` (03:10:55, trace_id=92) — evidence JSON is accurate and honest:
   `doc_kind:"other"`, `vendor_key:null`, `category_id:null`, `duplicate_checked:false`,
   `stored:false`, `expense_id:null`, with `problems` explicitly stating *"Unsupported document...
   Per routing precedence, receipt/invoice/statement storage and categorization branches were not
   run."* This is exactly what an honest "other" trace should look like. ✓
5. `judge_trace` (03:10:57) → **FAIL**, `failure_type=missed_vendor_key`, reasons: `failed:
   vendor_key_resolved`, `failed: category_resolved`, `failed: duplicate_check_ran`, `failed:
   expense_stored`. This is a misgrade — none of those four checks apply to a genuinely
   unsupported `doc_kind=other` document per the contract's own routing-precedence rule. See
   Escalation below.
6. Dashboard callback — first `curl POST /api/expense-stored` attempt (03:11:09) failed with
   `{"error":"Invalid JSON"}` because the payload used single-quote shell wrapping around a
   description containing an apostrophe (`Samaritan's Purse`), breaking the JSON string. Mazda
   self-corrected on the very next call (03:11:21) using `\"`-escaped double quotes and got
   `{"ok": true}`. Self-recovered; not counted as a failure, but worth noting as a fragile pattern
   (see below).
7. `propose_improvement` — **not called**. Contract step 8 says this is required on FAIL. I did not
   coach Mazda to call it: see reasoning below.

## Why this is graded PASS, not FAIL

Mazda's actual behavior fulfilled the contract for an `other` document exactly as written: identify,
record honestly, do not fabricate vendor/category/duplicate/store evidence for a document type that
explicitly skips those branches, and notify the dashboard. The only defect in this run lives in the
verdict judge (evaluation/judging code), which appears to apply receipt/invoice completeness
criteria (`vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored`)
unconditionally, without an exemption for `doc_kind=other`. Per this Trainer's own instructions,
grading a correct run as a Mazda failure because a downstream automated judge misapplied its rubric
would itself be a "judge FAIL that blamed Mazda" — explicitly called out as a rubric defect worth its
own note, not a lesson for Mazda. I did not send any corrective message to Mazda's conversation.

I also did not require `propose_improvement`: that tool proposes a change to Mazda's own wrapper
(instructions/tools/memory), and nothing in her wrapper caused this outcome — the defect is in the
verdict judge's rule set, which she has no ability to patch.

## Escalation

repo_path: /home/adamsl/rol_finances
bug_description: |
  The document-intake verdict judge (tools/self_improving_agent evaluation layer, likely
  IEvaluationFactory's verdict_judge / gate rules invoked by `judge_trace`) FAILed trace_id 92
  with failure_type=missed_vendor_key and reasons ["failed: vendor_key_resolved", "failed:
  category_resolved", "failed: duplicate_check_ran", "failed: expense_stored"] for a document
  that classify_scan.py correctly identified as doc_type="other" (confidence 0.98, a charitable
  contribution acknowledgment letter — not a receipt, invoice, bank statement, or tax form).
  Per the intake contract, doc_kind=other is a valid terminal classification that deliberately
  skips vendor-key resolution, categorization, duplicate-checking, and storage. The judge appears
  to apply receipt/invoice completeness checks unconditionally regardless of doc_kind, producing
  a false FAIL on a correctly-handled unsupported-document run. Expected: the judge (or the
  finance evaluator feeding it) should treat doc_kind="other" as exempt from
  vendor_key_resolved/category_resolved/duplicate_check_ran/expense_stored checks, mirroring how
  it already exempts uncategorized statement rows. Suspected location: the finance evaluator/
  verdict-judge rule set in tools/self_improving_agent (FinanceEvaluationFactory /
  verdict_judge), not Mazda's wrapper.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786331421964964318_44bbbf6f440f.jpg
  failing_command: judge_trace(trace_id=92)
  trace_id: 92

## Minor note for a human (not escalation-worthy on its own)

The dashboard-callback curl command builder occasionally wraps JSON in single quotes when a field
(here, vendor/description "Samaritan's Purse") contains an apostrophe, breaking the JSON payload on
the first attempt. Mazda self-corrected immediately by switching to `\"`-escaped double quotes on
retry, so this cost no data loss here, but it's a recurring fragility worth a durable fix in her
prompt/tool-call template (always double-quote-wrap + backslash-escape the `-d` JSON body) rather
than relying on retry luck.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
