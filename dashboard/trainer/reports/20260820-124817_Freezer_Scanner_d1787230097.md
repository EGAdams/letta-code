# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

- Scanner: Freezer Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1787230093589872568_3eb5bf8f627a.jpg`
- Dispatch timestamp: 1787230097 (2026-08-20T12:48:17Z)
- Mazda conversation: `conv-edf08363-e4ba-4120-86c3-bf4acde2c945`
- Facade result: `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` — this dispatch required Mazda to run STEP 0 (self-classification via `classify_scan.py`).

## What I observed

Polled `GET /v1/conversations/conv-edf08363-e4ba-4120-86c3-bf4acde2c945/messages?limit=60` roughly every 30s from dispatch to **T+916s** (>15 minutes).

- The conversation's `in_context_message_ids` contains exactly **one** message the entire time: the generic Letta-Code boilerplate `system_message`, timestamped 2026-08-20T12:48:17+00:00 (dispatch instant).
- `GET /v1/conversations/conv-edf08363-e4ba-4120-86c3-bf4acde2c945` shows `created_at` = `2026-08-20T12:48:17.120821Z` and `last_message_at` = `2026-08-20T12:48:17.620965Z` — i.e. the conversation was created and touched once, half a second later, and **never again**. No user (dispatch) message, no tool calls, no assistant turns.
- `GET /api/expense-stored-events?since=1787230097` returned `[]` — no storage activity of any kind, consistent with Mazda never having engaged the document.

## Checklist against the intake contract

None of the required steps (`load_wrapper_revision`, STEP 0 classification, investigate, categorize, store, `record_trace`, `judge_trace`, dashboard notify) appear anywhere in the transcript, because the transcript contains no dispatch content at all — only the boilerplate system message.

## Diagnosis

This is **not a Mazda wrapper defect** — there is nothing in her transcript to coach against; she never received the task. The evidence (conversation created, single system message attached, zero subsequent activity for 15+ minutes, no expense-stored events) points to an **infrastructure problem** in how this dispatch was delivered: either the dispatcher created the conversation but failed to POST the actual task/user message into it, or the message was sent but Mazda's agent loop never picked it up and produced a turn.

Per the Trainer instructions, a stall where Mazda never picked up the dispatch is an infrastructure issue, not a lesson for her — no corrective message was sent to her conversation.

## For a human

- Check whatever process dispatches scans to Mazda (the Freezer Scanner path) for this run — it appears to have created `conv-edf08363-e4ba-4120-86c3-bf4acde2c945` and attached the base system message but never delivered the actual dispatch/task content as a user message.
- Confirm Mazda's agent (`agent-6b536cf4-ec88-4290-b595-fed21d14bd8e`) is otherwise healthy/responsive (e.g. via a manual test message) — worth ruling out a Letta-server-side delivery failure vs. a dispatcher-side bug before the next scheduled scan.
- No document was ever stored or duplicate-checked for `scan_freezer_1787230093589872568_3eb5bf8f627a.jpg`; it still needs a real intake run once the delivery issue is fixed.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
