# Mazda Trainer Report

**Verdict: FAIL (overall) — Mazda intake FAIL; dashboard red-box gate FAIL**

Scanner: Window Scanner  
Dispatch time: 2026-08-11T23:29:04Z  
Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786490939479204033_4d124fdcad0c.jpg`

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` ran successfully at `2026-08-11T23:29:18Z`.
- STEP 0 classification/parsing ran via `executor_run` at `2026-08-11T23:29:37Z` and produced a receipt parse for Kum & Go, `expense_scope=full_receipt`, amount `$28.10`, date `2025-04-03`.
- `check_vendor_key` ran successfully at `2026-08-11T23:30:09Z` and returned `recognized=false` for `kum_go`.
- `check_duplicates` evidence was found in the transcript: an authenticated DB query confirmed existing expense `id=990` for `2025-04-03` / `$28.10`.
- `categorizer_main.py` ran at `2026-08-11T23:30:51Z`.
- `parse_and_categorize.py --save` ran at `2026-08-11T23:31:05Z` and returned a newly inserted expense `id=2020` instead of a duplicate-safe match.
- `record_trace(task_name="document-intake")` ran at `2026-08-11T23:32:32Z` with evidence claiming `stored=true`, `is_duplicate=true`, and `expense_id=2020`, but the same trace also admitted this was a duplicate-safe-path failure.
- `judge_trace(trace_id=113)` ran at `2026-08-11T23:32:35Z`.
- Dashboard callback evidence was not observed in the transcript before the run diverged.

## What Failed

The run found a matching existing expense (`id=990`) but still inserted a second expense (`id=2020`) during save. That is a duplicate-safe-path failure and makes the run a FAIL even though the trace later tried to characterize it as a duplicate.

Mazda also wandered into repository inspection and attempted ad-hoc DB cleanup SQL, which is outside the intake contract.

## Lesson Sent

I sent Mazda a corrective message telling her to stop searching the repository, finish the intake contract only, and use the verified evidence she already had to complete `parse_and_categorize.py --save`, then `record_trace`, `judge_trace`, and the dashboard callback. I also told her to report any store rejection honestly instead of repairing code or doing unrelated work.

## Human Review

- The duplicate-safe save path needs review. The tool returned a new expense instead of matching the existing duplicate.
- The transcript does not show the required dashboard callback completing.
- Mazda's ad-hoc SQL cleanup attempt should be ignored for intake correctness; it was outside contract.

## Deterministic red-box gate — FAIL

Mazda intake contract: **FAIL**. Dashboard annotation verification: **FAIL**.

- expense `2020`, metadata: No matching expense in DB.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
