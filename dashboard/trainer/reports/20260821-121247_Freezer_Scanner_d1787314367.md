# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

- Scanner: Freezer Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1787314364283055486_a1d312944378.jpg`
- Dispatch: 2026-08-21T12:12:47Z (1787314367)
- Mazda conversation: `conv-657efff2-4d37-40f2-abee-7673e8ef4468`
- Facade: `doc_kind=unknown` (JPEG path, STEP 0 vision required — expected)

## Step-by-step checklist (original run)

1. `load_wrapper_revision` → `wrap-v081`. ✓ 12:12:56
2. STEP 0 `classify_scan.py` → `doc_type=receipt`, merchant "Walgreens", confidence 1.0. ✓ 12:13:25
3. STEP 0b `parse_and_categorize.py --json --write-parsed-json` → success, but `gemini-3.6-flash` itself raised `engine_failure.kind=no_receipt_identity` ("does not look like a single receipt") and fell back to `local-fallback`, producing garbled `merchant_name` and a single fabricated item `"fQTAL FE AND RX ITEM:"` = $41.23. ✓ 12:13:49
4. `check_vendor_key(vendor_key="walgreens")` — present, recognized (Mazda correctly overrode the garbled merchant text with the classifier's high-confidence brand). ✓ 12:14:07
5. `check_duplicates` (placeholder date 1970-01-01) — not a duplicate. ✓ 12:14:12
6. `categorizer_main.py` → category_id 243 ("RM — Priority Health"). ✓ 12:14:20
7. `parse_and_categorize.py --save --category-id=243` (loaded the cached parse artifact, did not re-run vision) → `expense_id=2212`, `stored:true`. ✓ 12:14:28
8. `check_category` confirms category 243 matches vendor history. ✓ 12:14:38
9. `record_trace` (task_name `document-intake`) with honest `problems[]` entries disclosing the OCR degradation. ✓ 12:14:50
10. `judge_trace(432)` → **PASS** (deterministic checks all green). 12:14:53
11. Dashboard callback `/api/expense-stored` `expense_id=2212`. ✓ 12:15:04

## Defect found on independent review

I opened the archived receipt image directly (`readable_documents/receipts/2026/august/august_21/psa_rx_sl_fed_15_44_08_21_26_41_23.jpg`). It plainly prints **two real RX charges** — $25.79 and $15.44 — plus a $0.00 line, summing exactly to the $41.23 total. Mazda's `items[]` had collapsed this into **one fabricated item** equal to the total, which was actually OCR of the *summary* line ("TOTAL FSA AND RX ITEM"), not a purchased item. Her own `raw_text` already contained both real amounts (`"FSA hy ET , 25,79"`, `"PSA RX Sl fed? 15,44"`) — a mismatch between `items[]` (1 entry) and `raw_text` (2 distinct dollar amounts) that should have triggered re-derivation and itemization. She stored expense 2212 as a single STANDALONE row instead. The deterministic judge did not catch this (PASS on trace 432), so this was a real intake defect that survived to a PASS verdict — a rubric gap, not just a wrapper gap.

## Wrapper defect diagnosed

Missing guard: when a receipt parse's own `engine_failure`/degraded-OCR fallback produces `items[]` that disagrees with the distinct dollar amounts visible in `raw_text`, the wrapper has no rule telling Mazda to distrust the collapsed item list and re-derive items before storing. She trusted the single-item output at face value.

## Lesson sent to Mazda (12:31 UTC, this session)

Full corrective message posted to her conversation: pointed out the two real line items ($25.79, $15.44), explained the raw_text/items[] mismatch as the signal she missed, and required (1) `itemize_existing_expense` on 2212 with two line items reconciling to $41.23, (2) generic-but-correct-amount item descriptions, (3) verification of `itemization_parent_id`/`itemization_reconciled`/child ids, (4) a fresh trace/judge/callback, (5) a durable memory note capturing the general rule (items[] vs raw_text amount-count mismatch ⇒ re-derive before storing).

## Correction — verified

- `itemize_existing_expense(expense_id=2212)` → `itemizable:true`, `itemized:true`, reconciled exactly, difference `0.00`. New line items **2213** ($25.79) and **2214** ($15.44), both category 243. ✓ 12:31:49
- `record_trace` (corrected, itemized evidence) → trace_id 434. ✓ 12:32:00
- `judge_trace(434)` → **PASS**. ✓ 12:32:03
- Dashboard callback repeated with `expense_ids:[2212,2213,2214]`, `stored:3`. ✓ 12:32:10 — confirmed via `/api/expense-stored-events`: second event for this `conversation_id`/`dispatched_at` shows `expense_ids:[2212,2213,2214]`, `stored:3`.
- `propose_memory_note` (Rule 64, missed_itemization) → gate chain `allow` (safety/cost/regression all allow), proposal_id 263. ✓ 12:32:23

Document is now correctly stored: one reconciliation-anchor parent (2212, category NULL by convention) plus two itemized RX charges reconciling cent-exact to the printed total.

## Escalation

repo_path: /home/adamsl/letta-code (dashboard) or /home/adamsl/rol_finances (unclear which side owns the lookup)
bug_description: |
  Before coaching/itemization, `GET /supporting-document/2212/receipt` returned HTTP 200
  with the real 433,796-byte receipt JPEG. After `itemize_existing_expense` split expense
  2212 into parent 2212 + children 2213/2214, the SAME endpoint began returning 404 for
  2212, and also 404 for the new children 2213/2214 — reproduced on 4 separate retries
  over ~45 seconds, not a one-off race. `POST /api/supporting-documents {"expense_id":2212}`
  (and 2213, 2214) all now return `{"ok": false, "error": "No matching expense in DB."}`
  even though `check_category`/`itemize_existing_expense` tool returns confirm these rows
  exist and are correctly categorized. This means the Set-Category "View Receipt" dialog
  action and the red-box annotator are currently unreachable for this document's expense(s)
  — an unverifiable receipt per the intake contract's dialog-visibility requirement — even
  though the underlying finance-DB storage and itemization succeeded. Suspect either a stale
  read-cache/connection in the dashboard's supporting-document lookup, or a query that
  doesn't account for a just-itemized parent row. This is not something Mazda's wrapper can
  fix; her tool returns and the finance DB (via her own MCP tools) are self-consistent.
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2026/august/august_21/psa_rx_sl_fed_15_44_08_21_26_41_23.jpg
  failing_command: 'POST /api/supporting-documents {"expense_id":2212}' and 'GET /supporting-document/2212/receipt'

## For a human

- Verify red-box rendering for expenses 2212/2213/2214 once the supporting-document lookup issue above is resolved — it could not be checked at all during this run.
- Minor, non-blocking cosmetic note: the receipt archive filename is derived from the garbled OCR merchant text (`psa_rx_sl_fed_15_44_...`) rather than the resolved `walgreens` vendor key, and the store tool's own `archive_years:[1970]` (from the placeholder date) disagrees with the actual `2026/august/august_21` archive folder (today's date). Neither affects correctness of the stored data, but both are worth a look if the store tool is touched.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- expense `2212`, metadata: No matching expense in DB.
- expense `2213`, metadata: No matching expense in DB.
- expense `2214`, metadata: No matching expense in DB.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
