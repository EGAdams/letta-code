# Trainer Report — Window Scanner

**Document:** `window_scan_1786495488379227824_cca2bb28b112.jpg` (Culver's #264 receipt, Jenison MI, 2025-04-24, $34.58)
**Dispatch:** 2026-08-12T00:44:52Z · **Conversation:** conv-d582420e-6708-4954-bcf2-57c572de983e
**Verdict: FAIL**

> This is an **application defect**, not a Mazda mistake — see Escalation. Mazda followed
> the contract exactly, including her own fallback duplicate check, and self-reported the
> anomaly in her `record_trace` `problems` array without being prompted. No coaching
> message was sent because there is no wrapper fix that would prevent this; the defect is
> in `rol_finances`' save-path duplicate linking.

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned wrap-v005, instructions loaded | ✅ |
| STEP 0 classify (facade `doc_kind=unknown`) | `classify_scan.py` → Gemini 401, fell back to Codex CLI → `receipt`, confidence 0.99, Culver's | ✅ correct fallback |
| Receipt parse | `parse_and_categorize.py --json` → Gemini 401, fell back internally to chatgpt-oauth (gpt-5.4) → full itemized JSON, `expense_scope: full_receipt`, `handwritten_arithmetic_total: 34.58` matches printed total | ✅ |
| `check_vendor_key` | supplied `culvers` → normalized to `culvers_of_jenison`; Mazda adopted the normalized key in every subsequent call | ✅ |
| `check_duplicates` (MCP tool) | failed: `Access denied for user 'adamsl'@'localhost'` | ❌ tool-level auth failure |
| Fallback duplicate check (wrapper Rule 3) | raw `executor_run` pymysql query with explicit creds found **existing expense id 979**, same date (2025-04-24), same amount (34.58), description `CULVER'S OF JENISON JENISON MI`, all four supporting-doc fields null | ✅ correctly executed fallback, correctly identified a real duplicate |
| `categorizer_main.py` | category_id 392 via gemini provider | ✅ |
| `check_category` | present, matches vendor | ✅ |
| `parse_and_categorize.py --save --category-id=392` | **created a brand-new expense id 2024** with `"duplicate": false, "linked_counterpart": false` — despite the fallback check above finding an exact date+amount+vendor match on id 979 | ❌ **contract violation — see Escalation** |
| `itemize_existing_expense` | refused fail-closed: one line item (Culver's Vinaigrette) has a non-positive $0.00 amount, violates `chk_expense_amount_pos` | ✅ correct fail-closed refusal, unrelated to the duplicate issue |
| `record_trace` | `task_name=document-intake`, `is_duplicate:true`, `stored:true`, `expense_id:2024`, `linked_counterpart:false`; `problems` array **explicitly documents the exact defect**: *"check_duplicates MCP authentication failed; equivalent authenticated executor DB check found existing expense id 979 with same date and amount, but receipt store created expense id 2024 instead of attaching as duplicate; duplicate_expense_ids=[979]"* | ✅ honest, accurate self-report |
| `judge_trace` | returned PASS ("all deterministic intake checks passed") | ⚠️ rubric doesn't currently check cross-reference/duplicate-linkage outcomes, so it missed this — worth a future rubric enhancement, but not the primary issue |
| Dashboard callback | first attempt failed (`Invalid JSON`, a curl heredoc/quoting issue — self-diagnosed and retried via a temp file); retry succeeded with `{"ok": true}`, correctly carrying `expense_ids:[2024]`, `duplicate_expense_ids:[979]` | ✅ (after self-correction) |

## Independent verification (red box / dialog visibility)

- `GET /supporting-document/2024/receipt` → 200, JPEG, bytes differ from the raw archived file (annotated copy confirmed present). Dialog visibility contract satisfied **for the wrongly-created expense**.
- `GET /supporting-document/979/receipt` → 404 (expected — 979 never got a receipt attached, which is exactly the problem).

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py --save
  created a new expense (id 2024) for a Culver's #264 receipt (2025-04-24, $34.58)
  even though an existing expense (id 979, id_light
  culvers_of_jenison_04_24_25_34_58, same date/amount/description
  "CULVER'S OF JENISON JENISON MI") was already on file with no receipt_url attached.

  Expected: per the intake contract, a detected receipt duplicate should cause --save
  to attach/upgrade receipt_url on the MATCHED expense (979) and return
  duplicate:true / linked_counterpart:true, inserting no second row.

  Actual: --save returned {"duplicate": false, "linked_counterpart": false,
  "expense_id": 2024}, silently inserting a second expense row for the same
  real-world transaction. The $34.58 Culver's charge is now double-counted in
  nonprofit_finance.expenses (979 and 2024).

  Mazda (the calling agent) is not at fault: she ran the correct fallback
  duplicate check per her wrapper's Rule 3 (the check_duplicates MCP tool itself
  failed with "Access denied for user 'adamsl'@'localhost' (using password: NO)"),
  found the exact match via a manual authenticated query, and reported the
  discrepancy verbatim in her trace evidence's `problems` field before this
  Trainer ever inspected the transcript.

  Suspected cause: --save's own internal duplicate-detection likely hits the
  same MySQL auth/credentials problem that broke the check_duplicates MCP tool
  in this environment, and silently treats an unreachable/failed duplicate
  lookup as "no duplicate" instead of halting (contrast with the documented
  2026-08-02 halt-on-crash fix for a similar class of bug — that fix does not
  appear to cover this code path, since this run returned success, not
  {"halted": true}).

  Note: the current letta-code git branch is `fix/intake-duplicate-rows`,
  suggesting this exact defect class is already being tracked/worked.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786495488379227824_cca2bb28b112.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan.jpg> --save --category-id=392 --parsed-json /tmp/mazda_receipt_164c191df820.json --engine=gemini
  duplicate_pair: [979, 2024]
```

## For a human

Two live expense rows (979 and 2024) now represent the same $34.58 Culver's charge
on 2025-04-24. Recommend manually merging/attaching the receipt to 979 and
voiding/deleting 2024 once the underlying `--save` duplicate-detection bug above is
fixed, so this doesn't recur on the next receipt scan that happens to match an
existing statement-derived row.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
