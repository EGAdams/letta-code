# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1785010831907613219_e813fe613c09.jpg` (Meijer receipt)
- **Dispatch**: 2026-07-25T20:20:36Z, conversation `conv-d69f9c76-38ad-4417-8281-26f54ff0b685`
- **Verdict: CORRECTED** (initial run was a FAIL that Mazda self-judged PASS; corrected after coaching)

## Checklist (initial run, trace 197)

| Step | Evidence | Result |
|---|---|---|
| load_wrapper_revision | wrap-v048 loaded | OK |
| STEP 0 classify_scan.py | `doc_type=receipt, confidence=0.98` (Gemini quota-exhausted fallback path used) | OK |
| Dry-run parse (`--json`) | 3 items incl. `MEIJER SAVINGS -6.27`, `expense_scope=full_receipt`, `total_amount=22.89` | OK |
| check_vendor_key | `meijer`, recognized | OK |
| check_duplicates | ran at **amount=22.89**, not duplicate | OK (but see defect) |
| categorizer_main | `category_id=3` | OK |
| Save parse (`--save --category-id=3`) | **independent second Gemini call** returned only 2 items (dropped the savings line), `expense_scope=marked_items`, id_light `meijer_03_11_25_29_16` → stored at **$29.16** | **defect** |
| itemize_existing_expense | correctly refused (`itemizable:false`, items don't reconcile) | OK |
| record_trace | flagged the $22.89 vs $29.16 conflict in `problems`, but still reported `stored:true` without resolving it | **defect** |
| judge_trace(197) | PASS (deterministic checks only see "is there a category/vendor/dup-check", not cross-parse consistency) | **false PASS** |
| dashboard callback | sent with **wrong amount 29.16** | **defect** |

## Wrapper defect diagnosed

Two independent Gemini vision calls on the same receipt image (dry-run parse vs. save-time parse) disagreed on line items, scope, and total ($22.89 vs $29.16). The receipt's own OCR raw text confirms $22.89 as the printed total (subtotal $29.16 − $6.27 Meijer Savings discount). The save-time parse misread two "above/beside" handwritten labels ("Light Bulbs", "Church expense") as marked-item selection evidence, which the contract explicitly excludes (only circle/box/check/arrow/highlight/underline count). Mazda noted the contradiction in her own `problems` field but stored and reported the wrong ($29.16) amount anyway, and never re-ran `check_duplicates` against the value actually stored. This is a real risk of a $6.27 overcharge landing in the finance DB.

## Coaching sent

Sent a corrective message identifying the exact defect, requiring: (1) correct expense 1567 to $22.89/`full_receipt` scope, (2) no new duplicate check needed since $22.89 was already verified, (3) re-run record_trace/judge_trace with corrected evidence, (4) re-send the dashboard callback, (5) file `propose_improvement` since this was a FAIL.

## Corrective run (trace 198)

- Corrected expense 1567: `amount=22.89`, `id_light=meijer_03_11_25_22_89`, `receipt_url` renamed, metadata reset to 3-line `full_receipt` payload.
- `record_trace` → trace 198 with corrected evidence.
- `judge_trace(198)` → PASS.
- `propose_improvement` → proposal 147 filed (`failure_type=other`), summary correctly captures the same-image parse-reconciliation lesson.
- Dashboard callback re-sent with `amount:"22.89"` → `{"ok": true}`.

## Secondary defect (not blocking, flag for a human)

Mazda performed the DB correction via a raw ad-hoc Python script calling `app.db.pool.execute()`/`query_one()` directly (hand-written SQL), after her coaching explicitly said not to, because she could not find an exposed correction/update CLI path in `parse_and_categorize.py`. She surfaced this gap honestly rather than hiding it. **Recommend**: add a proper `--correct`/`--update` mode to `parse_and_categorize.py` (or a dedicated correction tool) so future amount/scope corrections don't require hand-rolled SQL.

## Summary

The initial intake run stored a Meijer receipt at $29.16 instead of the correct, OCR-confirmed $22.89 total, because two independent vision passes on the same image disagreed and Mazda's judge scored it PASS despite her own trace flagging the conflict — a genuine overcharge risk that would have gone unnoticed. After Trainer coaching, Mazda corrected the stored expense to $22.89, re-recorded and re-judged the trace (still PASS, now on accurate evidence), filed an improvement proposal capturing the reconciliation rule, and resent the dashboard callback with the corrected amount. Final state: **CORRECTED**. One secondary, non-blocking gap was surfaced: no canonical correction-CLI path exists yet, forcing a raw DB script for the fix — recommended as a follow-up wrapper improvement.
