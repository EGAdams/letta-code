# Mazda Trainer Report — Freezer Scanner

**Document:** `scan_freezer_1786042374745407882_6c6e9b067879.jpg`
**Dispatch:** 2026-08-06T18:52:58Z
**Conversation:** conv-d3197fba-ee3a-47e7-9d83-02c412b3a719
**Run duration:** ~51s (dispatch to `judge_trace` return)

Verdict: PASS

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned wrap-v002 | ✅ |
| STEP 0 classify (facade returned `doc_kind=unknown`) | `executor_run classify_scan.py` → `{"doc_type":"other","confidence":0.96,"merchant":"RIVER OF LIFE MINISTRIES INC.","reason":"scanned check/deposit image, not a receipt, invoice, bank statement, or tax form"}` (Gemini 401'd, fell back to Codex CLI, which succeeded) | ✅ correct classification |
| Routing on `doc_type=other` | No receipt/invoice/statement/moms_ledger branch entered; no vendor/category/duplicate/store calls attempted | ✅ correct — `other` is genuinely unsupported per contract |
| `record_trace` | `task_name="document-intake"`, evidence honestly reflects `doc_kind="other"`, `stored:false`, `expense_id:null`, `vendor_key:null`, problems list names the unsupported classification and the Gemini→Codex fallback | ✅ accurate |
| `judge_trace` | Verdict **FAIL** (`missed_vendor_key`), reasons: failed vendor_key_resolved/category_resolved/duplicate_check_ran/expense_stored | ❌ **judge rubric defect**, not a Mazda failure — see Escalation |
| `propose_improvement` (required on FAIL) | Filed proposal 40, summary correctly names the rubric mismatch for unsupported docs | ✅ |
| `apply_proposal` | Activated wrap-v003 (gates all passed) adding an instruction note about the rubric | ⚠️ well-intentioned but cannot actually fix a deterministic Python rubric — see Escalation |
| Dashboard callback | `POST /api/expense-stored` → `{"ok":true}`, `status:"unsupported_document"`, `stored:0`, no expense ids | ✅ |

## Why this is graded PASS despite the judge saying FAIL

Per Trainer policy, the top-line verdict reflects Mazda's fulfillment of the intake
contract, not a blind pass-through of `judge_trace`. This document is a scanned
check/deposit image — explicitly out of scope (`doc_type=other` is the one
unsupported classification in the contract). Mazda classified it correctly,
correctly declined to run any receipt/invoice/statement/moms_ledger branch,
recorded truthful evidence, and notified the dashboard. Nothing in her handling
was wrong.

## Root cause of the judge FAIL (verified in source)

`agent_self_improvement/implementations/evaluation/intake_verdict.py` has
explicit not-applicable branches for `moms_ledger` (`_is_moms_ledger`, ~line 154),
`statement` (`_is_statement`, ~line 192), and `intake_halted` (~line 129), but
**no branch for `doc_kind == "other"`** (or any other unsupported/unclassifiable
kind). Such docs fall through to the generic receipt-path checks at line 227+,
which unconditionally require `vendor_key_resolved` etc. — fields that cannot
exist for a document with no vendor/category/transaction to extract. This is a
deterministic-code defect, not a wrapper/prompt issue: Mazda's `apply_proposal`
(wrap-v003 instruction note) has no ability to change this Python file, so the
next `other`-classified document will FAIL the same way until the code is
patched.

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  intake_verdict.py's IntakeJudge scoring has explicit not-applicable branches
  for doc_kind in {moms_ledger, statement} and for intake_halted runs, but no
  branch for doc_kind == "other" (the contract's designated unsupported-document
  classification). An "other"-classified document (e.g. a scanned check/deposit
  image) falls through to the generic receipt-path checks starting around line
  227, which require vendor_key_resolved/category_resolved/duplicate_check_ran/
  expense_stored — fields that are correctly never populated for an unsupported
  document. This produces a FAIL verdict (failure_type=missed_vendor_key) on a
  run where Mazda did everything the contract requires (classify, correctly
  decline to process, record honest evidence, notify dashboard).
  Fix: add an `_is_unsupported(doc_kind)` (or similar) branch mirroring the
  moms_ledger/statement carve-outs, auto-passing VENDOR_KEY_RESOLVED,
  CATEGORY_RESOLVED, DUPLICATE_CHECK_RAN, EXPENSE_STORED (and the
  itemization/statement/ledger not-applicable checks) whenever doc_kind is a
  recognized unsupported classification with reasonable confidence, so the
  verdict reflects the correct-non-store outcome instead of penalizing it.
  Mazda's own proposal 40 / wrap-v003 instruction note cannot fix this — it is
  a deterministic Python rubric, not something her wrapper controls.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786042374745407882_6c6e9b067879.jpg
  failing_command: judge_trace(trace_id=79) via Mazda's Letta tool, backed by agent_self_improvement's IntakeJudge
  trace_id: 79
  suspected_file: agent_self_improvement/implementations/evaluation/intake_verdict.py (~lines 148-232)
```

## For a human

- No coaching message was sent to Mazda — she did nothing wrong on this run.
- Trace 79 / proposal 40 / wrap-v003 are all legitimate self-improvement
  artifacts and can stay active; the instruction note is harmless even though
  it doesn't fix the underlying rubric bug.
- Once `intake_verdict.py` gets an unsupported-doc_kind branch, expect trace 79
  (and any future `other`-classified scan) to judge correctly without any
  further Mazda-side change.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
