# Mazda Trainer Report — Window Scanner

Verdict: PASS

**Document:** `window_scan_1785633172735141970_e41c3e52815d.jpg` (Window Scanner)
**Dispatch:** 2026-08-02T01:12:56Z · Mazda conversation `conv-12cf1090-1995-4bf3-a4ae-bb694c3309e9`
**Run duration:** ~3.5 minutes (01:13:00 → 01:16:06)

## Facade result
`doc_kind=unknown, confidence=0.0` — expected for a JPEG scan with no extractable text. STEP 0 vision classification required, and Mazda ran it.

## Step-by-step checklist (with evidence)

1. **`load_wrapper_revision`** (01:13:00) → `wrap-v070`, found=true. ✅
2. **STEP 0 classify** — `executor_run classify_scan.py` (01:13:42) →
   `doc_type=receipt, confidence=1.0, merchant="JEWS FOR JESUS"`. ✅
3. **Parse (non-save)** — `parse_and_categorize.py --json --engine=gemini` (01:14:13) →
   single line item "Gift designated as Jewish Evangelism", $45.00, transaction_date
   2025-03-20. Not a marked-items receipt (whole-receipt, single line, no selection marks) —
   correctly treated as full-receipt scope. ✅
4. **`check_vendor_key`** (01:14:18) → `vendor_key="jews_for_jesus"`, present=true,
   recognized=false (new vendor, not yet in the known-vendor map — expected, not an error).
   Mazda used this exact key downstream. ✅
5. **`check_duplicates`** (01:14:21) → `is_exact_duplicate=false, in_database=false`. ✅
6. **Categorize** — `categorizer_main.py --provider=auto` (01:15:04) → gemini/chatgpt-oauth
   returned no suggestion, gemini second pass resolved `category_id=220`. Fail-closed
   contract satisfied (non-null, non-zero category before save). ✅
7. **Store** — `parse_and_categorize.py --save --category-id=220` (01:15:19) →
   success, `expense_id=1979`, canonical archive path
   `readable_documents/receipts/2025/march/march_20/jews_for_jesus_03_20_25_45_00.jpg`. ✅
8. **`itemize_existing_expense`** (01:15:37) → `itemizable=false`, reason: "only one line
   item; a single line equal to the charge is standalone, not itemized". Correct fail-closed
   outcome — single-item receipt stays STANDALONE. ✅
9. **`record_trace`** (01:15:48) → `task_name="document-intake"`, trace_id 337, evidence
   matches the run (doc_kind=receipt, vendor_key_recognized=false, category_id=220,
   stored=true, expense_id=1979). ✅
10. **`judge_trace`** (01:15:53) → `verdict=NEEDS_REVIEW, failure_type=none`, reason
    "vendor_key resolved but not in the known-vendor map". This is a legitimate outcome for
    a first-time vendor, not a broken stage — no coaching needed. ✅
11. **Dashboard callback** — `POST /api/expense-stored` (01:16:06) → `{"ok": true}`,
    payload carries `expense_id=1979`, `stored=1`, correct archive path/vendor_key. ✅

No `propose_improvement` call — correctly omitted, since the verdict was not FAIL.

## Supporting-document / dialog-visibility check

`POST /api/supporting-documents {"expense_id":1979}` → `receipt_url` populated
(`jews_for_jesus_03_20_25_45_00.jpg`), `documents[]` reports `receipt` `available:true` /
"View Receipt". `document_url`, `scanned_statement_url`, `moms_ledger` are correctly absent
(not applicable to a receipt). Dialog-visibility contract satisfied.

## Dashboard defect found (not Mazda's fault)

`GET /supporting-document/1979/receipt` served bytes **identical** to the raw archived
file (no red-box highlight applied). Dashboard log:
`[supporting-document] opened without highlight expense_id=1979 type=receipt reason=No high-confidence expense row was found in the image.`

The annotator (`document_annotation.py`) failed to OCR-match a row on this receipt image
and served the unboxed original. Per the Trainer contract this is a dashboard-side
annotation failure, not something Mazda can fix — she stored the correct data with the
correct evidence; the viewer just can't outline it. Filing as an escalation note rather
than a wrapper lesson.

## Coaching sent

None — no defects in Mazda's own steps.

## For a human

- Expense **1979** (JEWS FOR JESUS, $45.00, 2025-03-20) has `vendor_key_recognized=false`
  — a good candidate to add to the known-vendor map so future Jews for Jesus receipts
  resolve without an LLM categorizer round-trip.
- The receipt viewer for expense 1979 opens without a red box (see escalation below).

## Escalation
repo_path: /home/adamsl/letta-code
bug_description: |
  document_annotation.py's OCR-match step failed to find a high-confidence expense row on
  a legitimately-stored receipt (expense_id=1979, JEWS FOR JESUS donation receipt, single
  line item "Gift designated as Jewish Evangelism", $45.00, 2025-03-20). The dashboard log
  records "[supporting-document] opened without highlight ... reason=No high-confidence
  expense row was found in the image." and GET /supporting-document/1979/receipt served
  the raw, unannotated file (byte-identical to the archived original) instead of a
  red-boxed copy. This is not the previously-documented handwritten-notes-across-amount
  case — it is a plain, unmarked receipt, so the OCR/line-matching logic in
  document_annotation.py is missing a case it should be able to handle.
metadata:
  document_path: /home/adamsl/rol_finances/readable_documents/receipts/2025/march/march_20/jews_for_jesus_03_20_25_45_00.jpg
  failing_command: GET /supporting-document/1979/receipt

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
