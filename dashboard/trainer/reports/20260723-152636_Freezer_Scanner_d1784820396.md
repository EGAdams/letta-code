# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-23T15:26:36Z
- Document path: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1784820393228325659_fe838c0f9985.jpg`
- Verdict: FAIL

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded at `2026-07-23T15:26:43Z` with `wrap-v035`.
- `classify_scan.py` returned `doc_type: receipt`, `confidence: 0.99`, merchant `MENARDS - COMSTOCK`.
- `parse_and_categorize.py` returned parsed receipt data for `2025-03-27` and `30.40`, then `check_vendor_key` returned `recognized: false` for `vendor_key: menards`.
- `check_duplicates` returned `is_exact_duplicate: false`.
- `categorizer_main.py` returned `category_id: null`.
- `parse_and_categorize.py --save --engine=gemini` stored `expense_id: 1530` with `stored: true`, `expense_status: NEEDS_VENDOR_KEY`, and `pending_vendor_review: true`.
- `itemize_existing_expense` returned `itemizable: false`, which was correct for a single-line standalone receipt.
- `record_trace` succeeded with `task_name: "document-intake"` and `trace_id: 139`.
- `judge_trace(139)` returned `NEEDS_REVIEW` with reason `vendor_key resolved but not in the known-vendor map`.
- Dashboard notify POST to `/api/expense-stored` returned `{"ok": true}`.
- I sent a corrective message telling Mazda to call `propose_improvement(trace_id=139, failure_type="missed_vendor_key")` and record the MENARDS vendor-mapping lesson.
- Mazda then filed `propose_improvement` successfully: `proposal_id: 102`.
- Mazda also filed a memory proposal: `proposal_id: 103`.

## Diagnosis

The run completed, but it did not produce a clean receipt intake. The receipt was stored, yet the vendor key stayed unrecognized and the category stayed `null`, so this is a wrapper/vendor-mapping failure, not a PASS.

## Lesson Sent

`The run stored expense 1530, but the receipt still ended with category_id=null / vendor_key="menards" not in the known-vendor map. That is a wrapper failure, not a clean pass. Call propose_improvement now with trace_id=139 and failure_type="missed_vendor_key". Also record this lesson in memory: MENARDS needs a recognized vendor mapping before save, otherwise the receipt stays in NEEDS_VENDOR_KEY review.`

## Human Follow-Up

- MENARDS needs a recognized vendor mapping so future receipts can resolve before save.
- The memory note was accepted as an approved proposal, but the live applier is not wired; if durable behavior is desired, apply the wrapper/memory fix in the normal maintenance path.
