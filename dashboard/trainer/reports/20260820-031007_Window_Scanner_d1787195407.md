# Trainer Report — Window Scanner

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

- Scanner: Window Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1787195404882410389_ea146ee66463.jpg`
- Dispatch timestamp: `1787195407` (2026-08-20T03:10:07Z)
- Mazda conversation: `conv-8f235c63-5b76-447a-899e-7d7a033bf6af`
- Deterministic facade result: `{"doc_kind": "unknown", "vendor": "unknown"}`

## What happened

Polled `GET /v1/conversations/conv-8f235c63-5b76-447a-899e-7d7a033bf6af/messages?limit=60`
repeatedly from dispatch time through **34 minutes** after dispatch (well past the
15-minute allowance). Every poll returned exactly **one message**: the standing system
prompt, timestamped the same second the conversation was created.

Fetched the conversation object directly to confirm this wasn't a pagination/query
artifact:

```
created_at: 2026-08-20T03:10:07.757551Z
updated_at: 2026-08-20T03:10:08.284286Z
in_context_message_ids: ["message-33598d6a-2373-4205-a7ed-2ca9f71e080b"]  (the system message only)
last_message_at: 2026-08-20T03:10:08.245941Z
```

The conversation was created and never touched again — `updated_at` /
`last_message_at` are ~0.5s after `created_at`. **No dispatch/user message was ever
posted into this conversation**, so Mazda never received the task at all: no
`load_wrapper_revision`, no STEP 0 classification, no tool calls of any kind.

Also checked `GET /api/expense-stored-events?since=1787195407` — empty, consistent
with no work having occurred.

## Checklist against the contract

Every required step (1–9) is **absent**, because the run never started. This is not a
graded failure of Mazda's behavior — there is no behavior to grade.

## Diagnosis

This is an **infrastructure problem** (per the instructions: "If she stalled entirely
and never picked up the dispatch, note that as an infrastructure problem (Letta server /
executor), not a Mazda lesson"). Specifically, the dispatch mechanism that is supposed to
post the intake task as a user message into `conv-8f235c63-5b76-447a-899e-7d7a033bf6af`
never delivered it — the conversation exists (created at the right second, tied to the
right agent, `agent-6b536cf4-ec88-4290-b595-fed21d14bd8e`) but received nothing after
its system prompt.

No coaching message was sent to Mazda: there is nothing in this conversation to correct,
and sending an arbitrary prompt into a stalled/undispatched conversation would not
reproduce the real intake flow the dispatcher is supposed to create.

## For a human

The Window Scanner dispatcher (whatever process creates the conversation and is supposed
to send the initial task/document message to Mazda) needs to be checked — it created
`conv-8f235c63-5b76-447a-899e-7d7a033bf6af` successfully but never sent the actual
dispatch content into it. This looks like a dispatcher-side bug (conversation created,
message send step skipped or failed silently) rather than a Letta-server-wide outage,
since conversation creation itself succeeded. Recommend re-dispatching this scan and
checking dispatcher logs around `2026-08-20T03:10:07–03:10:08Z`.

No escalation block filed — this is not an application-code defect in Mazda's tools or
the dashboard; it's a missing message delivery in the dispatch pipeline, outside what
this Trainer run can directly diagnose further from the conversation API alone.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
