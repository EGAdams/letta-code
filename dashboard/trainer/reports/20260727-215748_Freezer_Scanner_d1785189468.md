# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Scanned image:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785189463928856292_d2a50fdf911f.jpg`
- **Dispatch time:** 2026-07-27T21:57:48Z (unix 1785189468)
- **Mazda conversation:** `conv-f230feea-08a3-4969-840f-eb22ce25ff8c`
- **Verdict: PASS**

## Facade

`doc_kind=unknown, confidence=0.0` — STEP 0 (self-classification) correctly required and performed.

## Checklist (evidence = actual tool returns observed in transcript, all after dispatch ts)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v059` loaded. ✅
2. **STEP 0** `executor_run classify_scan.py` → `{"doc_type":"receipt","confidence":0.98,"merchant":"MR BURGER RESTAURANT 1"}`. ✅ Required and done since facade was unknown.
3. `executor_run parse_and_categorize.py --json --engine=gemini --write-parsed-json /tmp/mazda_receipt_01e30132454b.json` → source-bound parse artifact written. Single line item (`USD $31.93`), `expense_scope: "full_receipt"`, `total_amount: 31.93`. No visible marks in raw OCR text — `full_receipt` scope is correct (not a marked-item receipt). ✅
4. `check_vendor_key("mr_burger_restaurant_1")` → normalized to known key `mr_burger` (fuzzy match plausible given identical merchant name). Adopted correctly in trace evidence. ✅
5. `check_duplicates(expense_date=2025-03-27, amount=31.93, description="MR BURGER RESTAURANT 1")` → `is_exact_duplicate:true, exact_duplicate_expense_id:1673`. ✅ Store correctly skipped per contract ("detected duplicate means: skip store, still trace + judge").
6. `executor_run categorizer_main.py` → `category_id:137` (categorization run despite duplicate — harmless/optional, not required to skip). ✅
7. `record_trace(task_name="document-intake", ...)` → evidence JSON has `stored:false, expense_id:null, is_duplicate:true, duplicate_checked:true`, `problems:["exact duplicate detected in database: expense_id=1673"]` — matches the duplicate-only evidence contract exactly (canonical existing ID surfaced, never represented as newly stored). ✅
8. `judge_trace(274)` → `verdict:PASS, failure_type:none`. ✅ Consistent with a correctly-detected, correctly-recorded duplicate.
9. Dashboard callback: `POST /api/expense-stored` with `expense_id:null, expense_ids:[], duplicate_expense_ids:[1673], stored:0, parsed:1, receipt_url` and `document_path` both set to the scan path. ✅ Correct field (`receipt_url`, not `document_url`) for the receipt branch.
10. `propose_improvement` — not called, correctly, since verdict was PASS (no failure to diagnose).

## Wrapper defect diagnosed

None. This run followed every rule in her currently-active wrapper (`wrap-v059`), including the explicit "Operator guard: duplicate-only evidence" rule already baked into her instructions.

## Coaching sent

None required — no corrective message sent to Mazda.

## Notes for a human

None. Clean, fast (~2 min), fully-compliant duplicate-detection run.
