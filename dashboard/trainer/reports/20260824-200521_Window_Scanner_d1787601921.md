# Mazda Trainer Report — Window Scanner

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

- Scanner: Window Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1787601917376907745_39a65c67fed2.jpg`
- Dispatch time: 1787601921 (2026-08-24T20:05:21Z)
- Mazda conversation: `conv-72e6da6c-da3b-47f9-95d7-0e698dbf92a5`
- Deterministic facade: `doc_kind=unknown`, `recommended_action=reject`, `confidence=0.0` — so a correct run required Mazda to perform STEP 0 (self-classification via `classify_scan.py`) before anything else.

## What I observed

Polled `GET /v1/conversations/conv-72e6da6c-da3b-47f9-95d7-0e698dbf92a5/messages?limit=200` repeatedly for the full 15-minute grace window (checks at roughly T+0, then every ~30s out past T+15:00). At every single poll the conversation contained **exactly one message**:

```
2026-08-24T20:05:21+00:00  system_message  (no name)
```

The content of that message is the generic base Letta Code CLI system template ("You are Letta Code, a persistent coding agent that lives across sessions...") — infrastructure boilerplate, not Mazda's finance-orchestrator persona/human/db_schema/environment/team_agents/verification_procedure blocks described in her manual (Ch. 2.1, six projected blocks). Its timestamp coincides exactly with the dispatch time, consistent with it being the conversation's base system prompt rather than any content Mazda authored.

Critically: **no user-role dispatch message, no assistant reply, and no tool_call/tool_return entries ever appeared** — not `load_wrapper_revision`, not `classify_scan.py`, nothing. I confirmed the agent ID resolves correctly to an agent named "Mazda" (`GET /v1/agents/agent-6b536cf4-...` → `name: "Mazda"`), so this is the right agent/conversation pairing; the conversation simply shows no evidence the dispatch was ever delivered into it or that Mazda ever picked it up.

## Checklist against the contract

None of the required steps (1. `load_wrapper_revision`, STEP 0 classification, investigate, categorize, store, `record_trace`, `judge_trace`, dashboard notify) have any evidence in the transcript. There is nothing to grade — the run never started.

## Wrapper defect diagnosis

Not applicable to Mazda's wrapper — she never received a turn to act on. This looks like a **delivery/infrastructure problem**: either the dispatch message never reached `conv-72e6da6c-da3b-47f9-95d7-0e698dbf92a5`, or the Letta server/executor never scheduled a run for it. Per the Trainer contract, an unpicked-up dispatch is an infrastructure issue, not a Mazda lesson, and I did not send her any corrective message.

## For a human

- Confirm the dispatcher (`run_mazda_trainer.mjs` or whatever posted this dispatch) actually delivered a user message to `conv-72e6da6c-da3b-47f9-95d7-0e698dbf92a5` — the message list here never shows one.
- Check whether the Letta server or the agent's run queue is backed up/stalled for agent `agent-6b536cf4-ec88-4290-b595-fed21d14bd8e`.
- No document work happened at all for `window_scan_1787601917376907745_39a65c67fed2.jpg`; it will need to be redispatched once the delivery problem is fixed.

No escalation block filed — I have no evidence of an application-level defect (traceback, broken endpoint), only an absence of any activity, which points at message delivery/scheduling rather than a specific code path I can name.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
