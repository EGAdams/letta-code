# Trainer Report — Window Scanner

**Verdict: PASS**

- Scanner: Window Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785687293838487012_4468f22d353d.jpg`
- Dispatch: 2026-08-02T16:14:57Z
- Mazda conversation: `conv-69b5250c-0226-4ebe-8591-546add7315de`
- Facade result: `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required.

## Checklist (all with successful tool returns, in order)

1. `load_wrapper_revision` → `wrap-v071` loaded. ✅
2. STEP 0 self-classify: `executor_run classify_scan.py` → `doc_type=receipt, confidence=0.95` (an AT&T wireless bill notification showing a negative/no-balance-due balance). Since it resolved to `receipt`, ran the parser next per contract. ✅
3. `executor_run parse_and_categorize.py --json --engine=gemini --write-parsed-json` → single line item "Monthly wireless bill" $408.92, `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` (no override triggered — correct, nothing handwritten here). ✅
4. `check_vendor_key` → `at_t`, recognized=true. ✅
5. `check_duplicates` → `is_exact_duplicate=false`, `in_database=false`. ✅
6. `executor_run categorizer_main.py --provider=auto` → `category_id=124` (gemini succeeded, no fallback needed). ✅ Fail-closed category gate satisfied (nonzero, non-null).
7. `executor_run parse_and_categorize.py --save --category-id=124 --engine=gemini` → `{"success": true, "expense_id": 1984, "receipt_archive_path": ".../2025/march/march_10/at_and_t_03_10_25_408_92.jpg", "duplicate": false, "parse_artifact_verified": true, "expense_scope": "full_receipt"}`. Archive path matches the correct year/month/day convention. ✅
8. `itemize_existing_expense` → `itemizable:false`, reason "only one line item; a single line equal to the charge is standalone, not itemized". Correct fail-closed outcome for a single-line receipt — row correctly left STANDALONE, no hand-built SQL. ✅
9. `record_trace` — `task_name="document-intake"`, evidence JSON with `doc_kind`, `vendor_key`, `category_id`, `stored`, `expense_id` all populated and consistent with the store result. ✅
10. `judge_trace(343)` → **PASS**, `failure_type=none`. ✅
11. Dashboard callback `POST /api/expense-stored` → `{"ok": true}`, with `expense_id`, `expense_ids`, `vendor_key`, `archive_paths`, `conversation_id`, `dispatched_at` all correct. Confirmed landed in `/api/expense-stored-events?since=1785687297` matching this conversation/dispatch. ✅

## Independent verification (Trainer's own checks)

- **Merchant/category not blank/placeholder**: merchant="AT&T", category_id=124 — passes the fail-closed guard.
- **Red-box / dialog visibility**: fetched `GET /supporting-document/1984/receipt` from the local dashboard → HTTP 200, `image/jpeg`, 467730 bytes. Compared MD5 against the raw archived file (467317 bytes, different bytes/hash) — confirms the served copy is the annotated (boxed) version, not a pass-through of the original. `receipt_url` → View Receipt is correctly available and boxed.
- **Amount plausibility**: single $408.92 wireless-bill line item stored at the parsed total; no split/marked-item ambiguity, no handwritten arithmetic present to override scope.
- No other supporting-document types (statement/ledger) apply to this receipt — only `receipt_url` was expected and only `receipt_url` was set.

## Wrapper defect

None. Clean run, no coaching needed.

## For a human

Nothing to flag — routine successful receipt intake.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
