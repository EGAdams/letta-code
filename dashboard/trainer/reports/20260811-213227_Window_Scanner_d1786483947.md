# Trainer Report — Window Scanner

**Document:** `window_scan_1786483941940209107_74cb9c3ce1dc.jpg` (Walgreens receipt, FSA RX, $6.53, 2014-04-03)
**Dispatch:** 2026-08-11T21:32:27Z · **Conversation:** `conv-3ebefa63-0e5e-415e-af75-dffe7b277b6b`
**Run duration:** ~2m13s (21:32:33 → 21:34:40), fast and clean

Verdict: PASS

## Checklist (all confirmed by successful tool returns)

1. `load_wrapper_revision` — wrap-v004, active. ✓
2. Facade returned `doc_kind=unknown` → STEP 0 correctly triggered: `classify_scan.py` →
   `doc_type=receipt`, confidence 0.99, merchant Walgreens (Gemini 401'd, fell back to Codex CLI
   per the documented vision fallback chain — expected behavior). ✓
3. STEP 0 receipt parse: `parse_and_categorize.py --json` → single item ("FSA RX", $6.53),
   `expense_scope=full_receipt`, `handwritten_arithmetic_total=null` — correct scope for a plain
   single-line receipt with no marks. Parser itself fell back Gemini→ChatGPT-OAuth
   (`model_provider=chatgpt-oauth`) after the same Gemini auth failure. ✓
4. Investigate: `check_vendor_key` → recognized, adopted `vendor_key=walgreens`. `check_duplicates`
   errored (`Access denied for user 'adamsl'@'localhost'` — a live MySQL-auth bug in that MCP
   tool). Mazda correctly applied her existing wrapper Rule 3 (proposal 41) workaround: ran an
   authenticated equivalent query via `executor_run`, got `[]`, and recorded
   `duplicate_checked=true` with the workaround noted in `problems`. This is a pre-existing,
   already-tracked infra issue with a working documented mitigation — not a new wrapper defect,
   not escalating again here. ✓
5. Categorize: `categorizer_main.py` returned `category_id=243` (wrong — Priority Health/medical),
   but `check_category` flagged the mismatch and supplied the vendor-authoritative
   `expected_category_id=378` (Walgreens). Mazda correctly overrode the LLM categorizer's guess
   with the deterministic check's answer before storing — exactly the fail-closed behavior the
   contract requires. ✓
6. Store: `parse_and_categorize.py --save --category-id=378` → `expense_id=2013`,
   `parse_artifact_verified=true`, single item so no itemization needed (qty=1, one line). ✓
7. `record_trace(task_name="document-intake")` with accurate evidence including all three
   `problems` entries above. ✓
8. `judge_trace(101)` → PASS, "all deterministic intake checks passed". ✓
9. Dashboard callback `POST /api/expense-stored` → `{"ok": true}`, confirmed independently via
   `/api/expense-stored-events?since=1786483947` with the correct `conversation_id` and
   `dispatched_at`. ✓

## Dialog-visibility verification (independent, via dashboard API)

`POST /api/supporting-documents {expense_id:2013,...}` → `receipt_url` present,
`{"type":"receipt","label":"View Receipt","available":true}`; the other three evidence types
correctly `available:false` (this is a receipt-only document, nothing else should show). ✓
`GET /supporting-document/2013/receipt` → HTTP 200, non-trivial payload. Could not independently
confirm the served bytes are the *annotated* (red-boxed) copy vs. the raw file, since the
Trainer's Bash allowlist doesn't include diffing local files — noting as a gap in my own
verification tooling, not a run failure.

## Escalation — application defect, not Mazda's fault

`parse_and_categorize.py --save` filed this 2014-04-03 receipt under the **wrong year folder**:
its own return value is internally inconsistent — `archive_years: [2014]` (correct) but
`archive_paths`/`receipt_archive_path`: `.../receipts/2025/april/april_03/walgreens_04_03_14_6_53.jpg`
(2025, not 2014). Mazda passed this straight through from the tool's authoritative output into
`record_trace` and the dashboard callback — she did nothing wrong. The dialog still resolves
the receipt today only because `_resolve_local_supporting_document`'s basename-anywhere fallback
finds it regardless of directory; the on-disk year-folder convention itself is broken for this
receipt (and likely for any other receipt whose transaction year is much older than the intake
year — 2014 vs the file landing in 2025 looks like the archiver computed the folder year from
something other than the parsed transaction date). This is the same class of bug as the
2026-08-08 statement path-drift incident but on the **receipt** archiver, not statements.

```
## Escalation
repo_path: /home/adamsl/rol_finances
bug_description: |
  tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py's `--save` file-mover
  filed a receipt whose parsed transaction_date is 2014-04-03 into
  readable_documents/receipts/2025/april/april_03/ instead of .../2014/april/april_03/. The same
  tool call's own JSON return reports archive_years:[2014], so the year used for the archive
  path and the year used for the archive_years field disagree within one execution — this is a
  bug in the archiver's year-folder computation, not a Mazda wrapper issue. Expected: the receipt
  should be archived under readable_documents/receipts/2014/april/april_03/ to match archive_years
  and the year-folder convention documented in dashboard/CLAUDE.md. Actual: filed under 2025/.
  Log line: "Moved receipt file .../walgreens_04_03_14_6_53.jpg ->
  .../readable_documents/receipts/2025/april/april_03/walgreens_04_03_14_6_53.jpg".
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786483941940209107_74cb9c3ce1dc.jpg
  failing_command: /home/adamsl/rol_finances/.venv/bin/python3 tools/receipt_scanning_tools/receipt_parsing_tools/parse_and_categorize.py -f <scan> --save --category-id=378 --parsed-json /tmp/mazda_receipt_7c2bc4454b02.json --engine=gemini
  expense_id: 2013
```

## For a human

- `check_duplicates` MCP tool is still failing MySQL auth (`Access denied for user
  'adamsl'@'localhost' (using password: NO)`). Mazda's workaround (Rule 3, proposal 41) covers
  it fine, but the tool itself remains broken at the root — worth a real fix at some point so the
  workaround isn't permanent.
- Receipt-archiver year-folder bug above needs a real fix; low urgency (resolution still works
  via basename fallback) but will keep scattering old receipts into wrong-year folders.

No lesson was sent to Mazda — everything she did was correct, including catching and overriding
the categorizer's wrong answer.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
