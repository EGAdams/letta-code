# Trainer Report — Freezer Scanner

**Scanner:** Freezer Scanner
**Dispatch time:** 2026-08-14T16:27:55Z (unix 1786724875)
**Conversation:** conv-fca3dcba-b59f-4133-a39c-97f340e6863d
**Document:** `scan_freezer_1786724871975133679_594a26556882.jpg`
**Facade result:** `doc_kind=unknown, confidence=0.0` → STEP 0 required.

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

Fast, clean run (dispatch 16:27:55 → final assistant message 16:28:46, ~51s).

## What the document is

I viewed the scan directly. It is Samaritan's Purse's **"2025 Contribution Summary"** —
a year-end donor acknowledgment letter to Grand Rapids River of Life Ministry, dated
2026-01-20, stating a total deductible cash contribution for 2025 of **$3,633.00**
("This is a record of your 2025 contributions... by cash, check, or credit card").
This is an aggregate annual tax-substantiation letter, not a dated receipt/invoice for a
single transaction — the underlying checks/card payments that make up the $3,633 total
would already be captured individually elsewhere (bank statements) throughout the year.
Storing this letter as a single new expense would double-count donations already
recorded. `doc_type=other` is the correct classification per the manual's explicit
carve-out ("only `doc_type=other` is unsupported"), and I confirm it against the image,
not just Mazda's prose.

## Step-by-step checklist

1. **`load_wrapper_revision(agent_name="Mazda")`** — 16:28:00, returned `wrap-v074`,
   `is_active:true`. ✅
2. **STEP 0 classification** — 16:28:21, `executor_run` →
   `tools/classify_scan.py`, returncode 0, stdout:
   `{"doc_type":"other","confidence":0.97,"merchant":"Samaritan's Purse","reason":"...charitable contribution summary/acknowledgment, not a receipt, invoice, bank statement, or tax form."}`.
   Verified independently against the scan image — correct call. Per contract, `other`
   requires no receipt/invoice/statement/moms_ledger branch. ✅
3. **Investigate/Categorize/Store** — correctly skipped; `doc_type=other` is the one
   explicitly unsupported class in the contract. ✅
4. **`record_trace`** — 16:28:30, `task_name="document-intake"` (exact match),
   evidence JSON: `doc_kind:"other"`, `classification_confidence:0.97`,
   `vendor_key:null`, `category_id:null`, `stored:false`, `expense_id:null`,
   `is_duplicate:false`, `intake_halted:false`, `problems:["Unsupported document:
   charitable contribution summary/acknowledgment; explicit classifier doc_type=other,
   so receipt, invoice, statement, and Mom's ledger storage branches were not run."]`.
   Returned `trace_id:392`. ✅ Matches observed reality exactly.
5. **`judge_trace(392)`** — 16:28:34, returned `verdict:PASS, failure_type:none,
   reasons:["all deterministic intake checks passed"]`. Consistent with what I
   observed. ✅
6. **`propose_improvement`** — not required (verdict PASS, not FAIL). N/A. ✅
7. **Dashboard notify** — 16:28:42, `curl POST /api/expense-stored` with
   `conversation_id` and `dispatched_at` matching this exact dispatch,
   `expense_id:null, expense_ids:[], duplicate_expense_ids:[], stored:0, doc_kind:"other"`.
   Returned `{"ok":true}`. ✅

No supporting-document/dialog-visibility or red-box checks apply — nothing was stored,
so there is no expense row and no receipt/document/statement/ledger reference to verify.

## Wrapper defect diagnosed

None. No coaching message was sent.

## For a human

None — this run is a correct, complete, fail-closed no-op on an unsupported document
class. No escalation filed.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
