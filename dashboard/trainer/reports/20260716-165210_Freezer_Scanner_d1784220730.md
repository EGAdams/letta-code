# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-16T16:52:10Z
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: FAIL

## Checklist

1. `load_wrapper_revision` succeeded at `2026-07-16T16:52:14Z` with `wrapper_revision=wrap-v013`.
2. Vendor investigation ran: `check_vendor_key` succeeded and normalized `Chase` to `chase_mc_9915`; `check_duplicates` succeeded and returned `is_exact_duplicate=false`.
3. Categorization ran, but on the wrong branch: `categorizer_main.py` returned `category_id=3` via `executor_run`.
4. Store ran on the wrong branch: `parse_and_categorize.py --save --category-id=3` succeeded, but this was receipt storage, not statement storage.
5. No statement pipeline evidence appeared: no successful `parse_statement_scan.py` and no successful `store_statement_transactions.py`.
6. `record_trace` succeeded with `task_name="document-intake"` and `trace_id=80`, but evidence showed `doc_kind="statement"`, `transactions_parsed=0`, `transactions_stored=0`, and problems noting the statement branch was not used.
7. `judge_trace(80)` succeeded and returned `FAIL` with `failure_type="unsupported_document"` and reasons `statement_transactions_parsed` / `statement_transactions_stored`.
8. `propose_improvement(80, unsupported_document)` succeeded.
9. Dashboard `expense-stored` event stream for this dispatch was empty.

## Diagnosis

Wrapper defect: the statement branch was not enforced. Mazda routed a `doc_kind=statement` dispatch through the receipt/category/save path instead of `parse_statement_scan.py` then `store_statement_transactions.py`.

## Lesson Sent

I sent Mazda a corrective message stating that `doc_kind=statement/bank_statement` must use `parse_statement_scan.py` followed by `store_statement_transactions.py`, never `parse_and_categorize.py --save` or the categorizer, and to record that rule in memory.

## Human Follow-Up

Review why the statement branch was not applied despite the learned rule already being present in the loaded wrapper instructions. The current run is a clean FAIL, not a stalled run.
