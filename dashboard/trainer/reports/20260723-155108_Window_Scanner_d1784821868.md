# Mazda Trainer Report

- Scanner: `Window Scanner`
- Dispatch time: `2026-07-23T15:51:08Z`
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_1784821864340785162_b316f45a3e2e.jpg`
- Verdict: `PASS`

## Checklist

1. `load_wrapper_revision(agent_name="Mazda")`
   - Seen at `2026-07-23T15:51:13Z`
   - Returned `wrap-v036` active.

2. Classification
   - `executor_run` on `tools/classify_scan.py`
   - Returned `doc_type=receipt`, confidence `0.9`, merchant `Walgreens`.

3. Receipt branch
   - `parse_and_categorize.py --json --engine=gemini`
   - Returned parsed receipt data for `Walgreens`, date `2021-03-15`, total `141.26`.
   - `check_vendor_key` returned recognized `walgreens`.
   - `check_duplicates` returned non-duplicate on the first pass, then the later save path resolved to an existing duplicate `1533`.
   - `categorizer_main.py` returned `vendor_key=walgreens`, `category_id=243`.
   - `parse_and_categorize.py --save --category-id=243 --engine=gemini`
   - Final save return reported duplicate existing expense `1533` and `stored=0` on the corrected pass.

4. Trace / judge / callback
   - `record_trace(task_name="document-intake")` succeeded.
   - Final evidence payload at trace `144` was:
     - `doc_kind=receipt`
     - `vendor_key=walgreens`
     - `category_id=243`
     - `duplicate_checked=true`
     - `is_duplicate=true`
     - `stored=false`
     - `expense_id=null`
   - `judge_trace(144)` returned `PASS`.
   - Dashboard callback `POST /api/expense-stored` succeeded.
   - Expense-stored event recorded `duplicate_expense_ids=[1533]` for this conversation and dispatch.

## Wrapper defect observed

- Mazda initially stopped after classification and needed one corrective message to continue the receipt branch. That is a wrapper/procedure defect, not a model-quality issue.

## Lesson sent

- I told Mazda to continue the full receipt branch immediately after classification: parse, vendor key, duplicate check, categorize, save, then `record_trace`, `judge_trace`, and dashboard callback, and to record the lesson in memory.

## Human follow-up

- The final corrected run is clean and judged `PASS`.
- The duplicate path is important here: this scan resolved to existing expense `1533`, so the right outcome was duplicate-only intake with callback evidence, not a new store.
