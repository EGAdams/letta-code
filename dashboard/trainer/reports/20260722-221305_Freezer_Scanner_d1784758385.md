# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1784758382613651264_68b4024c53c6.jpg`
- **Scanner**: Freezer
- **Dispatch**: 2026-07-22T22:13:05Z (unix 1784758385)
- **Mazda conversation**: `conv-e561dc74-ad5b-4317-bd00-3033358a03f4`
- **Verdict: PASS**

## Facade result
`doc_kind=unknown, confidence=0.0` — JPEG scan, no extractable text, correctly routed to STEP 0
vision classification.

## Checklist (all confirmed via successful tool returns in the transcript, post-dispatch)

1. ✅ `load_wrapper_revision` — `wrap-v032` active, logged at 22:13:10.
2. ✅ STEP 0 vision classify — `executor_run` → `classify_scan.py` returned
   `doc_type=receipt, confidence=1.0, merchant=HOOGERHYDE SAFE & LOCK`. Explicit `receipt`
   routing correctly took precedence; no generic prose override.
3. ✅ Parse — `parse_and_categorize.py --json` returned structured fields: date `2025-01-20`,
   amount `20.14`, merchant `HOOGERHYDE SAFE & LOCK`.
4. ✅ Investigate:
   - `check_vendor_key` returned `recognized:true`, normalized `hoogerhyde_safe_lock` →
     `hoogerhyde_safe_lock_grand_rapids_mi`. Mazda adopted the normalized key in all
     downstream calls (categorizer input, record_trace evidence) — correct.
   - `check_duplicates` returned `is_exact_duplicate:true, exact_duplicate_expense_id:1173`.
5. ✅ Categorize — `categorizer_main.py --provider=auto` returned a valid
   `category_id:343` (gemini succeeded on first try, no fallback needed) for the normalized
   vendor key.
6. ✅ Store — correctly **skipped** per contract: a detected duplicate means skip store, still
   trace + judge. No `--save` call was made, consistent with the duplicate outcome.
7. ✅ `record_trace` — `task_name:"document-intake"` (exact match). Evidence JSON is internally
   consistent: `stored:false, expense_id:null, is_duplicate:true, duplicate_checked:true,
   vendor_key` is the normalized key (not the raw guess), `category_id:343`, `problems` names
   the duplicate expense_id/id_light. Trace 126 saved.
8. ✅ `judge_trace(126)` → verdict `PASS`, `failure_type:none`, reason "all deterministic intake
   checks passed" — consistent with a correctly-detected duplicate.
9. ✅ Dashboard notify — `POST /api/expense-stored` fired with `stored:0,
   duplicate_expense_ids:[1173], expense_ids:[], parsed:1`, correct `conversation_id` and
   `dispatched_at` matching this dispatch. Returned `{"ok": true}`.

`propose_improvement` was correctly **not** called — no FAIL verdict.

## Wrapper defect diagnosed
None. This run cleanly followed every learned rule already encoded in `wrap-v032`
(explicit doc_type routing precedence, receipt branch through vendor/duplicate/categorize).

## Lesson sent to Mazda
None needed — no coaching message sent.

## For a human
Nothing to review. A clean, correctly-skipped duplicate store, end-to-end in under 90 seconds
from dispatch to dashboard notify.
