# Mazda Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Scanned image:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785509158200167715_fbabdc57e9d3.jpg`
- **Dispatch time:** 2026-07-31T14:46:01Z
- **Mazda conversation:** `conv-67d3e549-b2b0-400f-b5eb-d14b3468f843`
- **Verdict: PASS** (judge_trace returned `NEEDS_REVIEW`, which is the *correct* verdict for this document, not a failure)

## Document
A signed receipt ("I received $50.00...") from **Prestige Mobile Mechanic**, Grand Rapids MI —
vehicle work, single $50.00 line item. Facade returned `doc_kind=unknown` (expected for a JPEG
scan), correctly triggering the STEP 0 vision branch.

## Step-by-step checklist

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | ✅ 14:46:05 — loaded `wrap-v070` |
| STEP 0 classify (facade was unknown) | ✅ 14:46:27 — `classify_scan.py` → `doc_type=receipt`, confidence 0.95, reason: "I received ... signature" (correctly recognized as a paid receipt, not an invoice) |
| STEP 0 parse | ✅ 14:46:49 — `parse_and_categorize.py --json` → merchant, $50.00, `expense_scope=full_receipt`, artifact written to `/tmp/mazda_receipt_3f8baa217e1a.json` |
| `check_vendor_key` | ✅ 14:46:52 — `prestige_mobile_mechanic`, `recognized:false` (genuinely new vendor) |
| `check_duplicates` | ✅ 14:46:55 — no exact/fuzzy duplicate |
| Categorize | ✅ attempted 14:49:01 — `categorizer_main.py --provider=auto` tried **all three** fallback providers and **all three failed**: gemini timed out, chatgpt-oauth produced no category, anthropic hit `Could not resolve authentication method` → `category_id: null` |
| Save | ✅ 14:49:08 — `parse_and_categorize.py --save` (category omitted per the FAIL-CLOSED CATEGORY RULE / wrapper Rule 17), archived to `readable_documents/receipts/2026/july/july_31/prestige_mobile_mechanic_07_31_26_50_00.jpg`, `expense_id=1974` |
| Itemization | ✅ 14:49:18 — `itemize_existing_expense` correctly refused (`itemizable:false`, "only one line item; a single line equal to the charge is standalone, not itemized") — row stays standalone, matches contract |
| `record_trace` | ✅ 14:49:26 — `task_name=document-intake`, `pending_vendor_review:true`, `category_id:null`, `stored:true`, `expense_id:1974`, `problems` lists the categorizer failure and a store-side format-string error (see below) |
| `judge_trace` | ✅ 14:49:29 — `NEEDS_REVIEW`, reason "vendor_key resolved but not in the known-vendor map" — correct, per wrapper Rule 17 a null-category receipt must never be blocked from saving nor scored as a plain PASS |
| `propose_improvement` | Correctly **skipped** — verdict was `NEEDS_REVIEW`, not `FAIL` |
| Dashboard callback | ✅ 14:49:38 — `POST /api/expense-stored` returned `{"ok": true}`, `status="awaiting_vendor_review"` |

Mazda's final summary message accurately reflects the transcript — no discrepancy between her
prose and the tool returns.

## Wrapper compliance
This run is a clean textbook application of **wrapper Rule 17** (operator correction, 2026-07-20):
a receipt with an unrecognized vendor and no resolvable category must still be saved, with
`pending_vendor_review=true`, so the judge can score `NEEDS_REVIEW` instead of losing the receipt
to an over-eager block. No coaching was needed.

## Issues found (not Mazda's fault — flagging for a human)

1. **All three categorizer fallback providers failed on this vendor** (gemini timeout →
   chatgpt-oauth no result → anthropic auth error). This is exactly the "every tracked tier's
   last attempt failed" condition the `mazda_categorizer_fallback_health` dashboard tile exists to
   catch — worth checking that tile went red/yellow for this run. Mazda's fail-closed handling
   correctly prevented a bad guess, but a human still needs to assign a category to expense 1974.
2. **Store-side error in stderr:** `Source-counterpart lookup failed: not enough arguments for
   format string`. Non-fatal (the save still succeeded), but it's a latent bug in the store path's
   counterpart-lookup formatting that should be looked at.
3. **Red-box verification could not be completed this run.** `POST /api/supporting-documents` and
   `GET /supporting-document/1974/receipt` against `localhost:8765` both failed —
   `"DB error: No module named 'pymysql'"` / `404`. This is a local environment gap in the
   dashboard process this Trainer talked to (missing `pymysql`), not something Mazda did wrong,
   but it means the receipt/red-box annotation contract for expense 1974 is **unverified** rather
   than confirmed. Recommend a human re-check `View Receipt` for expense 1974 manually once the
   dashboard's DB connectivity is confirmed.

## Lesson sent to Mazda
None — no coaching was necessary; the run followed the contract and the learned wrapper rules
correctly.

## Deterministic red-box gate — PASS

0 available receipt/source viewer(s) returned `highlighted=true`.
