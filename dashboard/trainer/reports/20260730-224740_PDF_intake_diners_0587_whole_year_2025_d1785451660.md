# Trainer Report — PDF intake: diners_0587_whole_year_2025

- **Document**: `/home/adamsl/rol_finances/readable_documents/bank_statements/january/diners_0587_whole_year_2025/diners_0587_year_2025.pdf`
- **Dispatch**: 2026-07-30T22:47:40Z (unix 1785451660)
- **Mazda conversation**: conv-6b96fc78-048f-422c-9da8-ffd632d47a2e
**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## Facade

Deterministic facade returned `ok:false`, `doc_kind:null` — `GeminiCliError: gemini CLI is not installed or not on PATH`. STEP 0 self-classification was correctly required and performed.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | tool return `wrap-v069`, active | ✅ |
| STEP 0 classify | `executor_run` → `classify_scan.py`: `doc_type=bank_statement`, confidence 1.0, merchant "Diners Club International" | ✅ |
| Last-4 resolution | Workbook lookup (`Known_Credit_Cards_and_Banks.xlsx`) → `diners_club_0587` / `0587`; `account_last4_source=known_cards_workbook` | ✅ correct, matches contract (workbook is authoritative on a unique letterhead match) |
| Parse | `parse_statement_scan.py --help` first (correctly checked, but that tool is scanned-*image*-only); fell back to a hand-written PyPDF2 text-extraction + regex script (`parse_method: direct_pdf_text_extraction_with_PyPDF2`), 39 transactions | ⚠️ see defect #1 below — acceptable engineering call, not itself a contract violation, but unvetted |
| `run_claude_code_sdk` attempt | Timed out after 180s before the PyPDF2 fallback | Infra flakiness, not a Mazda defect |
| Store | `store_statement_transactions.py` → `ok:false`, `outcome:FAILED`, `stored:0`, entire batch (39 rows, 26 matched as duplicates) **rolled back** because expense 1638 already carries a different `scanned_statement_url` (same-type supporting-document conflict) | ✅ correct fail-closed behavior per the supporting-document invariants — nothing was silently overwritten |
| `record_trace` | `task_name=document-intake`, trace_id=318, full evidence incl. `association_status:"conflicted"`, `requires_verification:true`, `stored:false` | ✅ |
| `judge_trace` | **Initially skipped entirely** — she went straight from `propose_improvement` to a final summary message | ❌ → coached, then ✅ (see below) |
| `propose_improvement` | proposal_id=219, failure_type=`other`, correctly describes the conflict and asks for the scanned_statement_url conflict-handling contract to be clarified | ✅ |
| Dashboard callback | `POST /api/expense-stored` with `stored:0`, `duplicate_expense_ids` (26 ids), correct `conversation_id`/`dispatched_at` | ✅ |

## Wrapper defect diagnosed

**`judge_trace` was skipped.** Mazda ran `record_trace` → `propose_improvement` → final prose summary, never calling `judge_trace(318)`, which the contract requires *always*, pass or fail. This breaks the evidence chain the self-improvement loop depends on (Run → Judge → Propose). This is not a one-off: her final assistant message reads like a "done" report without ever exposing a verdict, suggesting a wrapper prompt-ordering gap that lets her treat `propose_improvement` as the natural stopping point after a failure, skipping the mandatory judge step that normally comes *before* propose.

**Lesson sent to Mazda** (verbatim): required calling `judge_trace(trace_id=318)` immediately, framed as a standing memory rule that judge_trace is never optional even on FAIL/conflict outcomes.

**Result**: she called `judge_trace(318)` within the same turn upon coaching. Verdict: `FAIL`, `failure_type=unsupported_document`, reasons `["failed: document_classified", "failed: statement_transactions_stored"]`. Run is now contract-complete.

## Secondary observation for a human (not blocking, not coached this run)

`store_statement_transactions.py`'s only file-reference flag is `--source-file`, documented as "Path of the scanned image," and it hardcodes the target field to `scanned_statement_url` (`scanned_statement_attached`/`scanned_statement_path` in its output) — there is no way to route a document through it as `document_url`. This PDF is a clean, native-text annual statement (PyPDF2 extracted real text, not OCR garbage), which per the four-field supporting-document contract (`document_url` for bank-downloaded/primary-source documents vs. `scanned_statement_url` for paper scans) looks like a `document_url` candidate, not a scan. Because this run's entire batch rolled back on an unrelated conflict, nothing was actually mis-tagged yet — but the tool has no parameter to express that distinction, so a future clean run through this exact script would misfile a native PDF as a "scanned statement." Worth a human decision on whether `store_statement_transactions.py` needs a `--document-kind {scan,document}` flag before another PDF-intake statement goes through it successfully.

## Summary

FAIL verdict on the underlying document-intake outcome — the statement itself is legitimately blocked pending human review of expense 1638's conflicting `scanned_statement_url` (this is by design: same-type conflicts must never be silently overwritten). Mazda's pipeline work up through the conflict was correct (classification, workbook-based last-4 resolution, fail-closed rollback, trace, proposal, callback), but she skipped the mandatory `judge_trace` call; one coaching message got her to call it immediately, closing out the run as CORRECTED. A human should resolve the expense-1638 supporting-document conflict, and separately consider whether the statement-store tool needs a document-vs-scan flag.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
