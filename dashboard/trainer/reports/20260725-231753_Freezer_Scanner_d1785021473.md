# Trainer Report — Freezer Scanner

- **Document:** `scan_freezer_1785021470237750550_60e62b987b25.jpg` — bank statement, Chase Amazon, account last4 5783 (source: `known_cards_workbook`)
- **Dispatch:** 2026-07-25T23:17:53Z
- **Conversation:** `conv-b210e7df-e65a-4967-8ec1-951bdc35b073`
- **Verdict: FAIL** (Mazda's own `judge_trace` said PASS — overridden; see below)

## Checklist

| Step | Evidence | OK? |
|---|---|---|
| `load_wrapper_revision` | `wrap-v054` loaded | ✅ |
| STEP 0 vision | Skipped correctly — dispatch supplied validated `*.statement.json` preflight, doc_kind=statement | ✅ |
| Store (`store_statement_transactions.py`) | `transactions_parsed:6, skipped_credits:1, duplicates:5, stored:0`, `duplicate_expense_ids:[1610,1611,1616,1613,1614]`, `archive_paths:[".../bank_statements/2025/april/chase_amazon_5783_april_29__may_22/..."]` | ✅ (all-duplicate outcome is correct — this statement was also scanned via the Window scanner minutes earlier) |
| `apply_statement_annotations.py` | ran, `annotation_provider:"codex-cli"`, `applied:[]`, `unrecognized:[]` (no handwritten notes on this page) | ✅ |
| **Report at `<archive_dir>/report.html`** | **Never created.** Mazda instead hand-patched the shared `tools/receipt_scanning_tools/incoming_scans/report.html` — a path already holding a *different* run's data (12 transactions from a Window-scanner scan, `window_scan_1785021324448974847`). She then hand-edited `data-expense-id`/`data-vendor-key` strings via inline Python find/replace instead of the canonical script chain. `hydrate_report_categories_from_db.py` never returned a clean `ok:true` — her own trace recorded `report_audit_status:"warn:no source PDF found in report directory; hydrate failed blank data-expense-id rows"`. | ❌ **FAIL** |
| `record_trace` | `task_name="document-intake"` ✅, but `report_path` field absent and the audit-status she recorded (quoted above) is itself failing evidence | ⚠️ |
| `judge_trace` | Returned **PASS** — the deterministic judge doesn't currently check report/hydration completeness, so it missed this defect | ❌ (verdict wrong) |
| `propose_improvement` | Not called (judge said PASS) | — |
| Dashboard callback (`/api/expense-stored`) | Sent, but **omitted `report_path` entirely**, required on every statement callback | ❌ |

## Wrapper defect diagnosed

Two related defects, both coached in this conversation:
1. **Wrong report location + cross-run contamination.** Mazda generated/repaired the verification report at the shared `incoming_scans/report.html` instead of `<archive_paths dir>/report.html`. Because the Window scanner had processed a related statement into that same shared file minutes earlier, her "report" mixed in another run's 12 transactions — exactly the shared-path contamination the manual (`Statement parse JSON is run-scoped evidence`) warns about for parse JSON, and it applies equally to `report.html`.
2. **Hand-editing HTML instead of the canonical script chain.** Rather than running `restructure_verified_transactions.py` → `hydrate_report_categories_from_db.py` → `audit_statement_reports.py` against the archive directory, she wrote ad hoc inline Python to string-replace `data-expense-id` attributes directly — the exact "hand-editing a category" anti-pattern the contract forbids, and it never actually reconciled to a passing hydrator run.
3. Callback missing `report_path`.

## Lesson sent to Mazda

Two corrective messages (23:25:41Z, 23:41:19Z) instructing her to: regenerate the report at the correct archive path, run the three canonical scripts one at a time (not hand-edit HTML), re-run `record_trace`/`judge_trace` with corrected fields, and re-POST the callback with `report_path` included. She also filed a `propose_memory_note` on `system/persona` capturing "build report.html only at the statement archive directory... never hand-edit."

## Outcome of re-grade attempt

On retry, **`executor_run` failed with infrastructure-level errors**: first `MCP error -32001: Request timed out` and `MCP error -32000: Connection closed`, then on the second coaching pass every call returned **`Not connected`**. Mazda correctly stopped and reported the infrastructure failure rather than fabricating a completion — this is the right behavior and does not itself indicate a wrapper defect.

**This document's intake remains incomplete**: `report.html` still does not exist at the archive directory, `record_trace`/`judge_trace` were never re-run with corrected evidence, and the callback was never re-sent with `report_path`. The original FAIL stands as of this report.

## For a human

1. **Executor (`:8799` / `run_claude_code_sdk` MCP bridge) is down** — `Not connected` on every call as of 2026-07-25T23:44Z. This blocks Mazda from finishing this run and likely blocks other concurrent intake work. Needs an infra check independent of this document.
2. Once the executor is back, someone (Mazda or a human) needs to build `/home/adamsl/rol_finances/readable_documents/bank_statements/2025/april/chase_amazon_5783_april_29__may_22/report.html` via the canonical script chain, covering duplicate expense ids 1610, 1611, 1616, 1613, 1614 (1613→category 142, 1614→category 398 already categorized; 1610/1611/1616 are genuinely uncategorized in the DB and should show as such, not be guessed).
3. Consider tightening the deterministic `judge_trace` rules to check `report_audit_status` / hydration success for statement runs — this run's judge said PASS despite the trace itself recording a hydrate failure.
