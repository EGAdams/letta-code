# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-16T15:52:36Z
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer.jpg`
- Verdict: FAIL

## Checklist

- `load_wrapper_revision(agent_name="Mazda")` succeeded at `2026-07-16T15:52:44Z` and returned `wrap-v006`.
- `classify_scan.py` succeeded at `2026-07-16T15:52:56Z` and classified the document as `bank_statement` with confidence `0.98`.
- The statement branch was attempted at `2026-07-16T15:53:04Z`, but `parse_statement_scan.py` failed with `ValueError: No GEMINI_API_KEY or GOOGLE_API_KEY in environment or rol_finances/.env`.
- No successful `store_statement_transactions.py` return was observed.
- No `record_trace` or `judge_trace` call was observed after the failure.
- No `expense-stored` dashboard event was recorded for this dispatch.

## Diagnosis

This was an infrastructure/credentials failure, not a Mazda decision defect. The run correctly identified the document as a statement, but the executor environment lacked Gemini credentials needed by the statement parser.

## Lesson Sent

- None. This stopped on missing credentials before there was a wrapper-level recovery instruction to apply.

## Human Follow-Up

- Verify `GEMINI_API_KEY` or `GOOGLE_API_KEY` is available to the executor environment or in `/home/adamsl/rol_finances/.env`.
- Re-run the dispatch after the parser can execute successfully.
