# Mazda Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Scanned image:** /tmp/x.jpg
- **Dispatch timestamp:** 1785010104 (2026-07-25T20:08:24.000Z)
- **Mazda conversation ID:** conv-debug-test2
- **Deterministic facade result:** `{}`
- **Verdict: STALLED (infrastructure — invalid conversation ID)**

## What happened

Before polling, I validated that the supplied conversation ID
(`conv-debug-test2`) is reachable:

```
curl -s "http://100.80.49.10:8283/v1/conversations/conv-debug-test2/messages?limit=60"
```

The Letta API rejected it with HTTP 422:

```
{'type': 'string_pattern_mismatch', 'loc': ('path', 'conversation_id'),
 'msg': "String should match pattern "
        "'^(default|conv-[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}"
        "|agent-[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$'",
 'input': 'conv-debug-test2'}
```

`conv-debug-test2` is not a well-formed Letta conversation id (it is not a
UUID-suffixed `conv-...`, nor `default`, nor an `agent-...` id). This is not a
real intake dispatch that reached Mazda — there is no transcript to read, no
tool calls to grade, and the deterministic facade result is empty (`{}`),
consistent with no document ever having been classified.

## Checklist

Every contract step (load_wrapper_revision, STEP 0 classification,
investigate/categorize, store, record_trace, judge_trace,
propose_improvement, dashboard notify) is **unverifiable** — the endpoint
that would prove any of them cannot even be queried with the ID given.

## Diagnosis

This is an infrastructure/dispatch problem, not a Mazda wrapper defect: the
caller supplied a malformed `conversation_id` (looks like a hand-typed debug
placeholder, e.g. `conv-debug-test2`, rather than a real
`conv-<uuid>`-format id generated at dispatch time). No coaching message was
sent to Mazda — there is no valid conversation to send it to, and per my
allowlist I only POST to the named conversation's `/messages` endpoint after
confirming it's real.

## For a human

Check whatever produced this dispatch (a debug/manual trigger, by the name
`conv-debug-test2`) and confirm it passes the real conversation ID Mazda
was actually given for this document, if one exists. If this was a
deliberate synthetic/test dispatch with no real Mazda run behind it, no
further action is needed on the Mazda side.
