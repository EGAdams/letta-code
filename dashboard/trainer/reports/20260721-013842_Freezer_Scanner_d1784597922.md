# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1784597919543032368_8dd5cf6e6345.jpg`
- **Scanner**: Freezer
- **Dispatch**: 2026-07-21T01:38:42Z
- **Mazda conversation**: `conv-803121a8-1cb9-442c-b8b1-c6f4625dee3b`
- **trace_id**: 123
- **Verdict**: **CORRECTED** (functional outcome was already correct; one process defect coached and recorded)

## Facade result
`doc_kind: unknown, confidence: 0.0, recommended_action: reject` — required STEP 0 self-classification.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 01:38:45, `wrap-v032`, success | ✅ |
| STEP 0 classify (facade=unknown) | `executor_run classify_scan.py` 01:39:10 → `doc_type: receipt, confidence: 1.0, merchant: MB MARKETS` | ✅ |
| STEP 0 parse (receipt branch) | `executor_run parse_and_categorize.py --json` 01:39:28, success | ✅ |
| `check_vendor_key` | 01:39:33 → `vendor_key: mb_markets, recognized: false` | ✅ (correctly surfaced as unrecognized) |
| `check_duplicates` | 01:39:39 → `is_exact_duplicate: true, exact_duplicate_expense_id: 1518` | ✅ detected |
| Categorize | `executor_run categorizer_main.py` 01:41:21 → gemini CLI timed out, `category_id: null` | ⚠️ ran despite known duplicate (see defect below) |
| Store attempt | `executor_run parse_and_categorize.py --save --category-id=0` 01:41:43 → `success: false, duplicate: true` (tool's own guard rejected it) | ❌ should never have been attempted |
| `record_trace` | 01:41:49, `task_name: document-intake`, evidence: `stored:false, is_duplicate:true, expense_id:null, category_id:null, problems:[...]` | ✅ accurate |
| `judge_trace` | 01:41:52 → `NEEDS_REVIEW` (vendor_key unrecognized) | ✅ reasonable |
| Dashboard callback | `POST /api/expense-stored` 01:41:59 → `{"ok":true}`, `stored:0, duplicate_expense_ids:[1518]` | ✅ fired even for non-store outcome |

Final result: **not stored** (correct — exact duplicate of expense_id 1518), NEEDS_REVIEW verdict for the unrecognized vendor key.

## Wrapper defect diagnosed

After `check_duplicates` already returned `is_exact_duplicate=true`, Mazda continued the
pipeline: ran `categorizer_main.py` (which failed/timed out) and then attempted
`parse_and_categorize.py --save --category-id=0` — trying to force a store through with a
placeholder/zero category on a document she had already confirmed was a duplicate. This
violates two contract rules simultaneously: (1) a detected duplicate must skip
categorization/store entirely, and (2) a null/zero/invalid `category_id` must always block
storage, never be forced through. No data was corrupted only because
`parse_and_categorize.py --save` has its own internal duplicate guard that happened to
reject the write — that guard should never be relied on as the sole line of defense.

## Coaching sent

Sent one corrective message to `conv-803121a8-...` explaining the defect and requiring: stop
the pipeline immediately once any duplicate check reports `is_duplicate=true`/`is_exact_duplicate=true`;
never call `categorizer_main.py` or `parse_and_categorize.py --save` afterward; never pass
`--category-id=0`/placeholder values to force a save past the fail-closed guard.

Mazda acknowledged and called `propose_memory_note` (block `system/persona`, mode append,
proposal_id **98**), which passed all four gates (safety/cost/regression/usefulness — allow)
and was recorded as an approved proposal (no live-memory applier wired, so it's pending
application, consistent with prior runs).

## For a human

None of this run's outcomes require intervention — expense_id 1518 remains the correct,
un-duplicated record. The only follow-up worth tracking is whether this exact
duplicate-short-circuit rule actually sticks in future Mazda runs (watch the next few
Freezer/Window trace_ids for a repeat of "categorize-then-forced-save-after-duplicate").
