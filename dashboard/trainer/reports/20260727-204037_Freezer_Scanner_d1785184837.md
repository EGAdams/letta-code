# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785184831815149370_5c01939e391f.jpg`
- **Dispatch time:** 2026-07-27T20:40:37Z (unix 1785184837)
- **Mazda conversation:** conv-1902d623-9614-4d6f-8e3e-de0894dffe91
- **Verdict: CORRECTED** (PASS after one coaching round)

## Facade result
`doc_kind=unknown, confidence=0.0` (expected for JPEG scans) → routed to STEP 0 self-classification.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v059` at 20:40:41Z | ✅ |
| STEP 0 classify (`classify_scan.py`) | `doc_type=receipt, confidence=0.95, merchant=DTE` | ✅ |
| STEP 0 parse (`parse_and_categorize.py --json`) | wrote `/tmp/mazda_receipt_397ad89e554b.json`, date `2025-03-12`, amount `53.06` | ✅ |
| `check_vendor_key` | `vendor_key=dte`, `recognized=false` (not in known-vendor map) | ✅ (correctly not fabricated as recognized) |
| `check_duplicates` | `is_exact_duplicate=false`, `in_database=false` | ✅ |
| STEP 3 `categorizer_main.py --provider=auto` | **all three providers failed**: gemini timed out, chatgpt-oauth failed, anthropic hit an auth/credential error → `category_id: null` | Provider-chain failure (see defect below) |
| STEP 4 store (`parse_and_categorize.py --save`, `--category-id` omitted) | `success:true, expense_id:1672, pending_vendor_review:true` | ✅ correctly followed the dispatch's FAIL-CLOSED CATEGORY RULE (operator-approved wrap-v059 Rule 17): null category on a receipt is stored with `NEEDS_VENDOR_KEY`/`pending_vendor_review:true`, not blocked |
| `record_trace` (task_name=`document-intake`) | trace_id 268, evidence JSON matches reality (`stored:true, expense_id:1672, category_id:null, pending_vendor_review:true, vendor_key_recognized:false`) | ✅ |
| `judge_trace` | `NEEDS_REVIEW`, `failure_type:none`, reason "vendor_key resolved but not in the known-vendor map" | ✅ verdict consistent with observed evidence |
| `propose_improvement` | **missing on first pass** — dispatch's FAIL-CLOSED CATEGORY RULE requires filing a proposal whenever category resolution degrades to pending-review, regardless of judge verdict wording | ❌ → coached, then ✅ (proposal 188) |
| STEP 8 dashboard callback | `POST /api/expense-stored` with `expense_id:1672, stored:1, doc_kind:receipt, status:"awaiting_vendor_review"` — confirmed via `/api/expense-stored-events` | ✅ |

## Wrapper defect diagnosed

Not a categorization defect in the finance sense — the categorizer provider chain (gemini → chatgpt-oauth → anthropic) genuinely failed all three legs for vendor `dte` (timeout / OAuth failure / missing credentials), which is exactly the kind of infrastructure problem `propose_improvement` exists to surface into the self-improvement loop. Mazda executed the FAIL-CLOSED CATEGORY RULE's storage half correctly but stopped one step short — she never called `propose_improvement`, even though the dispatch text explicitly says to do so whenever a receipt degrades to a pending-review save. This looks like an instruction-following gap under the cheap model rather than a wrapper text gap (the dispatch is unambiguous on this point).

## Lesson sent to Mazda

Sent to `conv-1902d623-9614-4d6f-8e3e-de0894dffe91` at 20:45:57Z: praised the correct fail-closed store/callback, then required her to call `propose_improvement(trace_id=268, failure_type="missed_vendor_key", ...)` now for this document, and to record the lesson in memory (do not call `apply_proposal` unilaterally).

**Result:** Mazda responded within 10 seconds — filed `propose_improvement` (proposal 188, all 4 gates passed as informational/no-op since improvement wasn't auto-applied) and a `propose_memory_note` (proposal 189, gates all `allow`, but no live-memory applier wired so it's recorded as an approved proposal only, not yet live). She correctly did **not** call `apply_proposal` herself.

## For a human to look at

- Expense **1672** (DTE, $53.06, 2025-03-12) is parked with `category_id=NULL`, `expense_status=NEEDS_VENDOR_KEY` — needs a human to pick/confirm the vendor category via the dashboard.
- The categorizer provider chain failed all three legs for this run (gemini timeout, chatgpt-oauth failure, anthropic auth error) — worth checking whether this is a transient blip or the provider-health issue already tracked elsewhere (see dashboard's "LLM Provider Fallbacks (Categorizer)" tile / `provider_health.json`).
- Memory-note proposal 189 passed gates but has "no live-memory applier wired" — it is not yet actually in Mazda's live memory; someone should decide whether/how to wire that gap-filler.
