# Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785325636122622027_68c0b68905bb.jpg`
- **Dispatch:** 2026-07-29T11:47:20Z (unix 1785325640)
- **Conversation:** `conv-3836a7e4-4810-47ab-be87-3c6873301f6a`
- **Verdict: PASS**

## Facade result
`doc_kind=unknown, confidence=0.0, action=reject` — STEP 0 self-classification via `classify_scan.py` was required, and Mazda correctly performed it.

## Step-by-step evidence (all successful tool returns, in order)

1. **11:47:24** `load_wrapper_revision` → found, `wrap-v060`.
2. **11:47:40** STEP 0 `executor_run classify_scan.py` → `doc_type=receipt, confidence=1.0, merchant="Mr Burger"`. Receipt branch confirmed, so parse required next.
3. **11:48:02** `executor_run parse_and_categorize.py -f ... --engine gemini` (parse only) → extracted `transaction_date=2023-03-27, total_amount=31.93, merchant=MR BURGER RESTAURANT 1, expense_scope=full_receipt` (single line item, no marked-item selection visible — correct, no marked-item scope logic needed).
4. **11:48:06** `check_vendor_key` → `vendor_key="mr_burger", recognized=true`. Mazda adopted the normalized key.
5. **11:48:10** `check_duplicates` → `is_exact_duplicate=false, in_database=false`. Not a duplicate; proceed to store.
6. **11:48:22** `executor_run categorizer_main.py` (via inline python, gemini provider) → `category_id=137` (valid, non-null/non-zero).
7. **11:48:33** `executor_run parse_and_categorize.py --save --category-id=137` → success; `expense_id=1676`, `receipt_url='mr_burger_restaurant_1_03_27_23_31_93.jpg'` stored, receipt metadata row created (id 118). Merchant/vendor not a placeholder.
8. **11:48:47** `itemize_existing_expense` → `itemizable=false` ("only one line item; a single line equal to the charge is standalone, not itemized"). Correct fail-closed outcome — row remains standalone, no hand-built parent/child SQL.
9. **11:48:56** `record_trace` with `task_name="document-intake"` → `trace_id=276` saved.
10. **11:49:00** `judge_trace(276)` → **PASS**, `failure_type=none`, "all deterministic intake checks passed".
11. **11:49:08** `curl POST /api/expense-stored` (via `executor_run`) → `{"ok": true}`. Payload correctly carries `expense_id=1676, expense_ids=[1676], duplicate_expense_ids=[], parsed=1, stored=1, vendor_key=mr_burger, receipt_url=<scan path>, conversation_id, dispatched_at=1785325640.155323, doc_kind=receipt`.
12. **11:49:14** Assistant summary confirms all of the above; turn ended cleanly (no premature stop).

## Checklist against the contract
- STEP 0 classification present and required (facade unknown) — ✓
- Vendor key resolution + adoption — ✓
- Duplicate check (not a duplicate) — ✓
- Categorization returns a valid positive `category_id` (137) before store — ✓
- Store returns `expense_id` + `receipt_url` — ✓
- Itemization correctly declined (single line item, standalone) — ✓
- `record_trace` with exact `task_name="document-intake"` — ✓
- `judge_trace` always called — ✓
- Dashboard callback `/api/expense-stored` always called (present even though this was a straightforward store, not a duplicate) — ✓
- No stage was skipped, faked in prose, or run out of order.

## Wrapper defect diagnosed
None. This run required no coaching.

## Lesson sent to Mazda
None — no corrective message was needed.

## Anything for a human to look at
None. Total run time from dispatch to callback: ~1m48s, well within budget. Single clean PASS.
