Verdict: PASS

**Scanner:** Window Scanner
**Document:** `window_scan_1786486883256194729_692ce9ca4b60.jpg`
**Dispatch:** 2026-08-11T22:21:27Z
**Conversation:** conv-e06c6924-09e7-4da8-aa7e-74fb396c094f
**Facade result:** `doc_kind=unknown, confidence=0.0` (expected for a JPEG scan) → routed to STEP 0.

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 22:21:36 return, wrap-v004 | ✅ |
| STEP 0 classify | `classify_scan.py` → `doc_type=receipt, confidence=0.99, merchant="Mr Burger Restaurant 1"` | ✅ |
| STEP 0 parse | `parse_and_categorize.py --json` — Gemini 401'd, script auto-fell-back to `chatgpt-oauth` (gpt-5.4) internally and returned a full parse (single line item, `expense_scope=full_receipt`, total $31.40) | ✅ |
| `check_vendor_key` | normalized `mr_burger_restaurant_1` → `mr_burger` | ✅ |
| `check_duplicates` | **MCP tool errored**: `Access denied for user 'adamsl'@'localhost' (using password: NO)`. Mazda self-corrected with an authenticated `executor_run` SQL query (date=2025-04-10, amount=31.40) returning zero rows, and logged both facts in `problems` | ⚠️ see Escalation |
| `categorizer_main.py` | `vendor_key=mr_burger, category_id=137` | ✅ |
| `parse_and_categorize.py --save` | `expense_id=2016`, `receipt_url=mr_burger_restaurant_1_04_10_25_31_40.jpg` | ✅ |
| `check_category` | `matches_vendor:true`, category 137 "Mr. Burger" | ✅ |
| Itemization | single line item ("Approved USD $31.40") — itemization correctly not attempted | ✅ |
| `record_trace` | trace_id 104, task_name `document-intake`, full evidence incl. both provider-fallback problems | ✅ |
| `judge_trace` | PASS, failure_type none | ✅ |
| Dashboard callback | `POST /api/expense-stored` → 200, correct `expense_id`, `receipt_url`, `conversation_id`, `dispatched_at` | ✅ |

## Independent red-box/dialog check
`GET /supporting-document/2016/receipt` → 200, served bytes (md5 `edbccade...`) differ from the raw archived file (md5 `4ff02dbd...`); annotated copy is cached at `dashboard/.cache/document-annotations/expense-2016-ecf025f98fb55b7611ab9da4.jpg`. No `opened without highlight` log line. Red box / View Receipt dialog confirmed working for this expense.

## Wrapper defect diagnosed
None on Mazda's side. Gemini quota/auth exhaustion is a known, already-designed-for provider-fallback case, and both `classify_scan.py`'s and `parse_and_categorize.py`'s ChatGPT-OAuth fallback fired correctly and transparently — no coaching needed.

## No coaching sent
Run was clean; no corrective message required.

## Escalation
repo_path: /home/adamsl/rol_finances
bug_description: |
  The `check_duplicates` MCP finance-verifier tool (finance_mcp server, :8791) failed with
  a MySQL access-denied error: `Access denied for user 'adamsl'@'localhost' (using password: NO)`.
  This means the tool's DB connection is being made with no password, unlike the working
  ad-hoc `executor_run` pymysql connection Mazda used as a workaround (host=127.0.0.1,
  user=adamsl, password set, db=nonprofit_finance, port=3306) — the MCP tool's credential
  loading path is broken or stale (missing/blank password env var, wrong config file, or a
  host-vs-127.0.0.1 auth-plugin difference). Mazda correctly treated the failure as
  non-blocking and independently verified no duplicate existed via a direct authenticated
  query, and logged both facts honestly in her trace evidence — this is not a wrapper/coaching
  issue, it's a tool-configuration bug in the finance MCP server (see Ch.2 Table 2.2 in the
  Mazda manual: `check_duplicates` is documented as needing DB creds and returning a structured
  error rather than crashing, but a silent no-password connection attempt is not "structured
  error visibility," it's a misconfigured credential — every future receipt/invoice run will
  hit this same failure and rely on the same manual SQL workaround until it's fixed).
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786486883256194729_692ce9ca4b60.jpg
  failing_command: check_duplicates MCP tool call, id_light="scan", expense_date=2025-04-10, amount=31.40

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
