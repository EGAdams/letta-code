# Trainer Report — Freezer Scanner

- **Scanner**: Freezer Scanner
- **Image**: `scan_freezer_1784486024433375130_d17d432de564.jpg`
- **Dispatch**: 2026-07-19T18:33:48Z
- **Conversation**: `conv-b382270b-f622-4637-b27c-741abb8e4cbd`
- **Verdict**: **CORRECT FAIL-CLOSED (no coaching required)** — judge_trace verdict is FAIL (`wrong_category`), but Mazda's handling of the run was fully compliant with the contract.

## Checklist (all with successful tool returns)

1. `load_wrapper_revision` → `wrap-v026` loaded. ✅
2. Facade returned `doc_kind=unknown, confidence=0.0` → STEP 0 correctly triggered.
   `classify_scan.py` (vision) → `doc_type=receipt, confidence=0.99, merchant="BJ's Restaurant & Brewhouse"`. ✅
   `parse_and_categorize.py --json` → parsed `transaction_date=2026-05-10`, `amount=40.77`, derived `vendor_key=bjs_restaurant_brewhouse`. ✅
3. Investigate:
   `check_vendor_key` → not recognized (new vendor, correctly not in map — no normalized key to adopt). ✅
   `check_duplicates` → not a duplicate. ✅
4. Categorize:
   `categorizer_main.py --provider=gemini` → **failed to resolve a category**: stderr shows `Research failed for vendor_key='bjs_restaurant_brewhouse': gemini CLI is not installed or not on PATH` → `category_id: null`.
   `check_category` (category_id=0) → correctly reports `present:false, matches_vendor:false` — confirms invalid category. ✅
5. Store: **correctly skipped** — fail-closed on invalid category, per contract. ✅ (no `--save` call attempted, no bad row written)
6. `record_trace` (`task_name="document-intake"`, full IntakeVerificationEvidence) → saved as trace 113. ✅
7. `judge_trace(113)` → `FAIL / wrong_category` (accurate — categorization did not resolve, so storage was correctly blocked). ✅
8. `propose_improvement` (trace 113, `failure_type=wrong_category`) → filed proposal 91, then `apply_proposal` activated it as `wrap-v028`. ✅
9. Dashboard notify (`POST /api/expense-stored`) → fired after trace/judge, `stored:0, parsed:1, expense_ids:[], duplicate_expense_ids:[]`. Confirmed via `/api/expense-stored-events`: event present, `conversation_id` and `dispatched_at` match this run exactly. ✅

## Root cause of the FAIL

The failure is **infrastructure, not a Mazda wrapper defect**: `categorizer_main.py`'s external vendor-research fallback shells out to a `gemini` CLI that is not installed/on PATH on the executor host, so any unrecognized vendor cannot get research-assisted category resolution right now. This is the same class of environment gap noted elsewhere (Model Stats / Antigravity CLI work), just hit here via the categorizer's research path. Mazda's own `propose_improvement` (wrap-v028) targets the vendor-key-resolution side of this, which is reasonable but does not fix the missing `gemini` binary — that's an ops fix (install/PATH the `gemini` CLI on the executor box, or make `categorizer_main.py` degrade to a non-CLI research path).

## Why no coaching was sent

Mazda executed every required step with a successful tool return, correctly refused to store an expense with an unresolved category (fail-closed as mandated), recorded accurate trace evidence, judged and proposed against the failure, and always-fired the dashboard callback with the correct `stored:0`. There is no wrapper-level mistake to correct in this conversation — the gap is a missing CLI dependency on the executor, which is outside what a corrective Letta message to Mazda can fix.

## Recommendation for a human

Install/`PATH` the `gemini` CLI on the Claude-SDK executor host (or add a non-CLI fallback in `categorizer_main.py`'s vendor-research path) so unrecognized-vendor receipts like this one (BJ's Restaurant & Brewhouse, $40.77, 2026-05-10) can resolve a category instead of failing closed. The receipt itself was correctly NOT stored and needs a re-run (or manual categorization) once that's fixed.
