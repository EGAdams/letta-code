# Trainer Report — Window Scanner

- **Document**: `window_scan_1785375180579246760_bf6820fdd860.jpg`
- **Dispatch**: 2026-07-30T01:33:04Z, conversation `conv-d2b6d39a-abae-498a-a295-b3084506dde4`
- **Facade result**: `doc_kind=unknown, confidence=0.0` → STEP 0 self-classification required
**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## What the document was

`classify_scan.py` (Gemini, after an initial 429 fallback) identified it as a `receipt`
(confidence 0.98): a donation confirmation from **Intercessors for America**, $30.50,
dated 2025-03-18, paid by Rosemary Barnes. Single line item, full receipt scope.

## Step-by-step (first pass)

| Step | Evidence | Result |
|---|---|---|
| STEP 1 load_wrapper_revision | tool return `wrap-v066` | ✅ |
| STEP 0 classify_scan.py | `doc_type=receipt, confidence=0.98` | ✅ |
| STEP 0b parse_and_categorize.py --json | merchant, amount, date extracted | ✅ |
| STEP 2a check_vendor_key | `vendor_key=intercessors_for_america`, recognized | ✅ |
| STEP 2b check_duplicates | `is_exact_duplicate=false`, `fuzzy_duplicate_expense_id=1547` ("Check 11051", same date+amount, no payee) | ✅ investigated — Mazda queried expense 1547 directly (read-only SELECT via executor_run) and confirmed it's the same real-world payment; correctly skipped STEP 4 storage |
| STEP 3 categorizer_main.py | `category_id=190` (Gifts & Love Offerings), matches existing expense's category | ✅ |
| STEP 5 record_trace | `task_name=document-intake`, `is_duplicate=true, stored=false` (trace 297) | ✅ but incomplete (see defect) |
| STEP 6 judge_trace | PASS | ✅ |
| STEP 8 dashboard callback | `duplicate_expense_ids:[1547], stored:0` | ✅ |

## Defect found

Mazda's own `record_trace` (297) `problems` field admitted: *"Existing matched expense
retains statement document_url; new receipt was not auto-attached because duplicate
intake path skips STEP 4 save."* The dispatch's SUPPORTING-DOCUMENT STORAGE CONTRACT
requires attaching a newly processed document to an existing matched expense whenever
possible, preserving the other two reference fields. Detecting and narrating the gap is
not the same as fixing it — expense 1547 would have kept `receipt_url=NULL` despite the
receipt scan now being on file, which means no "View Receipt" button in the Set Category
dialog for a payment we can actually document.

**Wrapper defect diagnosed**: the STEP 2b duplicate branch tells Mazda to skip STEP 4
storage but never names the tool/step for attaching supporting-document evidence to the
existing expense — an ambiguity in the dispatch, not a model failure.

## Coaching sent

One corrective message identifying the exact gap, instructing Mazda to locate the
`SupportingDocumentService` boundary (per CLAUDE.md, mirroring `update_expense_category.py`
for categories), attach `receipt_url` to expense 1547, confirm the peer fields are
preserved, and re-run record_trace/judge_trace/callback.

## Correction verified

Mazda searched the codebase, found `e_two_e_processing/supporting_documents.py`
(`SupportingDocumentService` + `DocumentKind`) and `ExpenseRepository`, and ran a script
calling `service.attach(1547, DocumentKind.RECEIPT, <scan path>)`. Result:

```json
{
  "association_status": "inserted",
  "target_expense_id": 1547,
  "target_field": "receipt_url",
  "requires_verification": false,
  "expense": {"id": 1547, "receipt_url": "<scan path>", "document_url": "<march statement pdf>", "moms_ledger": null},
  "preserved_peer_fields": {"document_url_preserved": true, "moms_ledger_preserved": true}
}
```

She then re-recorded trace 298 with `association_status`, `target_expense_id`,
`target_field`, and `preserved_fields` in the evidence JSON, re-ran `judge_trace` → PASS,
and repeated the STEP 8 dashboard callback. No ad-hoc SQL was used for the attach itself
(the earlier read was a read-only SELECT to confirm identity, not a write).

## For a human

None required — corrected within the same run, within the 15-minute window. Worth
considering: add an explicit sub-step to the STEP 2b duplicate branch naming
`SupportingDocumentService`/`DocumentKind` directly, so future cheap-model runs don't have
to rediscover the tool via code search.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
