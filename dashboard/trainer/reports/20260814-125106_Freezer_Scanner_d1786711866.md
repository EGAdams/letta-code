# Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

**Dispatch:** 2026-08-14T12:51:06Z (unix 1786711866)
**Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786711862781051927_4a7bfa92917e.jpg`
**Facade result:** `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` — so STEP 0 classification was required.
**Mazda conversation:** `conv-eaf1dd9a-df5a-4fb4-b6dd-5c8b7523adca`

## What happened

The scan turned out to be a blank page — `classify_scan.py` (Gemini vision) returned
`doc_type=other, confidence=1.0, reason="The document is a blank page with no visible
text or identifying information."` Per the dispatch contract, `doc_type=other` is the
one genuinely unsupported case, so no receipt/invoice/statement/categorization/
duplicate-check/storage branch should run — and none did.

## Checklist with evidence (all tool calls seen in this run, timestamps ≥ dispatch)

1. `load_wrapper_revision(agent_name="Mazda")` → `12:51:10Z`, returned
   `wrapper_revision=wrap-v073`, `is_active=true`. ✅
2. **STEP 0 classification** → `12:51:22Z` `executor_run` of
   `tools/classify_scan.py <path>` (cwd `/home/adamsl/rol_finances`,
   `PYTHONPATH` set) → `returncode:0`,
   `{"doc_type":"other","confidence":1.0,"merchant":null,"reason":"...blank page..."}`. ✅
   (stderr only carries an unrelated `google.generativeai` deprecation warning.)
3. Receipt/invoice/statement branches: correctly **not invoked** — `other` is
   unsupported per contract. ✅
4. `record_trace(task_name="document-intake", ...)` → `12:51:31Z` → `trace_id=378`.
   Evidence JSON matches reality exactly: `doc_kind=other`,
   `classification_confidence=1.0`, `vendor_key=null`, `vendor_key_recognized=false`,
   `category_id=null`, `duplicate_checked=false`, `is_duplicate=false`, `stored=false`,
   `expense_id=null`, `intake_halted=false`, and a `problems` entry explaining the
   blank-scan skip. ✅
5. `judge_trace(378)` → `12:51:35Z` → `{"verdict":"PASS","failure_type":"none",
   "reasons":["all deterministic intake checks passed"]}`. ✅ Consistent with a
   correctly-detected unsupported document.
6. `propose_improvement` — not called; correctly skipped since verdict is PASS. ✅
7. Dashboard notify → `12:51:43Z` `executor_run` curl `POST /api/expense-stored` →
   `{"ok": true}`. Payload's `conversation_id` (`conv-eaf1dd9a-...`) and
   `dispatched_at` (`1786711866.1880188`) match this exact dispatch — not a stale
   event. `stored:0`, `parsed:0`, `doc_kind:"other"` — consistent with the outcome. ✅

## Wrapper defect diagnosed

None. This run required no coaching — every step matches the contract for a blank/
unreadable scan.

## Lesson sent to Mazda

None — no lesson was needed.

## For a human

None. This was a physically blank page fed through the Freezer scanner (likely an
empty flatbed scan or accidental double-feed); no data was lost since nothing was on
the page. No dashboard/annotation checks apply since nothing was stored.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
