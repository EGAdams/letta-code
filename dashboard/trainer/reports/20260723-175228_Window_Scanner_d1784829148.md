# Trainer Report — Window Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_1784829144832113610_6db6ea3b01be.jpg` (Chase Amazon statement, Jan 2025, last4 5783)
- **Dispatch time:** 2026-07-23T17:52:28Z
- **Conversation:** `conv-962fde3f-9c46-421a-826d-530a8292b29d`
- **Verdict: CORRECTED** (initial run FAILED; corrected within the coaching deadline)

## Step-by-step evidence

1. `load_wrapper_revision` — loaded wrap-v038. ✅
2. `parse_statement_scan.py` — ran, returncode 0, correctly parsed 7 clean transactions for the current image (bank_name "Chase Amazon", last4 5783, unreadable_count 0). ✅
3. `store_statement_transactions.py -f /tmp/mazda_statement.json` — **rejected** (returncode 2) citing a missing-date row for "Microsoft*Microsoft 365 P 425-6816830 WA". That row belongs to a completely different, older scan.
4. Mazda self-investigated: `ls -l`/`stat`/`cat` on `/tmp/mazda_statement.json` proved the file on disk held stale content from an earlier, unrelated Chase/Microsoft-365 scan (mtime ~40 min before this dispatch), even though her own `parse_statement_scan.py` invocation had just printed the correct current-scan JSON to stdout. She re-ran the parse a second time and confirmed the file *still* did not get overwritten with the fresh data — a genuine race/caching defect in the fixed shared handoff path `/tmp/mazda_statement.json`, not a document defect.
5. **First-attempt handling was a FAIL, not a correct fail-close:** Mazda accepted the stale rejection at face value, quarantined a perfectly readable statement, ran annotations (0 ids, correctly a no-op), `record_trace`(158) → `judge_trace` → **FAIL**, `propose_improvement`(114) → `apply_proposal` → activated wrap-v039 (a systemic guard for future runs), and notified the dashboard with `stored:0, duplicate_expense_ids:[]`. This is the behavior my instructions explicitly forbid: filing a proposal for next time does not substitute for finishing *this* document.

## Coaching

Diagnosed as a wrapper defect: the shared fixed-path handoff file (`/tmp/mazda_statement.json`) is not reliably fresh at the moment `store_statement_transactions.py` reads it, and Mazda had all the evidence needed to catch and repair this herself but stopped instead of acting on it.

Sent one corrective message instructing her to:
- Overwrite `/tmp/mazda_statement.json` directly with the known-correct JSON (from her own successful parse output) and immediately run store in the same command to close the race window.
- Re-run annotations with the real ids this time.
- Record a fresh trace/judgment reflecting the true outcome.
- Send a corrected dashboard callback superseding the wrong `stored:0, no ids` one.

## Retry outcome (trace 159)

- Overwrote the handoff file, re-ran `store_statement_transactions.py` — **succeeded**: duplicate-only outcome, `transactions_parsed:7`, `transactions_duplicate:4`, `transactions_skipped_credits:3` (the 3 payment/bill-pay rows), `transactions_stored:0`, `duplicate_expense_ids:[1503,1504,1505,1506]`, one archive path under `bank_statements/2025/january/chase_amazon_5783_january_04__january_22/`.
- `apply_statement_annotations.py` ran on the 4 duplicate ids: 2 unchanged (`Office`/140), 1 unrecognized note (`Book`, expense 1504 — correctly surfaced in `problems` rather than silently dropped), 0 applied (expected, since EG's notes matched vendor defaults or weren't recognized).
- `record_trace`(159) → `judge_trace`(159) → **PASS**.
- `propose_memory_note`(115) filed to record the stale-shared-handoff lesson (gates passed; no live-memory applier wired yet, so it's an approved proposal only — a known tooling gap, not something Mazda could fix).
- Dashboard callback (`/api/expense-stored-events`) confirmed: a second event for this `conversation_id`/`dispatched_at` landed with `duplicate_expense_ids:[1504,1505,1503,1506]`, superseding the earlier wrong `stored:0, []` callback.

## Wrapper defect for a human to look at

`/tmp/mazda_statement.json` is a single shared fixed path used to hand parse output to the store step. On this run it was stale by ~40 minutes when store read it, despite a parse call immediately beforehand reporting success — suggesting either a write-not-flushed race, a different code path in `parse_statement_scan.py` that doesn't always write that path, or contention from a concurrent scanner run. Given the project's prior history of Window/Freezer concurrent-scan clobbering (see `mazda_concurrent_scan_context_and_trainer_retry_gap` memory), this looks like the same class of bug resurfacing via a different shared resource. Recommend either giving each dispatch a unique statement-JSON path, or having `parse_statement_scan.py` fsync/verify its own write before returning success.

## Summary

The initial run incorrectly quarantined a fully readable Chase Amazon statement due to a stale shared temp-file race, filing a proposal instead of completing the work. After one coaching message, Mazda correctly repaired the handoff file, completed the store (duplicate-only, 4 matches against existing expenses 1503/1504/1505/1506), ran the handwritten-annotation step, and sent a corrected dashboard callback — final verdict **PASS**, overall run **CORRECTED**.
