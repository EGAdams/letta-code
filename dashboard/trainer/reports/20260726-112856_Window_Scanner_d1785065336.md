# Trainer Report — Window Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1785065332184921912_3b95da5ec4aa.jpg`
- **Dispatch:** 2026-07-26T11:28:56Z (`conv-16564223-b767-4ccb-84c1-d3a8ab904e91`)
- **Verdict: PASS (Mazda's execution) — judge/system verdict is a known-defective FAIL**

## Checklist (all confirmed by successful tool returns)

1. `load_wrapper_revision(Mazda)` → `wrap-v057` loaded, 11:29:01. ✅
2. STEP 0a classify: `executor_run` → `classify_scan.py` → `{"doc_type":"other","confidence":1.0,"reason":"blank page with no visible text or information"}`, 11:29:20. ✅
3. Per the ROUTING PRECEDENCE rule and wrapper rules 4/17/20/23/25/28 (all: "doc_type=other → stop, no parse/vendor/category/duplicate/store"), Mazda correctly did **not** run parse/investigate/categorize/store. This is the required unsupported-document branch, not a skipped step. ✅
4. `record_trace` (trace_id 236) with `task_name="document-intake"`, `doc_kind:"unknown"`, all receipt fields correctly null/false, `problems` naming the exact classify_scan.py reason. ✅
5. `judge_trace(236)` → **FAIL**, `failure_type=unsupported_document`, reasons all "failed: X_ran/resolved/stored". ❌ **This is the defect** — see below.
6. `propose_improvement` (trace 236) → proposal 177, reasonable summary/expected_benefit. ✅
7. `apply_proposal(177, ...)` → activated as `wrap-v059`, all 4 gates passed. ✅ (but see recurrence note)
8. Dashboard callback (`/api/expense-stored`, stored:0, parsed:0, all ids empty) → `{"ok": true}`. ✅

Total run time: dispatch to completion in ~73 seconds — fast, single clean pass, no coaching needed for Mazda's own behavior.

## Wrapper/system defect diagnosed

The document was a genuine blank page. Mazda's actions were textbook-correct per her own accumulated learned rules. Yet `judge_trace` still returned FAIL for missing vendor/category/duplicate/store — exactly the outcome that wrapper rules **4 (2026-07-16), 17/20 (2026-07-16/23), 23 (2026-07-23), 25 (2026-07-24), and now 28 (proposal 177, this run, → wrap-v059)** were each supposed to have already fixed. Five-plus near-identical "fixes" have been proposed and activated across 10 days without the judge's actual verdict changing.

This strongly suggests `apply_proposal`'s wrapper-revision text patches are being appended to Mazda's own `instructions` (which she reads and correctly follows), but the **deterministic verdict rubric inside `judge_trace`/the evaluation factory itself never consults that instruction text** — it appears to run a fixed completeness check (`document_classified`, `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored`) with no `doc_kind=unknown`/`unsupported_document` exemption baked into the rubric code. No amount of further `propose_improvement`/`apply_proposal` cycling will fix this — it needs a direct code change to the judge/evaluator (the actual `IEvaluationFactory`/verdict-judge implementation) to special-case unsupported-document traces, not another wrapper-instructions rule.

## Lesson sent to Mazda

None — she did not need coaching this run. No corrective message was sent.

## For a human to look at

The `document-intake` judge rubric needs a real code fix (not another wrapper-text rule) to stop FAILing correctly-handled unsupported-document (`doc_type=other`) runs. Recommend inspecting the evaluator/rubric implementation directly rather than routing another proposal through the same instruction-text mechanism that has already failed to fix this ~5 times.
