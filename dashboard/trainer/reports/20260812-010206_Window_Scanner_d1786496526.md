# Trainer Report — Window Scanner

**Verdict: PASS**

- Scanner: Window Scanner
- Document: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786496520808280779_5e7954eacf9b.jpg`
- Dispatch: 2026-08-12T01:02:06Z (unix 1786496526)
- Mazda conversation: `conv-ef47a79d-0903-4202-a825-5d691fd478c1`
- Facade result: `doc_kind=unknown, confidence=0.0` → STEP 0 required

## Checklist (evidence = successful tool returns observed in this run's transcript)

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | 01:02:19 — returned `wrap-v005`, active |
| STEP 0 classify | `classify_scan.py` (01:02:38) → `doc_type=receipt, confidence=0.99, merchant="Lake MI Drive BP"` |
| STEP 0 parse | `parse_and_categorize.py --json --write-parsed-json /tmp/mazda_receipt_50256e58ff95.json` (01:03:04). Gemini engine 401'd internally but the tool's own fallback chain produced a valid parsed artifact (confirmed by the later save step's `model_provider: 'chatgpt-oauth'`). |
| `check_vendor_key` | 01:03:10 → raw guess `lake_mi_drive_bp` normalized to `lake_michqps_grand_rapids_mi`; Mazda adopted the normalized key consistently in every subsequent call and in `record_trace`. |
| `check_duplicates` | 01:03:14 → **errored**: `Access denied for user 'adamsl'@'localhost' (using password: NO)`. See defect below. |
| Categorize | `categorizer_main.py --provider=auto` (01:03:39) → `category_id: 167` (valid, non-null) |
| Store | `parse_and_categorize.py --save --category-id=167 --parsed-json /tmp/mazda_receipt_50256e58ff95.json` (01:03:51) → success, `expense_id: 2029`. Correctly reused the exact STEP 0 artifact file rather than re-parsing. Single line item, so no itemization required (`itemization_attempted:false`) — correct. |
| `record_trace` | 01:04:02 → `task_name=document-intake`, full IntakeVerificationEvidence, `problems` transparently lists both the check_duplicates failure and the Gemini→fallback vision failure. |
| `judge_trace` | 01:04:09 → **PASS**, `trace_id=125`, "all deterministic intake checks passed" |
| Dashboard notify | 01:04:21 → `curl POST /api/expense-stored` → `{"ok": true}`. Confirmed independently via `/api/expense-stored-events?since=1786496526`: `expense_id=2029`, `conversation_id` and `dispatched_at` match this run exactly. |

Final stored record: expense 2029, Lake MI Drive BP, $56.13, 2025-04-19, vendor_key `lake_michqps_grand_rapids_mi`, category 167, archived at `readable_documents/receipts/2025/april/april_19/lake_mi_drive_bp_04_19_25_56_13.jpg`. Not a duplicate.

## Wrapper defect found and coached (run still PASSES — outcome was correct)

When `check_duplicates` errored, Mazda substituted her own ad-hoc SQL via `executor_run`:
```
python3 - <<'PY'
import pymysql
conn=pymysql.connect(host='127.0.0.1',user='adamsl',password='Tinman@2',db='nonprofit_finance',...)
...
```
Two problems: (1) a real database password was embedded in plaintext directly in the tool-call arguments, which get persisted into the transcript and trace evidence — a credential-exposure risk independent of outcome; (2) hand-rolled SQL bypasses the sanctioned `check_duplicates` tool boundary, so there's no guarantee it reproduces the tool's real matching semantics (fuzzy vendor_key matching, `id_light` handling). This is the same "never ad-hoc SQL in an intake handler" principle documented for supporting-document associations, and it applies to duplicate-checking too.

The query itself found no match (empty result), and Mazda honestly recorded the workaround in her `problems` evidence rather than hiding it — good instinct, wrong mechanism.

**Lesson sent** (01:06:21, in-conversation): never embed credentials in `executor_run` commands; on a `check_duplicates` failure, report it and stop — do not substitute raw SQL. Mazda acknowledged (01:06:27) and called `propose_memory_note` against `system/persona.md` (proposal_id 66, gate decision: allow) codifying the rule for future runs.

## Escalation
repo_path: /home/adamsl/rol_finances
bug_description: |
  The check_duplicates MCP tool fails on every call with a MySQL auth error:
  "Access denied for user 'adamsl'@'localhost' (using password: NO)" — i.e. the
  tool is connecting without a password even though a working password exists
  (Mazda's own raw pymysql connection using the same user succeeded with a
  password supplied). This points to check_duplicates' own DB connection
  wiring/credential loading being broken or missing an env var, not a data
  problem. This is a real (if currently low-impact, since the deterministic
  find_category/parse layer still functions) infra defect: every intake run
  hitting check_duplicates will error and force agents to either skip the
  check or invent a workaround, which is exactly what happened here.
metadata:
  failing_command: check_duplicates(id_light="scan", expense_date="2025-04-19", amount="56.13", description="Lake MI Drive BP")
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/window_scan_1786496520808280779_5e7954eacf9b.jpg

## For a human
- check_duplicates' DB credentials need to be fixed at the tool/service level (see Escalation above) — this will keep recurring on every intake run until fixed.
- No action needed on expense 2029 itself; it stored correctly.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
