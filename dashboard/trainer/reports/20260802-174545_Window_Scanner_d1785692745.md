# Trainer Report — Window Scanner

**Document:** `window_scan_1785692741442405252_acd367594b8d.jpg`
**Dispatch time:** 2026-08-02T17:45:45Z
**Mazda conversation:** conv-999156c4-0688-4020-9666-f6d2106efc4c

## Verdict: PASS

## Summary

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), so Mazda correctly
ran STEP 0 self-classification. Document was an unpaid Priority Health billing statement
($55.00, due date present) — correctly classified as an **invoice**, not a receipt (no
evidence of payment). Full receipt/invoice branch executed cleanly end to end with no
coaching required.

## Step-by-step evidence

1. **`load_wrapper_revision`** — `wrap-v071`, `found:true`. ✓
2. **STEP 0 classification** — `classify_scan.py` → `doc_type=invoice, confidence=0.99,
   merchant="Priority Health", reason="billing statement ... due date and a request for
   payment of $55.00"`. Correctly routed to invoice branch. ✓
3. **STEP 0 parse** — `parse_and_categorize.py --json --engine=gemini` →
   `expense_scope=full_receipt`, one line item ("Current invoice amount", $55.00),
   `handwritten_arithmetic_total: null` (no handwriting present — correctly not
   applicable). ✓
4. **Investigate** — `check_vendor_key("priority_health")` → normalized/fuzzy-matched to
   `priority_health_9392`, adopted correctly. `check_duplicates` → not a duplicate. ✓
5. **Categorize** — `categorizer_main.py --provider=auto` (gemini tier) →
   `category_id=243` (Rosemary/RM health), positive/valid. ✓
6. **Store** — `parse_and_categorize.py --save --invoice --category-id=243
   --engine=gemini` → `{"success": true, "expense_id": 1988,
   "parse_artifact_verified": true, "expense_scope": "full_receipt", "duplicate": false}`.
   Renamed/archived to
   `readable_documents/receipts/2025/april/april_01/priority_health_04_01_25_55_00.jpg`. ✓
7. **Itemization** — `itemize_existing_expense` correctly returned
   `itemizable:false` ("only one line item; a single line equal to the charge is
   standalone, not itemized") — correct fail-closed outcome, row left STANDALONE. ✓
8. **`record_trace`** — `task_name="document-intake"`, trace_id 347, evidence JSON
   matches reality (`stored`, `expense_id=1988`, normalized vendor_key). ✓
9. **`judge_trace(347)`** — `PASS`, `failure_type=none`, "all deterministic intake
   checks passed". ✓
10. **Dashboard callback** — `POST /api/expense-stored` with `expense_id:1988,
    expense_ids:[1988], stored:1, parsed:1, receipt_url, archive_paths`, correct
    `conversation_id`/`dispatched_at`. ✓

## Independent verification (Trainer-side, not Mazda's work)

- `POST /api/supporting-documents {expense_id:1988}` → `receipt_url` present,
  `available:true`, dialog action **View Receipt** correctly surfaced; the other three
  fields correctly absent/unavailable (this document has no source statement, scanned
  statement, or Mom's ledger reference).
- `GET /supporting-document/1988/receipt` → 200, served bytes differ from the raw
  archived file (md5 mismatch), cache file present at
  `.cache/document-annotations/expense-1988-*.jpg`, and the dashboard log shows **no**
  "opened without highlight" line for expense 1988 — the red box rendered successfully.

## Wrapper defect / coaching

None. No corrective message was sent.

## For a human

Nothing to flag. Clean PASS, no escalation.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
