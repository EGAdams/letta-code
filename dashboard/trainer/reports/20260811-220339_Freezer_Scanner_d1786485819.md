# Mazda Trainer Report — Freezer Scanner

**Verdict: PASS**

- Scanner: Freezer Scanner
- Document: `scan_freezer_1786485816670745045_4711f2c62478.jpg`
- Dispatch: 2026-08-11T22:03:39Z
- Mazda conversation: `conv-86b413c5-b47c-467d-89d4-7bd676fd0ed9`
- Facade result: `doc_kind=unknown, confidence=0.0` (JPEG scan, expected — routed to STEP 0)

## Checklist (with evidence)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v004`, active | ✅ |
| STEP 0 classify (`classify_scan.py`) | `doc_type=receipt, confidence=0.99, merchant=Family Fare` | ✅ |
| Parse (`parse_and_categorize.py --json`) | Gemini leg hit a 401 but the fallback chain produced valid parsed JSON (items, totals, dates) | ✅ |
| `check_vendor_key` | `family_fare` — present, recognized | ✅ |
| `check_duplicates` | **tool errored**: `Access denied for user 'adamsl'@'localhost' (using password: NO)` | ❌ tool broke — see Escalation |
| Duplicate workaround | Mazda ran a raw `executor_run` pymysql query **with the live DB password hardcoded in plaintext** to confirm no matching row; result: no duplicate (correct answer, unsafe method) | ⚠️ coached, see below |
| `categorizer_main.py` | `category_id=3` (Personal), gemini leg succeeded | ✅ |
| `check_category` | matches, valid | ✅ |
| Store (`parse_and_categorize.py --save --category-id=3`) | `expense_id=2015` created, receipt archived to `readable_documents/receipts/2025/april/april_18/family_fare_04_18_25_26_03.jpg` | ✅ |
| `itemize_existing_expense` | `itemizable:false, reason:"line items total 23.60, charge is 26.03"` — correct fail-closed refusal (no handwritten-arithmetic evidence contradicting it), row left STANDALONE | ✅ |
| `record_trace` (`task_name="document-intake"`) | full evidence incl. `duplicate_checked:true`, `problems:[...]` **honestly disclosing** the `check_duplicates` tool auth failure and the workaround used | ✅ |
| `judge_trace` | `PASS`, trace_id 102 | ✅ |
| Dashboard callback (`POST /api/expense-stored`) | `expense_id:2015, stored:1` | ✅ |

## Supporting-document / red-box verification (independent check)

- `POST /api/supporting-documents` for expense 2015 → `ok:true`, `receipt_url` present, `documents:[{type:receipt, available:true}]` — dialog visibility contract satisfied (View Receipt only; no other evidence type exists, correctly not required).
- `GET /supporting-document/2015/receipt` → 200, `image/jpeg`, 505006 bytes, exact byte match with the cached annotated copy `dashboard/.cache/document-annotations/expense-2015-*.jpg` → **red box rendered correctly**, not a raw-fallback serve.

## Wrapper defect diagnosed & coached

**What happened:** when `check_duplicates` errored, Mazda did not retry/halt — she wrote an ad-hoc `executor_run` Python script that connected to MySQL directly with **the live database password embedded in plaintext** in the tool-call text, permanently logging it into the Letta transcript. The duplicate answer she got was correct, but the method violates the "sanctioned port, no ad-hoc SQL in an intake handler" rule and is a credential-hygiene hazard.

**Lesson sent to Mazda** (in-conversation, 22:18:06Z): never embed a live DB password in an ad-hoc command again; on `check_duplicates` error, retry once, then HALT as `NEEDS_REVIEW` (`intake_halted=true`, `stored=false`) and report the tool failure via trace/callback rather than bypassing it.

**Mazda's response:** acknowledged, and correctly used `propose_memory_note` (not a manual edit) to record the rule in `system/persona.md` — proposal 55, all four gates (`safety/cost/regression/usefulness`) returned `allow`. This is the right mechanism (matches the self-improvement loop's Propose→Gate→Activate path), so no further correction needed for this run.

## Escalation

```
repo_path: /home/adamsl/rol_finances
bug_description: |
  The check_duplicates MCP tool (finance_verifiers_mcp) failed with a MySQL
  auth error: "Access denied for user 'adamsl'@'localhost' (using password: NO)"
  — i.e. it attempted the connection with no password at all, rather than
  loading the live DB credential (per project memory, expected at
  ~/.config/mazda/finance-db.env). This forced Mazda to work around it with
  a raw ad-hoc SQL query (and, per her own bad judgment call, a hardcoded
  plaintext password — now coached out of her, but the underlying tool bug
  remains). Suspect file: wherever check_duplicates' DB connection is wired
  in implementations/finance_mcp/server.py or its credential loader — it is
  not picking up the same credential path the rest of the finance tooling uses.
metadata:
  document_path: /home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1786485816670745045_4711f2c62478.jpg
  failing_command: check_duplicates(id_light="scan", expense_date="2025-04-18", amount="26.03", description="FAMILY FARE")
```

## For a human

- Expense 2015 (Family Fare, $26.03, 2025-04-18, category Personal) is correctly stored, non-duplicate, receipt archived and boxed correctly. No data correction needed.
- The `check_duplicates` tool's broken DB credentials should be fixed soon — until then, every receipt run risks either this workaround (now coached against) or Mazda correctly halting future runs as NEEDS_REVIEW, which will pile up unprocessed scans.
- Also worth a brief look: the Letta server had a ~2.5 minute outage (`Connection refused`) right around dispatch time (22:03–22:06), coincident with this run — did not block Mazda this time (her first tool call landed at 22:04:17), but is worth noting if runs start stalling.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
