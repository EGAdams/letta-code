# Mazda Trainer Report — Freezer Scanner

Verdict: PASS

**Document:** `scan_freezer_1786495416343009993_d1706c140df0.jpg` (Freezer Scanner)
**Dispatch:** 2026-08-12T00:43:40Z · **Mazda conversation:** `conv-1feca7e7-3933-4962-b4e4-f88d6a2313b6`
**Run duration:** ~2m49s (00:43:46 load_wrapper_revision → 00:46:29 judge_trace)
**Result:** Samaritan's Purse donation receipt, $378.00, 2025-04-21 → expense_id **2023** (new store, not a duplicate)

## Checklist (all evidence from tool returns in this run, timestamps ≥ dispatch)

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | 00:43:46 → `wrap-v005`, found=true |
| STEP 0 classify (facade returned `doc_kind=unknown`, so required) | 00:44:07 `classify_scan.py` → `doc_type=receipt, confidence=0.98` |
| STEP 0 parse | 00:44:27 `parse_and_categorize.py --json --engine=gemini` → Gemini 401'd, tool self-fell-back to `chatgpt-oauth` (`gpt-5.4`), returned full parsed JSON (single line item, $378.00, matches printed total) written to `/tmp/mazda_receipt_caf229ab2512.json` |
| `check_vendor_key` | 00:44:33 → normalized `samaritans_purse` → `5_3_online_pymt_to_samaritan_acct_8855`, `recognized=true`; this normalized key is what she used in categorizer/store/trace thereafter |
| `check_duplicates` | 00:44:50 → tool auth error (`Access denied for user 'adamsl'@'localhost'`) → per wrapper Rule 3, Mazda ran the equivalent authenticated date+amount query (00:45:19, `python3`+pymysql against `expenses`) → `()` no match → correctly proceeded as non-duplicate |
| Categorize | 00:45:55 `categorizer_main.py --provider=auto` → `category_id=215` (gemini succeeded), non-null/non-zero |
| Store | 00:46:10 `parse_and_categorize.py --save --category-id=215 --parsed-json /tmp/mazda_receipt_caf229ab2512.json` → reused the STEP 0 parse artifact rather than re-running vision (`model_provider: chatgpt-oauth` in the save-time metadata matches the STEP 0 parse, confirming `parse_artifact_verified`) → `expense_id=2023`, archived to `readable_documents/receipts/2025/april/april_21/samaritans_purse_04_21_25_378_00.jpg` |
| Itemization | Single line item ("Gift donation" $378.00 = printed total) → correctly not itemized (`itemization_attempted:false`) |
| `record_trace` | 00:46:23, `task_name="document-intake"`, full IntakeVerificationEvidence: `doc_kind=receipt`, `classification_confidence=0.98`, `vendor_key_recognized=true`, `category_id=215`, `duplicate_checked=true`, `is_duplicate=false`, `stored=true`, `expense_id=2023`, `intake_halted=false`, `problems` documents the check_duplicates auth failure + fallback |
| `judge_trace` | 00:46:29 → **PASS**, `failure_type=none` |
| Dashboard callback | Confirmed via `GET /api/expense-stored-events?since=1786495420`: event for `expense_id=2023`, `conversation_id` and `dispatched_at` match this dispatch exactly, `stored=1`, `archive_paths` matches |

## Supplementary checks (Trainer's own verification, beyond Mazda's transcript)

- **Dialog visibility** — `POST /api/supporting-documents` for expense 2023 returns `receipt_url` populated and `documents[0] = {type: receipt, label: "View Receipt", available: true}`; the other three (source/scanned_statement/moms_ledger) correctly `available:false` (none apply to a plain receipt).
- **Red-box annotation** — `GET /supporting-document/2023/receipt` returned HTTP 200, 416165 bytes, vs. the raw archived file at 398431 bytes (a cached annotated copy exists at `.cache/document-annotations/expense-2023-*.jpg`), confirming the served copy is boxed, not the raw original.

## Wrapper defect diagnosed

None. Both the Gemini→chatgpt-oauth vision fallback and the check_duplicates→manual-authenticated-query fallback (wrapper Rule 3) fired and worked exactly as designed. No coaching message was sent.

## For a human

Nothing to flag. `check_duplicates`'s direct MySQL auth (`adamsl`@`localhost`, no password) is still broken on this host — Rule 3's fallback is carrying it without issue, but if it's cheap to fix the tool's own DB credentials that would remove a step from every future run.

## Deterministic red-box gate — PASS

1 available receipt/source viewer(s) returned `highlighted=true`.
