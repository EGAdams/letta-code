# Mazda Trainer Report

- **Document**: PDF intake — `diners_0587_whole_year_2025` (Diners Club annual summary, 2025-01-01 to 2025-12-31)
- **Path**: `/home/adamsl/rol_finances/readable_documents/bank_statements/january/diners_0587_whole_year_2025/diners_0587_year_2025.pdf`
- **Scanner/source**: PDF intake (not a scanner)
- **Dispatch**: 2026-07-30T23:05:19Z (unix 1785452719)
- **Conversation**: `conv-f1aa5914-0352-4684-b51d-5a309c2988ae`
- **Facade result**: `ok:false` — `GeminiCliError: gemini CLI is not installed or not on PATH` (facade never identified the document; STEP 0 was required)

**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## What Mazda actually did (evidence from transcript, all after dispatch)

1. `load_wrapper_revision` — `wrap-v069`, ok.
2. **STEP 0 classification** — `classify_scan.py` hit a Gemini 429 quota error. Mazda fell back to an
   ad hoc inline PyPDF text-extraction heuristic (`bank_statement`, confidence 0.91, correctly
   identified the Diners Club/BMO branding). Reasonable recovery, but not the sanctioned deterministic
   path — she correctly flagged this in `propose_improvement` rather than silently normalizing it.
3. **Workbook last4 resolution** — `KnownCardsWorkbook().lookup_last4('Diners Club')` →
   `last4=0587`, `matched=diners_club_0587`, unambiguous. Correct use of the workbook per contract.
4. **Parse** — `parse_statement_scan.py` succeeded: 1 statement, period 2025-01-01→2025-12-31,
   48 transactions parsed, 0 unreadable rows.
5. **Store** — `store_statement_transactions.py` returned `"ok": false, "outcome": "FAILED"`:
   - 27 duplicate-matched existing expenses, 8 skipped credit/payment rows, 0 newly stored, 1 failed.
   - **Rolled back entirely** (`rolled_back_row_count: 40`) due to
     `DocumentConflict: expense 1638 already has a different scanned_statement_url`.
   - `association_status: "conflicted"`, `requires_verification: true` — this is the correct
     fail-closed supporting-document behavior per the contract (same-type conflict must not
     silently overwrite the original reference, and no `NEEDS_DOCUMENT_VERIFICATION` status was
     invented). An archive copy was still written to
     `readable_documents/scanned_statements/2025/diners_club_0587_january_01__december_26.pdf`.
6. **Dashboard callback** — `POST /api/expense-stored` sent successfully with
   `expense_ids: []`, `duplicate_expense_ids: [...27 ids...]`, `kind: statement`. Reasonable given the
   rollback — nothing was actually newly stored or durably attached.
7. `record_trace` — `task_name: document-intake`, trace_id 319, evidence JSON accurately reflects the
   conflict/rollback outcome (`result: "review_required_document_conflict"`, `stored:false`).
8. `propose_improvement` (proposal_id 221) — correctly diagnosed as a wrapper defect in two parts:
   the Gemini-quota classification fallback should be a first-class deterministic path, and the
   judge/report contract should recognize `scanned_statement_url` DocumentConflict outcomes.
9. **`judge_trace` was NOT called** before the run ended — the assistant's final "Processed" message
   summarized the outcome and stopped without judging. This is a hard contract violation
   (`judge_trace ALWAYS, success or failure`).

## Coaching sent

At 2026-07-30T23:12:51Z I sent a corrective message to this same conversation instructing Mazda to
call `judge_trace(trace_id=319)` immediately and to record in memory that every document-intake run
(including review-required/conflict outcomes) must end with `judge_trace`.

Mazda responded within the same turn:
- `judge_trace(319)` → **verdict: FAIL**, `failure_type: unsupported_document`,
  reasons: `failed: document_classified`, `failed: statement_transactions_parsed`,
  `failed: statement_transactions_stored`.
- `propose_memory_note` (proposal_id 222, gates all `allow`) — captured the "always judge_trace"
  rule for future runs.

## Not required this run

- `apply_statement_annotations.py` / report.html generation — the store transaction rolled back
  completely (0 rows durably stored or attached), so there is nothing to annotate or report on yet.
  These become required once the `expense 1638` conflict is resolved and a clean store succeeds.

## Human follow-up needed

- **Expense 1638** has a conflicting existing `scanned_statement_url` that blocked this whole
  statement from attaching. A human should inspect expense 1638's current scanned-statement
  reference vs. this year-2025 Diners Club annual summary and decide which is correct before
  re-running this document.
- Gemini CLI quota/installation issue on the executor is recurring (also hit `classify_scan.py`
  here) — worth checking `provider_health.json` / the categorizer fallback-health tile.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
