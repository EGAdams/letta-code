# Trainer Report — Freezer Scanner

- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785446888965440928_86dd63733fbc.jpg`
- **Dispatch:** 2026-07-30T21:28:12Z
- **Mazda conversation:** `conv-10dc48b4-4966-43b1-be67-8aa6d7933fbd`
**Verdict: FAIL (overall) — Mazda intake CORRECTED; dashboard red-box gate FAIL**

## Facade & classification

Deterministic facade returned `doc_kind=unknown` (JPEG scan, no extractable text), correctly
routing to STEP 0. `classify_scan.py` returned `doc_type=moms_ledger`, confidence 0.98
("combines multiple separate purchase fragments and categories into one assembled worksheet") —
consistent with the moms_ledger definition (composite worksheet, not a single receipt/statement).
Branch selection was correct.

## Checklist (first pass, 21:28:16–21:30:16Z)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | wrap-v068 loaded | ✅ |
| STEP 0 classify | `classify_scan.py` → moms_ledger 0.98 | ✅ |
| `moms_ledger_reconciler.py` | returncode 1, `ok:false`, 4 rows all `not_found` | genuine failure |
| `record_trace` / `judge_trace` | trace 313, FAIL (`missed_duplicate`) | ✅ correctly graded |
| `propose_improvement` | proposal 216, self-`apply_proposal`'d → wrap-v069 | ✅ filed |
| Apply fix to **this** document | — | ❌ **missing** — she activated wrap-v069's fallback-retry instruction but never used it on the document in front of her; went straight to the no-store dashboard callback and stopped |

**Defect 1 (wrapper/process):** self-activating a wrapper revision mid-run in response to a
FAIL, then finishing without applying that fix to the current document. Filing a proposal for
future runs is not completion — the corrected behavior had to be exercised now.

**Coaching sent (1st message, 21:32:20Z):** required her to re-run the reconciler with the
fallback normalization/search her own proposal described, record a fresh trace/judge either way,
and record the "apply the fix now" lesson in memory.

## Checklist (corrected pass, 21:32–21:41Z)

- Re-ran `moms_ledger_reconciler.py` directly (crashed, see Defect 2 below), then investigated
  manually: read the reconciler's own vision extractor (`VisionLedgerReader`) and ran a read-only
  `SELECT id, expense_date, amount, ... FROM expenses WHERE expense_date=%s AND amount=%s` lookup
  for each of the 5 extracted rows (Office Depot $27.02, rosemary barnes $66.09, Calendar book
  $10.59, A5 filler paper $49.79, magnesium gel $0/no date). All returned zero candidates — this
  is read-only diagnostic evidence, not a write, and it independently confirms the 4 usable rows
  genuinely have no matching expense in the database (not a matcher bug on the identity side).
- New trace 314 recorded and judged: FAIL (`missed_duplicate`) — correctly graded, since the
  underlying rows remain unreconciled.
- `propose_memory_note` (proposal 217, gate-approved but not live-applied — "no live-memory
  applier wired," an infra limitation, not her fault) capturing the apply-the-fix-now lesson.
- Fresh dashboard callback sent reflecting final state (`expense_ids: []`, no store).

**Defect 2 (tool bug, distinct from Defect 1):** the direct reconciler retry crashed —
`returncode 1`, `ValueError: Unsupported scheduled_date format: 'September 3, 2025'` — the
reconciler's own date parser cannot handle the natural-language date format its own vision
extraction step emits. This is a genuine, fixable tool defect, separate from the (correct,
fail-closed) finding that none of the 5 ledger rows match an existing expense.

**Gap after corrected pass:** trace 314 was also FAIL, and per contract `propose_improvement`
is required on every FAIL with no exception. Mazda substituted `propose_memory_note` (which
only captures the process lesson) and never filed an improvement proposal naming the date-parser
crash.

**Coaching sent (2nd message, 21:46:03Z):** required `propose_improvement(trace_id=314, ...)`
naming the date-format parser crash as the concrete tool defect, distinct from the no-match
finding.

**Result:** Mazda filed proposal 218 for trace 314 within ~10s, correctly summarizing the
parser-format bug as the defect and the no-match finding as a separate, non-defect outcome.

## Final state

- Trace 313 (FAIL) → proposal 216 → self-activated wrap-v069 (retry-fallback instruction for
  future moms_ledger FAILs).
- Trace 314 (FAIL, after applying the wrap-v069 fallback investigation to this document) →
  proposal 217 (memory note, gate-approved, not live-applied — infra gap) → proposal 218
  (improvement proposal for the reconciler's date-parser crash).
- Dashboard notified twice; final callback correctly reports no expenses stored
  (`expense_ids: []`, `duplicate_expense_ids: []`) — appropriate since none of the 5 ledger rows
  could be reconciled to an existing expense, and the contract forbids storing ledger rows as new
  expenses.
- No expense was stored, so the supporting-document/red-box invariants do not apply to this run.

## For a human

- `tools/categorizer/moms_ledger_reconciler.py`'s date parser needs to accept `"Month D, YYYY"`
  format (e.g. `strptime(..., "%B %d, %Y")`) — currently a `ValueError` aborts the whole
  reconciliation with `returncode 1` before any partial matching happens. Tracked as proposal 218.
- The 5 ledger line items (Office Depot, rosemary barnes reimbursement, calendar book, A5 filler
  paper, magnesium gel) have no corresponding `expenses` row by date+amount. If these purchases
  were never separately scanned/entered, they may need manual entry or a separate receipt scan;
  this is not something Mazda should force-create per the "never store printed payments as new
  expenses" rule.
- Proposal 217 (memory note) shows `applied: false` — "no live-memory applier wired." This is a
  known framework gap (memory notes are gate-approved but never actually written to a block),
  worth fixing so lessons like this one persist beyond wrapper-revision proposals.

## Deterministic red-box gate — FAIL

Mazda intake contract: **CORRECTED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
