# Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1785006422469303223_03bfdee7c37a.jpg`
- **Dispatch**: 2026-07-25T19:07:05Z (unix 1785006425)
- **Mazda conversation**: conv-ce8654eb-eb51-49ce-921d-87dbf5a0452f
- **Verdict: PASS**

## Facade result
`doc_kind=unknown, confidence=0.0` — routed to STEP 0 self-classification as expected.

## Checklist (all confirmed by successful tool returns in this conversation, after dispatch ts)

| Step | Evidence |
|---|---|
| `load_wrapper_revision` | 19:07:09 — wrap-v048, found |
| STEP 0 classify | `executor_run classify_scan.py` 19:07:28 → `{"doc_type":"receipt","confidence":0.99,"merchant":"Burlington"}` |
| Parse | `executor_run parse_and_categorize.py --json` 19:07:50 → date 2025-03-27, $13.77, Burlington, items |
| `check_vendor_key` | 19:07:53 → vendor_key `burlington`, present=true, recognized=false (not in known-vendor map) |
| `check_duplicates` | 19:07:57 → not duplicate, in_database=false |
| Categorize | `executor_run categorizer_main.py --provider=auto` 19:08:41 → `category_id=3` (fail-closed check satisfied: nonzero/non-null) |
| Store | `executor_run parse_and_categorize.py --save --category-id=3 --engine=gemini` 19:09:02 → success, `expense_id=1565` |
| Itemization | `itemize_existing_expense` 19:09:14 → `itemizable:false` ("only one line item... standalone, not itemized") — correct fail-closed outcome, contract-compliant |
| `record_trace` | 19:09:25 → `task_name="document-intake"`, trace_id=193 |
| `judge_trace` | 19:09:29 → **NEEDS_REVIEW**, failure_type=none, reason: vendor_key `burlington` unresolved against known-vendor map |
| Dashboard notify | `executor_run curl POST /api/expense-stored` 19:09:37 → `{"ok": true}` |

Verified independently via `GET /api/expense-stored-events?since=1785006425`: one event with matching `conversation_id` and `dispatched_at`, `expense_id=1565`, `stored=1`.

## Assessment

Full contract satisfied end to end: classify → parse → vendor/duplicate checks → categorize
(fail-closed, category_id=3) → save (expense_id=1565) → itemization attempt correctly declined
(single line item) → trace → judge → callback. Merchant and category are both non-placeholder.
`vendor_key` is unresolved against the known-vendor map, which the judge correctly reflected as
`NEEDS_REVIEW` rather than a hard failure — no wrapper defect here, this is the intended
soft-flag path for a new vendor. Verdict is not FAIL, so `propose_improvement` was correctly
not called.

## Coaching sent

None — no defect found. No corrective message was sent to Mazda.

## For a human

None required. `burlington` vendor is new; if it should map to a category automatically going
forward, add it to the known-vendor map — routine data maintenance, not a Mazda/wrapper bug.
