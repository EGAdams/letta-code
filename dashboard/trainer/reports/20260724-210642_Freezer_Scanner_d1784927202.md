# Mazda Trainer Report

**Scanner:** Freezer Scanner
**Document:** `scan_freezer_1784927198353883199_616f83b9b26d.jpg`
**Dispatch:** 2026-07-24T21:06:42Z
**Conversation:** conv-2027a544-c2b1-4998-9820-b24395023069
**Verdict: CORRECTED** (intake work complete and correct; one residual infrastructure defect flagged for a human, not Mazda)

## What the document was

`classify_scan.py` correctly identified this as `doc_kind=moms_ledger` (confidence 1.0,
merchant "Fifth Third Bank") — a Mom check-payment/ledger worksheet, not a receipt,
invoice, or statement. The deterministic facade had returned `unknown`, so STEP 0
self-classification was required and executed correctly.

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v043` | OK |
| STEP 0 classify | `classify_scan.py` → `moms_ledger`, confidence 1.0 | OK |
| Route to `moms_ledger_reconciler.py` (not receipt/statement storage) | correct branch chosen | OK |
| Reconciler run | see below — required 2 rounds of coaching | eventually OK |
| `record_trace` (task_name=`document-intake`) | trace 184, 185, 186 — 186 has full standard schema | OK (186) |
| `judge_trace` | 184 FAIL, 185 FAIL, 186 FAIL — see "Residual defect" below | FAIL each time, root cause is the judge, not Mazda |
| `propose_improvement` on FAIL | proposal 136 (trace 184), proposal 138 (trace 185) | OK |
| `apply_proposal` | 136→wrap-v044, 138→wrap-v045, both auto-gated `allow` | OK |
| Dashboard no-store callback (`/api/expense-stored`) | sent after trace 184 and again after the corrected trace 186 rerun, `stored:0`, `expense_ids:[1579,1578,1577]`, `duplicate_expense_ids:[1579,1578,1577]` | OK |

## Round 1 — wrong reconciler path + a real code bug (coached, fixed)

- Mazda first tried a non-existent path (`tools/receipt_scanning_tools/moms_ledger_reconciler.py`),
  self-corrected via `find` to the real path (`tools/categorizer/moms_ledger_reconciler.py`).
- The reconciler then crashed: `ValueError: time data '2025-04-16' does not match format
  '%m/%d/%Y'` — a genuine bug (date parser only accepted MM/DD/YYYY; this ledger row was ISO).
- Mazda recorded trace 184, got FAIL (`missed_duplicate`), filed proposal 136, and
  **self-activated wrap-v044** — but the proposal only told her to "record the limitation," it
  did not fix the bug, so the document remained unprocessed. Sent a no-store callback with
  `stored:0, expense_ids:[]`.

**Coaching sent:** told her (1) to save the corrected path to memory, (2) that documenting the
failure mode does not fix it, and (3) to patch `moms_ledger_reconciler.py`'s date parser to
accept both `%m/%d/%Y` and `%Y-%m-%d`/`fromisoformat`, rerun against the same image, and redo
trace/judge/callback.

**Result:** Mazda filed `propose_memory_note` (proposal 137, correct path) and directly patched
the script via `executor_run` (verified via `sed`/`grep` before and after). Rerun succeeded with
no crash: 11 transactions read, 3 matched to existing expenses (1579, 1578, 1577), 8 genuinely
unmatched/ambiguous (Samaritan, Salvation Army, Guiding Light, DTE Energy, AT&T U-verse, 2
blank-payee rows, 1 ambiguous vs. expense 987/1576 — all real "not in DB yet" gaps, a correct
fail-closed outcome per the manual's `not_found`/`ambiguous` handling, not a bug).

## Round 2 — evidence-schema mismatch (coached, fixed)

- Trace 185's `record_trace` used only moms_ledger-specific custom fields
  (`matched_expense_ids`, `unmatched`, etc.) and omitted the standard
  `IntakeVerificationEvidence` fields (`vendor_key`, `category_id`, `stored`, `expense_id`, …).
  `judge_trace` spuriously reported `document_classified`/`vendor_key_resolved`/
  `category_resolved`/`expense_stored` as failed even though classification and partial
  reconciliation had genuinely succeeded, and no `propose_improvement` was filed for it.

**Coaching sent:** required the standard evidence fields (with correct moms_ledger values —
`vendor_key: null`, `category_id: null`, `stored: false`, `expense_id: null`) alongside the
reconciliation-specific fields, and required `propose_improvement` for the missed step.

**Result:** trace 186 correctly includes every standard field with the right null/false values
for this doc_kind, plus `problems` naming the 8 unresolved rows. Proposal 138 filed
(evidence-schema gap) and activated as `wrap-v045`.

## Residual defect (flagged for a human, not a Mazda coaching item)

`judge_trace(186)` **still** returned the identical FAIL reasons
(`document_classified`, `vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`,
`expense_stored`) even with a fully standard-conformant evidence payload. For `doc_kind=moms_ledger`,
`vendor_key`/`category_id`/`stored` are *correctly* `null`/`false` by design (the manual is explicit:
"must not store the printed payments as new expenses"). The verdict-judge's rubric appears to
unconditionally require those fields to be populated/true, which means **no moms_ledger trace can
ever pass**, regardless of how well the document was actually processed. This is a defect in the
judge rubric itself (`agent_self_improvement` evaluation family), not in Mazda's wrapper or in
this document's processing — it is outside both Mazda's wrapper-edit toolset (prompt/tool/
memory/context commands, not judge scoring logic) and the Trainer's Bash allowlist. **A human
developer should add a moms_ledger-aware branch to the intake verdict rubric** (e.g., grade
against `ledger_rows_matched`/`unmatched`/`categories_applied` instead of vendor/category/stored
for this doc_kind) so correctly-handled ledger runs can score PASS/needs-review appropriately.

I did not spend a third coaching message chasing this — patching the judge's scoring logic is
infrastructure work outside intake-document processing and outside what a single dispatch's
Trainer should ask a cheap-model agent to attempt unsupervised.

## Bottom line for this document

Mazda did the actually-correct thing for this document: classified it, ran the (now-fixed)
reconciler, matched 3 of 11 rows to existing expenses, correctly declined to invent/guess the
other 8, did not store anything, and told the dashboard the true matched/duplicate set. The
repeated FAIL verdicts are an artifact of the judge, not evidence of bad intake work. No further
action is needed on this document; the 8 unmatched rows are a legitimate case for a human to
investigate (payments not yet reflected in the finance DB) if EG wants to chase them down.
