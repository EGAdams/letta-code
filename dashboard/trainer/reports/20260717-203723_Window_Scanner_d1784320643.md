# Trainer Report — Window Scanner

- **Scanner:** Window Scanner
- **Document:** `scan_1784320639832216381_588bdba1c673.jpg` — receipt, "Children's Vision Int. Inc." (Bangor, WI), 9 donation line items totaling $3047.00, transaction date 2026-01-06
- **Dispatch:** 2026-07-17T20:37:23Z
- **Conversation:** `conv-2b232507-b07a-46e7-b072-dd6be389e00c`
- **Verdict: CORRECTED**

## Facade → Mazda's own classification
Facade returned `doc_kind=unknown` (JPEG, no extractable text) as expected. Mazda correctly
ran STEP 0: `classify_scan.py` → `doc_type=receipt, confidence=0.95`, then
`parse_and_categorize.py --json` to extract structured fields.

## Step-by-step (original run, trace 101)
| Step | Evidence | Result |
|---|---|---|
| load_wrapper_revision | tool return, wrap-v023 | ✅ |
| classify_scan.py (STEP 0) | executor_run return, doc_type=receipt/0.95 | ✅ |
| parse_and_categorize.py --json | executor_run return, full parsed fields | ✅ |
| check_vendor_key | `childrens_vision_int_inc` → fuzzy-normalized to `michigan_corporations_division` | ⚠️ **defect** |
| check_duplicates | not duplicate | ✅ |
| check_category / categorizer_main.py | resolved category_id 357 ("Licensing") via the wrong vendor_key | ❌ wrong, consequence of the defect above |
| parse_and_categorize.py --save | expense_id 1502 stored, category_id 357 | stored but **miscategorized** |
| record_trace / judge_trace | trace 101, verdict PASS | judge couldn't see the semantic mismatch — false PASS |
| dashboard notify | `POST /api/expense-stored` fired with expense_id 1502 | ✅ |

## Wrapper defect found
Every mechanical step in the contract succeeded and the deterministic judge returned PASS,
but the run was substantively wrong: `check_vendor_key`'s fuzzy matcher normalized
`childrens_vision_int_inc` to `michigan_corporations_division` — two unrelated entities (a
Bangor, WI charity vs. a Michigan government licensing office) — and Mazda accepted the
match without checking plausibility against the merchant name/address. That drove
`categorizer_main.py` to the wrong category (357, "Licensing") for a batch of charitable
donations, and that wrong category_id was stored on expense_id 1502. This is a `wrong_category`
wrapper defect in the vendor fuzzy-match plausibility check, not a model mistake — it would
recur on any receipt whose vendor name happens to fuzzy-collide with an unrelated known key.

## Corrective coaching (one message sent)
Told Mazda directly, in this conversation: reject implausible fuzzy vendor matches, determine
the correct vendor_key/category for this specific document (a nonprofit donation), fix
expense_id 1502's stored category, file `propose_improvement` for the fuzzy-match defect, and
record a durable memory note never to accept an unrelated fuzzy match again.

## Mazda's correction (verified)
- Investigated the categories table herself (`SELECT ... WHERE name LIKE '%donat%' OR
  '%gift%'`) and found category 190, "Gifts & Love Offerings" — the correct category for this
  document.
- Ran a direct `UPDATE expenses SET category_id=190, notes=... WHERE id=1502` via
  `executor_run` (returncode 0, `stdout: "updated"`, transactional `with conn:` context →
  committed).
- Filed `propose_improvement` (trace 101, failure_type=`wrong_category`) describing the
  `check_vendor_key` fuzzy-match plausibility gap, then `apply_proposal` (proposal_id 80) with
  a concrete instruction note — **gated, allowed, and activated** as wrapper revision
  `wrap-v024` (previous: wrap-v023). This is now live for future runs.
- Filed `propose_memory_note` (proposal_id 82, allow/gated) recording the durable lesson.
- Recorded a corrective trace (102) and called `judge_trace` again; that judge run itself
  returned FAIL (`other`, "failed: expense_stored") because the judge's rubric interprets any
  non-storage rerun as a failed store — a *separate*, minor wrapper gap (the judge has no
  concept of a "correction-only" trace). Mazda correctly recognized this was a rubric gap, not
  a real failure, and filed `propose_improvement` (proposal_id 81) about it rather than
  papering over it.

## Verification
- `expenses.id=1502` category_id confirmed changed 357 → 190 via the executor's own read-back
  before/after in the transcript, with `returncode 0` on the UPDATE.
- Wrapper revision `wrap-v024` is now active — the fuzzy-match plausibility rule is in force for
  the next scan.

## Items for a human
- Minor, non-blocking: the judge has no distinct handling for a "correction-only" rerun trace
  (always expects a fresh store). Mazda already filed proposal 81 for this — worth reviewing
  when convenient.
- Mazda did not re-POST `/api/expense-stored` after the category correction. The original
  callback (with the correct expense_id/amount/date) already fired, so the Recent Report page
  is not blank, but its cached category label may lag until the page is reloaded/DB re-queried.
  Not required by the intake contract, but worth confirming the dashboard reflects category 190
  the next time that report is viewed.

## Summary
The original run passed every mechanical check and even got a false PASS from the
deterministic judge, but the fuzzy vendor-key match silently miscategorized a $3047 charitable
donation as "Licensing." Coached once; Mazda diagnosed the defect correctly, repaired the
stored expense to the right category, activated a wrapper fix (wrap-v024) so future runs reject
implausible fuzzy matches, and filed a follow-up proposal for a smaller judge-rubric gap she
found along the way. Final state: **CORRECTED**.
