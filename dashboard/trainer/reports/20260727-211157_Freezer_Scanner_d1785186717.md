# Mazda Trainer Report — Freezer Scanner

- **Document**: `scan_freezer_1785186712668104209_a4e0f8fcd56f.jpg`
- **Dispatch**: 2026-07-27T21:11:57Z (unix 1785186717)
- **Conversation**: `conv-2f9e1d3f-5327-4d62-9bd6-692939f194ba`
- **Facade result**: `doc_kind=unknown, confidence=0.0` → STEP 0 vision classification required
- **Verdict: PASS** (judge returned `NEEDS_REVIEW`, which is the correct, non-blocking
  outcome for an unrecognized vendor key per Mazda's own memory Rule 17 — not a Trainer FAIL)

## Checklist (all with successful tool returns)

1. `load_wrapper_revision(agent_name="Mazda")` → `wrap-v059` ✓ (21:12:02)
2. **STEP 0** `executor_run classify_scan.py` → `doc_type=invoice, confidence=0.95,
   merchant=AT&T, reason="does not show evidence of payment"` ✓ (21:12:28)
3. Receipt/invoice parse: `parse_and_categorize.py --json --write-parsed-json
   /tmp/mazda_receipt_866d5710b970.json` → single line item "Internet 500 (Fiber 500)",
   total $80.24, date 2025-03-19 ✓ (21:12:51)
4. `check_vendor_key(vendor_key="at_t")` → `recognized=false` (no normalized alternate
   key returned, so `at_t` correctly carried forward) ✓ (21:12:55)
5. `check_duplicates` → not a duplicate ✓ (21:12:59)
6. Categorize: `categorizer_main.py --provider=auto` → gemini timed out, **chatgpt-oauth**
   fallback succeeded → `category_id=124` (positive, valid) ✓ (21:14:57)
7. **Store**: `parse_and_categorize.py --save --invoice --category-id=124 --parsed-json
   /tmp/mazda_receipt_866d5710b970.json` (same artifact written in step 3 — correctly
   reused rather than re-running vision) → `success=true, expense_id=1675,
   parse_artifact_verified=true, expense_status=WAITING_FOR_PAYMENT_COUNTERPART` ✓
   (21:15:06) — correct for a genuinely unpaid invoice.
8. `itemize_existing_expense` → `itemizable=false, reason="only one line item...
   standalone, not itemized"` — correct fail-closed single-item outcome ✓ (21:15:16)
9. `record_trace(task_name="document-intake")` with full evidence JSON (`doc_kind`,
   `vendor_key`, `vendor_key_recognized=false`, `category_id=124`, `duplicate_checked`,
   `is_duplicate=false`, `stored=true`, `expense_id=1675`, `problems=[...]`) ✓ (21:15:24)
10. `judge_trace(272)` → `NEEDS_REVIEW, failure_type=none`, reason "vendor_key resolved
    but not in the known-vendor map" ✓ (21:15:28)
11. Dashboard notify: `curl POST /api/expense-stored` with `expense_id=1675,
    receipt_url=<scan path>, doc_kind=invoice, vendor=AT&T` → `{"ok": true}` ✓ (21:15:36)

## Wrapper defect

None found. This is a clean, contract-complete run. `NEEDS_REVIEW` is the intentionally
non-blocking verdict for an unrecognized vendor key (Mazda's own memory Rule 17 exists
precisely so this doesn't regress into a save-blocking failure), and `category_id=124`
being positive means the "never PASS a null/zero category" guard doesn't apply here.
`propose_improvement` was correctly *not* called since the verdict was not FAIL.

## Coaching sent

None — no corrective message was needed.

## For a human

Expense 1675 (AT&T, $80.24, 2025-03-19) is stored with `expense_status =
WAITING_FOR_PAYMENT_COUNTERPART` and `vendor_key = at_t` unrecognized in the known-vendor
map. No action required unless EG wants to add `at_t` to the vendor map to suppress
future `NEEDS_REVIEW` flags for AT&T bills.
