# Mazda Trainer Report — Window Scanner

- **Document:** `window_scan_1785372521218567114_1081446646c0.jpg` (Jacob Menninga invoice/receipt)
- **Dispatch:** 2026-07-30T00:48:45Z
- **Conversation:** conv-55098424-d39a-4d7c-bbd9-8eef262f5e8b
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Checklist (evidence from transcript, all timestamps after dispatch)

1. `load_wrapper_revision` — wrap-v065 ✓ (00:48:49)
2. Facade returned `doc_kind=unknown` → STEP 0 required and performed:
   - `executor_run classify_scan.py` → `doc_type=receipt`, confidence 0.95, reason: "invoice for editing services... marked as paid with a handwritten 'pd 3/11/25' note and a calculator tape snippet showing the total." (00:49:02)
   - `executor_run parse_and_categorize.py --json` → 3 line items ($17.75+$14.50+$19.00=$51.25), **`expense_scope: "full_receipt"`**, **`handwritten_arithmetic_total: 51.25`** matching the printed subtotal/total exactly. (00:49:26)

   **This is the same Jacob Menninga document flagged in the Trainer manual as a prior FAIL** (2026-07-29, a circled row incorrectly filed as `marked_items` against a fully-paid invoice). This run's deterministic guard correctly demoted the scope to `full_receipt` this time — the fix described in the manual (`handwritten_arithmetic_total` field) is working as intended.
3. `check_vendor_key` → `jacob_menninga`, recognized=true (00:49:32)
4. `check_duplicates` → exact duplicate found: `expense_id=1696`, `id_light=jacob_menninga_02_28_25_51_25` (00:49:39)
5. `categorizer_main.py` → `category_id=197` (00:49:52) — run even though duplicate; harmless, doesn't violate contract.
6. Store correctly **skipped** (exact duplicate). No itemization attempted, none required.
7. `record_trace` (task_name="document-intake") — evidence matches reality exactly: `stored:false`, `expense_id:null`, `is_duplicate:true`, duplicate id 1696 named in `problems`. (00:50:00)
8. `judge_trace(290)` → **PASS**, failure_type=none (00:50:04)
9. Dashboard callback `POST /api/expense-stored` → `{"ok": true}`, `duplicate_expense_ids:[1696]`, `stored:0` (00:50:11)

All 9 contract steps present with successful tool returns, in the correct order. Trace evidence and callback payload are internally consistent and match the transcript.

## Dashboard defect found (not Mazda's fault — do not coach)

Checked the local viewer for the graded duplicate ID per the red-box contract:
`GET /supporting-document/1696/receipt` → 200, served bytes differ slightly in size
from the raw scan (451689 vs 451573 bytes — re-encoded, not boxed). Dashboard log
confirms:

```
[supporting-document] opened without highlight expense_id=1696 type=receipt reason=No high-confidence expense row was found in the image.
```

Per the manual, a handwritten "pd 3/11/25" note plus a calculator-tape total pasted
over the printed values is exactly the kind of annotator-defeating handwriting that
can suppress the red box. This is a **dashboard defect to file**, not something
Mazda should be told to fix or re-store around — she did her job correctly.

## Coaching sent

None. No defect in Mazda's own run.

## Human follow-up

- Investigate why `document_annotation.py`'s receipt-image strategy can't locate a
  high-confidence row on expense 1696's Jacob Menninga scan (handwritten "pd 3/11/25"
  + calculator tape likely defeat OCR line matching, similar to the amount-column
  pen-crossing case already documented for statements). Not urgent — expense is
  correctly a duplicate, not newly stored.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- expense `1696`, receipt: No high-confidence expense row was found in the image.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
