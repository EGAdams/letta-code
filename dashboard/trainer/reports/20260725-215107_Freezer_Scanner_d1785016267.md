# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-25T21:51:07Z
- Document: receipt scan at `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785016264656087515_c6d504d0a3f5.jpg`
- Verdict: PASS

## Checklist

- `load_wrapper_revision` ran successfully at `21:51:12Z` with `wrap-v050`.
- `classify_scan.py` returned `doc_type=receipt`, `merchant=Meijer`, `confidence=0.93`.
- First parse attempt failed and Mazda incorrectly probed a missing `/tmp/mazda_receipt_9b746344db3d.json`; I coached her to rerun the parse until a real artifact existed.
- Mazda reloaded the wrapper at `21:54:07Z` (`wrap-v051`) and reran the receipt branch successfully.
- `parse_and_categorize.py --json` succeeded and wrote a verified parse artifact.
- `check_vendor_key` returned `vendor_key=meijer`, `recognized=true`.
- `check_duplicates` returned `is_exact_duplicate=false`.
- `categorizer_main.py` returned `vendor_key=meijer`, `category_id=3`.
- `parse_and_categorize.py --save --category-id=3` stored `expense_id=1578` with `expense_date=2025-03-11`, `amount=15.89`, and `parse_artifact_verified=true`.
- `itemize_existing_expense` ran and correctly refused itemization: `itemizable=false`, because line items totaled `29.16` while the charge was `15.89`.
- `record_trace` saved trace `207`.
- `judge_trace(207)` returned `PASS`.
- `expense-stored` dashboard callback posted the successful receipt outcome with `expense_id=1578` and `stored=1`.

## Wrapper Defect

- The initial wrapper path treated a failed parse as if a usable artifact existed. The corrected behavior is to treat a nonzero `parse_and_categorize.py` return as fatal for that attempt, rerun the parse, and only then continue into vendor, duplicate, category, save, trace, and judge.

## Lesson Sent

- I told Mazda: the parse step returned nonzero and did not write `/tmp/mazda_receipt_9b746344db3d.json`; do not `cat` a presumed artifact after a failed parse. Rerun the same parse command until it succeeds and the JSON file exists, then continue the receipt contract in order and record the lesson in memory.

## Notes

- The final stored receipt is a full receipt purchase, not a marked-item subset.
- No human follow-up is required for this run.
