# Mazda Trainer Report — Freezer Scanner

- **Scanner**: Freezer Scanner
- **Scanned image**: `scan_freezer_1784561387621911517_85fc58a6b1ca.jpg`
- **Document**: BJ's Restaurant & Brewhouse receipt, 2026-05-10, $40.77
- **Dispatch**: 2026-07-20T15:29:51Z
- **Conversation**: `conv-3a1786a8-7a75-4852-a6ab-87cb6bed7d5d`
- **Trace ID**: 114
- **Verdict**: **CORRECTED**

## Checklist (evidence from transcript, all timestamps ≥ dispatch)

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | 15:29:56 return, `wrap-v029` | ✅ |
| STEP 0 classify (facade was `unknown`) | `executor_run` → `classify_scan.py` at 15:30:15, `doc_type=receipt, confidence=1.0` | ✅ (one prior attempt at 15:30:01 failed with a malformed shell command — self-corrected on retry) |
| Parse | `executor_run` → `parse_and_categorize.py --json --engine=gemini` at 15:30:35, merchant/date/total/line-items extracted | ✅ |
| Investigate — `check_vendor_key` | 15:30:39, `vendor_key=bjs_restaurant_brewhouse`, `recognized:false` | ✅ (correctly not adopted as a fabricated match) |
| Categorize | `executor_run` → `categorizer_main.py --provider=gemini` at 15:30:47 → `category_id: null` (research step failed: **"gemini CLI is not installed or not on PATH"** on the executor) | ✅ fail-closed correctly triggered |
| Store | Not attempted — correct, since category_id was null | ✅ (fail-closed honored) |
| `record_trace` | 15:30:54, `task_name="document-intake"`, `stored:false, expense_id:null, is_duplicate:false, problems:["vendor key unrecognized", ...]` | ✅ |
| `judge_trace` | 15:30:59, `FAIL / wrong_category`, reasons: `category_resolved`, `duplicate_check_ran`, `expense_stored` all failed | ✅ correct verdict |
| `propose_improvement` | 15:31:04, proposal 92, vendor-key fallback resolution before categorization | ✅ |
| `apply_proposal` | 15:31:07, activated `wrap-v030` (learned rule #15) | ✅ |
| **STEP 8 — dashboard notify** | **Missing** — Mazda's turn ended at 15:31:12 with a summary assistant message and never called `/api/expense-stored` | ❌ initially — **CORRECTED** below |

## Wrapper defect diagnosed

Every step of the failure-handling contract (record/judge/propose/apply) executed correctly,
but Mazda stopped one step short: she never ran the mandatory STEP 8 dashboard notify, even
though the contract is explicit that it must run "ALWAYS ... even when nothing new was
stored." Her final assistant message treated `apply_proposal` as the end of the run. This is
a wrapper gap, not a model failure — the instructions don't yet make clear that the pipeline
isn't finished until the dashboard callback fires, regardless of the propose/apply detour.

## Root cause of the actual FAIL (informational, not the coaching target)

`categorizer_main.py`'s vendor-research fallback shells out to a **"gemini CLI"** binary that
is not installed on the executor (`gemini CLI is not installed or not on PATH`), while the
sibling `parse_and_categorize.py --engine=gemini` step succeeds because it uses the Python
`google-generativeai` library directly rather than a CLI. This is the same infra gap already
tracked in memory (`mazda_trainer_verified_wrap_v029_manual_fix_2026_07_19` — "missing gemini
CLI on executor"). Mazda's proposed wrapper fix (resolve the vendor key to a recognized entry
before invoking the researcher) is a reasonable mitigation for this specific vendor since a
recognized vendor key skips the CLI-dependent research path, but it does not fix the missing
CLI itself — flagging for a human to install/alias `gemini` CLI on the executor box, or repoint
`categorizer_main.py`'s researcher at the same `google-generativeai` path `parse_and_categorize.py`
already uses successfully.

## Coaching sent

One corrective message to `conv-3a1786a8-7a75-4852-a6ab-87cb6bed7d5d` at 15:36:xx instructing
Mazda to run STEP 8 now via `executor_run`, with the exact `curl -X POST
http://localhost:8765/api/expense-stored` command populated for this trace
(`stored:0, expense_id:null, parsed:1`), and to record in memory that STEP 8 is mandatory even
on a failed/no-store run.

Mazda complied immediately: `executor_run` at 15:36:50 returned `{"ok": true}`, and the event
was confirmed present via `GET /api/expense-stored-events?since=1784561391`. She also filed a
`propose_memory_note` (proposal 93) to record the lesson.

## For a human

- Install (or otherwise make available) a `gemini` CLI binary on the executor box, or change
  `categorizer_main.py`'s vendor-research fallback to use the `google-generativeai` Python
  library the same way `parse_and_categorize.py` does — this is the second observed FAIL on
  this exact BJ's Restaurant & Brewhouse receipt traceable to the missing CLI (see memory
  `mazda_trainer_verified_wrap_v029_manual_fix_2026_07_19`).
- The receipt itself is still unstored (fail-closed, correctly) — once the vendor/category
  path is fixed, this document will need a re-scan or manual reprocess to actually land in
  the finance DB.
