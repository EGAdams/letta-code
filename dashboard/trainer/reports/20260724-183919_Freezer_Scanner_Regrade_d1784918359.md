# Trainer Regrade Report — Freezer Scanner (trace 183)

- **Scanner:** Freezer Scanner (regrade of an already-completed run)
- **Document:** `scan_freezer_1784918356348439942_f87d54710753.jpg` — Fifth Third Bank statement, acct ...6285
- **Dispatch timestamp:** 1784918359 (2026-07-24T18:39:19Z)
- **Mazda conversation:** conv-3033e7e3-9b90-4e64-963f-593739c6d3e8
- **Verdict: CORRECTED** (trace 183 itself was already correct; a downstream coaching error was fixed during this regrade)

## What actually happened (verified against raw tool returns, not prose)

1. `load_wrapper_revision` — wrap-v042 loaded. OK.
2. Dispatch was statement-only, preflight-validated JSON supplied; Mazda correctly did not re-run vision.
3. First `store_statement_transactions.py` attempt (18:39:43) used `set -euo pipefail` and the store script exited rc=2 (statement rejected — see below), which killed the whole bash script before output was captured. Empty stdout/stderr, rc=2. This *attempt* also referenced `hydrate_report_categories_from_db.py` / `audit_statement_reports.py` at the wrong path (`tools/receipt_scanning_tools/...` instead of `tools/python_tasks/verification_lib/...`).
4. Mazda self-corrected (18:40:01): re-ran without `set -e`, captured the store script's real output: **correctly fail-closed rejection** — 10/17 rows unreadable (4 missing dates, 4 missing descriptions, 2 missing description+amount), quarantined to `_needs_review/20260724-144002_6285_..._f87d54710753.jpg`. This is the contractually-correct outcome for an unreadable row (per the "one unreadable row rejects the whole statement" rule) — PASS.
5. Mazda researched the correct paths for the postprocessing scripts (18:40:06–18:40:31), then re-ran report generation with corrected paths (18:41:34). **Actual tool return:**
   - `restructure_verified_transactions.py` → **succeeded** (`updated .../report.html`)
   - `hydrate_report_categories_from_db.py` → ran at the **correct path**, returned a legitimate `{"ok": false, "error": "verified transaction row has blank data-expense-id"}` — expected for a zero-stored quarantine, not a path error
   - `audit_statement_reports.py` → **errored** with `No report.html files found under .../report.html` because Mazda passed the `report.html` **file path** where the script expects the containing **directory** — this is the one real, narrow defect
   - Final marker check (read back from the actual file on disk): `has_verified_transactions_id: True, has_data_vendor_key: True, has_picker_marker: True` — **all true**
6. `record_trace` (trace 183): evidence exactly matched the above (`report_generated=true`, all three markers true, `report_audit_status=fail`). **Accurate.**
7. `judge_trace` → FAIL (deterministic rule: `statement_transactions_stored`). Reasonable given the naive rule, since nothing was stored.
8. `propose_improvement` (134) + `apply_proposal` (134, → wrap-v043): recalibrates the judge to treat a quarantined statement with `needs_review_path`/`row_errors` as a correct fail-closed outcome rather than a bare storage failure. Sound proposal.
9. Dashboard callback (`/api/expense-stored`) sent with `stored:0, expense_ids:[], duplicate_expense_ids:[]` — correct.
10. Mazda's own final summary to the user (18:42:33) matched the true tool output exactly (markers yes/yes/yes, audit fail, reason: blank expense-id). **Not fabricated.**

## The defect this regrade found and fixed

An earlier Trainer coaching message (sent into this same conversation at 18:49:18, reviewing this same trace 183) **misread the tool return** and told Mazda:
- that `restructure_verified_transactions.py` hit a `FileNotFoundError` (it did not — it succeeded),
- that `hydrate`/`audit` both errored on a wrong path (only `audit` had a real problem, and it was an argument-shape issue, not a wrong install path),
- that the markers were all `false` (the real tool output showed all `true`),
- and therefore that Mazda's summary was dishonest.

Mazda, trusting the Trainer, accepted this false premise and filed **proposal 135**: a memory note instructing herself to skip `report.html` generation entirely on any full-quarantine statement outcome. This was doubly wrong: founded on fabricated evidence, and it would have made Mazda **disobey future dispatches that explicitly require a report** (this dispatch itself said "BUILD THE DASHBOARD REPORT ... regardless").

This regrade sent a corrective message quoting the actual raw tool output back to Mazda, clarified that her original summary/trace were accurate, and asked her to withdraw/replace proposal 135. Mazda responded within seconds, filed **proposal 172** superseding 135 with the accurate lesson (don't skip required reports; `audit_statement_reports.py` wants a directory argument, not the `report.html` file path; trust raw tool output over trainer prose when they conflict), and confirmed proposal 135 was never live-applied (no live-memory applier is wired — proposals are recorded, not auto-activated). No rework needed on trace 183 itself.

## For a human to look at

- Neither proposal 135 nor 172 is auto-activated (no live-memory applier is wired for `propose_memory_note` on this deployment) — they're recorded only. Worth confirming during a future memory-review pass that 172 (not 135) is what eventually gets promoted into Mazda's active wrapper/persona block.
- `audit_statement_reports.py`'s directory-vs-file-path argument expectation is a minor but recurring trap; consider hardening the tool itself to accept either a directory or a `report.html` path (resolve to parent), which would remove this whole class of Trainer/Mazda confusion at the source.
