# Trainer Report — Freezer Scanner

- **Scanner:** Freezer Scanner
- **Document:** `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1785079125089709944_e735ee7a0e1f.jpg`
- **Dispatch time:** 2026-07-26T15:18:48Z (unix 1785079128)
- **Mazda conversation:** `conv-0c83b16b-533a-49b1-8dcd-9efb9cd06652`
- **Verdict: CORRECTED** (PASS on the intake pipeline; dashboard-callback omission fixed during this session)

## What the document was

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text) — STEP 0 required.
Mazda classified it herself: a **Guiding Light gift/payment receipt** (merchant "Guiding
Light", 255 Division Ave S, Grand Rapids, MI), transaction date 2025-03-14, amount $76.00,
single line item (no itemization needed).

## Step-by-step checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | returned `wrap-v059`, active | ✅ |
| STEP 0 classify (facade was `unknown`) | `classify_scan.py` → `doc_type=receipt, confidence=0.99` (Gemini quota exceeded, fell back to OpenAI Codex CLI per `problems`) | ✅ |
| Parse | `parse_and_categorize.py --json --engine=gemini` → structured receipt JSON, $76.00, 2025-03-14 | ✅ |
| `check_vendor_key` | raw `guiding_light` normalized to known `guiding_light_255` (fuzzy match), adopted | ✅ |
| `check_duplicates` | `is_exact_duplicate=false`, `in_database=false` | ✅ |
| Categorize | `categorizer_main.py --provider=auto` → `category_id=217` (via gemini provider) | ✅ |
| Store | `parse_and_categorize.py --save --category-id=217` → `expense_id=1651`, `returncode=0` | ✅ |
| `record_trace` | `task_name="document-intake"`, evidence matches store result exactly (`stored:true, expense_id:1651, category_id:217, vendor_key_recognized:true, is_duplicate:false`) | ✅ |
| `judge_trace` | verdict `PASS`, `failure_type=none` | ✅ |
| `propose_improvement` | not applicable — PASS | N/A |
| Dashboard notify (`POST /api/expense-stored`) | **Missing** at initial grade — confirmed absent via `/api/expense-stored-events` (no event for this `conversation_id` or `expense_id=1651`) after judge_trace + ~4 min of silence | ❌ → coached → ✅ |

## Wrapper defect diagnosed

Mazda completed the entire intake pipeline correctly and stopped after `judge_trace`
without sending the mandatory dashboard callback (contract step 9: "ALWAYS after
trace/judge"). This is a real omission, not a false alarm — verified independently
against `/api/expense-stored-events` before and after coaching. It's a step Mazda's
wrapper doesn't yet treat as part of "done," distinct from the storage/categorization
logic which all worked correctly.

## Coaching sent

Sent one corrective message to the intake conversation: named the missing step, gave the
exact `curl POST http://localhost:8765/api/expense-stored` body with all fields for this
document (kind, expense_id=1651, expense_date, amount, vendor_key, description,
receipt_url/document_path, expense_ids, duplicate_expense_ids, doc_kind, vendor,
conversation_id, dispatched_at), and instructed her to record the rule in memory.

Mazda responded within ~15s: ran the exact `executor_run` curl, got `{"ok": true}` back,
then filed a `propose_improvement`-style memory note (`propose_memory_note`, proposal_id
184, gates all `allow`) capturing "the dashboard callback is mandatory on every run,
including success/duplicate/failure outcomes."

## Verification

`GET /api/expense-stored-events?since=0` now shows two events for
`conversation_id=conv-0c83b16b-533a-49b1-8dcd-9efb9cd06652` / `expense_id=1651` (the
original send from Mazda's coached call, plus a related event) — confirmed the callback
landed with the correct amount ($76.00), date (2025-03-14), vendor_key
(`guiding_light_255`), and `stored=1`.

## Anything a human should look at

- Minor, non-blocking: the store step logged `"Receipt save warned: Could not update
  source_file to /home/adamsl/rol_finances/readable_documents/receipts/2025/march/march_14/guiding_light_03_14_25_76_00.jpg"`.
  The expense stored fine (`expense_id=1651`), but the receipt archive file link may not
  have been written — worth a quick check that the receipt file actually landed at that
  path.
- The dashboard-callback omission recurred despite Mazda's wrapper containing prior
  learned rules (Rules 1-4+ in `wrap-v059`) about other intake stages; this specific
  gap wasn't previously covered by a learned rule. The new `propose_memory_note` (id 184)
  should close it going forward — worth checking on the next run whether it actually
  changes behavior (the tool return noted "no live-memory applier wired; note recorded as
  an approved proposal only", so it may need human/automatic activation before it takes
  effect).
