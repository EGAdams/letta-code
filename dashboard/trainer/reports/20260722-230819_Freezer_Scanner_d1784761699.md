# Mazda Trainer Report

- **Scanner:** Freezer Scanner
- **Document:** `scan_freezer_1784761695622528990_27d81404e596.jpg`
- **Dispatch:** 2026-07-22T23:08:19Z
- **Conversation:** conv-26e6d568-364e-42ff-b0f9-ef56d77d1042
- **Verdict:** **PASS**

## Summary

Facade returned `doc_kind=unknown` (JPEG scan, no extractable text), so Mazda correctly
ran STEP 0 vision classification herself and proceeded through the full contract without
any coaching needed. The document turned out to be a duplicate of an already-stored
expense (id 1521); Mazda handled that as a correct skip-store outcome, not a failure.

## Checklist (all steps observed with successful tool returns)

1. `load_wrapper_revision(agent_name="Mazda")` → 23:08:29, found `wrap-v033`, active. ✅
2. **STEP 0** `executor_run` → `classify_scan.py` → 23:09:03, `doc_type=receipt`,
   `merchant=Consumers Energy`, `confidence=0.9` (utility bill payment stub). ✅
3. `executor_run` → `parse_and_categorize.py --json` → 23:09:26, parsed
   `transaction_date=2025-01-23`, merchant Consumers Energy. ✅
4. `check_vendor_key("consumers_energy")` → 23:09:30, normalized/fuzzy-matched to known
   `consumers_7996`; Mazda adopted the normalized key in later calls. ✅
5. `check_duplicates` → 23:09:37, `is_exact_duplicate=false` at that point (pre-final-save
   check). ✅ (informational; final duplicate guard below is authoritative)
6. `executor_run` → categorizer via gemini → 23:09:46, `vendor_key=consumers_7996`,
   `category_id=3` (nonzero, valid). ✅
7. **Store attempt** `executor_run` → `parse_and_categorize.py --save` → 23:10:05,
   `returncode=1`, `{"success": false, "duplicate": true, "id_light":
   "consumers_energy_01_23_25_222_65", "existing": {"expense_id": 1521, ...}}` — the
   store path's own final duplicate guard (on the parsed/final date+amount) correctly
   caught a prior match and refused to insert a second row. ✅ Correct fail-closed
   behavior, not a bug.
8. `record_trace(task_name="document-intake")` → 23:10:16, evidence JSON matches reality
   exactly: `is_duplicate=true`, `duplicate_checked=true`, `stored=false`,
   `expense_id=null`, `vendor_key=consumers_7996` (normalized), `category_id=3`,
   `problems=["save step reported duplicate of existing expense_id 1521 ..."]`. ✅
9. `judge_trace(trace_id=129)` → 23:10:20, verdict **PASS**, `failure_type=none`,
   "all deterministic intake checks passed". ✅
10. Dashboard callback `POST /api/expense-stored` → `expense_id:null,
    duplicate_expense_ids:[1521], parsed:1, stored:0, conversation_id` matches this run,
    `dispatched_at` matches dispatch timestamp. ✅ (fired even though nothing new stored,
    as required)

## Wrapper defect diagnosed

None. No coaching message was sent — nothing to correct.

## Notes for a human

None — clean run. The 2026-01-23 Consumers Energy payment stub was already in the
database as expense_id 1521; this scan was a legitimate re-scan/duplicate and Mazda
handled it exactly per the fail-closed contract.
