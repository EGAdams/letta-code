# Mazda Trainer Report — Freezer Scanner

**Verdict: FAIL (overall) — Mazda intake STALLED; dashboard red-box gate FAIL**

- Scanner: Freezer Scanner
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1787601849319128069_b2e96d8ed599.jpg`
- Dispatch timestamp: 1787601852 (2026-08-24T20:04:12Z)
- Mazda conversation: `conv-62ee742c-0440-4538-8264-2ea5ac86e405`
- Facade result: `doc_kind=unknown`, `confidence=0.0`, `recommended_action=reject` — this dispatch required a STEP 0 self-classification via `executor_run` (`classify_scan.py`).

## What happened

Polled `GET /v1/conversations/conv-62ee742c-0440-4538-8264-2ea5ac86e405/messages?limit=60`
every 30 seconds for the full 15-minute grace window (20:04:12 → 20:19:55 UTC, ~16 minutes).
At every poll the conversation contained exactly **one** message: the `system_message`
carrying the dispatch itself. Mazda never posted a reasoning step, never made a single tool
call (not even `load_wrapper_revision`), and never sent any reply.

Also checked `GET /api/expense-stored-events?since=1787601852` on the dashboard — empty
array, consistent with no work having happened.

## Checklist against the contract

| Step | Evidence |
|---|---|
| 1. `load_wrapper_revision` | **Missing** — no tool call at all |
| 0. Self-classify (`classify_scan.py`) | **Missing** |
| Investigate / categorize / store | **Missing** |
| `record_trace` | **Missing** |
| `judge_trace` | **Missing** |
| Dashboard notify | **Missing** |

Every required step is absent because Mazda never picked up the dispatch.

## Diagnosis

This is not a wrapper defect in Mazda's instructions, tools, or memory — there is no
transcript activity to diagnose a mistake from. Per the Trainer manual, an agent that
"stalled entirely and never picked up the dispatch" is an infrastructure problem (Letta
server / executor), not a Mazda lesson. No corrective message was sent to Mazda's
conversation, since there is nothing in her behavior to correct — sending a lesson into an
empty conversation would not address an unresponsive agent/infrastructure layer.

## For a human

- Mazda's conversation `conv-62ee742c-0440-4538-8264-2ea5ac86e405` never received/processed
  the dispatch. Check whether the orchestrator agent (`agent-6b536cf4-ec88-4290-b595-fed21d14bd8e`)
  is live and whether the dispatch mechanism that posts into her conversation actually
  delivered the message that shows up here as the lone `system_message`.
- Worth checking Letta server health and the executor (`:8799`) independently, since a
  completely silent agent (zero tool calls, not even step 1) points upstream of Mazda's own
  logic.
- No escalation filed against `rol_finances` or the dashboard — nothing in this run's
  evidence implicates that code; the failure is "agent never started," which is outside what
  this Trainer can diagnose further from the conversation API alone.

## Deterministic red-box gate — FAIL

Mazda intake contract: **STALLED**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
