# Mazda Trainer Report

- Scanner: Freezer Scanner
- Dispatch time: 2026-07-23T15:42:47Z
- Scanned image: `/home/adamsl/rol_finances/tools/receipt_scanning_tools/incoming_scans/scan_freezer_1784821362803663822_5aa136d6cd34.jpg`
- Verdict: PASS

## Checklist

1. `load_wrapper_revision(agent_name="Mazda")`
   - Succeeded at `2026-07-23T15:42:52Z`
   - Wrapper: `wrap-v036`

2. STEP 0 classify + parse
   - `classify_scan.py` returned `doc_type=receipt`, `confidence=0.97`, merchant `Family Fare`
   - `parse_and_categorize.py --json --engine=gemini` returned receipt metadata with `transaction_date=2025-03-04`

3. Investigate
   - `check_vendor_key` succeeded with `vendor_key=family_fare`, `recognized=true`
   - `check_duplicates` succeeded with `is_exact_duplicate=false`

4. Categorize
   - `categorizer_main.py` returned `vendor_key=family_fare`, `category_id=3`

5. Store
   - `parse_and_categorize.py --save --category-id=3 --engine=gemini` succeeded
   - Stored expense id: `1532`
   - Dashboard event confirmed `stored=1`, `expense_id=1532`, `vendor_key=family_fare`

6. Itemization
   - `itemize_existing_expense` was attempted
   - Correct fail-closed outcome: `itemizable=false`
   - Reason: line items totaled `19.44` while charge was `20.61` (`difference=1.17`)
   - Result stayed standalone, which matches the contract

7. `record_trace`
   - Succeeded with `task_name="document-intake"`
   - Evidence included `doc_kind=receipt`, `classification_confidence=0.97`, `vendor_key=family_fare`, `category_id=3`, `duplicate_checked=true`, `is_duplicate=false`, `stored=true`, `expense_id=1532`

8. `judge_trace`
   - Succeeded with `trace_id=142`
   - Verdict: `PASS`

9. `propose_improvement`
   - Not called, correctly, because the verdict was PASS

10. Dashboard notify
   - `POST /api/expense-stored` succeeded with `ok=true`

## Notes

- No wrapper defect was identified.
- Mazda reported two non-blocking warnings in trace evidence:
  - Gemini tier hit quota, then ChatGPT OAuth fallback classified successfully
  - Save step warned it could not update `source_file` to the final receipt path
- Human follow-up is optional: review the save-path warning if you want to confirm whether it is expected for this receipt flow.
