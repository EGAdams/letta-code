# Trainer Report — Window Scanner

- **Document:** `window_scan_1785371446360842781_63f9da45e563.jpg`
- **Dispatch time:** 2026-07-30T00:30:50Z
- **Conversation:** conv-a3921f6e-0fee-4828-9880-22703e4441a9
- **Trace ID:** 288
**Verdict: FAIL (overall) — Mazda intake PASS; dashboard red-box gate FAIL**

## Checklist

| Step | Evidence | Result |
|---|---|---|
| `load_wrapper_revision` | wrap-v065 loaded | ✅ |
| STEP 0 classify (facade returned unknown) | `classify_scan.py` → `doc_type=receipt, confidence=0.95`, reason: paid invoice ("pd" notation) | ✅ correctly routed a paid invoice to the receipt branch |
| STEP 0 parse | `parse_and_categorize.py --json --write-parsed-json` → 3 line items, only one (`$19.00`, "Karen Cook's sermon") has `selected_for_expense:true` with `selection_evidence:"Circle around description"`; `expense_scope:"marked_items"` | ✅ marked-item scope correctly detected — one distinct localized circle, not the sweeping/margin marks that don't count |
| `check_vendor_key` | `jacob_menninga`, recognized=true | ✅ |
| `check_duplicates` | not a duplicate | ✅ |
| Categorize | `beneficiary_hint:"Rosemary \| rbarnesrol@aol.com"` passed through unchanged to `categorizer_main.py` → `category_id=197` | ✅ Rule 18 followed correctly |
| Store | `--save --category-id=197 --parsed-json <same artifact file>` → `success:true, expense_id:1696, parsed_amount:19.0, expense_scope:marked_items, parse_artifact_verified:true` | ✅ same artifact reused, no re-parse; stored amount matches the selected row only ($19), not the $51.25 full total |
| Itemization | `itemize_existing_expense` → `itemizable:false, reason:"line items total 51.25, charge is 19.00"`, `review_required:true` | ✅ correct fail-closed — itemizing would have wrongly expanded a single selected-item receipt into 3 children; expense stays STANDALONE |
| `record_trace` | `task_name=document-intake`, all required fields present, `stored:true`, `expense_id:1696` | ✅ |
| `judge_trace` | trace 288 → **PASS**, failure_type=none | ✅ |
| Dashboard callback | `POST /api/expense-stored` → `{"ok":true}` | ✅ |

## Supporting-document dialog check

`POST /api/supporting-documents` for expense 1696 confirms:
- `receipt_url` = `jacob_menninga_02_28_25_19_00.jpg`, `available:true` → **View Receipt** ✅
- `document_url`/`moms_ledger` correctly `available:false` (not applicable to a receipt-only doc)

## Dashboard defect found (not Mazda's fault)

`GET /supporting-document/1696/receipt` served the **unannotated original** (byte-identical size to the archived file). Dashboard log:

```
[supporting-document] opened without highlight expense_id=1696 type=receipt reason=No high-confidence expense row was found in the image.
```

This receipt has three near-identical line-item rows (same vendor, same per-line format, one circled). The annotator's OCR-line matcher could not identify the circled `$19.00` row with high confidence, so no red box is drawn. This makes expense 1696 **unverifiable via View Receipt** even though the correct row/amount was stored. Per the Trainer's standing instructions, this is filed as a dashboard/`document_annotation.py` defect for a human to look at — Mazda's work is not at fault and she was not coached to "fix" it.

- Expense: id 1696, date 2025-02-28, amount $19.00
- Reason from annotator: "No high-confidence expense row was found in the image."

## Wrapper defect diagnosed

None — no corrective message was sent to Mazda.

## Summary

Mazda correctly classified a paid invoice as a receipt, correctly read the handwritten circle as a marked-item selection scoping the expense to one $19.00 line out of a $51.25 printed total, categorized it via the beneficiary-hint pass-through to Rosemary's category, stored it with a verified parse artifact, and correctly refused to itemize a receipt where only one item was actually charged. Trace, judge, and dashboard callback all completed. The only open item is a dashboard-side red-box annotation failure on this specific receipt (three visually similar line items defeat the OCR-line matcher) — a defect for a human to fix, not a Mazda regression.

## Deterministic red-box gate — FAIL

Mazda intake contract: **PASS**. Dashboard annotation verification: **FAIL**.

- unknown expense, audit: No expense IDs were found for this intake run.

Do not coach Mazda, re-store the expense, or edit finance data for this failure. The red box is produced by `dashboard/document_annotation.py`; this is a dashboard defect.
