# Trainer Report — Window Scanner

**Document**: `scan.jpg` — unpaid invoice for editing services, billed by "Jacob Menninga" (not a receipt/statement)
**Scanner**: Window Scanner
**Dispatch**: 2026-07-16T14:31:34Z
**Verdict**: **PASS** (Mazda's execution) — with a judge/rubric gap flagged for a human

## Checklist

| Step | Evidence | Result |
|---|---|---|
| STEP 1 `load_wrapper_revision` | 14:31:40, first tool call, returned `wrap-v005` | ✅ done first, as required by the 2026-07-16 12:58 lesson |
| STEP 0 classify (facade was `unknown`) | `classify_scan.py` → `{"doc_type":"other","confidence":0.94,"merchant":"Jacob Menninga","reason":"unpaid invoice for editing services, not a receipt, statement, or tax form."}` | ✅ correct vision classification, high confidence |
| Investigate (`check_vendor_key`/`check_duplicates`) | not called | ✅ correctly skipped — document is not a receipt, nothing to investigate |
| Categorize / Store | not called | ✅ correctly skipped — receipt-only steps, not applicable to an invoice |
| `record_trace` | 14:31:58, `task_name="document-intake"`, `trace_id=73`, `doc_kind:"unknown"`, `problems:["document classified as other/unpaid invoice; receipt intake not applicable"]` | ✅ called with correct task_name; minor: `doc_kind` recorded as `"unknown"` rather than `"other"` (classify_scan's own `doc_type`) — cosmetic, doesn't change routing |
| `judge_trace` | 14:32:03 → `{"verdict":"FAIL","failure_type":"unsupported_document","reasons":["failed: document_classified","failed: vendor_key_resolved","failed: category_resolved","failed: duplicate_check_ran","failed: expense_stored"]}` | ⚠️ see below |
| `propose_improvement` (required on FAIL) | 14:32:10, `proposal_id=52`, filed for trace 73 | ✅ called |
| `apply_proposal` | 14:32:15 → activated `wrap-v006`, gates all `allow` | ✅ |
| Dashboard notify | `POST /api/expense-stored` with `stored:0, parsed:0, expense_ids:[]`, confirmed via `/api/expense-stored-events` | ✅ fired even on the no-store path, exactly as required |

## Judge/rubric gap (flag for a human, not a Mazda lesson)

The judge scored this trace **FAIL** for `unsupported_document` by checking generic receipt-completion
criteria (`vendor_key_resolved`, `category_resolved`, `duplicate_check_ran`, `expense_stored`) that are
categorically inapplicable to a non-receipt document. Mazda's actual behavior was correct in every
respect: she classified it correctly, immediately recognized it was out of scope, and closed out the
run without forcing receipt-only steps — which is exactly what the existing wrap-v005 Rule 2 and this
run both demonstrate is the right call.

The `propose_improvement`/`apply_proposal` she filed (wrap-v006, "stop receipt intake immediately... do
not run receipt parsing, categorization, duplicate checks, or save on doc_type=other") restates
behavior she had *already performed correctly this same run* — it is a no-op patch layered on top of a
correct action, filed only because FAIL forced a propose_improvement call. This isn't harmful, but it
will keep happening on every future unsupported-document scan: the judge has no verdict path for
"correctly identified and correctly declined," so every such document files a redundant proposal
against an evaluator rubric that Mazda's wrapper cannot fix (see the manual's [[project-suzuki...]]
note that `judge_trace` is deterministic-rules-first with no receipt-intake rubric branch for
out-of-scope documents — same class of gap as `mazda-intake-tests-and-verdict-rubric-gap-2026-06-28`).

**Recommendation for a human**: give the judge a distinct verdict (PASS or a new `not_applicable`/
`out_of_scope` outcome) for traces where `doc_kind`/`doc_type` indicates a non-receipt, non-statement
document and every downstream field is legitimately null — instead of FAIL against receipt-completion
criteria.

## No coaching message sent to Mazda

Her execution this run had no defect — STEP 1 ordering, classification, correct skip of
investigate/categorize/store, trace with the right `task_name`, judge call, proposal-on-FAIL, and
dashboard notify were all present with successful tool returns. Sending her a corrective message would
misattribute an evaluator/rubric issue as a wrapper lesson she needs to learn, which is not the case.

## Summary

Mazda correctly identified an out-of-scope document (an unpaid invoice, not a receipt) via vision
classification, appropriately skipped all receipt-only pipeline steps, recorded a trace and judged it,
filed the required improvement proposal on FAIL, and notified the dashboard with a zero-store event —
full contract compliance. The FAIL verdict itself is an artifact of the judge's rubric having no path
for legitimately out-of-scope documents, not a real defect in Mazda's run; that gap should be fixed in
the judge/evaluator, not patched via another wrapper rule on Mazda's side.
