# Gemini Handoff - Thursday, May 7, 2026

## Objective

Improve the `buildFailurePrompt` method in `log-viewer.component.ts` so it correctly extracts the following fields from Python-originating logs:

- `codeContext`
- `failureMessage`
- `stackTraceBody`

## Current Status

### Log Sanitization Fix

Modified:

```text
/home/adamsl/letta-code/src/logger/RemoteLogger.ts
```

The change preserves newlines in log messages.

Previously, `sanitizeLogMessage` was replacing all whitespace, including newlines, with single spaces. That destroyed Python stack traces before they reached the viewer.

### Improved Parsing Logic

Updated `log-viewer.component.ts` with a more robust `buildFailurePrompt` method.

The improved method now:

- Looks back 50 log entries for context.
- Uses regex to identify standard Python traceback blocks.
- Specifically extracts the offending line of code.
- Extracts the one-line error message.
- Falls back to manual stack trace assembly if a full traceback block is not detected.

## Pending Tasks

### 1. Run Integration Test

Execute the user-provided integration test to generate real failure logs:

```bash
bun test /home/adamsl/letta-code/src/integration-tests/startup-flow.integration.test.ts --bail
```

### 2. Verify Extraction

Check the log-viewer UI, or simulate the click, to confirm that the debugger prompt generated by `buildFailurePrompt` matches the expected format.

Verify that the prompt correctly includes:

- `codeContext`
- `failureMessage`
- `stackTraceBody`

### 3. Refine Python Logger, If Needed

The user mentioned:

> You may need to modify the Python logger functions that this test runs to get the results that we need.

Investigate whether a Python-side logger or utility should send more structured data.

Possible locations to inspect:

```text
/home/adamsl/rol_finances/events/logger.py
```

Also inspect bridge-script logic inside:

```text
/home/adamsl/letta-code
```

### 4. LLM Refinement Option

If parsing proves difficult with regex alone, consider using `gemini-flash` through a sub-agent to extract the context from the log buffer, as suggested by the user.

## Files Modified

```text
/home/adamsl/letta-code/src/logger/RemoteLogger.ts
/home/adamsl/the-factory/src/components/log-viewer/log-viewer.component.ts
```

## Success Criteria

The task is complete when:

1. The integration test generates real Python failure logs.
2. Newlines are preserved all the way into the log viewer.
3. `buildFailurePrompt` extracts the correct code context.
4. `buildFailurePrompt` extracts the correct failure message.
5. `buildFailurePrompt` extracts the correct stack trace body.
6. The generated debugger prompt is clear enough for the next agent to act on.
